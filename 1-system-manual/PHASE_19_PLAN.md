# PHASE_19_PLAN.md — Phase 19: Paper Mode Audit & Debug — RUNNING PLAN

> **🔄 RUNNING GOVERNANCE DOCUMENT (Kyle directive 2026-06-12).** Tier-1 during Phase 19: updated after **EVERY Phase-19 batch AND sub-batch** — sequence position, per-item status, decisions taken. Created 2026-06-12 at phase kickoff (Claude New + Kyle planning session). Langston sequence review: **✅ APPROVED 2026-06-12** ("APPROVE the B1→B14 sequence as ordered — no re-sequencing required") with 4 required additions + 3 recommendations, ALL folded in same-day (B7a/B7b split §1, pre-flight checklist §6, disposition table §7, decisions §5).
>
> **One-home rule:** this doc owns Phase-19 **sequencing + live status + phase-scoped decisions**. Item DETAIL stays homed in `POST_AUDIT_ROADMAP.md` §3.2 (locked items 19-1…19-20, §19.6.x, 19.x) — entries here pointer to those anchors, never duplicate them. Batch-level detail lives in the normal scope/completion files per CLAUDE.md §2.
>
> **Retire from Tier 1 at Phase-19 close** (becomes a historical record).

---

## §1 — STATUS BOARD (update every batch/sub-batch)

> ## ★★★ CC-C RUN ORDER — SET BY KYLE 2026-08-10. **He has FROZEN the other sessions to focus on this.** Two batches, in order; proposal at `Claude Comms and Packages/Scope Files/STRATEGY_FEE_VIABILITY_TWO_BATCH_PROPOSAL.md`, **with Langston for approve/reject — NEITHER IS SCOPED UNTIL HE RULES.**
>
> **THE DRIVING QUESTION:** crypto produces **3 organic trades in 21 days; xStock produces 169** — on the **same shared pipeline** (`xstock_spot/active-dispatch.ts:6-10` routes onto the shared active pipeline: sizes → SQE → RTB). Same gate, same math ⇒ **the difference is in the INPUTS.** Measured: **99.1% of crypto SQE rejections are `NetEV <= 0`**, and the record says `chosen maker mode` — the cheaper 0.40%/side path is **already** selected and still negative. At the pWin ceiling crypto computes to **−0.007% — exactly zero**, versus xStock **+0.32%**. **Crypto sits on the knife edge.**
>
> - **⏳ `P19-B-FEEVIABILITY` (batch one) — Strategy Fee-Viability Survey + Marked Test Window.** Arithmetic survey of ~25–28 strategy×asset-class combos (**~18 crypto + 6–9 xStock**) → sort IMPOSSIBLE / BORDERLINE / WORKING → shadow-SQE over VTS to size changes → **one DATED per-strategy change set (the deploy IS the mark)** → two-lens before/after. **HARD PRECONDITION: the geometry/config-VERSION stamp lands FIRST** (0 of 8,767 declines carry a target today; fix is write-site parity at the SQE reject hook copying `vts-runner`). **Kyle's framing, binding: some combinations simply will not work at ~1.6% round-trip, and recording that IS a result — not a reason to turn dials until something squeezes out.**
> - **✅ `P19-B-PERPFEED` — crypto-perpetual FEED CAPTURE (interleaved into the CC-C order at Kyle's 2026-08-17 directive; CAPTURE-ONLY, perp TRADING stays Phase 26).** 184-member universe (field-driven classification, refuse-and-log; 74 UNCLASSIFIED refusals recorded not guessed), born-daily tables at the standing 30-day hot rule, one shared Kraken-Futures engine behind two facades, and the OBJ-7/OBJ-9/OBJ-10 storage arc that took the database **152.4 GB → 122 GB (78.9% → ~61% of cap)** with the OHLC tier path proven by a two-leg gate test. **Capture LIVE 2026-08-19T21:22Z (Kyle-directed switch-on after the §4 size report: ≈19.5 GB/month at 10 s pacing); landed rows 376,648 OHLC / 184 symbols + 987,460 ticker, measured by `count(*)`.** ⚠️ **Its own defect #704 (the missing UNIQUE ⇒ 368,841 bars scanned / 0 landed for ~15 h) was found at post-deploy verification, fixed same-turn, and fenced with a DERIVED subject.** Completion report written; **BATCH_CATALOG/PHASE_HISTORY/this plan + the Langston MEMORY sync land with the close.** Known limits: OBJ-5 + OBJ-11 PARTIAL, #705 filed (owner CC-C, due 09-05), the OHLC ×3 retention flip ARMED behind the 09-01 equality assertion.
> - ⛔⛔ **`B-SIZING-DEC-RESTORE` RESUMPTION — PLACED HERE 2026-09-12 (CC-C), BECAUSE IT WAS NOWHERE.** **Its start signal is the bullet immediately above: Kyle's sequencing was that it resumes once `P19-B-PERPFEED` closed, and that closed 2026-08-20 — so it is past its own trigger, not waiting on one.** ⛔ **HALF OF IT IS ALREADY LIVE and that is the reason this is not paperwork:** obj-1 fixed-notional sizing, obj-10 (11.7S posture damper deleted) and obj-11 (legacy adaptive tuner deleted) landed at `213e162dc` on 2026-08-07 and **fixed-notional sizing is what sizes every trade today.** **obj-2/3/4/5 and Steps 4-11 are NOT BUILT** — no review, no CI, no deploy of the remainder, no verification, no governance. **Scope of the resumption INCLUDES `#698`** (Kyle 2026-08-19: paper mode must trade live-realistic position sizes). **Owner CC-C.** ⚠️ **Declared OPEN in `GOVERNANCE_EXCEPTIONS.md` in this same commit rather than closed — a completion report now would claim work that did not happen.** ⭐ **ITS POSITION RELATIVE TO THE IN-FLIGHT PRICE WORK (`3n` / OBJ-8) IS KYLE'S CALL**, put to him by Langston 2026-09-12 as a queue-position question. **Nothing is unsafe while it waits; it is placed here so it is visible, not to claim priority.**
> - **⏳ `P19-B-FEEVIABILITY` — IN ITS MEASUREMENT TAIL.** The mark deployed 2026-08-16T23:38Z; OBJ-1 (pair-exclusivity persistence) and OBJ-2 (48 h shadow TTL) both PASSED their scheduled checks on live traffic (98 blocks with holders; TTL-cut share 27.3% → 3.6%). **The mark verification tripped the >15% revisit branch (50.5% marginal unreachable vs ~9% expected) and #696 is the arc: both discriminators now agree the 2.5R row is the cause, not the drought — Langston HOLDS the row pending his (a)-revert vs (c)-volatility-conditioned ruling.** The per-strategy reachability package is due to him ~08-25/27.
> - **⏳ `P19-B-DROUGHT-2` (batch two) — everything else surfaced 2026-08-09/10.** Incl. **the SEVEN UNTRIGGERED items of the observational decision gate (roadmap item `25-5`; `19.4.5` is a preserved section anchor, NOT its phase — `POST_AUDIT_ROADMAP:310/:291`).** ⚠️ **Its TWO TRIGGERED items are NOT here — they are `OBJ-0` of batch one** (confidence-vs-outcome inversion, and <5 signals/day), because both decide what batch one contains. **Per Langston 2026-08-11: OBJ-0 gates OBJ-4 + OBJ-5 and runs CONCURRENT with OBJ-1/OBJ-2** — the sequencing argument reaches admission only, and gating instrumentation behind an analysis task is a stall. **The gate has NEVER RUN — no `PHASE_19_OBSERVATIONAL_DECISIONS.md` exists (Langston-verified at the ref).** Also here: confidence calibration (⚠️ **the finding is that HIGHER-confidence signals LOSE MORE OFTEN — NOT an inverted score**); **DI recorded nowhere** (default 50 ⇒ pWin pinned at the 0.60 **ceiling** — a fail-OPEN default on a risk gate); the near-inert 3% spread ceiling; **order-book depth already stored** (`bid_qty`/`ask_qty`, 472 pairs, 100% populated, `min_depth_usd` NULL everywhere); the possibly-invalid volume floor (reported crypto volume inflated **1.25×–50×**); the combinations study; the mean-reversion style decision.

> ## ★★★ CC-C — PART F: THE ENTRY/EXIT FIDELITY SEQUENCE. **RUN ORDER SET BY KYLE 2026-08-26; SCHEDULED HERE ON HIS DIRECTIVE THE SAME DAY.**
>
> ⛔ **WHY THIS BLOCK EXISTS, stated because its absence was the defect:** every batch below was live work — some in flight, some already closed — and **NONE of it was written into this plan.** It lived in a scope file, a working checklist and `RUNNING_ISSUES`, none of which is the phase plan. **Kyle, 2026-08-26: *"If these are new batches or hotfixes to address the issues you've found, then, yes, they need to be created as new batches or hotfixes and scheduled somewhere within our phase nineteen plan, so that they're not missed."*** ⇒ **the ledger is where an issue is described; THIS TABLE is what schedules it. An issue with a `RUNNING_ISSUES` number and no row here is not scheduled — it is only written down.**
>
> ★★ **ORDER CHANGED BY KYLE 2026-08-27, AND HIS REASONING IS THE BINDING PART — DO NOT RE-SORT BY DEPENDENCY.** He moved `F-G` first, `F-5` second, `B-SCANNER-EGRESS-NORMALISE` third. **His reason: he needs to know whether this system can trade profitably AT ALL before we spend time calibrating thresholds** — *"I wanna start to see some positives… I think those three items will give us an indication of whether or not we have at least a place to start from."* ⇒ **#3 and #4 change whether the trades we already take are profitable; #5 only widens the universe by two pairs.** A dependency-optimal order would put #5 first because it is independent; **that would be optimising the wrong thing.**
>
> **THE PURPOSE OF THE WHOLE SEQUENCE (Kyle's framing, binding):** *ensure we enter and exit trades as close as possible to what will happen in live mode through Kraken.* We trade spot only, unleveraged — so any change that brings paper closer to that reality belongs **inside this sequence**, not after it.
>
> | # | batch | what it is | status |
> |---|---|---|---|
> | **1** | **`B-EXIT-PROVENANCE`** | every fill records WHICH price source drove it, how stale that price was, and the independent cross-check — so a trade proves its own prices instead of being reconstructed later. 14 columns on `closed_trades`. | ✅ **CLOSED 2026-08-30.** Steps 1-10 done, body `640ae5c7a`, close gate `#911` at `ed86a758e`. ⭐ **THE GATE IS SATISFIED NINETEEN TIMES OVER, NOT ONCE: 19/19 post-deploy closes carry both `exit_price_source` AND `exit_price_producer` — crypto 13, xStock 6, both close reasons.** Witness 17/19; the 2 nulls are both `TRUMP/EUR` against **0** `/EUR` rows vs **698,794** `/USD` in the archive ⇒ archive-universe gap, not a defect (`#954`), and **17/17 where the witness can exist.** ⚠️ **KNOWN LIMIT NOW LOAD-BEARING ELSEWHERE: the witness is NOT independent on xStock** (`depth-source.ts:89-93` — the fill reads the same table) ⇒ **this is the reason `F-G-2`'s xStock legs are held at 3b.d.** ⛔ **Governance was 66h late and the CHECKER caught it, not me** (alert `2200283b`): a batch held open by a close gate owes governance TWICE, and no progress report existed to hold the open state. Report: `B_EXIT_PROVENANCE_COMPLETION_REPORT.md`. |
> | **2** | **`B-EXIT-PROVENANCE-TICKER-RETENTION`** (**#911**) | retain the ticker bid/ask the WS handler already computes and throws away, so a row carries a **second, INDEPENDENT** feed. **`#741` is an ORDER-BOOK defect ⇒ a row holding only book numbers checks the suspect against itself.** | ✅ **LANDED `ed86a758e` 2026-08-27; GATE DISCHARGED, #1 CLOSED 2026-08-30.** Langston's Step-4 ruling held: the witness comes off a **separate socket** (`crypto_spot_ticker_snap`, written by the archiver), never from `_closeSnap` which the taker leg already held — *a cross-check drawn from the suspect book agrees with itself by construction and proves nothing.* ★ **It is already earning its keep: the machinery audit's §9.1 used `exit_price_producer` to establish, independently, that EVERY exit decision reads a midpoint — 23 of 23, zero from any last-trade producer. That finding is only expressible because this shipped.** |
> | **3** | **`F-G-1` — `B-GRID-REPRESENTABILITY`** ★ **THE SPLIT, Langston-ruled 2026-08-27** | Rounds every entry/stop/target to a price the venue can express, decides what happens when a rounded signal stops clearing its gates, and fixes the bar writer's silent dropped batches. ⛔ **SPLIT REASON IS A MEASUREMENT DEFECT, NOT SIZE: grid rounding MOVES THE STOPS (worst 12.96% of stop distance), so shipping it with `F-G-2` destroys `OBJ-0`'s before/after arm.** ★ **Kyle's OBJ-7-first directive is PRESERVED AND STRENGTHENED.** | ⛔ **OPEN — OBSERVATION WINDOW.** Deployed `56ac8067a` 2026-08-28T18:05:22Z, CI 4/4, Langston Step-4 APPROVED, UI verified both Filter Diagnostics tabs. **Record is a PROGRESS REPORT** (`F_G_1_PROGRESS_REPORT.md`); board card in `Observation`. **Criterion pre-registered before data, amended 9× by APPEND only, armed as self-firing alert `2093a98a` (fires 2026-09-04T16:08:02Z): 30 crypto opens or 7 days, per-class counters, 100% on-grid no tolerance.** At writing **crypto n=3/30 all on grid · xStock n=0/30**. Findings `#927`-`#939` all homed ➕ **2026-09-11 (Langston J9): OBJ-9 ① and ② are not met in production (`#1031`). The batch reopens at Step 3 for OBJ-9's ORDERING guarantee only; the owed conversion grades ① and ② FAIL (`F_G_1_PROGRESS_REPORT.md` §7). Crypto OBJ-7's PASS stands. The writers' other edges → `3b.h-8`.** |
> | **3b** | **`B-ASSET-CAPS-REMOVAL`** (`#917`) | Delete the orphaned `asset-capabilities` service — pre-governance code from 2025-10-10 collecting the very grid `F-G-1` needs, into a table holding **0 rows all time**; its only auto-refresh trigger has zero callers repo-wide. **Rule 18: DELETE, not stub.** | **AFTER `F-G-1`** — ⚠️ **re-homed 2026-08-27: "after F-G" became ambiguous at the split (Langston's objection).** ✅ **Its §9.5(a-ii) census is DONE and the orphan premise HOLDS** — neither module imports the other; `AutoMappingEntry` is written only from the asset-pairs service's own fetch |
> | **3b.b** | ✅ **CLOSED 2026-09-11 — window INCONCLUSIVE, stopped (Langston 17:23Z); the acceptance does not pass and re-arms on the post-OBJ-7 instrument; record `B_XSTOCK_FEED_SANITY_COMPLETION_REPORT.md` §4k.** ⛔ **`B-XSTOCK-FEED-SANITY` (`#943`) — RE-FRAMED 2026-08-29: a RE-DISCOVERY of the class already homed at `#531` ADDENDUM-2 and split out by Langston as `B-XSTOCK-EXIT-PLAUSIBILITY`; Kyle ruled *“accept and observe”* on 2026-07-19. THIS ROW IS THE EVIDENCE (cause + scale + structure + the FAVOURABLE bias direction), NOT a competing batch — Langston to rule on the merge.** | ⛔⛔ **THE xSTOCK PRICE FEED EMITS A BAD PRINT AT `00:15` UTC MOST DAYS AND THE ENGINE CLOSES POSITIONS ON IT — 65 closes since 2026-07-17, ZERO crypto ever.** That is **27.1% of all xStock stop-outs** and **29.5% of target-hits.** Read at the second in the runtime log: `NOW/USD` sat 10.2% clear of its stop at 00:14 at 143.15, then `CACHE_WRITE … price=118.75 source=kraken_equities_ws` at 00:15:00, read **fresh** at `ageMs=1479`, `distSL=-8.24%`, `EXIT_TRIGGER stop_hit` — against a venue book of **143.20/143.30**. ⭐ **IT HAS SURVIVED BECAUSE THE COHORT IS THE PROFITABLE HALF OF THE BOOK** (+$97.43 over 65 vs −$350.83 over the other 178): **a false stop firing above entry books a WIN**, and the rows look ordinary — `exit_price` and `exit_ticker_bid` are CORRECT; only the DECISION was wrong. | ⛔ **BEFORE `F-G-2` (3c) — and specifically before its xStock legs, which cannot be read until this cohort is identifiable.** Owner CC-C. ✅ **ELIMINATED WITH EVIDENCE, not left open:** not stale (`ageMs=1479` ⇒ the staleness guard **structurally cannot fire**) · not a restart (`pm2` uptime 18 h) · not the `last_known_good` fallback (stamped `kraken_equities_ws`) · not a cron (none at 00:15; the xStock universe cron is 06:00) · not stale-at-open. ✅ **ENTRIES ARE NOT AFFECTED — CHECKED, not assumed:** opens do **not** cluster at 00:15 (≤3 per minute-of-day since 07-17, against **65** exits in that one minute), and no open position carries a 00:15 `opened_at`. ★ **There is a structural reason: an exit is evaluated tick-by-tick against a live position, so one bad print acts immediately; an entry must traverse scanner → SQE → RTB → EV gate.** ⚠️ **This does NOT rule out the bad price contaminating RANKING inputs more diffusely — untested.** ⛔ **NO FIX IS PRE-JUDGED (rule 15): we do not know WHY the print exists, and a deviation threshold chosen before that is a patch.** ⚠️ **`F-G-2` meanwhile excludes `00:15` from every xStock population — a PROXY for the defect, not the defect; this batch owes a positive row-level identifier.** | ⭐⭐ **REFRAMED 2026-08-30 (`#958`, Kyle-directed): the 00:15 print is EXPLAINED and it is DOCUMENTED VENUE BEHAVIOUR — Kraken publishes FOUR xStock sessions (Overnight 8PM-4AM ET · Pre-market 4-9:30 · Open 9:30-4 · After-hours 4-8PM), and at 20:00 ET the market-maker pricing basis CHANGES from *“real-time reference pricing”* to *“ATS platforms, index futures, and internal models”*, with Kraken stating *“spreads are wider outside market hours.”*** ⇒ ⛔ **THIS BATCH'S SUBJECT CHANGES FROM *“find the bad print”* TO *“decide how the exit path treats each of the four documented sessions”* — rule-24 outcome (2), a SCOPE DECISION FOR KYLE, not a defect.** ⛔ **AND NOT BY EXCLUDING 00:15: the cause is a FOUR-HOUR session, so a one-minute filter would leave 3h59m untouched and hide the question.** ⭐ **THE ROUTE FROM HERE IS `1-system-manual/XSTOCK_PRICING_DECISION_PATH.md` — three questions, their evidence, their owners and their order.** ⚠️ **The session tag `is_extended_hours` is NOT a shortcut: it reaches the ARCHIVE only, never the decision point, and it is a BOOLEAN against a four-way distinction. The honest session key is the TIMESTAMP against the published boundaries.**
> | **3b.b-c** | **`B-ROLLBACK-EPOCH-FORWARD`** (`#1045`) — Owner CC-C (placed 2026-09-11; routed by Langston from CC-B's `B-XSTOCK-FEE-CONTRACT` Step 4) | ⛔ **`module_constants.updated_by` is a last-writer-wins slot, and a rollback that deletes only `WHERE updated_by = '<my batch>'` matches 0 rows, silently, once a later batch writes the row.** Instance: `2026-09-03-b-xstock-feed-sanity-rollback.sql:14-16` deletes the xStock `paper_sim` calibration-epoch row, and `B-XSTOCK-FEE-CONTRACT`'s migration rewrites that row. **The fix:** (a) that rollback deletes no epoch row, because epochs only step forward; (b) the `2026-06-10-item4-step2-calibration-epoch.sql` header's rollback instruction says the same; (c) any rollback still keyed on ownership checks its own row count. SQL and headers only; no runtime change. Langston reviews. | **After OBJ-7 deploys, before OBJ-8's build** — ahead of any moment a feed-sanity or fee-contract rollback could be run. |
> | ~~**3b.c**~~ | ⛔⛔ **`B-BOOK-BBO-DIVERGENCE` — WITHDRAWN 2026-08-29, SAME DAY, BY ITS AUTHOR. The 0.48% was a TIMING ARTIFACT: a continuous instrument (n=492, 4 symbols, no exits) puts the book/ticker median at EXACTLY 0.0000%, and the exit-derived gap is decided by whether the matched snapshot fell before the close (-0.5797%) or after it (-0.0696%). `#944` carries the retraction. F-G-2's sequencing REVERTS — no prerequisite.** *(original row text follows, struck)* | ⛔⛔ **OUR LOCALLY-MAINTAINED BOOK AND THE VENUE'S PUBLISHED QUOTE DISAGREE BY ~0.48% AT EXIT TIME, AND THEY ARE THE SAME STATISTIC.** `kraken-websocket-adapter.ts:910-918` takes `Math.max(bids)` / `Math.min(asks)` — **a TOP-OF-BOOK mid; “depth-10” names the SUBSCRIPTION, not the statistic** — so this is ONE quantity constructed two ways, not two statistics that may legitimately differ. ⇒ **a BOOK-STATE ERROR, `#507`/`#741` family.** **Measured (USD-quoted crypto): `exit_price` −**0.4229%** vs the venue mid and −**0.3029%** vs its BID, 16 of 18; xStock sits ON its mid (+0.0054%, n=105). Recorded slippage explains ~1/8.** ⚠️ **Langston's direction candidate — an orphaned ask dragging `Math.min(asks)` down — is a HYPOTHESIS, NOT MEASURED.** | ⛔⛔ **THE MEASUREMENT LEG IS A PREREQUISITE OF `F-G-2` IMPLEMENTATION, NOT A SUCCESSOR — after the `F-G-1` soak, BEFORE 3c.** Owner CC-C. ★ **WHY, and it is F-G-2's own argument turned around: `OBJ-1` SOURCES CRYPTO'S BID FROM THAT SAME MINI-BOOK**, so F-G-2 does not leave the divergence beside the fix — **it ROUTES THE NEW DECISION PRICE THROUGH THE OBJECT UNDER SUSPICION.** You cannot pre-register a rule-vs-rule before/after while the series both arms read is moving under you. ⛔ **THE INSTRUMENT MUST BE BUILT — censused, not assumed: NO table matching `book|depth|orderbook` exists and the book mid is NEVER persisted**, so every figure above is exit-derived by necessity. **Deliverable: book mid vs ticker mid, same symbol, same instant, continuously — then n stops being 18.** ⚠️ **The crypto ticker archive has ZERO coverage of `/EUR` pairs** (5 of the 6 non-joining rows), so it needs widening too. **FIX leg may sit in Phase 20 with `#507` if the measurement says CRC/resubscribe is the root.** |
> | **3b.c** | ⛔⛔⛔ **`B-EXIT-TRIGGER-FILL-PARITY`** (`#959`) — **CRITICAL. Reuses the slot vacated by the WITHDRAWN `B-BOOK-BBO-DIVERGENCE`.** | ⛔ **CORRECTION NOTE 2026-09-11 (CC-C): WITHDRAWN AS A SEPARATE DEFECT 2026-08-31 (`#959` am. 1-2).** The 14% trigger/fill gap was five xStock rows in the 00:15 UTC minute; outside it the gap is ~0.1% and the fill equals the ticker bid. Folded into `#943` / row 3b.b. The CRITICAL text below is the pre-withdrawal record. ⛔⛔ **THE EXIT TRIGGER AND THE EXIT FILL READ DIFFERENT PRICES FROM DIFFERENT SAMPLES.** TRIGGER = the **unthrottled in-memory MID** (`active-execution-engine.ts:1163`); FILL = a **depth-walked BID off the 4-SECOND-THROTTLED archive table** (`:1997` → `depth-source.ts:49-70`), booked at `order-placer.ts:99-103` — **the trigger price is used ONLY for the slippage figure.** **MEASURED, with a control: `xstock_spot` n=9 mean abs gap **14.038%**, max **48.01%**, and `stop_hit` signed **+17.16%**; `crypto_spot` n=15 gap **0.209%**, signed **−0.28%**.** ⭐⭐ **THE SIGN IS THE FINDING: a stop-out is a forced sale and must fill AT OR WORSE than the stop — crypto does; xStock fills 17% BETTER, WHICH BOOKS A GAIN.** Specimens: `TGT` stop at **106.075 booked at 157.00** · `NOW` 118.75 → 143.20 · `WEN` 13.31 → 8.562. | ⛔ **BEFORE `F-G-2` (3c), with 3b.b / 3b.d.** Owner CC-C. ⭐⭐ **THIS IS THE MACHINE BEHIND THE CONTAMINATED P&L — the burst window's 77% profit share is booked because the FILL COMES FROM A DIFFERENT SAMPLE, not merely because a stop was false.** ⚠️ **LIMITS: n=9, and `exit_decision_price` only ships from 2026-08-26 21:23Z, so the historical cohort is NOT recoverable — which corrects `#958`'s recoverability claim.** ⛔ **NO FIX PRE-JUDGED: the first question is which of the two reads is SUPPOSED to be authoritative, and `BATCH_65` already ruled that VTS and paper keep DIFFERENT fill conventions ON PURPOSE — search that record before calling either one wrong.** ★ **It was absent from my own “complete list”; Kyle asked whether the list was complete and the answer was no.** |
> | **3b.d** | ⛔⛔ **`B-XSTOCK-BOOK-LADDER`** (`#949`) — **PREREQUISITE of `F-G-2`, same standing as 3b.b** | ➡️ **ABSORBED INTO 3n r5 (2026-09-11) — D3: xStock 20-level book for held + queued xStocks, behind the book-unsubscribe fix (`Scope Files/PRICING_DECISIONS_2026-09-11.md`). The text below is the pre-absorption record.** ⛔⛔ **THE xSTOCK `book` CHANNEL WAS CONFIRMED TO EXIST, GIVEN A NAMED PLUMBING OBJECTIVE, AND IS STILL NOT SUBSCRIBED TO.** Four links, all verified at the ref: **(1) ASKED** — `BATCH_79_SCOPE.md:523-525` lists crypto's *“Order book depth (top N levels)”* against xStock's *“Order book depth: **TBD** — is the Kraken Equities WS book channel exposed?”* **(2) ANSWERED YES** — `B_XSTOCK_GLOBAL_FILTER_SCOPE.md:79`, O0, 2026-05-28, read-only capture: *“the ws-equities `book` channel returns a FULL 20-level depth ladder (price+qty) for both TSLA/USD AND thin GLD/USD → execution venue is a CLOB, not RFQ. Depth IS the binding gate.”* **(3) OBJECTIVE ASSIGNED** — same file `:81`, *“O2 — Order-book depth plumbing (R2)”*, homed by Langston's condition 2 in `xstock_spot/imf-liquidity.ts`. **(4) NOT SUBSCRIBED** — `equity-spot-archiver.ts:237-247` subscribes to **`ohlc` + `ticker` only**; repo-wide search for a `book` subscription returns ZERO. ★ **And `imf-liquidity.ts:18-22` carries the confirmation AS A COMMENT while consuming one `xstock_spot_ticker_snap` row synthesised into a single level (`depth-source.ts:49-70`).** ⛔ **The record contradicts itself:** `P19_B4b_1_SCOPE.md:76` *“if Kraken equities EVER exposes a book channel”* vs `MULTI_ASSET_VTS_EXPANSION_PLAN.md:172` *“20-level depth ladder CONFIRMED”*. | ⛔ **BEFORE `F-G-2` (3c).** Owner CC-C. ★★ **WHY IT IS A PREREQUISITE — RE-ARGUED 2026-08-30 ON LANGSTON'S CONDITION 3, OFF THE WITNESS AND NOT OFF THE SEED.** ⛔ **THE `min_levels` ARGUMENT IS WITHDRAWN AS THE LEAD:** the seed comment at `2026-06-16-p19-b4b1-fill-depth-gate-seed.sql:45` **says out loud** *“xStock has only top-of-book”* ⇒ **rule-24 outcome (2), a DOCUMENTED ACCOMMODATION, not a defect** — and I named the wrong gate besides: `SYSTEM_MANUAL.md:696` and `depth-source.ts:36` put the depth gate **before an active OPEN**, *“blocks the open / penalizes the close”*, so **a non-discriminating ENTRY gate does not gate an EXIT-side batch.** *(`warmth_max_age_ms` 15s against a ~4s cadence, and `sufficiency_multiple` 2×, DO come out differently — those are the live legs, and they ride on the one below.)* ⇒ ★★ **THE PREREQUISITE ARGUMENT IS THE WITNESS: `depth-source.ts:89-93` states that the independent cross-check IS NOT INDEPENDENT ON xSTOCK — *“the fill's own depth-walk reads `xstock_spot_ticker_snap`, THE SAME TABLE this function reads … it CANNOT corroborate the price against a second feed.”* ⇒ F-G-2's xStock legs would move the exit decision onto a bid that NO SECOND FEED CAN CONTRADICT.** THE ORIGINAL WORDING FOLLOWS: F-G-2 moves the exit decision onto the **bid** — and on xStock that bid comes from a **one-level ladder synthesised from a ticker row we never discriminate by frame type**, which is exactly the instrument that would produce `#943`'s impossible print. **The real ladder that could contradict it is one subscription line away.** ⚠️ **Kyle's standing instruction governs the shape: *“we shouldn't be having any ridiculous spreads — root out the issue”*** ⇒ this batch is about **measuring the book we actually have**, not about a deviation threshold. ⛔ **NO FIX PRE-JUDGED (rule 15).** Provenance: `EXIT_PATH_MACHINERY_AUDIT_2026-08-30.md` §8.7 | ✅ **ROW-0 RE-PROBE DONE 2026-08-31T20:39Z, ON A LIVE MARKET (4,476 frames / 447 symbols in the preceding 5 min; the 08-30 probe's zero was a SATURDAY, not a finding).** **All four subscriptions ACK `success:true`; snapshot level counts per side: `TSLA/USD` **20/21** · `KRAQ/USD` **2/1** · `AMBR/USD` **2/2** · `SLMT/USD` **2/2**.** ⇒ ★★ **A REAL ~20-LEVEL LADDER WHERE A MARKET EXISTS, 1-2 LEVELS WHERE IT DOES NOT — so subscribing does NOT uniformly buy depth, and the thin names where fill modelling matters most are exactly where it buys least.** ⛔ **STILL GATING THE SCOPE: does `F-G-2`'s xStock half need DEPTH or only SIDE? If only SIDE, this row is not its prerequisite and drops to the tail.**
> | **3b.e** | **`B-XSTOCK-LIVE-FEED`** (`#950`) | ⛔ **THE xSTOCK FEED WAS BUILT AS AN ARCHIVE THAT WAS TO SHARE *NO STATE* WITH TRADING, AND BECAME THE TRADING FEED WITHOUT A DECISION.** `BATCH_74_SCOPE.md:37` scoped it as a passive archive **before xStocks were a trading asset class**, and `:93` said *“no shared state with FX5 / signal-orchestrator / VTS”*; B79.0a then wired the live scanner to read `equity_spot_ticker_snap` every 30 s. ⇒ **crypto reads the exchange; xStock reads our own archive.** The cause is forced — **Kraken has no public REST for xStocks** (`AssetPairs` returns zero; closed at B79.0k, re-verified 2026-05-15) — **but the inversion is nowhere declared as a design decision.** ⛔⛔ **AND B79 NAMED ITS SUCCESSOR AND TRIGGER:** `:391` *“Currently archiver is passive-only… **Critical question** — if VTS needs real-time, we need to build/extend a live-pricing-adapter for the equities WS endpoint”*; `:135` *“**B79.5** — live-pricing adapter for `wss://ws-equities.kraken.com` — **TRIGGERED by Phase 19 active-trading prerequisite**”*. **Phase 19 active trading is ON. The trigger fired. `B79.5` is not in the batch record.** | **AFTER `B-XSTOCK-FEED-SANITY` (3b.b) and `B-XSTOCK-BOOK-LADDER` (3b.d)** — both must characterise the defect before a feed is rebuilt on top of it. Owner CC-C. ⚠️ **Does NOT block `F-G-2`**: 3b.b and 3b.d are the prerequisites; this is the structural replacement they will scope. ★ **The asset-class divergence itself was deliberate and well-argued** (hours, volatility, microstructure, macro inputs, failure modes, sector correlation, tokenisation-vs-underlying) under the doctrine *“centralize the dispatcher; share the methods; vary only the DATA — never fork LOGIC”* ⇒ **the question this batch answers is which xStock asymmetries are the doctrine working and which forked LOGIC while claiming to vary only data.** Provenance: audit §8.9 |
> | **3b.f** | **`B-PRICE-AGE-TRUTH`** (`#951`) | ⛔⛔ **A RATE-LIMITER BRANCH RETURNS A CACHED PRICE AND THE CALLER STAMPS IT AS A FRESH VENUE READ.** `live-pricing-adapter.ts:624-631 (was `:582-586`, stale — corrected 2026-08-31 at Step 1)` — when the per-symbol REST cooldown blocks, `fetchFromKrakenRest` **returns `cached?.price`, a bare number with no age**; the caller `:540-550 (was `:496-505`, stale — corrected 2026-08-31 at Step 1)` stamps it `source:'kraken_rest'`, `producer:'kraken_rest_poller'`, **`observedAt: Date.now()`** beside the inline comment **`// a genuine venue read: observed now`** — **false on that branch** — and `kraken_rest` **passes the engine's `isKrakenVenueSource` actionability gate.** ⇒ **an arbitrarily old price has its age reset to zero and is then treated as actionable; every downstream staleness guard reads a manufactured timestamp.** ★ **The `#940` shape — witness right, witnessed thing wrong — defeating exactly the guards written to catch a stale mark.** ➕ **CARRIES `#952`: the v1 `c` field (*last trade closed*) is overwritten with `(bid+ask)/2` at `kraken-v2-translator.ts:52-58`, while `live-pricing-adapter.ts:47-49` and `kraken-websocket-adapter.ts:943-944` both describe it as *“a clean ticker PRINT”* ⇒ **the two `kraken_ws` producers differ by WHICH BBO, not midpoint-vs-print, so any check treating the ticker leg as a trade-print control was comparing two midpoints.** | **DECOUPLED FROM `F-G-2` — Langston ruling 2026-08-30, both stated grounds discharged, not one.** Owner CC-C, **no downgrade: this remains a real defect.** ★ **GROUND 1 discharged by the differential argument: a stale bid and a stale mid are stale IDENTICALLY, so the age defect does not bias F-G-2's arms and is not a confound for a which-SIDE comparison.** ★ **GROUND 2 discharged by `#952`'s narrowing: the PRINT control dies, the BID control survives on a separate socket returning raw bid/ask.** ⚠️ **A release resting on one of two stated grounds would have been a hole — both are named because both were claimed.** ⛔⛔ **CARVE-OUT THAT BINDS `F-G-2`'s DESIGN: F-G-2 may NOT use `observedAt`, `cachedAt` or any age-derived value as a SAMPLE FILTER OR COVARIATE in its before/after arms — if it does, this prerequisite REATTACHES BY CONSTRUCTION, because the fabricated clock would then be selecting the population. The F-G-2 scope must STATE which it is.** ⚠️ **RATE NOT RE-DERIVED (rule 29): the independent reader measured 995 blocked / 264 allowed over a busy 25-minute window; my own 10,613-line scan on a CLOSED-market Saturday gave 6 / 5 — enough to prove the branch is live and often taken, far too small to carry a proportion. First deliverable is an honest rate over a representative open-market window.** ⛔ **NO FIX PRE-JUDGED (rule 15) — and note the founding invariant F7 already prescribes an answer: *“if no real price is available, the operation must fail or wait.”*** Provenance: audit §9.3(b) | ⛔ **FIRST DELIVERABLE IS TWO COLUMNS, NOT ONE: (1) an honest laundering rate over a representative OPEN-MARKET window; (2) was there a CONCURRENT WS-WRITE GAP on that same symbol during the `REST_BLOCKED` burst — Langston's hypothesis is that the laundered entry competes for the slot precisely when WS is quiet, which is the same condition that drives the REST poll, so *“it usually loses the race”* cannot be assumed uniform.** ⏳ **STATUS 2026-08-31 — SHIPPED AND OPEN ON AN OBSERVATION WINDOW, NOT CLOSED.** Deployed `2af2e0bacc1430a6452559b83ba7d3be15adc7be` @ `11:30:47Z`, CI 4/4. **Step 4 APPROVED** (3 conditions applied) · **Step 8 CONFIRMED WITH CORRECTIONS.** ⛔ **CANNOT CLOSE: 0 post-deploy closes against 3 open positions**, so the token has not yet reached `closed_trades`. Record: `B_PRICE_AGE_TRUTH_COMPLETION_REPORT.md`. ✅ **DELIVERABLE (2) — THE CONCURRENT-WS-WRITE-GAP COLUMN — IS ANSWERED, AND IN THE STRONG FORM (Langston re-derived it):** post-deploy `ENGINE_WS_PRICE` = **982 lines, `ZEC/USD` ONLY, zero on all five other symbols.** ⇒ **the hypothesis that the laundered entry wins precisely when WS is quiet is CONFIRMED in the strong form: on five of six symbols the re-serve does not win a race — THERE IS NO COMPETITOR.** ⚠️ **Causal direction (position → subscription) is CORRELATION ON n=1 SYMBOL — the hypothesis to test, not a finding.** ◐ **DELIVERABLE (1) — the honest rate — MEASURED FOR CRYPTO ONLY AND THE SHAPE REPLACES THE RATE:** n=975 post-deploy re-serves form a **DETERMINISTIC 4-RUNG SAWTOOTH (14.3 / 29.3 / 44.3 / 59.3 s)** — observations land every 60 s, the serve fires every 15 s. Per symbol: `ZEC/USD` n=164 median **−0.5 s** (WS-fed, honest ≈0) vs five REST-only symbols median **29.3 s**, max **59.3 s**. ⛔ **A SINGLE MEDIAN IS A MIXTURE AVERAGE AND MUST NOT BE THE HEADLINE (Langston).** ⚠️ **NOT a representative OPEN-MARKET window for xStock — measured on a Sunday with xStocks closed; the xStock leg (`aee:1244`) is UNTOUCHED by this batch and UNMEASURED.** ⛔⛔ **BOUNDARY, VERBATIM, so this row is not read the other way: *“crypto exits are fed, xStock exits are unknown.”*** WS coverage of open positions survives a reconnect only via an **un-awaited** `i8cResubscribeAllOpenPositions()` with a swallowing `catch` (`:2743`) ⇒ **30 s worst-case repair window**, not unconditional coverage. ⚠️ **SIZING WARNING FOR 3b.f-b AND THE 15 s GUARD: 15 s lands INSIDE the densest rung — 261 of 975 readings within 0.7 s of it. Size off the sawtooth, never the median.** ✅ **CLOSED 2026-09-11 (Langston confirmed at Step 11): the window ended at the OBJ-7 deploy with the arm empty; PASS 2 re-scoped onto the runtime re-serve, the `closed_trades` leg retired with a stored-row tripwire at `3n` row `8g-bis`; completion report `Batch Completion/B_PRICE_AGE_TRUTH_COMPLETION_REPORT.md`.**
> | **3b.f-a** | ⭐⭐ **`B-OPENTRADE-REFRESH-LANE`** (`#977`) — Owner CC-C. **PLACED BEFORE `3b.f-b` BY KYLE, 2026-08-31, and the ordering is the point.** | ➡️ **ABSORBED INTO 3n r5 (2026-09-11) — D7: open positions enrolled in the 2-second lane; queued and held symbols carry their own subscription reasons (`Scope Files/PRICING_DECISIONS_2026-09-11.md`). The text below is the pre-absorption record.** ⛔⛔ **THE 2-SECOND REFRESH LANE BUILT FOR OPEN POSITIONS EXISTS, ITS LOOP RUNS EVERY SECOND, AND NOTHING IS SUBSCRIBED TO IT.** `price-cache.ts:59-62` defines four buckets — `openTrade` **2000ms**, `readyToBuy` 15000ms, `fx5Snapshot` 30000ms, `vtsSimulation` 60000ms — and `server/index.ts:394` calls `priceCache.initialize()`, which arms a **1s** `refreshBuckets()` loop (`:91-96`). **MEASURED, the service's own health line, every minute on staging: `open=0 rtb=2 fx5=0 vts=157 weight=0/10`.** ⇒ **two of four buckets are DEAD — and they are exactly the two serving the active trading path.** ⛔ **THE COLLISION, and the numbers are the same number: the exit monitor asks for 2-second freshness** (`active-execution-engine.ts:1257`, `getPriceWithFallback(symbol, 2000)`) **while the lane built to deliver 2000ms sits empty**, so the engine falls to `fetchFromKrakenRest`, whose floor is `restRateLimiter`'s **60s per-symbol cooldown** — **30x coarser than the design intent.** ★ **AND THE PREMISE THAT WOULD JUSTIFY THAT CONSERVATISM IS REFUTED ON EVIDENCE. WE ARE NOT EXCEEDING KRAKEN'S LIMITS AND NEVER WERE:** across five log windows, **58,236 limiter decisions, of which exactly ONE was token exhaustion** — every other block was the self-imposed cooldown; total Kraken REST volume **~4,853 requests / 14,134s = 0.34 req/s**; shared-cache budget reads **`weight=0/10`** all day. ⛔ **ZERO venue-side rate-limit rejections across two days, ON A PROVEN INSTRUMENT (`#453` discharged): Kraken errors DO reach `error__*.log` — 8 on 08-30, 17 on 08-31 — and ALL 25 are `EQuery:Unknown asset pair`. NOT ONE is a rate limit.** ⇒ the zero is EARNED, not an unvalidated silence. ★ **PRIOR ART, Langston-verified, NOT re-derived (`§9.5(b-ii)`): `mini-book-integrity-monitor.ts:121,:234` already records that `getTicker` -> `makePublicRequest` is a bare fetch that does NOT enter the price-cache budget but **COMPETES WITH IT FROM OUTSIDE** — measured here at **1,850 calls / 3h55m**. At least FIVE limiter modules exist and none shares a view of total load.** ⇒ **this is a COORDINATION defect, not a compliance one.** | **IMMEDIATELY BEFORE `3b.f-b`, AFTER `3b.f`.** ⛔⛔ **THE REASON FOR THAT SLOT: this may REMOVE the need for `3b.f-b` and reshape `XSTOCK_PRICING_PLAN` P1.** The 15s freshness guard was scoped against a **60s floor**, where refusal is the only available answer. **On a live 2s lane the guard may need no refusal path at all** — so ruling on refusal before this lands is ruling on a world that may not exist. ⚠️ **NOT A ONE-LINE SWITCH, and I will not scope it as one: the `openTrade` bucket has NEVER held a symbol in production, so its refresh path is UNEXERCISED; and the engine reads the adapter's PRIVATE map, not the shared cache** (the adapter WRITES to the shared cache at `:805`/`:1026` and READS it never). **Re-pointing the engine's read is the real work.** ⚠️ **`fx5Snapshot=0` is a SECOND dead lane, deliberately NOT folded in — it is the scanner's, not the exit path's; it gets its own disposition rather than padding this scope.** ➕ **TWO ITEMS ADDED 2026-09-06 (`#977` amendment 3; Langston ruled NO NEW BATCH — this row was opened on exactly these symptoms):** (a) **`open=0 / fx5=0 / weight=0/10` RE-MEASURED UNCHANGED** across six consecutive health ticks 10:50:06Z-10:55:06Z, so the original finding is live today; and (b) ⛔⛔ **THE ACTIVE LANE IS SERVED FROM THE VTS ISOLATION BUCKET** — `vtsSimulation` holds 180 of 185 cache entries, its own declaration (`price-cache.ts:91-92`) says it exists so *"VTS data isolation prevents simulation from affecting live trading cache"*, and **1,634 `active` crypto signal-birth reads were served from it in one 10-minute window.** ⚠️ **A TAXONOMY QUESTION, NOT A DECLARED DEFECT — the subscription decision must be read in code first, and NO consequence is claimed.** |
> | **3b.f-b** | **`B-PRICE-AGE-REFUSAL`** — Owner CC-C, **GATED on `#971`** | ➡️ **ABSORBED INTO 3n r5 (2026-09-11) — D7 REPLACES the blanket refusal: a re-serve or failed refresh never renews observation age or advances smoothing, and every action rechecks against D6; the limiter goes on the direct REST path (`Scope Files/PRICING_DECISIONS_2026-09-11.md`). The text below is the pre-absorption record.** ⛔ **CARVED OUT OF `B-PRICE-AGE-TRUTH` AT STEP 2, ON LANGSTON'S BLOCKER-1.** The honest `source` literal that makes a rate-limited re-serve NON-ACTIONABLE. **It cannot ship with its parent**: relabelling makes the engine gate at `active-execution-engine.ts:1277` fail, and its else-arm (`:1289-1300`) calls `krakenService.getTicker` → `kraken.ts:177` — **a bare `fetch` with NO limiter**, while `restRateLimiter.check` has exactly ONE production caller. ⇒ **the relabel would route exactly the rate-limited population AROUND the limiter whose purpose is ban prevention.** **Population: {open crypto position} × {WS quiet} × {block}; crypto opens are 0 today, so added volume ≈0 and scales with positions.** ⛔ **PRE-REGISTERED DECISION RULE, written before the measurement: this ships ONLY IF the else-arm first passes through `restRateLimiter` — a STRUCTURAL precondition, not a magnitude one, because routing around a ban-prevention limiter is not a thing to trade off against a percentage.** | **IMMEDIATELY AFTER 3b.f, and gated on `#971` landing the limiter leg.** ⚠️ **The split is deliberate: the parent ships the provenance half (age recoverable, no decision path changed) rather than parking a correct zero-behaviour-change fix behind an unscheduled architectural read.** ★ **Same boundary I drew for F-C — recoverability is not a bound — applied to my own batch.** Also carries **P4** (extend BOTH unions, do not ride the `as` cast — restored after being dropped from the rebuilt plan), **P6** (the measurement, explicitly NON-GATING for the parent) and **P8** (the literal's name against BOTH substring matchers). ⛔⛔ **THE GATE MUST NOT BECOME A PARK (Langston condition 3): if `#971`'s three-outcome read lands NO limiter leg, THIS BATCH ABSORBS THE LIMITER ROUTING AS ITS OWN FIRST ITEM.** Written now, not deferred. |
> | **3b.f-c** | **`B-XSTOCK-SESSION-FRESHNESS`** — ➕ **ENTRY-SIDE ITEM FOLDED IN 2026-09-04 (Langston routed, alert `16500abc`): xStock active fill blocked, ASTS/USD price 19,251 ms against a FLAT `active_fill_max_age_ms` = 15,000 ms.** ⛔ **Kyle’s 2026-09-03 ruling discharged the EXIT half only; the ENTRY side still uses a FLAT ceiling while the exit side uses a RISK-DERIVED one — that inconsistency is this row’s subject.** ⛔ **CORRECTED 2026-09-23: this read *"the alert row stays ACTIVE"*, and that was WRONG. The key is ONE for every xStock symbol (`xstock-stale-fill-block`), and an open row blocks every fresh mint on it (`system-alerts.ts:509`), so parking it mutes the stale-fill alarm for ALL xStocks, a genuine regular-hours stall included. RESOLVE each instance with evidence; never ack, never park.** (`16500abc` was resolved 2026-09-06.) ➕ **INSTANCE 2026-09-23 (Langston routed, alert `3d05a4cd-9c54-48d2-8899-bf4d1a7f9cbb`, resolved by him with evidence):** MARA/USD 28,150 ms against the flat 15,000 ms at 04:49 ET, pre-market. **His census on the key, reported in that alert's resolution evidence, not re-derived here: 71 rows 2026-07-15 → 09-23, 68 of them outside US regular hours.** Nothing new to decide: it is this row's entry-side subject plus its keep-the-emit, cut-the-notify split, and **there is no loosening without the evidence Kyle required on the exit side.** ➕ **2026-09-23 10:3xZ (Langston routed, alert `9d52b695-929b-45ad-a172-80312dc5d079`, resolved by him with evidence), an EVIDENCE item, not work:** his 72-row census on the key is **bimodal, with a hard floor at 30.0 min** (nothing between 4.5 and 30.0 min) and **0 of the 22 long-age rows inside 13:30-20:00Z.** ⚠️ **Reported, not re-derived.** His reading, a roughly half-hourly off-hours refresh of these names, is a **HYPOTHESIS** for this row's Step 2 to test at the feed. It bears on the entry ceiling: a flat 15 s limit against a source that may refresh every ~30 min out of hours cannot pass in those hours, by construction. ➕ **2026-09-23 13:50Z, a NEW datum, IN-SESSION (Langston routed, alert `3104646a-d6bd-4e46-b04c-fb15559c2bff`, resolved by him):** COPX/USD refused at **66,888 ms**, at 09:36 ET. **It is the first refusal inside US regular hours since 2026-08-14, and the largest.** His measurement, reported and not re-derived: the feed covers only **224-276 of 466 names per minute from 13:25 to 13:39Z**, reaches full coverage at **13:45Z**, and did the same on 09-22. ⇒ **The flat 15 s ceiling also refuses in the first ~15 min of regular hours, not only off-hours.** ✅ **HIS HYPOTHESIS, A SCHEDULED SUBSCRIPTION REFRESH ON OUR SIDE, IS REFUTED AT THE CODE (CC-C, same day):** (1) the fill gate reads its age from `xstock_spot_ticker_snap` (`active-dispatch.ts:74-85`), the same object he measured; (2) the recorder subscribes once per connection (`equity-spot-archiver.ts:316-325`), with no periodic re-subscribe; (3) no reconnect and no re-subscribe from 13:27:08 to 13:55:08Z: 0 subscribe lines in that window's log file, against a control of 2 in the 11:38 file, matching the 11:20 and 11:34 reconnects in `error.log`; (4) the writer's only drop is a 1 s per-symbol throttle (`ticker-batch-writer.ts:108-115`), not an unchanged-quote filter. ⇒ **per-minute coverage is what the venue SENT. The 9:45 ET restore is VENUE-SIDE, and at this row's Step 1 it is read from Kraken's xStock documentation BEFORE it is measured further (`vendor-docs-unread`).** ➕ **Langston's rider on alert `30a0a0cb-efb7-4c22-8554-90523cb83f0e` (2026-09-24), *"the reported age may be ceiling-capped near 32 min by how far back we keep prices"*: REFUTED at the object (CC-C, same hour).** The gate's age is `NOW() - MAX(captured_at)` (`active-dispatch.ts:74-85`), a read of the NEWEST frame, and retention deletes only OLD frames, so it cannot move that number. MDLZ/USD: body **1,919,308 ms** at `triggers_at` 03:08:19.432Z ⇒ newest frame **02:36:20.1Z**, which matches the stored frame at 02:36:20.107Z; the NEXT frame came at 03:11:18.823Z. So the full gap was **35 min** and the age read at 32 min is simply where in that gap the attempt landed. ⇒ **Not a cap.** Why off-hours ages cluster at 30-33 min is still OPEN, a question for Step 1 about WHEN entry attempts land inside the venue's quiet gaps. — **Owner CC-C** (his freshness lane; row PLACED by CC-B 2026-09-02 on Langston's alert routing, not taken) | **The xStock price-freshness ceiling is calibrated to regular-hours quoting and applied around the clock.** Alert `1d1573c7` (RIOT fill REFUSED, mark 55 s vs a 15 s ceiling, three minutes after the 16:00 ET close) and its exit-side sibling `b1f58a01` (MDT/USD EXIT CHECKS SKIPPED, mark 124 s vs 65 s). Langston's ledger census, 2026-09-02: **36 of the 39 fill-refusal occurrences since mid-July fired OUTSIDE US regular hours; the three inside were 16-23 s over the line; the feed was healthy — 479/479 tracked symbols still reporting through the session flip, RIOT's quote cadence dropping from every ~4 s to every 13-20 s the moment extended hours began.** Bug-taxonomy outcome (2), working-as-designed-but-UNADDRESSED: **what is missing is a DECISION, Kyle's — does the active xStock lane attempt entries outside the regular session at all, and what ceiling governs EXIT checks there** (refusing an open costs nothing; a skipped exit check leaves a position unwatched — the dearer side). Rule 17 applies: xStock trades 24/5, so "after hours" is thinner quoting, not a closed market. ✅ **READ AT THE CODE 2026-09-02 (CC-C, `origin/migration/aws-supabase`):** (a) **NEITHER gate knows sessions.** The FILL gate is `active-dispatch.ts:180-186` — `ageMs > safety.activeFillMaxAgeMs` (15 s) against the latest `xstock_spot_ticker_snap` row, and its RTH CLOCK gate was deliberately RETIRED by `#295` (*the clock was a proxy for "is the book deep enough"; it was wrong in BOTH directions*); the EXIT gate is `mark-staleness.ts` — `ceiling = clamp(budget_k × room-to-stop / σ_rate, floor_ms, cap_ms)`, live xStock knobs `floor 15 s · cap 300 s · budget_k 0.5 · σ window 30 min · σ max age 15 min · classwide percentile 0.90` (`mark_staleness`, `p19-b8.5e`), no session input (the file matches zero of `session|extended|market hours`); Kraken's own `is_extended_hours` flag is archived and read by NOTHING (`B_XSTOCK_FEED_SANITY_SCOPE.md` §15.2). (b) **TWO DIFFERENT GATES, same class:** fill = a DB-resolved age limit (`xstock_fill_safety.active_fill_max_age_ms`, currently 15 s — a settings row, NOT a constant; corrected 2026-09-03); exit = the risk-derived ceiling, 15-300 s. Off-hours quote cadence (13-20 s after-hours per Langston's census; 3-4 min pre-open on MDT) exceeds the fill limit routinely and exceeds the exit ceiling whenever σ is small — **freshness limits calibrated to RTH cadence, applied to sessions quoting 5-50× slower.** ⚠️ And `B_XSTOCK_FEED_SANITY_SCOPE.md` §15.6 records the flip side as a hypothesis: the skipped pre-market exit checks are what has kept the pre-market's 288 collapsed bids a day from producing ANY false stop-out — so 3b.b and 3b.f-c must be decided TOGETHER, not separately | **AFTER `3b.f-b`, inside the freshness cluster — a scope decision first, then whatever it implies.** ⛔ **Do NOT ack `1d1573c7` (dedupe-keyed — an ack silences every future regular-hours occurrence) or `b1f58a01`; resolve both only when the policy lands.** ⛔ **ONE ITEM, TWO CLOSE CRITERIA (Langston, 2026-09-02): entry-side and exit-side are different seams — (i) ENTRY: over a post-landing window covering at least one full extended-hours session, the fill-refusal alarm fires zero times outside the policy's own stated attempt window (ledger count, whole file); (ii) EXIT: over the same window, zero `exit checks skipped` rows on xStock symbols with the policy's ceiling in force, AND every skip that does occur carries the session it happened in. Neither alert resolves on the other's criterion.** Both ids cross-referenced on `#977` (amendment 2). ✅✅ **BLOCKER DISCHARGED 2026-09-03 — KYLE RULED ON BOTH QUESTIONS** (`#994` amendment 3, `acd2d1d2a`; relayed by CC-INFRA, who asked on my behalf under §4.7 and wrote nothing into this row). **RULING 1 — off-hours ENTRIES: YES, attempted on exactly the same bar as every other hour, no session gate in either direction. RULING 2 — EXIT freshness: the same standard round the clock, DO NOT LOOSEN** — *"if that means we don't get a chance to exit overnight … then so be it. We just hold that until we start getting fresher data"* — **with an evidence gate attached, which is the operative half: *"if we can PROVE that the risk is not increased by relaxing that during off hours, then I'm willing to listen."*** ⇒ **the EXIT half resolves to CONFIRMED-NO-CHANGE on trading policy** (`mark-staleness.ts` already carries no session term), and the only live piece on that side is the ALERT. **RULING 3 — holidays PARKED by Kyle explicitly; `#392` stays open and stays unplaced; do not re-scope it as a gap.** ⛔⛔ **AND THE RULING BREAKS BOTH CLOSE CRITERIA ABOVE — MINE TO FIX, FLAGGED THE SAME TURN I READ IT (CC-C, 2026-09-03). EACH ONE PRESUPPOSES A POLICY CHANGE THAT KYLE HAS NOW RULED WILL NOT HAPPEN, SO EACH IS UNSATISFIABLE AS WRITTEN — and, worse, each would read as a FAILING batch while the system does exactly what Kyle asked.** (i) names *"outside the policy's own stated attempt window"*; **the ruling says the attempt window is every hour, so there is no outside** and the criterion collapses to *zero fill-refusals ever*, which no un-relaxed gate can deliver. (ii) names *"with the policy's ceiling in force"*; **there is now no new ceiling**, and Kyle's stated intent is that skips SHOULD keep happening when data is stale — so *zero skip rows* is the opposite of the ruling. ⭐ **PROPOSED REPLACEMENTS, dispatched to Langston as author of the originals — the shape changes from *did the refusals stop* to *did the SHOUTING stop while the PROTECTION and its COUNT continued*:** **(i-r) ENTRY** — the entry-side asymmetry is real (exit scales with risk, entry is a flat clock) but a fix is EVIDENCE-GATED by Kyle's own bar, so the criterion becomes a pre-registered MEASUREMENT of what a risk-derived entry budget would admit, delivered before any mechanism is proposed — never a mechanism shipped on the argument that it is more consistent. **(ii-r) EXIT** — over the window: every exit-skip remains PERSISTED and COUNTABLE (cutting the emit would make the batch unclosable — Langston, amendment 2 correction 5); ZERO of them raise a breakage NOTIFICATION while feed-wide liveness shows the market merely quiet; and every notification that DOES fire coincides with a genuinely impaired feed, shown against the other tracked books as the control. ⚠️ **Suppression is never an ack — an ack is non-terminal and silences the key forever (`system-alerts.ts:388-389`).** ✅✅ **LANGSTON ACCEPTED (i-r) AND (ii-r) 2026-09-03 07:15Z AND VACATED HIS OWN (i)/(ii), WITH FOUR BINDING CONDITIONS.** **C1 — (ii-r) MAY NOT PASS ON SILENCE:** a window with zero false notifications AND zero genuine-impairment events is `INCONCLUSIVE-EXTEND`, never PASS (`#661` — a control proves capability only). Require in-window ≥1 notification against a genuinely impaired feed AND ≥1 persisted skip carrying its session label; either at n=0 ⇒ extend once, then REBUILD the criterion rather than pass it. **C2 — the quiet/impaired discriminator is PRE-REGISTERED, never adjudicated:** name the control books and write the predicate BEFORE the window opens, or post-hoc classification makes every notification *“impaired”* by construction and (ii-r) is unfalsifiable. **C3 — (i-r) registers an estimand and SUBSTITUTES THROUGH it before he sees numbers** — object, population, positive control, and an explicit arm for *no change warranted*; *“an algebraically degenerate estimand is indistinguishable from a measured zero”* (his own A2 retraction). **C4 — STATE PLAINLY THAT (i-r) LEAVES ENTRY BEHAVIOUR UNGATED FOR THIS CLOSE:** it is a delivered measurement, not an outcome bar, and must never be written up as an entry-side pass. ⛔⛔ **I CLAIMED THE RETROSPECTIVE ESTIMAND WAS NOT MEASURABLE. THAT WAS A FALSE ABSENCE AND IS WITHDRAWN — LANGSTON REFUTED IT AT THE OBJECT AND I VERIFIED HIS CITATION MYSELF.** The refusal PATH is indeed traceless (`_staleSkips` in-memory `:52`, GLOBAL `dedupe_key` `:96`, no persisted age — all correct); **the ATTEMPT is not.** `eval-cycle.ts:1179` `registerOpenVtsTrade` sits UNCONDITIONALLY, no branch, immediately above `:1185 dispatchXstockActiveSignal`. ⭐ **VALIDATED RECONSTRUCTION: attempt time = `vts_open_trades.inserted_at`; gate = `NOW() - MAX(captured_at)` on `xstock_spot_ticker_snap` (verified `active-dispatch.ts:74-85`).** **POSITIVE CONTROL PASSED ON ALL THREE OF LANGSTON'S PROBES:** `7526b48a` PCG stated 19,366 → 19,299 · `130aa417` RIOT 45,397 → 45,307 · `1d1573c7` RIOT 55,473 → 55,365. ⚠️ **C2 PROPERTY, STATED NOT FOOTNOTED: the residual is one-directional, 67-108 ms, so the reconstruction UNDERSTATES true age — the fail-safe direction — and a verdict can flip only within ~110 ms of the limit; the flip count ships beside the estimate.** ⛔ **My first control MISSED all three in both directions and I read that as proof no control existed — it was aimed at the alert's `fired_at`, which is MINT time (lag 38 s / 450 s / 726 s), not gate time.** ✅ **C3 discharged:** `xstock_fill_safety.active_fill_max_age_ms` = 15000, `updated_at` 2026-06-15 — PREDATING the window, so a write during it would have moved that stamp into it; positive evidence, not today's value painted backward. ✅ **C4 discharged for three of its four gates:** `system_context` paper row reads `is_engine_active`=true and `xstock_spot`=true with `updated_at` 2026-07-27 and `last_mode_change` 2026-07-16, both predating the window — **and `last_heartbeat` reads 2026-07-16, i.e. the table is NOT self-churning, which is what makes `updated_at` a witness rather than a clock**; the fill-safety rows existed throughout. ⚠️ **GATE 3 (`_noOrchSkips`) IS BOUNDED, NOT DISCHARGED** — in-memory + a `console.warn` into a log retaining hours, so its silence over three weeks carries nothing. Bounded by consequence: xStock opens landed on ALL TEN trading days in the window (3/8/1/4/2/3/3/7/4/4), so the handle existed at multiple moments daily; **residual is sub-day and travels WITH the denominator, in the same sentence as any rate.** Sequences behind `#992` (the depth gate reads one side only; loosening entry freshness first is the wrong order). ✅✅ **(i-r) DELIVERED 2026-09-03 — PRE-REGISTERED VERDICT: INCONCLUSIVE, NOT A PASS.** Criterion + instrument committed UNRUN at `0879666fa`; result at `678d91781` §12 of `Scope Files/B_XSTOCK_SESSION_FRESHNESS_ESTIMAND_REGISTRATION.md`. **705 in-window attempts · 55 refused by the 15,000 ms clock · D = 36** (pre-F-G-1 32, post-F-G-1 4, split per Langston C5). ✅ **C2 discharges OUTRIGHT: ZERO attempts within 500 ms of `L`, so the 67-108 ms offset flips nothing — a measured zero, not a small number.** ⛔ **TWO INDEPENDENT REGISTERED ARMS RETURN NO-CHANGE-WARRANTED: arm (c) — 32 of 36 in D rest on a CLASSWIDE σ, 88.9% against a registered 50%; arm (a) — the post-deploy arm alone is n=4 against a floor of 30. THE §5 RISK TEST WAS NEVER REACHED and must not be reported as passed or failed.** ⭐⭐ **THE FINDING, AND IT IS STRUCTURAL: THE CONDITION THAT TRIGGERS THE REFUSAL IS THE CONDITION THAT STARVES THE σ ESTIMATE.** Admitted-by-clock attempts are 93.5% σ-eligible (median 390 obs); REFUSED attempts are **9.1%** eligible (median **43**), only 5 of 55. ⇒ **a risk-derived entry budget is structurally LEAST able to be symbol-specific precisely on the attempts it would newly admit.** ⚠️ **My first explanation — off-hours cadence starving the 200-obs floor — was REFUTED by measurement: 95.6% RTH and 62.7% off-hours attempts CLEAR the floor.** ★ **And the existing design already resists it: the classwide fallback is the 90th percentile, so a high σ gives a SMALL ceiling, and since `floor_ms` = `L` a classwide σ collapses the ceiling toward the entry limit and shrinks D toward empty by construction.** ⛔ **PER LANGSTON C4 THIS LEAVES ENTRY BEHAVIOUR UNGATED FOR THIS CLOSE — a delivered measurement, never an entry-side pass.** ⭐ **The bar on any FUTURE proposer is now HIGHER than this batch set out to test: not merely that movement stayed inside the budget, but that a budget computed from a CLASS-AVERAGE σ means anything on the symbols it would admit.** ⛔⛔ **(ii-r) IS FOLDED OUT OF THIS ROW — LANGSTON RULED *FOLD* 2026-09-03 07:55Z. IT GOES TO `#526` / `B-VENUE-QUIET-ALERTING`, OWNER CC-B; THIS ROW KEEPS A STATED **DEPENDENCY**, NOT A NARROW SLICE.** ⭐ **WHY I STOPPED BEFORE STEP 1 RATHER THAN AFTER IT:** the §9.5(b-ii) ledger search found `#526`, OPEN since **2026-07-17**, already carrying the measurement (7 alerts overnight 07-16/17, all the 40-skip signature, every one stale-by-recovery), the correct diagnosis (*“the rail itself is CORRECT; the defect is alert ECONOMICS”*), three candidate designs and a named home. ⇒ **`#994` IS NOT A NEW FINDING — IT IS KYLE INDEPENDENTLY RE-REPORTING `#526` SEVEN WEEKS LATER, which is the strongest evidence the noise reached him, and Langston directed that the priority argument go in CC-B's entry IN THOSE WORDS.** ⛔ **HIS DECISIVE REASON: *a “narrow slice” of an alert-economics policy is a second alert policy* — two arms deciding independently when a per-symbol quiet event pages is the two-sources-of-truth failure, built by two sessions who each think theirs is the general case.** **BINDING CONDITIONS ON THE FOLD:** (1) it must not PARK the directive — the batch gets a POSITION in the plan, never a date, and if CC-B cannot place it this week that is a Kyle escalation for ORDERING, **not a licence for me to build it in parallel**; (2) **the emit STAYS and the persisted row keeps its session — what changes is DELIVERY, not EMISSION** (cutting it makes (ii-r) unsatisfiable by construction), and **suppression is NEVER an ack**; (3) the COHORT discriminator is CC-B's design input, handed over, **plus Langston's mirror — the cohort test is only as good as the cohort's own liveness, so the design must say what it does when the COMPARISON SET is thin** (measured and handed over: 62.7% of off-hours attempts still have ≥200 ticks in the prior 30 min, so the cohort is NOT thin overnight); (4) `#572` stays adjacent and unfolded UNLESS the design mutates a long-lived alert's body semantics; (5) **CC-B writes the ledger entry — I have written nothing into `#526` and will not.** ⚠️ **Feed-LEVEL silence cannot separate a shut market from a broken feed, so `#594`'s `lastDataMsgAt` does NOT answer this despite sounding like it should — and the calendar is PARKED by Kyle (`#392` stays open and unplaced; do not re-scope it as a gap).** ⛔⛔ **ALERT DISPOSITION REVERSED 2026-09-03 10:39Z — ALL FIVE RESOLVED, AND *“LEAVE ACTIVE SO THEY KEEP SURFACING”* IS FACTUALLY BACKWARDS.** `system-alerts.ts:480-491` (header `:444-450`), re-derived at the ref: **`addAlert` finds any same-key row with `state !== 'resolved'` and RETURNS IT, WRITING NOTHING.** ⇒ **an ACTIVE row suppresses new raises exactly as an acknowledged one does; ONLY `resolved` frees the key, and re-surfacing REPLAYS THE FROZEN BODY while minting nothing.** ⚠️ **MEASURED COST: `b1f58a01` held its key 24h+; `1d1573c7` holds `xstock-stale-fill-block`, which is GLOBAL — every stale-price fill refusal across all 479 xStock symbols minted nothing for 14h.** All five HAD resurfaced (MDT 2×, rest 1×), replaying obsolete numbers — which is the *“302 s old”* figure Langston saw six hours stale. ✅ **WHAT DECIDED IT, CHECKED BEFORE ACTING RATHER THAN ASSUMED: (ii-r) DOES NOT DEPEND ON THOSE ROWS.** The `PRICE_SKIP_ESCALATION` line is a `console.error` at `aee:391`, emitted BEFORE the `addAlert` call and NOT deduped — **118 in yesterday's archive against 4 alert rows** — and `error.log` reaches 14 days. Resolving costs nothing evidentially; the fired rows persist. ⛔ **HONEST DEFECT, NAMED: `resolve` MEANS FIXED AND THIS IS NOT FIXED.** A terminal verb used for a non-terminal reason because the tooling has no third state — `#982`, owner CC-A. **Every evidence string says so in plain words.** ⇒ **Reverses Langston's own do-not-resolve tags, on the mechanism rather than on preference, stated to him before acting; undone by the condition recurring, and I re-raise on request.** ✅✅ **POSITIVE CONTROL ON THE REVERSAL — PREDICTION AND MEASUREMENT EACH QUOTED AT THEIR SOURCE, IN ORDER, BECAUSE A CONTROL IS WORTH MORE THAN THREE OF US RE-READING THE SAME LINES ONLY IF THE PREDICTION IS RECOVERABLE IN ITS PRE-CONFIRMATION FORM (Langston's ask).** **(1) PREDICTION, `/var/log/cc-discord-inbox.jsonl` @ `2026-09-03T10:41:11`, author ANALYST Claude, verbatim:** *“What it regains: new occurrences mint again, with current numbers, on both the per-symbol keys and the global one.”* **(2) MECHANISM AT A REF @ `f7151df68`, committed `10:43:05Z`:** *“ONLY `resolved` frees the key, and re-surfacing REPLAYS THE FROZEN BODY while minting nothing.”* **(3) OBSERVATION, `/var/log/dawntrader/system-alerts.jsonl`: `efe7dca6` fired `11:01:42Z`** — **20m31s after the prediction, 18m37s after the ref** — a NEW id on the SAME key `price-skip-paper-NEM/USD` ⚠️ **HONEST LIMIT ON (1): the forward prediction is in the CHANNEL LOG, not at a git ref. The ref carries the MECHANISM it follows from, not the forward sentence. The channel log is append-only on Helsinki and not editable by me, which is why it is worth citing — but it is a weaker record than a commit and I am not calling it one.** ✅ **RE-DERIVED BY ME, ALL FOUR: the mechanism at the ref, the prediction timestamp read from the inbox log itself rather than from my memory of sending it, the observation from the alerts file, and the counts below. Langston has re-run none of them.** — a NEW id on the SAME key as `6339b2d9`, which I resolved at 10:40Z and which had fired 09-02T20:45.** ⇒ **before the resolve this occurrence would have returned the 14-hour-old row and written nothing; the key freed, it minted with its own body.** ★ **The reversal is confirmed by the thing it PREDICTED rather than by three of us re-reading the same lines — a control I could not have run before acting.** ⛔⛔ **BOTH OF MY TREADMILL FIGURES ARE WITHDRAWN. THE 1-4/day BOUND IS FALSIFIED, AND THE POPULATION I STATED WAS FALSE AT THE OBJECT (Langston, 2026-09-03; every number below re-derived by ME, and my series reproduce his exactly).** ⛔ **(a) POPULATION FALSE:** I wrote *“the retained `error__*` archives — five COMPLETE content-days”*. **Fourteen are retained (content 08-20→09-02). I measured 5 of 14 and described the subset as the retained set.** ⛔⛔ **(b) AND MY CAVEAT NAMED THE WRONG EXCLUSION, WHICH IS WORSE THAN NO CAVEAT — IT MADE THE POPULATION LOOK CHARACTERISED.** I said the glob *“skipped the weekend files”*. **08-29 and 08-30 ARE the weekend and BOTH carry escalations (BABA + PLTR, 2 symbols each). The real gap was 08-20→08-26.** ✅ **FULL 14 DAYS — escalation lines / distinct symbols per content-day:** 08-20 47/3 · 08-21 38/1 · 08-22 6/1 · **08-23 0/0** · 08-24 46/2 · 08-25 35/1 · 08-26 89/3 · 08-27 32/2 · 08-28 47/1 · 08-29 6/2 · 08-30 4/2 · 08-31 14/3 · 09-01 54/2 · 09-02 118/4. ⛔ **THE FLOOR IS ZERO, NOT 1 — 08-23 had no escalation anywhere.** ⛔⛔ **(c) THE DEEPEST ONE, AND IT IS THE LESSON: THE QUANTITY WAS DIRECTLY COUNTABLE IN `system-alerts.jsonl` THE WHOLE TIME, AND BOTH OF US REASONED ABOUT IT FROM LOG LINES INSTEAD.** Mints per day on `price-skip-paper-*`, counted from the ledger: 08-20 **7** · 08-21 0 · 08-24 6 · 08-25 3 · 08-26 5 · 08-28 0 · 08-29 6 · 08-30 2 · 08-31 2 · 09-01 1 · 09-02 **9** · 09-03 4 (partial); global `xstock-stale-fill-block` 0-2/day. ⭐ **“At most one mint per symbol per free-key period” HOLDS — but the free-key period is FAR SHORTER THAN A DAY because we resolve repeatedly: DD/USD minted 8 in one day, RIVN 7, WM 6, MDT 6 on 09-02.** ⛔ **And the converse falsifies the other endpoint: 08-27 and 08-28 carried 32 and 47 escalation lines and ZERO mints — key still claimed.** ⇒ ⛔ **NEITHER BRACKET ENDPOINT IS THE RATE. THE MINT RATE IS SET BY OUR OWN RESOLVE CADENCE AND IS A MEASURED 0-9/day, NOT 1-4.** ⭐⭐ **STRUCTURAL SHAPE, and it is the useful part for `#526`: THE TREADMILL IS ONE OR TWO PERSISTENT NAMES AT A TIME, ROTATING.** `TGT/USD` escalated on EIGHT days spanning 08-20→08-28 — *(Langston said nine consecutive and challenged my eight as reading an UNOBSERVED day as an observed zero — **his rule is right and his fact was wrong, settled with two controls: on 08-23 the instrument logged **227,222 lines including 57,570 `EQUITY_MARK`**, so it was demonstrably alive; and `TGT/USD` held ONE position `08-19 13:49:37 → 08-29 00:15:01`, so it WAS held that day. Instrument alive + position held + zero escalations = an OBSERVED ABSENCE. Eight stands.**)* — then vanished entirely. Then BABA+PLTR own 08-29→08-31, then DE/MDT, then the CTVA/MDT/NEM/PANW cluster. ⇒ **a design sized against “many names occasionally” is sized against the wrong shape, and a 5-day window catches only the tail of whichever name is currently running.** ⛔⛔ **AND MY OWN 0-9/day MINT RATE IS *ALSO* A SUPPRESSED COUNT — THIRD-ORDER, FOUND BY CC-B, RE-DERIVED BY ME.** `price-skip-paper-*`: **3 minted in the 24h BEFORE the 10:40Z resolves (0.12/hr); 3 minted in the 43 min SINCE (4.19/hr) — a 35× difference.** ⇒ **every historical count in this row, mine and Langston's alike, was measured while the keys were mostly CLAIMED. The 231 all-time rows are what SURVIVED suppression, not what occurred.** ✅✅ **THAT IS WHAT THE REVERSAL ACTUALLY BOUGHT, and it is worth more than the visibility: the true recurrence rate was UNMEASURABLE while the keys were held, and freeing them measured it in 43 minutes.** ⛔⛔ **BUT IT ALSO KILLS “RESOLVE EVERY RECURRENCE” AS A POLICY: 4.19/hr is ~100 manual resolves a day, indefinitely. I told the crew 1-4/day. THAT WAS WRONG BY AN ORDER OF MAGNITUDE and it is withdrawn.** ⭐⭐ **REFINED POLICY, ON A DECISION RULE RATHER THAN A PREFERENCE — BLAST RADIUS × RATE, and the two key types sit at OPPOSITE CORNERS:** **(a) THE GLOBAL KEY `xstock-stale-fill-block` STAYS FREE — resolve each recurrence.** Rate 0 in 43 min post-reversal, 1 in the prior 24h, 39 all-time ⇒ ~1/day, sustainable; and when claimed it blinds **ALL 479 symbols at once**. ⛔⛔ **(b) REVERSED WITHIN THE HOUR — PER-SYMBOL KEYS STAY **FREE** FOR ONE FULL 24-HOUR CYCLE. And BOTH numbers I built (b) on are WITHDRAWN (CC-B declined them, and they were derived from HIS OWN finding, which is why he had to).** **(i) `4.19/hr` IS FALSE PRECISION:** n=3 over 0.72 h gives a Poisson 95% interval of roughly **0.9-12/hr**. **(ii) THE `35×` IS A MEASUREMENT-REGIME CHANGE, NOT A RATE CHANGE** — the denominator window had its keys blocked BY DESIGN, so nothing about the world changed at 10:40Z, only our ability to record it, **and a multiple asserts the former.** **(iii) `~100/day` COMPOUNDS BOTH WITH A DAYPART ERROR, and that one is measurable rather than arguable** — re-derived by me over `price-skip-paper-*`, whole file, **n=231, by New York daypart: regular 09:30-16:00 **15 (6.5%)** · after-hours **97 (42.0%)** · overnight **77 (33.3%)** · pre-market **42 (18.2%)** ⇒ **93.5% OUTSIDE regular hours.** A rate sampled at 11:00Z (pre-market) CANNOT be multiplied by 24. ★ **And the COUNT is suppression-contaminated while the TIMING is NOT — a suppressed occurrence still had its spell-start recorded.** **(iv) The rate SCALES WITH HOLDINGS** (2-3 positions open in that window), so it is not a property of the system alone. ⛔⛔ **AND THE CONFLICT THAT DECIDES IT, WHICH NEITHER OF US NAMED AT FIRST: MY “LEAVE THEM CLAIMED” POLICY WOULD FORECLOSE THE EXACT MEASUREMENT THAT SETTLES IT.** CC-B needs **one full 24-hour cycle with keys FREE, bucketed by daypart, with the open-position count stated** — and claiming the keys makes that unmeasurable, by construction, in the same way the last three weeks were. ⇒ **A policy resting on a withdrawn number must not be the thing that prevents the number being measured.** ✅ **THEREFORE: per-symbol keys stay FREE and I resolve recurrences for ONE 24-HOUR CYCLE, ending ~2026-09-04 11:00Z, purely to enable that measurement. Cost accepted and UNKNOWN, bounded only by the Poisson interval. Then (b) is re-decided ON THE DATA.** ⚠️ **Until it lands, the design sizes on SHAPE — per-symbol fan-out, benign recurrence, quiet-hours concentration — and NOT on any rate, mine included.** ~100/day if kept free, blast radius is ONE symbol, and **the `PRICE_SKIP_ESCALATION` log line carries the evidence regardless** (not deduped, 14-day reach). ⚠️ **SO THE TWO CURRENTLY-DUE ROWS (`0aadd37a` LI, `65e665cf` NEM) ARE LEFT ACTIVE DELIBERATELY — documented, quantified suppression, NOT the accidental blindness of this morning. The difference is that we now know its rate and its cost.** ⛔ **This REFINES the 10:40Z reversal, it does not repudiate it: the reversal was right to free the global key, and right to make the per-symbol rate measurable — which it was not, by construction, while the keys were held.** ⭐ **IT ALSO SHARPENS WHAT THE SUPPRESSION MUST BE: not *‘don't raise’* — the raise is what makes the recurrence visible at all, and cutting it re-creates the 14-hour hole from the other side. DELIVERY is the noise, EMISSION is the evidence, and the current tooling can only switch both together** (Langston's binding condition, now with a control under it). ➕ **FOLDED IN 2026-09-06 (Langston routed, ALERT `5b2849e0-e586-4fa5-80a7-d851ad7c5702`, `Exit checks skipped — mark older than ceiling for SLV/USD`, owner CC-C):** this is the class Kyle ruled on 2026-09-03 (`#994` am. 1-3). ⛔ **THE EXIT-SIDE DISPOSITION IS *CHANGE NOTHING* — the same freshness standard round the clock, relaxation evidence-gated, burden on the proposer.** ⇒ **KEEP THE EMIT, CUT THE NOTIFY** — which is exactly the delivery-vs-emission split this row already carries, now with a named instance behind it. ⚠️ **NOT ACKED: an ack SILENCES an event-wait alert and there is no unack verb (`#982`), so acking it to signal ownership would destroy the signal.** Ownership was stated in-channel instead. ⛔ **STRUCK 2026-09-23: *"it stays active until the fix lands"* was WRONG. An open row blocks every fresh mint on its key (`system-alerts.ts:509`), so parking it would have muted SLV's next skip alarm. Resolve with evidence.** (`5b2849e0` was resolved 2026-09-09.) | ➕ **ITEM FOLDED IN 2026-09-13 (§9.4 — 2), SURFACED BY LANGSTON'S `8a-P1` BLOCKER-1 AND RE-DERIVED BY ME AT THE OBJECT: `hollowSkips` ON EVERY `active_open_positions` ROW UNDERCOUNTS BY ROUGHLY 10x.** `checkOpenPositions` RE-FETCHES the position every cycle (`aee:1252` -> `storage.ts:3772-3776`, a bare `db.select()` with no cache), so `_recordBookStateEvent:482` reads `hollowSkips` off the **PERSISTED** row while `:486` persists only on `streak === 1 || streak % 10 === 0` => **the stored value advances once per FLUSH, not once per event.** &#9989; **CONTROL, which is what makes this a measurement rather than a reading of the code: `yields` is UNAFFECTED, because every yield flushes (`:486`'s first disjunct).** &#9888; **CONSEQUENCE FOR THIS ROW'S OWN EVIDENCE: any count of hollow skips taken off a position row - including in this batch's record - is low by about an order of magnitude and must be re-derived from the log line, which fires per event.** &#9940; **NOT folded into `8a`: a batch that both depends on a counter and repairs it grades its own dependency (`3n.f`'s reasoning, applied to me).** ➕ **TWO CORRECTIONS TO THIS ROW'S OWN PREMISE, FOLDED IN 2026-09-14 FROM A LIVE EPISODE IN WHICH I GOT THE INSTRUMENT WRONG THREE TIMES.** ⛔ **(a) THIS ROW DOES NOT NEED TO BUILD A DISCRIMINATOR - IT NEEDS TO READ THE ONE THAT ALREADY EXISTS** (Langston, re-derived by me at the object). The `[B74][equity-spot]` liveness line already carries `last_data_msg_age_ms` and `rows_persisted_60s` ON THE EQUITY SOCKET - the class's OWN feed. &#9888; **Every substitute I reached for failed: the CRYPTO feed cannot speak for the equity feed (different socket, different channel); and "connected + heartbeats + no data" DOES NOT DISCRIMINATE AT ALL, because `channel:heartbeat` flows regardless of subscription state - it is the SAME signature a shut venue produces.** ⛔ **(b) A NAMED NEGATIVE CONTROL, AND IT WOULD HAVE FOOLED THE OBVIOUS PREDICATE: `rows_persisted_60s = 468` IS THE SUBSCRIBE-TIME SNAPSHOT, ONE ROW PER SYMBOL - NOT LIVE TICKS.** It appears in the minute after EVERY subscribe, alongside `age ~58,268`, and a `rows > 0 => alive` test reads HEALTHY at `00:01:18` while the feed was dead for the next twelve minutes. **LIVE is 2,000-2,800 per 60 s against 475 subscribed.** &#8658; **the predicate must require a SMALL AGE ON THE NEXT POLL, or rows sustained across >= 2 consecutive polls. One poll cannot tell a subscription from a feed.** &#9888; **AND IT CAUGHT MY OWN CONTROL: I read a 468-row window as "the shut market, one row per symbol" - directionally right, mechanically wrong, and right for a reason I had not established.** &#11088;&#11088; **AND THE `inLiquidWindow` CONTRADICTION IS RESOLVED AT THE CONFIG - IN NEITHER DIRECTION IT WAS POSED (2026-09-14). THERE IS NO CALENDAR BUG.** Langston framed it as *"either the detector's liquid-window calendar doesn't cover the Sunday open, or the open isn't 20:00 ET"*. **BOTH BRANCHES ARE FALSE, because the field was never about the 24/5 open.** `fill-safety-config.ts:29-36` states its own scope: `liquidFillWindowOpenMinEt`/`CloseMinEt` are **US REGULAR TRADING HOURS**, *"NO LONGER an active-fill gate - RETAINED ONLY to select the silent-stall watchdog's RTH-vs-off-RTH reconnect threshold"*. **MEASURED LIVE: open=570, close=960 minutes ET = 09:30-16:00.** &#8658; **at 20:13 ET on a Sunday `inLiquidWindow=false` IS CORRECT, and its only consequence is picking 750,000 ms (off-RTH) over 75,000 ms (RTH).** &#11088; **THE REAL FINDING IS A NAMING ONE AND IT IS SMALL, DURABLE AND ALREADY EVIDENCED TWICE: a field whose NAME invites a reading its DEFINITION does not support, PUBLISHED IN AN ALERT BODY WHERE THE READER HAS NO ACCESS TO THE DEFINITION. Langston and I misread it in the same hour, in the same direction.** &#9940; **AND THE NOISE COMPLAINT SURVIVES ONLY IN A NARROWER FORM, WHICH IS THE USEFUL ONE: the watchdog knows TWO states (RTH / off-RTH) for a THREE-STATE world - RTH, off-RTH-but-24/5-OPEN, and off-RTH-and-WEEKEND-CLOSED.** The weekend gets the off-RTH threshold of 750 s, so ordinary weekend silence trips a CRITICAL. **That is the `#994` requirement stated as a mechanism rather than a wish, and it is what this row must fix.** &#11088;&#11088; **TWO MORE DESIGN INPUTS, 2026-09-14, BOTH FROM LANGSTON'S INDEPENDENT CHASE OF THE SAME NIGHT - AND THE FIRST IS A STRUCTURAL DEFECT IN THE CEILING ITSELF, NOT IN THE ALERT.** &#9940;&#9940; **(c) A BURSTY SYMBOL VIOLATES A CONTINUOUS-FEED CEILING BY CONSTRUCTION.** MDB's tokenised share prints in CLUSTERS - quotes every 4-7 s, then gaps of **45-90 s with nothing** - against a trust window of **25 s**. &#8658; **IT FALLS OUTSIDE THE CEILING ROUTINELY AND CORRECTLY, AND THE EXIT MONITOR HOLDS, WHICH IS KYLE'S 09-03 STANDARD WORKING.** But the ceiling was sized as though arrival were CONTINUOUS, so for a bursty symbol *"stale"* means *"between bursts"*, not *"impaired"*. &#11088; **THAT IS A THIRD STATE THE GATE CANNOT SEE, distinct from the RTH/off-RTH/weekend triple in (the note above): a per-symbol ARRIVAL SHAPE.** A ceiling that is one number per class cannot express it. ⇒ **this row must state whether the ceiling becomes arrival-shape-aware or whether bursty symbols are simply expected to hold between bursts - and say which, rather than leaving the alert to carry the question.** &#9989; **(d) SCOPE DISCIPLINE ON THE SILENCE NUMBERS, because two true statements about the same night disagree until you name the object.** The stall COUNTER read **22,500 s** - anchored on the socket-generation reconnect. The ACTUAL market silence was **~47 h**. **MEASURED by Langston across every one-minute health line in the window: 763 lines, 761 reporting ZERO rows persisted; the only writes were the one-off snapshot per reconnect** - which is (b)'s trap, observed at scale. &#11088; **AND THE CONTROL THAT MAKES IT A MEASUREMENT: Friday overnight, a NORMAL closed-market night, still stored 20,000-50,000 rows/hour. Saturday and Sunday stored almost nothing.** &#8658; **a market being shut, not a line going down** - and the discriminator is the ADJACENT CLOSED NIGHT, not the open one. &#10133; **(e) THE ENTRY HALF, FOLDED 2026-09-14 (Langston at the `8a-P1` approval; alert `7d1ec6d6`) - AND IT IS THE SIDE KYLE'S 09-03 RULING ACTUALLY LEAVES OPEN.** His ruling (1) says ENTRIES take NO session gate; the exit side does not loosen off-hours. &#9940; **SO THE INCONSISTENCY IS NOT THAT ENTRIES ARE GATED - IT IS THAT `active_fill_max_age_ms` IS A FLAT 15,000 ms (set 2026-06-15) WHILE THE EXIT CEILING IS RISK-DERIVED PER SYMBOL.** One side of the same feed is judged by a constant and the other by a computed ceiling, so the two can disagree about whether the identical price is usable. &#8658; **THIS ROW NOW OWNS BOTH HALVES OF THE OFF-HOURS BLINDNESS - the EXIT skips (`dc6de017`, `8b122d28`), the WATCHDOG threshold (`e2d5d778`), and this ENTRY block - because they are ONE MECHANISM and splitting them would put it in three rows.** ➕ **2026-09-25 22:0xZ — CENSUS UPDATED AND A SECOND POPULATION FOUND (Langston, alert `21b1dbbe`, resolved by him; every number below RE-DERIVED by CC-C from the whole `system-alerts.jsonl`, all 84 stale-fill rows 2026-07-15 → 09-25).** **84 rows: 79 outside US regular hours, 5 inside** (inside ages 16.4 / 19.3 / 22.6 / 66.9 / 87.4 s, all ≤ 1.5 min). This supersedes the 09-23 figure of 71 / 68 above, which stands as that day's count. ⭐ **THE AGES FORM TWO POPULATIONS WITH NOTHING BETWEEN THEM:** 59 rows from 15.1 s to 269.7 s (4.5 min), **then no row at all from 4.5 to 30 min**, then 25 rows from **1,800.738 s** to 2,507.9 s (30.0 to 41.8 min). **MECHANISM LEAD — A HYPOTHESIS, cited, not yet a finding (rule 29(c), rule 24):** the xStock scanner reads bid and ask for its spread filter from `xstock_spot_ticker_snap` with `captured_at > NOW() - INTERVAL '30 minutes'` (`scanner.ts:647`). A symbol with no row in that window gets the sentinel `-1`, and the spread filter SKIPS the check on `-1` (`global-filter.ts:176-180`, *"data unavailable … skip check"*). The comment calls this deliberate (*"we're enriching, not gating"*, `scanner.ts:627-629`). ⇒ **A symbol whose last quote is 4.5-30 min old carries its real (off-hours, often wide) spread and is filtered out; one whose last quote is just OVER 30 min old passes the filter on missing data, is selected, and is then refused by the 15 s fill guard at an age just over 30 min.** The floor of 30 min + 0.738 s is what that predicts when the fill check follows the scan by under a second. **TEST RUN, 11 of 25 readable:** the last visible spread before each refusal was 17.8 / 0.75 / 6.2 / 14.3 / 4.2 / 1.8 / 4.5 / 0.71 / 3.4 / 4.6 / 1.7 %, so **9 of 11 exceed the 1.00% threshold most paper active xStock paths carry** (`screener_filters.max_bid_ask_spread`; `active_breakout` 0.50) and would have been filtered had their data been inside the window. ⚠️ **Reach:** the 14 rows before September have no ticker rows left (retention), so they cannot be tested. ⚠️ **Not shown:** that the scan admitted each on the sentinel. That needs the scan's own record, which is the next read. **COST IF IT HOLDS:** no bad fill (the fill guard refuses every one), but each is a SELECTED signal that could not fill, so that cycle's pick was a symbol with no live price. **Bug-taxonomy outcome (2), working as designed but unaddressed:** missing data skipping a filter is written as intended; whether it should instead fail closed is a design decision for this row, not a fix made here. **DISPOSITION: folded into this row as the entry side's second population, per Langston's routing. No code change.** ➕ **2026-09-25 23:1xZ — AN AFTER-HOURS ENTRY REFUSAL, MEASURED (Langston, alert `61ce5c4f`, resolved by him 23:05:57Z; re-derived by CC-C at the table).** RIOT/USD refused at `22:54:58.628Z` at **15,178 ms**, 178 ms over the flat 15,000 ms. Its own cadence, 22:25-23:05Z: **22 snapshots, 19 of 21 gaps over 15 s, mean gap 102.1 s, longest 380.1 s.** The feed was alive in the refusal minute: **385 rows across 124 symbols.** ⇒ Bucket 2 (the symbol's own quote gap), not a feed stall: after hours this name quotes about every 100 s, so a flat 15 s limit refuses it most of the time whatever the risk. Entry-side evidence for the flat-versus-risk-derived question above. **No loosening without the proof Kyle required on 2026-09-03.**
> | **3b.f-d** | **`B-OBS-WINDOW-EVIDENCE-CAPTURE`** (`#1044`) — Owner CC-C (placed 2026-09-11 on Langston's `#943` closing read, §13) | ⛔ **An observation window that qualifies its evidence from a size-rotated log must capture that evidence at the event, not at the read.** `#943`'s two graded arms went INCONCLUSIVE because the `out.log` side was never extracted after each handoff (§4d specified it) and `out.log` retention halved without warning (about 3.8 days on 09-03, about 1.6 days on 09-11). Every future window that leans on `out.log` inherits this. | After 3b.f-c. Scope: capture at the event for window evidence drawn from size-rotated streams, with retention measured at capture, and a check that fails loudly when a scheduled capture is missed. No threshold and no retention change pre-judged. |
> | **3b.f-e** | ⭐⭐ **`B-EQUITY-RECONNECT-STALL-TIMER`** — Owner **CC-C**. ⛔ **r1 OF THIS CELL READ "ROUTING TO LANGSTON" AND THAT WAS A RULE I HOLD AND BROKE: LANGSTON REVIEWS AND NEVER PUSHES.** His ruling classified the FINDING as feed infrastructure — a SUBJECT class, not a who-writes-the-code boundary — and the fix is a reconnect-silence timer in the equity socket adapter under `server/`, which is repo code he does not write. **An owner cell naming the reviewer is a row that cannot be worked, i.e. the same defect as no home at all.** ✅ Accepted by me: I hold `3b.f-c` and `3b.f-d` on the same socket and re-derived this sequence myself. **Opened from critical alert `e2d5d778`, root-caused by LANGSTON on staging `out.log`, re-derived by me against the same lines.** | ⛔⛔ **A FAILED RECONNECT'S OWN SUBSCRIBE SNAPSHOT RESETS THE STALL TIMER, SO A DEAD SOCKET BUYS ITSELF A FULL FRESH THRESHOLD — EVERY TIME.** The `[B74][equity-spot]` liveness line: `00:00:20 connected (attempt 2) / subscribed 475 symbols` → `00:01:18 last_data_msg_age_ms=58268 rows_persisted_60s=468` ⇒ **the clock restarted on the snapshot.** The socket then delivered `rows_persisted_60s=0` for twelve minutes while the age climbed 118,268 → 778,268, and STALL #2 did not fire until **778,404 ms** at `00:13:20`. ⇒ ★ **~13 MINUTES OF UNGUARDED EXPOSURE PER FAILED RECONNECT, BY CONSTRUCTION — and on 2026-09-14 it happened TWICE in fifteen minutes.** Three xStock positions had their stop/target checks refused throughout, which is the guard behaving correctly on an input that was not arriving. ⛔ **BUG-TAXONOMY OUTCOME (1) — A REAL DEFECT WITH A ROOT CAUSE, not a working-as-designed.** The reset belongs on a LIVE DATA message, never on a subscribe acknowledgement: a snapshot proves the subscription, not the feed. ⚠️ **AND IT MUST NOT BE RECORDED AS "the reconnect did not work" — it did work, twice; what failed is that the FIRST one's silence was invisible for a full threshold.** STALL #3 is a matter of when, not if. | CC-C | **AFTER `3b.f-d`.** ⛔ **`e2d5d778` stays ACTIVE until this is discharged** (Langston's routing, and mine now). ★ **Materially related to `3b.f-c` (mine) but NOT the same object: `3b.f-c` owns whether the ALERT should fire; this owns why the FEED was silent.** |
> | **3b.b-b** | **`B-XSTOCK-ENTRY-COMPARATOR`** (`#996`) — Owner CC-C | ⛔ **AT THE ENTRY SEAM THE BOOK-STATE PREDICATE RUNS ON ITS TWO COMPARATOR-FREE ARMS ONLY** — `advanceBookStateComparator` is called solely from the EXIT loop, so an UNHELD symbol has no comparator and any two-sided quote reads `unknown`; `bid_collapsed` / `ask_spiked` / `mark_deviation` are unreachable there and 8 of 12 knobs inert, while a collapsed-but-positive bid also clears the depth snapshot's own filter. **The weaker gate lands on the name we are about to take risk on.** Seeding the comparator at entry was REFUSED by Langston (second writer to a SIM-registered singleton; destroys the `COMPARATOR_SEEDED` control; un-evicted growth to ~479 with stale `priorAtMs`). **OBJ-1 MEASURES first** — on unheld names that cleared warmth and sufficiency, how often the three arms would have fired — pre-registered estimand, population and positive control; **OBJ-2 picks the mechanism conditional on that number.** Placed AFTER the OBJ-9 alert-policy increment. No date. | ☐ placed 2026-09-03 |
> | **3b.f-c-a** | ✅ **ABSORBED 2026-09-03 into `B-XSTOCK-FEED-SANITY` (3b.b) on Kyle's option-(D) ruling — the entry-seam gate and the coupled re-entry change ship there, not here. Row kept as the pointer; `#992` records the absorption.** **`B-XSTOCK-ENTRY-BOOK-GATE`** (`#992`) — Owner CC-C | ⛔ **THE ENTRY DEPTH GATE READS THE ASK SIDE ONLY** (`active-execution-engine.ts:247-258`: `assessWarmth(snapshot,'asks')` + `assessSufficiency(snapshot,'asks',…)`), so a HOLLOW book — bid collapsed, ask held — passes entry, and any change that stops treating a hollow close as a stop-out (B-XSTOCK-FEED-SANITY OBJ-7(ii), the RTB re-entry gate) would let the symbol re-queue straight into the book that just closed it (Langston, 2026-09-02 23:31Z: *ship together or neither*). **The item: apply the same DB-knobbed book-state predicate B-XSTOCK-FEED-SANITY builds for the exit path at the OPEN seam, both sides — option (D) in that scope's §17.4, the non-clock answer consistent with `#295`'s own reasoning.** Where it lands follows Kyle's decision on off-hours entries: (D) ⇒ folded into B-XSTOCK-FEED-SANITY with OBJ-7(ii); deferred ⇒ this row, carrying OBJ-7(ii) with it. No date. | ☐ placed 2026-09-02 (after 3b.f-c); awaits Kyle's pick |
> | **3b.f-d** | ★★ **`B-VENUE-QUIET-ALERTING`** (`#526`, **+ Kyle's `#994` FOLDED IN on Langston's ruling 2026-09-03**) — **Owner CC-B. Langston gates the design.** | **ALERT ECONOMICS, NOT TRADING POLICY — and Kyle's ruling the same day fixes the boundary: the freshness standard does NOT loosen** (*"we just hold that until we start getting fresher data"*), **entries are attempted round the clock on the same bar as everything else, and the exit gate already applies one standard with no session term.** ⇒ **nothing about WHEN we trade or HOW fresh a mark must be is in scope; what is in scope is that a protection engaging PAGES A HUMAN every time.** The rail is correct and stays: a position genuinely cannot exit on a quiet venue. The defect is that xStocks trade 24/5 and go per-symbol quiet overnight, so the design mints up to N-positions × M-windows alerts a night — **four fired on the night of 2026-09-02 alone on the fan-out leg** (`b1f58a01` MDT · `6339b2d9` NEM · `1ea0a78f` CTVA · `ab16f068` LI) — **training everyone to skim them, which is how the real one gets missed.** ⛔⛔ **THE FIRST DESIGN DECISION, AND IT IS A CORRECTION OF THIS ROW'S OWN FIRST DRAFT (Langston, 2026-09-03; re-derived at the object before folding — disposition 1): THE FIVE ALERTS THAT NIGHT ARE NOT ONE FAMILY, AND THE SCOPE MUST NOT INHERIT AN IMPLIED SINGLE MECHANISM FROM A COUNT.** **MEASURED over `dedupe_key` across the whole alerts file (790 rows), 2026-09-03:** the four exit-skips carry `price-skip-paper-<symbol>` — **a PER-SYMBOL key, 228 rows over 79 distinct symbols all-time; this is the leg that fans out N-positions × M-windows and the one the batching state machine is for.** `1d1573c7` (RIOT) carries **`xstock-stale-fill-block` — a SINGLE GLOBAL key, 39 rows all-time — an ENTRY blocked rather than an exit skipped, with a different trigger and a different resolve condition, and ALREADY SELF-BATCHING BY CONSTRUCTION** (one key for every symbol, so it cannot fan out). ⇒ **the design must state EXPLICITLY whether the entry leg joins the batch or stays out; it may not be swept in by resemblance.** ⭐ **AND THE POPULATION IS ONE ASSET CLASS — Langston's §13, folded here 2026-09-03, re-derived by me at the object with a control:** all **79** distinct symbols on the per-symbol key are **xStock equity tickers; ZERO crypto.** ⚠️ **The key READS `price-skip-paper-*`, which names a MODE, not a class — the population is xStock only.** *(Control: keys naming a crypto pair anywhere in the 790-row file = **0**. My own classifier flagged one symbol, `PATH/USD`, and that flag was MY hand-built list's error — PATH is UiPath, an equity: its alert carries a computed per-symbol ceiling of 169 s, which is the xStock risk-derived mechanism; crypto runs a flat 2,000 ms with ZERO `mark_staleness` rows.)* ⇒ **TWO CONSEQUENCES BINDING ON THE DESIGN: (1) anything sized against "paper" here is sized against xSTOCK-lane staleness specifically — the `cap_ms` / quiet-name surface, NOT a cross-class one; (2) it REMOVES asset class as a reason to keep the entry leg out — both legs are xStock, so the join-or-stay-out call rests ENTIRELY on trigger and resolve condition, and now carries the whole weight.** ⛔ **AND THE PRIORITY ARGUMENT IS THE DURABLE MAGNITUDE, NOT THE QUEUE SNAPSHOT (Langston, and he is right): "every active unacknowledged row is this surface" is TRUE and is n=5 — a small-denominator statement that would order this batch on an instantaneous reading. THE ARGUMENT IS 228-rows-over-79-symbols against 39-on-one-key, plus Kyle re-reporting it independently seven weeks on.** **Scope options from `#526`, unchanged by the fold:** a per-class VENUE-QUIET state batching per-symbol events into ONE standing alert per window (resolve-on-venue-resume) · a DB-governed per-class overnight suppression knob with the skip streak still logged · escalation only if a position stays unpriceable INTO liquid hours (the genuinely alarming case). ⛔ **TWO BINDING CONSTRAINTS (Langston, 2026-09-03):** (1) **THE EMIT STAYS and the persisted row keeps its session — what changes is DELIVERY, not emission**; cutting the emit makes `3b.f-c`'s criterion (ii-r) unsatisfiable by construction. (2) **SUPPRESSION IS NEVER AN ACK** — an ack is non-terminal and silences the dedupe key permanently; only a resolve frees it (`#982`). ★ **INPUT HANDED OVER BY CC-C, his to give and mine to design with:** the cohort discriminator that separates a QUIET MARKET from an IMPAIRED FEED — feed-level silence cannot make that split. ✅ **MEASURED AND RE-DERIVED BY CC-B 2026-09-03 — the mirror condition is ANSWERED: the comparison set NEVER goes thin.** Object: distinct tracked symbols with ≥1 tick per minute; population: every minute 2026-08-27..09-02 in the 24/5 window; denominator 479. **Overnight minimum 88, overnight median 131, after-hours median 269, off-hours median 35.1% of the universe; control 478/479 at 10:00 ET.** *(An earlier 62.7% was withdrawn by CC-C — attempt-denominated, biased toward reassurance; nothing was sized against it.)* ⇒ ⛔ **THE THIN-COHORT BRANCH STAYS IN AS A LIVE PATH (Langston; CC-C retracted his own "robustness guard" conclusion and I had already folded it — corrected 2026-09-03).** **88 is a SAMPLE MINIMUM over seven ORDINARY sessions — no holiday, no half-day, no venue incident, i.e. exactly the states that thin a cohort — and it is a COUNT, not a COHORT: if overnight liveness is concentrated in the same names, a symbol signalling at 03:00 with its own peers dark has an effective cohort far under 88. Composition-stability and signal-set coverage are UNMEASURED (requested from CC-C; load-bearing on the branch's TRIGGER, not on its existence).** *(The grid approximation still understates liveness at bin edges — that direction is unchanged and remains conservative.)* **Langston's mirror, binding on the design: the cohort test is only as good as the cohort's own liveness, so the design must state what it does when the COMPARISON SET is thin.** ⚠️ **`#572` (`B-ALERT-DEDUPE-REASON-DRIFT`, CC-B) stays ADJACENT and UNFOLDED — unless the chosen design mutates a long-lived alert's body semantics, in which case it comes along**, because a batching state machine that re-renders a standing row is exactly `#572`'s subject (Langston condition 4). | **CC-B** | ⛔ **PLACED HERE 2026-09-03 AND THAT IS THE POINT — `#526` has been NAMED with an owner since 2026-07-17 and was NEVER PLACED: its home read *"sequenced after the B8.7 Step-9 close"*, a moment that passed weeks ago. Seven weeks, naming without placing (§9.4).** **Position: immediately AFTER `3b.f-c` in reading order, but its DELIVERY GATES `3b.f-c`'s CLOSE** — that row's criterion (ii-r) closes on this policy landing. ⚠️ **ORDERING vs my own `2.4a`/`2.4b` is KYLE'S CALL and is surfaced to him, not assumed** (Langston condition 1: a fold must not park the directive). | ★ **THE PRIORITY ARGUMENT, in Langston's words:** *Kyle re-reported this independently seven weeks later* — the strongest evidence the noise reached him. |
> | **3b.g** | ⛔⛔ **`B-DECIDED-INTENT-INDEX`** (`#956`) — **PRECEDES THE EXIT-PATH REDESIGN. Langston-directed 2026-08-30; he rules on the placement.** | ⛔⛔ **A SESSION CANNOT ANSWER *“IS THIS BEHAVIOUR INTENDED?”* WITH A SINGLE SEARCH, AND THAT IS A SYSTEM DEFECT, NOT A DISCIPLINE ONE.** **MEASURED: three times in one audit I reported a deliberate, documented, REVIEWED decision as a defect — rule 1b, P6, and my own §12.6 item 7. Every citation that dissolved them was in a batch COMPLETION REPORT or a CODE HEADER. NONE was in `RUNNING_ISSUES`.** ⇒ ★★ **AND IT IS NOT ONE SESSION'S CARELESSNESS: ME AND TWO INDEPENDENT FRESH READERS ALL DEFAULTED TO THE LEDGER, because the ledger is the corpus the rules name and the only one with a searchable shape.** ⚠️ **§9.5(b-ii) ALREADY SAYS *“grep RUNNING_ISSUES + BATCH_CATALOG + the completion reports”* — and all three of us still went to the ledger.** ⇒ ⛔ **A RULE THAT NAMES A CORPUS NOBODY CAN SEARCH IS NOT A RULE THAT FIRES. The fix is an INDEX, not a stronger sentence.** | ⛔ **BEFORE the exit-path redesign; AFTER `B-XSTOCK-BOOK-LADDER` (3b.d).** Owner CC-C. **DELIVERABLE (shape, not fix — rule 15): one searchable index of DECIDED INTENT — batch id → numbered objectives → the `file:line` or doc carrying the decision — spanning completion reports, scope files and load-bearing code headers.** ★ **It precedes every disposition in audit §10-§15 because every one of them depends on being able to answer *“was this decided, and where.”*** ⚠️ **Langston's ruling that makes it blocking: *“findings survive as MEASUREMENTS and are NOT CERTIFIED AS DISPOSITIONS”* — so every *“this is wrong / unintended / nobody decided this”* sentence in the audit is UNCERTIFIED until this exists.** Provenance: audit §15 ⭐ **THREE LEADS HOMED HERE 2026-08-30 from `B-EXIT-BOOK-AGE-STAMP`'s audit — all correct TODAY, all behaviour-adjacent, so OBJ-3 forbade fixing them in that batch: (i) the two `getBookForFill` callers disagree on SYMBOL FORM — `active-execution-engine.ts:1383` normalises, `depth-source.ts:43` passes the caller's string, so one side could read a book and the other nothing; (ii) `isRestFallbackSource` (`live-pricing-adapter.ts:208-211`) does a SUBSTRING test over a bare `string`, and `kraken_rest_engine_fallback` contains `kraken_rest`; (iii) `isKrakenVenueSource(source: string)` (`:180`) ALSO takes a bare `string` while THREE members (`kraken_equities_ws`, `mock`, `entry_seed`) live in BOTH the source and producer unions — a caller passing a producer would compile and look plausible. ★ **Langston's reason for homing all three rather than noting them: *"a lead with no home is the one that goes missing."*** |
> | **3b.h** | ⭐ **`B-EXIT-BOOK-AGE-STAMP`** (`#961` + `#962`) — **PHASE A of `XSTOCK_PRICING_PLAN.md`. Owner CC-C.** | **Records two facts on every close and CHANGES NO BEHAVIOUR: the age of the book the FILL walked (`exit_fill_depth_age_ms`, new column), and WHICH KIND of number drove the exit — a midpoint or a last trade.** ⭐⭐ **DESIGN (B), LANGSTON-RULED 2026-08-30: the kind is recorded by SPLITTING three coarse `PriceProducer` members into `_mid`/`_last`, NOT by a new column — `exit_price_producer` already carries the value on every row, and two fields recording overlapping facts can contradict each other with nothing to catch it.** **4 conditions: pure re-description · `kraken_rest_poller` does NOT split (3 arms, `#951`) · strict suffixes + record the SPLIT EPOCH · `non_architecture` only while OBJ-3 holds.** | ⛔ **BEFORE `F-G-2` (3c) — but NOT a hard prerequisite: it changes no behaviour and blocks nothing.** ⭐ **It ships first so `F-G-2`'s read-out uses READ values instead of the reconstruction `#961`'s headline currently rests on.** ✅ **STATUS: CLOSED 2026-09-07 — all four conditions PASS; C4 on an exact `JUP/USD` 40=40 pairing, Langston-ruled. Record: `B_EXIT_BOOK_AGE_STAMP_COMPLETION_REPORT.md` §11-§12.** Deployed `104fa755bf28b852c7c648081aa32a9683424d9f` 2026-08-30T12:05:09Z; Langston APPROVED Step 4 at the ref; CI 4/4; card in `Observation`.** **Record is a PROGRESS REPORT (`B_EXIT_BOOK_AGE_STAMP_PROGRESS_REPORT.md`); criterion pre-registered BEFORE data and armed as alert `65a1379e`, fires 2026-09-06T12:05:09Z — 20 post-deploy closes or 7 days.** ⛔ **CONVERTS only when the data is in AND a decision is taken.** ⚠️ **KNOWN LIMITS CARRIED: `P11`'s test is load-bearing for ONE of the three members · the entry/exit book-age columns are a FALSE PAIR (three instants, and on xStock the entry value is a DB row age, not a book age) · no reader of either column exists, so that harm is PROSPECTIVE.** |
> | **3b.h-1** | ⭐ **`B-TICKER-BBO-TRIGGER`** — owner CC-C, **Langston-placed 2026-09-07**. | ➡️ **ABSORBED INTO 3n r5 (2026-09-11) — D3: crypto ticker moves to `event_trigger: bbo`; production ACCEPTED it on the live probe 2026-09-11 14:28Z, the xStock endpoint REJECTED it (`Scope Files/PRICING_DECISIONS_2026-09-11.md`). The text below is the pre-absorption record.** ⛔⛔ **OUR TICKER IS SUBSCRIBED TRADE-TRIGGERED.** `kraken-websocket-adapter.ts:1427-1434` sends `{channel:'ticker', symbol, snapshot:true}` with **no `event_trigger`**, and Kraken's default is `trades` — *"on every trade"* rather than *"on a change in the best-bid-offer."* ⇒ **on a quiet pair the price is silent between trades while the real quote moves underneath.** ★ **The adapter's own comment states the original intent verbatim: *"ticker: provides trade-based updates (fast for liquid pairs) / book: provides BBO updates (continuous for illiquid pairs)"* — the book was added to compensate for exactly this.** | ⛔ **IMMEDIATELY BEFORE `3n`** — `B-PRICE-SIDE-BY-JOB` cannot rule on which price a level's basis should be while that basis is silent between trades. ⚠️ **The control is a live subscribe ACK, not a doc read: the archive records `event_trigger:'bbo'` being tried and REJECTED by production v2.** No date. |
> | **3b.h-2** | **`B-UNIVERSE-REFRESH-ACTS`** — owner CC-C. | **The daily crypto-universe refresh recomputes and LOGS but does not act.** Its own docstring: *"Does NOT modify the running archiver subscriptions (those happen at next full PM2 restart, which is rare). The daily refresh is informational."* ⇒ **a pair that crossed the $10k volume floor this morning is in the computed universe and NOT in the running subscription until the process restarts.** ★ **The mitigation is accidental — we now deploy often, so the "restarts are rare" assumption is false in the direction that helps us.** Evidence: `PRICING_DATA_ARCHITECTURE.md` §5b.1, 130 runs on file. | **AFTER `B-TICKER-BBO-TRIGGER` (3b.h-1).** No date. |
> | **3b.h-3** | **`B-TSC-GUARD-DETERMINISM`** (`#1019`) — owner **CC-A**, **Kyle-assigned 2026-09-07**. | **The tsc-baseline push guard refused a markdown-only push citing 209 error-groups dropping below baseline in files the push never touched, then PASSED the identical commit on retry with nothing changed.** ✅ **The errors had not vanished — positive control returned 9 in a file the guard listed at zero, and the 377 total was identical on both runs (a partial parse LOWERS the total).** ⛔ **A gate that refuses correct work intermittently teaches sessions to reach for the override, and the override is one keystroke.** | **AFTER `B-UNIVERSE-REFRESH-ACTS` (3b.h-2).** ⛔ **OBJECTIVE 1 IS THE MEASUREMENT, NOT THE FIX: run the guard N times against an unchanged tree and record the verdict distribution — a single passing reproduction proves nothing about an intermittent fault.** ⭐ **Then test whether this is the SAME defect CC-A already fixed once at `4a988bf32` ("the false-absence push gate", 2026-08-21).** No date. |
> | **3n.0** | **`B-VTS-MARK-SIDE`** — owner **CC-C**, **Langston-ruled 2026-09-07 (§9.4 disposition 3)**. | ⛔ **DOES THE VTS LANE'S POSITION MARK MOVE TO THE TRANSACTABLE SIDE, AS THE PAPER LANE'S DOES?** **Do NOT bind it while `F-G-2` OBJ-5's observation window is open** — binding rewrites the learning corpus mid-flight **against the very comparison that window is measuring**, and F7's training-vs-trading fork survives a few more weeks intact where a voided window does not. ⚠️ **`PRICING_DATA_ARCHITECTURE.md` §6.2 row 5 first wrote this cell as *"unresolved"*, which §9.4 does not accept as a disposition.** | **IMMEDIATELY AFTER `3n`, AHEAD OF `3n.a`**, and **GATED on `F-G-2`'s disposition being recorded at the ref.** ⚠️ **RENAMED from `3n-a` (r4): it collided with `3n.a` `B-ORPHAN-ROOT-SCANNER` one separator apart — a home cited by an id that collides is not a place.** No date. |
> | **3b.h-4** | ⭐ **`B-SYMBOL-CLASS-IDENTITY`** (`#1024`) — owner **CC-C**, **KYLE-RULED 2026-09-09**. | **An xStock and a crypto coin sharing a ticker are DIFFERENT ASSETS; both trade; identity becomes `(symbol, asset_class)`.** ✅ **Already reached a real trade: `DASH/USD` has 4 closes across BOTH classes** — Dash the coin and DoorDash the equity under one identity. ⛔⛔ **NOT a schema edit — a KEY-SHAPE CHANGE across every symbol-keyed store** (`price-cache.ts:101`, `ohlc-cache`, `market-context-engine`, `market-data`, `market-volume-cache` all `Map<string, …>`). | **AFTER `B-UNIVERSE-REFRESH-ACTS` (3b.h-2).** ⚠️ **OBJECTIVE 1 IS THE CENSUS OF SYMBOL-KEYED STORES, NOT THE MIGRATION** — or we fix the one instance a reviewer happened to name. **Correctness-driven, not incident-driven.** No date. |
> | **3z** | **`B-TOTAL-DRAWDOWN-WARNING`** — owner **CC-C**, **KYLE-RULED 2026-09-09**, **placed at the END of Phase 19**. | ⛔ **The daily-loss budget counts CLOSED trades only** (`daily-loss-budget.ts:110`; open mark-to-market is outside the numerator — `#303`). ⇒ **you can be past the limit in real money while the brake reads zero.** ✅ **KYLE'S RULING: add a total-drawdown measure that WARNS ONLY.** ⛔⛔ **IT MUST NOT BLOCK NEW TRADES AND MUST NOT FLATTEN** — his explicit instruction. | **END of Phase 19.** ⚠️ **DEPENDS ON THE PRICING WORK: a mark-to-market measure is only as good as the mark, and ours is a midpoint of a sometimes-stale, sometimes-hollow book.** ★ **Warning-only is what makes that dependency tolerable — a bad number misinforms rather than liquidates.** No date. |
> | ~~**3b.h-5**~~ | ⛔ **`B-TSC-GUARD-NONCODE-EXEMPT` (`#1025`) — STRUCK 2026-09-10, NOTHING TO BUILD.** | ✅ **The premise was falsified at the object by the session that filed it: the push guard ALREADY has the content-keyed exemption this row existed to build** (`guard-push-tsc-baseline.mjs` — `if (files.length && relevant.length === 0) process.exit(0)`), **and a fifth code-free push of the same shape passed clean on 2026-09-10 (`1453f9709..57332538d`, 16 files, 0 trigger-matching, positive control returning 5 on a known-TypeScript range).** ⛔ **The live defect in that guard is `#1019`/`#1020`, owner CC-A, homed to `P19-B12` — the `PreToolUse` cwd inheritance, which refuses independently of content.** | — **Struck, not deferred. `#1025` WITHDRAWN under §9.4 disposition 5 with its dissolving citation.** |
> | **3b.h-6** | **`B-OHLC-FRAME-GUARD`** (`#1028`) — owner **CC-C**, **opened 2026-09-11 from critical alert `b48a743f`**. *(Position ✅ confirmed by Langston.)* | ⛔⛔ **A ONE-MINUTE PRICE BAR ARRIVING WITH NO PRICE BECOMES THE TEXT "undefined", AND THE WRITER THROWS AWAY THE WHOLE BATCH IT ARRIVED IN — GOOD BARS INCLUDED, UNRECOVERABLY.** Both spot archivers carry the IDENTICAL DEFECT (`equity-spot-archiver.ts:92-95`, `crypto-spot-archiver.ts:112-115`) — ⚠️ **the blocks themselves are NOT identical** (Langston, Step 1): crypto's has no liveness stamp and **counts at the caller** *(r3 correction)*; neither spot class has a re-fetch path. ⭐ **The 1-minute table feeds the 15-minute xStock signal bars**, so a lost minute can mis-state a signal bar. ✅ Latent since B74 (2026-05-01), surfaced by `F-G-1`'s alerting. A symptom of `#950`. | **OBJ-1 is ONE exported validator; OBJ-2 applies it at all three producers and SKIPS the incomplete frame before it is buffered.** ⛔ **NOT a `null` fix — the price columns are `NOT NULL`, so `null` fails identically.** **NOT folded into `#950` (`3b.e`):** that batch is xStock-only and waits on `#943`; crypto carries the same defect. No date. ➕ **Step 2 folded in `#1029` (OBJ-7 — the archive panel's counters count different things in different producers). Two more defects found there were re-homed at the review cap: `#1030` → `3b.h-7`; `#1031` → `F-G-1` OBJ-9 ②.** ✅ **Step 2 APPROVED 2026-09-11 with seven conditions (scope §15); the writers' other edges → `3b.h-8`.** Scope: `B_OHLC_FRAME_GUARD_SCOPE.md`. |
> | **3b.h-7** | **`B-FUTURES-BAR-FINAL`** (`#1030`) — owner **CC-C**, **split out of `3b.h-6` at its Step 2, 2026-09-11**. *(Position ✅ confirmed by Langston, J7.)* | ⛔ **THE FUTURES BAR POLLER STORES EACH MINUTE AS IT STOOD WHEN FIRST POLLED.** The venue serves the minute in progress and the poller never re-reads a minute it has seen, so stored perp bars are short: stored volume below the venue's final bar in 2–24 of 116 minutes on three crypto perps and 1 of 116 on three xStock perps, reproduced by an independent reader. **Capture-only tables — no trading impact.** Design inputs carried from `3b.h-6`'s review (`#1030` amendment): completeness judged against the fetch's start time, not a clock clamp; a monotonic mark read after the fetch; overlapping polls; future-stamped candles. | **AFTER `3b.h-6` AND AFTER `F-G-1`'s OBJ-9 ② fix** — a re-read exposes the futures leg to `#1031`. Instrument ready: `scripts/analysis/b_ohlc_frame_guard_futures_partial_bar_check.py`. No date. |
> | **3b.h-8** | **`B-ARCHIVE-WRITER-LIFECYCLE`** (`#1034`, `#1032`) — owner **CC-C**, **split out of `3b.h-6`'s Step 2 by Langston's J9 ruling, 2026-09-11**. *(Position PROPOSED — Langston to confirm.)* | ⛔ **THE ARCHIVE WRITERS' UNBOUNDED EDGES.** A database wait with no time limit; a buffer cap that holds only on retry; a shutdown drain the process exit can cut off; and the ticker writer inserting already-saved snapshots a second time when a later chunk of a flush fails. **Not `F-G-1`'s scope** — that reopen is bounded to OBJ-9's ordering guarantee. | **AFTER `3b.h-7`.** No date. ➕ **Step 1 also reviews `#1036`** (added 2026-09-11 from `B-OHLC-FRAME-GUARD`'s Step 4): a bar with a finite but out-of-range time passes the frame guard and still drops its chunk, because no DEFAULT partition exists — decide between a routing check in the guard, row isolation in the writer, or no work. ➕ **Step 1 also reviews `#1037`** (added 2026-09-11): the signal-evaluation archive writer dropped 1,015 rows while the Drift Dashboard tab loaded — decide whether the Data Archive panel's untimed full-table counts starve it, and what the writer should do on a transient failure. ➕ **ITEM ADDED 2026-09-22 (`#1072`, three legs, one lifecycle root): the crypto snapshot archiver's membership is ONE live Kraken observation at startup, frozen for the process lifetime** (the daily cron writes a cache NOTHING reads), so the archive is a sequence of populations with unlogged boundaries that drops the marginal-liquidity tail (`#596`); plus a startup fetch failure leaves the archiver dark for the whole lifetime (`crypto-spot-archiver.ts:264-267`), and a partial ticker failure silently freezes up to 100 pairs out (`:236-238`). |
> | **3b.i** | **`B-DISPATCH-STAGING-VERIFY`** (`#964`) — Owner CC-C | ⛔⛔ **A FAILED `scp` FOLLOWED BY A `cp` PUTS A STALE FILE OF THE SAME NAME IN FRONT OF THE REVIEWER, UNDER THE RIGHT FILENAME.** Hit TWICE in one session: once a leftover `/tmp/q.sql` was executed against the live database, once a 28KB document I never wrote landed in Langston's inbox as my Step-2 audit. ★ **Every command behaved correctly and the failure PRINTED — `;` does not stop the chain and `/tmp` is shared and never cleaned.** ⛔ **`CLAUDE.md` §6.4/§6.5 TEACH the vulnerable shape, so every session dispatching a multi-line message runs it.** | **AFTER 3b.h. Blocks nothing and is independent of the exit-path work — placed here because that is where it was found.** **Deliverables: (a) replace the scp-then-reference pattern in `CLAUDE.md` with `ssh 'cat > path' < file` · (b) require a byte-count check at both ends before a staged path is named · (c) per-session unique filenames · (d) diagnose WHY `scp` fails on this box.** ⚠️ **(d) is NOT a precondition for (a)-(c): a `/tmp` collision needs no transfer failure — two sessions using one filename is enough.** |
> | **3b.j** | **`B-SCANNER-DEDUPE-DEAD-TABLE`** (`#965`) — Owner CC-C | ⛔⛔ **THE MARKET SCANNER'S "already has an open position" CHECK READS A TABLE THE ACTIVE PATH DOES NOT WRITE.** `getActiveTrades` selects from **`trades`** (`storage.ts:1535-1543`); **MEASURED 2026-08-30: `trades` = 0 rows**, against positive controls `active_open_positions` = 2 and `closed_trades` = 665. ⇒ **`activeTradeSymbols` is EMPTY FOR EVERY SYMBOL**, so the dedupe at `market-scanner.ts:773`/`:782` has never excluded anything and `already_active` counts a constant 0. ★ **`bridge/canonical/DawnTrader_System_Invariants_Design_Guarantees.md:30` states INVARIANT T2 — *maximum one open position per symbol* — and THIS was the leg I wrongly cited as enforcing it.** ⚠️ **T2 may well be enforced ELSEWHERE (the execution engine is the likely home); the batch's FIRST job is to establish WHERE, not to assume it is unenforced.** | **AFTER 3b.i.** Surfaced by Langston at `B-SCANNER-EGRESS-NORMALISE` Step 4. **Deliverables: (a) find every enforcement site for T2 and name them; (b) decide whether `trades` is legacy-to-remove (rule 18) or a writer that was lost; (c) fix or delete the dead check — never leave it reading as coverage.** |
> | **3b.k** | **`B-CHANGE-CLASS-PARSER`** (`#968`) — Owner CC-C | ⛔ **THE GOVERNANCE CHECKER CANNOT READ THE MOST NATURAL WAY TO WRITE A CHANGE-CLASS DECLARATION.** Its marker (`config.mjs:180`) requires the token to BEGIN a line AND the colon to sit OUTSIDE the bold: `**change-class:** x` FAILS, `**change-class**: x` PARSES — **one character's position, and the two render identically.** ⚠️ **MEASURED: of 329 SCOPE-named files the checker reads, 136 parse and 17 declare a class it cannot see — across CC-A, CC-B and CC-C, so it is not one author's habit.** Each fires a false *"change-class undeclared"* alert; one fired on `B-SCANNER-EGRESS-NORMALISE` on 2026-08-30 and is how this was found. ✅ **Direction is SAFE — fail-closed grades against the strictest doc-set, so nothing was ever under-graded**; the cost is a routinely-wrong alert, and an alert that is routinely wrong stops being read. ★ Same family as the skill-description colon-space trap in `CLAUDE.md` §0.a: valid file, plausible text, silently dropped value. | **AFTER 3b.j.** ⛔ **FIX THE PARSER, NOT THE 17 DOCUMENTS (rule 15)** — editing every scope to satisfy a regex that contradicts the documented convention is the patch. **Deliverables: (a) accept mid-line and colon-inside-bold; (b) all six measured forms as test cases; (c) re-run over the 329 and report the new count; (d) reconcile `CLAUDE.md` §3.0's wording with the parser so they stop describing different things.** ⚠️ **GUARD: four of the 17 are documents DISCUSSING the mechanism — a loosened regex must not bind a sentence instead of a declaration. A confidently wrong class is worse than the current fail-closed.** |
> | **3b.k-a** | **`B-CHECKER-CORE-PATHS`** (`#993`) — Owner **CC-A (proposed — the governance checker's owner; to accept or re-home)** | ⛔ **THE UNDER-DECLARATION CROSS-CHECK CANNOT SEE THE LIVE ENGINE:** `scripts/governance-checker/config.mjs:187-196` `CORE_ENGINE_PATHS` lists the orchestrator, MCE, SQE, TEC, the strategy engine, the regime metrics, the canonical map and `server/asset_classes` — **NOT `server/services/active-execution-engine`**, so a batch declaring `non_architecture` for an exit-path decision change passes the class↔diff cross-check (Langston caught B-XSTOCK-FEED-SANITY r2's class by reading, not by the checker — 2026-09-02 23:31Z). The item: add the engine (and audit the list against the SIM's core-component set) with the same grep-verified-resolves discipline the file's own header demands. No date. | ☐ placed 2026-09-02 (after 3b.k, the sibling checker item) |
> | **3b.l** | **`B-TWO-CACHE-INTENT`** (`#971`) — Owner CC-C | ➡️ **ANSWERED BY D7 AND ABSORBED INTO 3n r5 (2026-09-11) — keep both caches; the decision is recorded on `#971` (`Scope Files/PRICING_DECISIONS_2026-09-11.md`). The text below is the pre-absorption record.** ⛔ **THE CANONICAL DESIGN SAID ONE RATE-GOVERNED PRICE CACHE AS THE SINGLE SOURCE OF TRUTH, WITH "no direct Kraken calls" (`bridge/canonical/DawnTrader_System_Architecture_Execution_Flow.md:194/:257/:633`). THERE ARE TWO** — `price-cache.ts` and `live-pricing-adapter.ts:258`'s own private Map, **each with its own rate limiter**, and the second calls Kraken REST directly. **Two interfaces even share the name `CachedPrice`.** ⚠️ **NOT filed as a defect — the canonical corpus is intent, never current truth, and the split may be deliberate and correct.** ⛔ **THE FINDING IS THE SILENCE: no entry in `RUNNING_ISSUES`, `BATCH_CATALOG` or the SIM records the decision, and the SIM documents both caches without ever saying why there are two.** ★ **It matters because `B-PRICE-AGE-TRUTH`'s defect sits exactly at that seam: the provenance census that covered four stale-price legs missed the fifth, and the fifth belongs to the second cache's own rate limiter — a mechanism the canonical design did not expect to exist.** | **AFTER 3b.k.** ⛔ **FIRST DELIVERABLE IS THE THREE-OUTCOME READ, NOT A FIX: deliberate-and-correct (record it and close) · correct-but-undecided (a decision is owed, and it is Kyle's) · drift that should converge (a real consolidation with its own plan). NO code changes until that read is done.** ⚠️ **And search harder first — my search was by term, so a decision recorded in different words would not have been found; an absence from a term-search is not an absence.** ↔ same family as `#229`. |
> | **3b.m** | **`B-PROVENANCE-LOSS-CENSUS`** (`#976`) — Owner CC-C | ⛔ **PROVENANCE IS LOST AT SUBSTITUTION BOUNDARIES ACROSS THE PRICING LAYER, AND A RETURN-SITE SEARCH CANNOT SEE MOST OF IT.** A claim-only reader found **six mechanisms** that evade the search I used; two re-derived at the ref. The sharpest is `cost-cache.ts:106-115` — a retained measurement carried forward and **re-stamped with a fresh timestamp**, i.e. `#951`'s own defect class in a different cache, **on the WRITE side where no return-site or quote-constructor census can reach it**. Also `signal-orchestrator.ts:2387` reading a **TTL-less** `getCachedPrice()` and turning an absent price into **zero** via `|| 0`. ⛔ **NOT filed as defects — no verdict was given, and `getPairHistoryDays` is the same shape being harmless.** | **AFTER 3b.l.** ⛔ **FIRST DELIVERABLE IS THE SEARCH, WRITE-SIDE AND TYPE-SHAPED, THEN A THREE-OUTCOME READ PER INSTANCE — not a fix.** ⚠️ **NOT a prerequisite of 3b.f.** ★ **Exists because I claimed "exactly one site" one line after correctly stating my search was bounded — a stated limit does not neutralise the overclaim beside it.** |
> | **3c** | **`F-G-2` — `B-EXIT-TRANSACTABLE-SIDE`** — the ORIGINAL batch | ➡️ **ABSORBED INTO 3n r5 (2026-09-11) — the exit switch is decision D1 of the three-way pricing decisions (`Scope Files/PRICING_DECISIONS_2026-09-11.md`). F-G-2 is retired as a deploy gate, VOID kept; its before-record is 24/24 below stop, median 0.166% (D10). The text below is the pre-absorption record.** The exit decision reads a **midpoint** while a sell fills on the **bid**; **POST-EPOCH (≥ `2026-08-22T22:01Z`, the book-truncate fix `e6f7c70b3`) 24 of 24 stop-outs fill BELOW their stop, median 0.166%** — pre-epoch is 48% and is a DIFFERENT INSTRUMENT (crossed book 31.08%→0), so the two eras must never be pooled. Plus `OBJ-8`, the exit-fill instrument (1-minute bar, through-not-touch, ratio recorded not thresholded). | ⛔ **HARD PREREQUISITE: `F-G-1` DEPLOYED AND SOAKED FIRST** — `OBJ-0`'s shadow run must sit entirely inside the post-gridding era or its read-out is invalid. Scope `B_EXIT_TRANSACTABLE_SIDE_2_SCOPE.md` **r7** — Step 1 APPROVED by Langston 2026-08-29 with three conditions; §2 restated on the MECHANISM (`exit_decision_price = exit_book_mid` 12/12, ≠ `exit_ticker_bid` 9/9) | ⛔⛔ **SPLIT BY ASSET CLASS — LANGSTON RULING 2026-08-30, re-derived by him at `0797e0132b`.** **CRYPTO LEGS PROCEED ON THE `F-G-1` SOAK ALONE; xSTOCK LEGS SIT BEHIND 3b.b + 3b.d.** ★ **THREE of my four prerequisite legs were xStock legs and the fourth was not shown to reach F-G-2 at all** — and **crypto book quality was raised as an F-G-2 prerequisite and WITHDRAWN BY ME 24 HOURS EARLIER** (`#944` / row 3b.c, continuous instrument n=492 at **exactly 0.0000%**, that row saying in terms *“F-G-2's sequencing REVERTS — no prerequisite”*). ⛔ **Re-imposing a crypto hold today on xStock-derived evidence re-asserts a conclusion I had just retracted.** ✅ **Post-epoch crypto is instrumented: crossed-book detections 0 of 31,059, checksum 18,758/18,758, a real 10-level ladder, and an independent witness wired.** ⚠️ **And the cost of holding is live and directional — BABA's decision price sat 2.84% above the last trade against a target of 124.5567.** **CONDITIONS on the crypto leg before Step 3: (1) cite `#951`'s four hops · (2) `#952` re-filed as `#941`'s leg ✅ DONE · (3) this split recorded + 3b.d re-argued ✅ DONE.** ⭐ **BOARD-FIELD DEPENDENCY WRITTEN INTO THE PLAN 2026-09-01 (Langston-directed, after he re-derived the field's timestamps himself).** **The card's `Blocked on` read `KYLE`. Written `2026-08-29T18:11:32Z`, ONE SECOND after `Status → Implementation`, and never touched since — so it PREDATES Kyle's 08-31 four-session decision by two days and was NOT set for it.** ⇒ **RULED STALE FOR THE CRYPTO HALF, TRUE FOR THE xSTOCK HALF; cleared to `Nothing` and the real dependency recorded here and in the card body.** ⛔⛔ **BECAUSE ONE BOARD FIELD CANNOT CARRY AN ASSET-CLASS-SPECIFIC STATE, AND A DEPENDENCY LIVING ONLY IN A BOARD FIELD IS INVISIBLE TO EVERYONE — INCLUDING ITS OWNER.** **THE REAL KYLE DECISION IN THIS CHAIN IS ROW `3b.b`'s** (`:36`, *"a SCOPE DECISION FOR KYLE, not a defect"*, routed through `XSTOCK_PRICING_DECISION_PATH.md`) ⇒ **it gates THIS row's xSTOCK legs and NOT its crypto leg.** ⏳ **AND IT IS NOT YET ASKABLE: it rests on the bid-drop-vs-wide-spread discriminator, tabled as `3b.b`'s `OBJ-0` on Kyle's direction 2026-08-31 — because the *"stub for ~19 h a weekday"* premise came from a `spread > 20%` WIDTH test, not a bid-drop test.** ⚠️ **DO NOT CITE THE BOARD'S `creator` FIELD TO ATTRIBUTE ANY OF THIS — it reads `kylegjordan` on every field including ones Langston set, because the PAT is his. Authorship does not discriminate Kyle from a Claude here.** ✅ **CRYPTO HALF + FOLDED OBJ-5 DEPLOYED 2026-09-02 (`2cc4a03ec`, Step 8 CONFIRMED on the deployed state) — ⏳ IN OBSERVATION.** Kyle 2026-09-02: *"fold it into F-G-2"* — OBJ-5 is DECIDED: VTS books mark-booked exits (crypto rows; xStock keeps the clamp behind a seam), one maker leg where a maker was paid (three write paths incl. `planTwin`), one class-scoped `vts` epoch bump. The OBJ-0 shadow arm is LIVE on crypto but NOT ARMED as a read-out until F-G-1's PASS (09-04) + the pending follow-up deploy (`f36c8f496`/`f2cc6ee29`); pre-registered criteria (14 d, dollar estimand, INCONCLUSIVE-EXTEND arms, denominator = positions that entered the arm) in `F_G_2_PROGRESS_REPORT.md` §4. xStock legs still HELD.
> | **3c.a** | **`B-DEPLOY-REF-DECLARATION`** (**#988**) | ★ Langston's knock-on from the F-G-2 pre-run deploy (2026-09-02 11:40Z): **one shared branch, four sessions, whole-tree deploys ⇒ every deploy silently ships every other session's pushed work with NO ref declaration and NO owner consent.** MEASURED: deploying `093d1878f` over the box's `2cc4a03ec` would have carried 44 commits / 48 files (60 / 55 by 12:12Z), 13 under `token-watch/` — Infra Claude's live batch. **The INSTANCE did not hold (Langston retracted his own premise 12:12Z): their receiver runs from `/opt/token-watch`, hand-copied, not the deploy worktree, so the deploy could not touch what they execute; the only app-runtime file in range was F-G-2's own.** The CLASS holds: 60 commits ship blind with no declaration. **The batch: a deploy DECLARES the range it carries (`box sha..target sha`) and names every other owner whose paths are in it; the REFUSE trigger is a path EXECUTED FROM the deploy worktree — not a service-shaped path merely in range — and on it an unconfirmed owner = REFUSE (prefer impossible over intercepted — `dt-deploy` can compute the range itself).** Interim rule, binding now: `git diff --stat <box>..<target>` before every deploy (detection), and the deploy post NAMES the other owners of worktree-executed paths and QUOTES their ref confirmation (the binding half — Langston 11:45Z); no confirmation, no deploy. | ☐ owner CC-C — placed here by Langston; scoped after F-G-2's pre-run deploy lands (`#988`)
> | **3d** | **`B-MIN-STOP-DISTANCE`** | ★ Langston's knock-on from F-G-1 Q3: **if gridding moves a stop distance 12.96%, that stop sat within a few ticks of entry.** A K-tick minimum-stop-distance floor. **Rule 24 outcome (2) — working-as-designed but unaddressed: a DECISION, not a defect.** | **AFTER `F-G-2`**, before `F-5`. Owner CC-C. ⛔ **THE NUMBER IS CC-C + LANGSTON'S, NOT KYLE'S (his direction 2026-08-27).** ⛔⛔ **NO FLOOR IS BUILT — recommendation WITHDRAWN 2026-08-27 on Kyle's caution + the evidence.** **Kyle: *"every time we've instituted floors and ceilings, it hasn't worked out well… we clamp something, and then we see trading impacts we're not sure why."* Correct, and specifically about CLAMPING: widening a too-tight stop hands the trade a geometry nobody designed.** **And it is not established: 5 trades under 3× spread, 80% stopped out vs a 36.1% base — one-sided `p=0.060`, SUGGESTIVE NOT SIGNIFICANT at n=5.** ★ **Also NOT caused by rounding — Langston inferred it from the rounding tail, so the rounding measurement was the flashlight, not the cause. F-G-1 does not need it resolved.** ⇒ **RECORD the spread-multiple, DO NOT MECHANISE. Re-ask at n=10 in-band (p=0.006 — the smallest n that returns an answer); if confirmed, REJECT, never clamp.** Owner CC-C + Langston jointly |
> | **3e** | **`B-GUARD-COVERAGE-AUDIT`** (`#919`) | Geometry guards run at **18 of 19** signal-construction sites. `detectLiquidityTrap` returns unguarded geometry (deliberate, commented) — fenced out of the active orchestrator path, **but still reachable from `stage-b-validator.ts:350` and `routes.ts:11092`.** ⇒ *"the guards run in every strategy"* is FALSE, and a false universal is what a future audit builds on. | **AFTER `F-G-2`, alongside `B-MIN-STOP-DISTANCE`.** Owner CC-C. **Deliverable: can either reach path produce a signal that TRADES? Fence them, or state why they need no guard.** Blocks neither F-G batch |
> | **3f** | **`B-VALIDATE-OBSERVABILITY`** (`#922`) | The July `validate=true` leg has three outcomes and only two are observable — `rejected` logs and counts, `skipped` counts, **`ok` does nothing at all.** So *"ran and passed"* is indistinguishable from *"never ran"* on a component that **blocks opens**. **The `#704` shape, in the execution path.** | **AFTER `F-G-2`**, with `B-MIN-STOP-DISTANCE` and `B-GUARD-COVERAGE-AUDIT`. Owner CC-C. ⚠️ **The leg is NOT redundant** — it caught a real `volume minimum not met` refusal the VPG's price work could never see |
> | **3f.b** | **`B-POST-GRID-MUTATION-CENSUS`** (`#923`, INVESTIGATION ONLY — NO CODE) | ⛔ **KYLE-DIRECTED 2026-08-28: establish whether the trailing-stop defect is a symptom of a much bigger problem, BEFORE deciding where any fix goes.** F-G-1 puts entry/stop/target on the venue grid **at signal birth** — **what mutates that price for the rest of the trade's life has never been enumerated.** **THREE sites are already on the record and ALL THREE WERE FOUND INCIDENTALLY:** the trailing ratchet (`#923`), the 5% stop buffer sent to Kraken and the AMR overlay recompute (both `#939`). ⇒ census every site that reads or writes entry/stop/target between the seam and the venue — signal birth → RTB refresh → promotion → open → exit management → live order placement — in §9.5(a)'s who-writes / who-mutates / who-deletes form. **OUTPUT IS A DISPOSITION, NOT A FIX:** whether this is a handful of sites or a structural gap, and THEN where the work belongs — Phase 16, Phase 20, or sooner if anything is live-harmful. ⚠️ **Kyle explicitly declined to pre-place the fix** (he had been thinking Phase 16 or 20) **so the placement is evidence-led.** ★ **It determines row 3g's scope, which is why it runs first.** |
> | **3g** | **`B-GRID-LIVE-PATH-PARITY`** (`#939`) | Two sites mutate a gridded price AFTER the VPG: `trading-engine` multiplies a stop by a 5% buffer and sends it to Kraken; the execution engine recomputes the stop from AMR overlay multipliers. | **AFTER `F-G-2`**, with the other execution-surface items. Owner CC-C. ⚠️ **BOTH DORMANT TODAY** — the first is a live path and live is Phase-21 gated; the second is an identity while AMR is inactive. **A live-readiness item, not a Phase-19 trading-behaviour one.** ↔ `#734` |
> | **3h** | **`B-INTENT-ENTRY-PARITY`** (`#928` + `#929`) | ⛔ **THE HONEST LIMIT ON F-G-1's GUARANTEE, and it must be read beside it.** F-G-1 puts prices on the venue grid **as the signal orchestrator emits them** — and there are TWO other ways into execution that do not pass through it. (a) An HTTP path takes a price triple straight from the request body into `processSignal`, validating only symbol and strategy **while its own error string claims it checks prices**; the downstream distance check uses `Math.abs`, so an inverted triple passes silently. (b) Position sizing has a SECOND caller — the fallback-sizing arm of the promoted-signal path — which never consults the VPG. | **AFTER `F-G-2`**, with the other execution-surface items. Owner CC-C. ★ **Surfaced by a fresh-context reader on F-G-1, by asking the §9.5(a) census question — "who ELSE calls this?" — which a forward trace from the orchestrator structurally cannot answer.** First question for the batch is whether the HTTP intent path should exist at all, not how to guard it | ➕ **`#953` ADDED 2026-08-30 — THE SAME TWO ROUTES, ON THE **EXIT** SIDE, AND IT SURVIVES 3h.b.** `trading-engine.closeTrade` has THREE callers: `trading-engine.ts:703` is the DEAD limb 3h.b deletes, but `routes.ts:5054` (`'manual'`) is **LIVE — an authenticated HTTP route — and appears nowhere else in this plan.** ⚠️ **CORRECTED 2026-08-30 same day: I ALSO CALLED `command-router.ts:240` LIVE AND IT IS DEAD** — `CommandRouter` is constructed once at `routes.ts:110` and no method is ever invoked on it (control: `getActiveTrades(` = 31 call sites). ★ **I enumerated callers of `closeTrade` and stopped, without asking whether those callers are reachable — one hop, not two. A caller list is not a reachability proof.** ⛔⛔ **AND THAT ROUTE PLACES A REAL MARKET SELL AND THEN BOOKS A RANDOM HAIRCUT AS THE EXIT PRICE** — `trading-engine.ts:633-647`: `kraken.addOrder({type:'sell', ordertype:'market'})`, then `exitSlippage = Math.random() * 0.1` (live) / `* 0.05` (paper), then `exitPrice = marketPrice * (1 - exitSlippage/100)`. **It never reads the fill.** ⭐ **Founding invariant F7 forbids exactly this** — *"mock pricing is prohibited in production"* — and this is NOT the env-gated mock path; it is unconditional. **PHASE-21 GO-LIVE BLOCKER class, no paper-mode impact today.** ⇔ `#734`. ★ **Homed HERE and not as its own batch because 3h's first question already IS *“should these routes exist at all?”* — removing the `Math.random()` alone would leave a real order booking a modelled exit, which is the `fix-follows-pointer` shape.**
> | **3h.b** | ⛔ **DEAD EXIT LIMB — `trading-engine` + `strategy-engine`** (F-G-2 Step-2 FINDING A1; **Langston-ruled 2026-08-29 as a NAMED item, not implied by 3h's adjacency**) | ⛔⛔ **A SECOND, SEPARATE EXIT-DECISION IMPLEMENTATION THAT NEVER IMPORTS `evaluateTECExit` AND READS A THIRD PRICE SOURCE** — `this.kraken.getTicker(symbol).c[0]`, the **v1 REST ticker**, distinct from both live paths. **DEAD END-TO-END, presence-evidenced repo-wide (tests excluded):** `trading-engine.ts:677 monitorActiveTrades` — **ZERO callers, the name appears exactly ONCE, its own definition** → `:688 checkTradeExitConditions` (sole caller `:684`) → `strategy-engine.ts:1106 checkExitConditions` (sole caller `trading-engine:696`) → the `:1123-1138` switch → **six per-strategy exit helpers `:1151-1199`, one caller each.** ➕ **AND TWO GOVERNED DOCS DOCUMENTED IT AS LIVE** — `SYSTEM_MANUAL.md:4556` + `sections/PHASE5_TRADE_EXECUTION_AND_LIFECYCLE.md:129` listed *“Monitoring │ `monitorActiveTrades()` via strategyEngine”* in a two-engine comparison table. ✅ **BOTH CORRECTED 2026-08-29** (Langston's condition 2). | ⛔ **PLACED UNDER 3h BUT DECIDABLE WITHOUT IT — that is the ruling and it is deliberate.** ★ **My “same module” premise was HALF WRONG and it is the half that decides the home: only three methods are `#928`'s module; the other ~95 lines sit in `strategy-engine.ts`, which is the DETECT-METHOD HOME for the ACTIVE path.** ⇒ **3h owning the HTTP intent path does not own a dead limb in `strategy-engine` by adjacency, and if 3h decides the HTTP path STAYS, this limb is still dead and still a rewire-bypass hazard for F-G-2.** Owner CC-C. ⚠️ **RULE 18: it gets DELETE-ON-THE-SPOT or a dated deletion — not a shrug.** ⛔ **F-G-2 does NOT touch it**, and F-G-2's completion report names the six helpers as DELIBERATELY UNTOUCHED so a later grep does not read them as a missed `evaluateTECExit` call site. |
> | **3i** | **`B-TARGET-FABRICATION`** (`#927`) | The promotion path INVENTS a target price — `entry * 1.02` when the stored one is null — and the same rule is re-implemented in **three** places, one of which is inside the RTB **ranking** key. So pool ORDER can depend on an invented number. ⚠️ **Sharper, same expression: these columns arrive from Postgres as STRINGS, so a stored `"0"` is TRUTHY** — the null guard passes and a **zero target** reaches sizing; the stop leg has no guard at all and yields `NaN`. | **AFTER `F-G-2`**. Owner CC-C. **Deliverable: one home for the default-target rule, and a string-aware null guard.** ⛔ Deliberately NOT folded into F-G-1 — that batch rounds at signal BIRTH; this is a fabrication at PROMOTION, on a path F-G-1 does not touch |
> | **3i.b** | **`B-STRING-TRUTHINESS-GUARDS`** (`#930`) | A stored `"0"` in a decimal column is a **truthy string**, so a `x ? parseFloat(x) : fallback` null-guard passes and installs a **zero target** — an infinite-RR signal. Guard line, column type and arithmetic all cited on the issue. ⚠️ **The defect is confirmed by construction; the INCIDENCE is unmeasured** — the columns are `notNull`, so the exposure is a stored zero rather than a null, and nobody has queried whether one exists. | **AFTER `B-TARGET-FABRICATION`**. Owner CC-C. **Scope is the CLASS, not the site: a repo-wide census of that expression shape over decimal columns.** ★ Split out of `#927` on Langston's condition — an independent live-path defect has no business as a footnote of a rounding batch |
> | **3i.c** | **`B-CANONICAL-FREEZE`** (`#948`) — governance hygiene, NOT on the trading path | ⚠️ **`bridge/canonical/` IS NOT FROZEN, AND TWO OF OUR OWN SKILL FILES SAY IT IS.** `workflow-01-scope/SKILL.md:23` and `:56` call it *“never edited”* / *“NEVER edited (frozen historical record)”*, and **every batch is sent there for original intent on that understanding.** `git log -- bridge/canonical/` returns governance-era commits — `69b3ed8f2`, `af99bd5dd`, `8ef70628d` — with per-file last-commit dates spanning **2025-12-13 → 2026-06-11**. ⛔ **And the edit left the corpus self-contradictory:** `..._Execution_Flow.md:299` carries a 2026-05-15 note correcting *“5-minute intervals”* to 60-minute bars while **`..._Current_State_Reference.md:174` still reads *“721 candles per symbol (5-minute intervals)”*** ⇒ a session consulting the sibling reads a known-wrong value as original intent. | **AFTER the exit-path redesign work** — it is corpus hygiene and blocks nothing. Owner CC-C. **Deliverable is a CHOICE, not both: freeze it for real** (move the three edits into a dated addendum, revert the originals) **or correct the rule** (and stamp every edited file with its edit date + the batch that touched it). ⚠️ **Leaving both standing is the failure the rule exists to prevent.** ★ **Interim: every provenance citation in audit §8 was taken at a named ref or commit for exactly this reason.** |
> | **3j** | **`B-FUNNEL-PERP-CLASSES`** (`#925`, amended) | The active funnel can only key `crypto_spot` and `xstock_spot`, but F-G-1's passthrough arm explicitly names `xstock_perp` — so the moment perps are wired, an `xstock_perp` signal would ship with unrounded prices and be counted **nowhere**. `crypto_perp` separately hard-refuses, because the published-tick lookup does not index Kraken Futures pairs. | ⛔ **BEFORE the perp active-path wiring item, not after** — this is the one row here whose ORDER is load-bearing. Owner CC-C. ⚠️ **INTERIM SHIPPED IN F-G-1:** an uncountable grid event now announces itself on stderr rather than counting nothing quietly. **My first disposition on `#925` was NO WORK and it was wrong on facts I had written myself** — the amendment on the issue records why |
> | **3k** | **`B-VENUE-PAIRS-REINIT`** (`#933`) | The published Kraken asset-pairs map is filled **once at boot** and nothing ever retries it — the failure path logs and continues, and a census finds no interval and no other production caller. **F-G-1 introduced the first consumer whose empty-map behaviour is STOP TRADING**, so a deploy during a venue blip halts crypto trading until a restart. ⚠️ **Frequency NOT measured — the mechanism is cited, not a rate.** | **AFTER `B-FUNNEL-PERP-CLASSES` (3j).** Owner CC-C. ✅ **F-G-1 landed the DETECTION half**: the state is now distinguishable from a per-symbol gap and raises one critical alert naming the restart. ⛔ **This row is the RECOVERY half** — a bounded boot retry plus a periodic re-check, and a decision on whether an unready map should gate the active engine as a whole rather than per-signal. **Not folded into F-G-1: it changes a SHARED service's lifecycle, not rounding** |
> | **3l** | **`B-VPG-ROW-ALIGN`** (`#934`) | The VPG row in the Filter Diagnostics Pipeline Summary emits its would-fail count in a `colSpan={2}` right-aligned cell, so under headers `Stage \| Quant \| Pattern \| Total` the count lands under **Pattern** and the population under **Total**. Seen on BOTH tabs (crypto 0/295, xStock 0/621). **The numbers are correct; the headers misdescribe them, and the VPG is lane-agnostic so the attribution does not exist.** | **AFTER `B-VENUE-PAIRS-REINIT` (3k).** Owner CC-C. ⚠️ **Display-only — the API payload is correct, verified independently.** ⛔ **Did NOT re-open F-G-1 for it:** Langston's ruling — a frozen batch does not re-open for a display defect whose value is correct. **If a Filter-Diagnostics display batch surfaces first, fold it in there and say so.** ★ Found at Step-7 UI verification, which is the step that exists to catch exactly this |
> | **3m** | **`B-RATE-LIMITER-RESET-DISPOSITION`** (`#936`) | `resetRateLimiter()` returns early when `NODE_ENV === production` and staging IS production, so it has **never run** — while its own comment claims it *"guarantees a clean state"*. What actually clears the login limiter on restart is the process being new. | **AFTER the F-G-1 close, before the next Part-F piece.** Owner CC-C. ⛔ **A BUG-TAXONOMY READ, NOT A LINE CHANGE** (Langston): the helper exists *"for automated testing"*, so whether anything automated logs into staging decides rule-24 outcome (2) *working-as-designed but unaddressed* vs (3) *legacy that no longer fits*. ✅ **NOT interfering with trading** — it never executes, and its body only touches the login limiter. ★ Kyle asked whether this was the old restart-reset that closed all trades and cleared the RTB: **it is not — that was the `initializeQueues` DB session sweep, DELETED 2026-07-16 (`#520`, `DELETED_COMPONENTS_LOG:694`)** |
> | **3m-ENUM** | ⭐ **RULED AN *ITEM*, NOT A BATCH (Langston 2026-09-13)** — reachable, so not latent: `pattern-recognizer.ts:461` emits `ABCD`, `volatility_edge` consumes it, both sinks reject it. **FIRST OBJECTIVE IS THE SIZING MEASUREMENT, NOT A MIGRATION — has the pattern-confirmed path ever fired, and is the rejection SURFACED or SWALLOWED?** ⚠️ **“One map entry” was too narrow: canonicalize-away is a FOUR-SITE change (`canonical-regime-strategy-map.ts:310`, `hybrid-compatibility-registry.ts:18`, `signal-orchestrator.ts:2965`, `volatility-edge.ts:264`) plus a second live producer at `selectContextAwareStrategy:860`.** ⛔⛔ **SCOPE NARROWED 2026-09-13 BY KYLE — READ `#1063`'s CORRECTION BLOCK BEFORE THIS ROW'S BODY. The regime half is WITHDRAWN (`GHOST_REGIME_NORMALIZATION` is a documented legacy→canonical converter, not drift); “three-wide drift” is WITHDRAWN; the real defect is ONE entry — `'volatility_edge': 'ABCD'` — in an eight-entry `STRATEGY_PATTERN_MAP` whose other seven are storable and whose PRECEDING LINE canonicalizes for exactly this reason. `abcd_long` is a QUANT STRATEGY with no pattern; `ABCD` is separately `volatility_edge`'s pattern label. ⚠️ **This may not warrant a batch at all — re-judge the size before scoping.** | ⭐⭐ **`B-PATTERN-ENUM-DRIFT`** (`#1063`) — **Owner CC-B** (traced it). ⛔⛔ **THE CODE DECLARES ENUM VALUES THE DATABASE CANNOT STORE, AND THE LIVE ONE SELECTIVELY DISCARDS OUR BETTER-EVIDENCED TRADES.** `shared/schema.ts:111` declares `pattern_type` with SIX values including `ABCD`; `migrations/0001_familiar_pete_wisdom.sql:1`, `drizzle/migrations/2026-04-22-initial-schema.sql:707` and live `pg_enum` all carry FIVE. ✅ **Positive-controlled: `ALTER TYPE … ADD VALUE` DOES appear in the corpus (`strategy_type` 'orb', `pair_regime`, `walter_memory_type`) and returns nothing for `pattern_type`** — the absence is real, not an instrument gap. ⛔ **SEVERITY IS SELECTIVE, NOT A CLEAN BLOCK (Langston):** `active-execution-engine.ts:4257` writes `sigMeta.patternType || null`, so `volatility_edge` signals arriving WITHOUT pattern confirmation open fine (11 rows in `closed_trades`, all `pattern_type` NULL) while the **pattern-CONFIRMED** subset from `STRATEGY_PATTERN_MAP` (`signal-orchestrator.ts:2952-2960`) can never open — both live sinks (`closed_trades` `:4368`, `active_open_positions` `:4479`) reject `"ABCD"`. ⭐ **We discard the confirmed half and keep the unconfirmed half, and it reads as normal in every count.** ⚠️ **NOT A REFLEX MIGRATION — the scope must rule (1) vs (3): `ABCD` is a HARMONIC PRICE STRUCTURE, not a candlestick, so “does it belong in `pattern_type` at all” is live.** If it stays: `ALTER TYPE … ADD VALUE` cannot run in-transaction — precedent `drizzle/migrations/2026-05-24a` (B79.0n). ⛔ **TWO SCOPE REQUIREMENTS (Langston, binding): (i) enumerate the FULL output set of `normalizePatternToCanonical` against the DB enum — fix the CLASS, not the instance; (ii) census EVERY `pgEnum` against `pg_enum`.** ✅ **(ii) ALREADY RUN, 2026-09-13 — the drift is THREE-WIDE, not one: `market_regime` missing ALL SIX code values (DB holds the RETIRED taxonomy `BULL_STABLE`/`BEAR_VOLATILE`/… — ZERO overlap with the 28-51-file live set), `pattern_type` missing `ABCD`, `tuning_status` missing `reverted`. ⚠️ **`market_regime` and `tuning_status` are LATENT — `adaptive_learning`, `telemetry_history` and `tuning_event` all hold ZERO rows** — but a future writer to any of them reproduces the live failure.** | **CC-B** | **MINTED 2026-09-13 ahead of 3n on Langston's `HOME:` line; SLOT is CC-B + CC-C's to settle, and Langston ratifies.** Root cause annotates **`#648`** (§9.5(b-ii) — CC-C logged the symptom 2026-08-01 and its causal explanation was withdrawn; no duplicate minted). ⚠️ **`#648` item 4 — whether the RTB removal OCCURS at all — blocks the SEVERITY claim, not the migration; it stays CC-C's.** ⛔ **Cite the warn at `active-execution-engine.ts:3495` — `#648` says `:2513-2516` and `ACTIVE_PATH_FLOW.md:249` says `:2228`; THREE refs for one line, `fix-follows-pointer`.** | — |
> | **3n** | ⭐⭐ **`B-PRICE-SIDE-BY-JOB`** — **Owner CC-C. KYLE DELEGATED THE DECISION TO CC-C + LANGSTON, 2026-09-03**, and his reasoning is the binding constraint: *"I want to see the trading results improve... so I would be biased in picking one that I think is going to improve our trading, but it might not actually be the correct thing to do if we're trying to have our system simulate as closely as possible the live trading."* ⇒ **THE TEST IS FIDELITY TO LIVE TRADING, NEVER BETTER-LOOKING NUMBERS.** | ⭐ **r5 (2026-09-11): THIS BATCH NOW CARRIES THE THREE-WAY PRICING DECISIONS D1-D10 (`Scope Files/PRICING_DECISIONS_2026-09-11.md`) — absorbs 3c, 3b.d, 3b.h-1, 3b.f-a, 3b.f-b, 3b.l, `#952`, `#966`. Commit boundary: OBJ-7 feed/plumbing lands and is proven live, then OBJ-8 decision layer switches on. Scope §7. Window reads on F-G-2 / `#951` / `#943` are Langston's.** ✅ **CORRECTION NOTE 2026-09-11 (CC-C, on Langston's review of the Codex grouped list): THE LEVEL-BASIS RULE WAS RULED 2026-09-04 AND THE TWO-CATEGORY WORDING BELOW IS SUPERSEDED.** Ruled: *a level's basis must be a price the market ACTUALLY PRINTED, carrying its age, or one we could ACTUALLY TRANSACT AT; a midpoint is neither* (`Scope Files/B_PRICE_SIDE_BY_JOB_LEVEL_CENSUS.md`; binding at `#927`; code `server/core/calculations/level-basis.ts`; OBJ-3a `884b9289e`). It admits venue-published closes, so only the crypto quant lane fails it. The text below is the pre-ruling record. ⛔⛔ **A PRICE DOES FOUR JOBS AND WE USE THE MIDPOINT FOR ALL OF THEM.** The midpoint is built at the FEED layer (`kraken-v2-translator.ts:73` overwrites the v1 `c` field), so crypto and xStock, paper and VTS, entry and exit all inherit it — `#952`/`#941`. The four jobs: **(1) SIGNAL GENERATION** — a setup exists, and where entry/stop/target SIT (`signal-orchestrator.ts:2387` reads the cache; `:2276-2278` set the levels off it); **(2) RANKING** across candidates; **(3) TRIGGERING** a stop or target (`active-execution-engine.ts:1503`); **(4) BOOKING** the result. ★ **THE RULE: a price that ESTIMATES VALUE stays the midpoint (indicators, regime, ranking, spread denominators — correct as-is); a price that BECOMES A LEVEL WE WILL ACT ON, FIRES an action, or is RECORDED as a result must be the side we could transact at** — entry on the ask, stop and target on the bid. ⛔⛔ **PER-LEG TRANSACTABILITY — AND “the same side” WAS MY WRONG r1 INVARIANT (Langston BLOCKER-2, 2026-09-03): an entry is a BUY and fills on the ASK, a stop and a target are SELLS and fire on the BID, so they are opposite sides BY CONSTRUCTION.** THREE sides are in play — level basis (mid), entry fill (ask), exit trigger (bid) — and a stop set 3% below a mid-derived entry, fired on the bid, against a real ask fill is **OUT BY ROUGHLY A FULL SPREAD, NOT HALF**, with R:R moving in OPPOSITE directions on the two legs. ✅ **THE INVARIANT: each leg expressed on the side THAT LEG transacts at, and R:R computed from those.** ⛔⛔ **THE JOINT IS OWNED HERE, AND ITS FORM IS DECIDED AT DEPLOY — NOT DEFERRED (Langston, r3).** `F-G-2` OBJ-0 is deployed but **NOT ARMED**; the live trigger stays on the mid for the whole window, and the switch at window close is **CONDITIONAL**. ⇒ ⛔ **THE GATE IS A DEPENDENCY, NOT A DATE (Langston C1): NO CRYPTO DEPLOY BEFORE `F-G-2`'s DISPOSITION IS RECORDED AT THE REF.** `2026-09-18T16:08Z` is the output of a conditional chain with five pre-registered ways to move (origin = LATER of {F-G-1 PASS, rider deploy}; a non-PASS; an in-window cadence SPLIT; one pre-registered EXTEND; VOID). **A date-shaped gate on a moving window lands the change INSIDE the window it protects.** ⇒ under the dependency the disposition is known at deploy **on every branch**: switch thrown ⇒ ship the coherence check as an ASSERTION; switch deferred behind the guard ⇒ counter form **and the guard batch is placed as a real plan row with an owner in the same turn**; window VOID ⇒ re-arm with the new window; mismatch becomes standing ⇒ assert the MISMATCH. **Never left as a counter with no reader.** ⚠️ **NOT A NEW OBSERVATION WINDOW — THIS IS A CORRECTNESS DECISION, NOT A PERFORMANCE ONE.** Measured spreads already in hand: median 0.199% crypto / 0.395% xStock, xStock p90 **11.784%** ⇒ the worst decile of xStock exits is judged ~6% above the bid. ⇒ **PLACED AFTER `3b.b` (`B-XSTOCK-FEED-SANITY`), WHICH IS ITS ONE REAL PRECONDITION** — a collapsing bid must be non-authoritative before anything triggers on the bid. ➕ **`#1047` — review at Step 9 with OBJ-7's residuals: a pre-existing WebSocket parse error drops about eleven messages a day, throwing shape unknown.** | **OPEN — RE-JUSTIFIED 2026-09-22 (alert `1ae9a06b`, open > 7 days).** OBJ-7 is done (deployed `b597f1bf2` 2026-09-11) and the exit/fill side's `8a-P2`, `8a-P3`, `8a-P4a`, `8a-P4b` are live and governed. **It stays open BY DESIGN until `8a-P4c` lands** — increment 1 is inside its pre-registered window to 2026-09-30T00:00Z, then increments 2-3 — because the two halves close on ONE completion report (Kyle, 2026-09-15). Declared open in `GOVERNANCE_EXCEPTIONS` (re-justified 13:24Z); the >7-day backstop cannot clear itself (`#1074`), so its alert stays active and is resolved by hand at close. Record: `B_PRICE_SIDE_BY_JOB_8A_P3_PROGRESS_REPORT.md`. Owner CC-C, Langston gates the design. |
> | **3n.a** | **`B-ORPHAN-ROOT-SCANNER`** — Owner CC-C. **Surfaced by Langston while verifying `B-PRICE-SIDE-BY-JOB`'s entry-point census; disposition (5) on inspection, CONFIRMED at the object by me.** | ⛔ **A SECOND, UNCOMPILED COPY OF THE FX5 SCANNER SITS AT THE REPO ROOT — 1,174 lines, 58,993 bytes, CARRYING ITS OWN `centralClock.subscribe('FX5Scanner', …)` AT `:292`** (plus `BATCH_19G_HF2/server/services/fx5-scanner.ts`). ✅ **DECISION-INERT TODAY, VERIFIED: `tsconfig.json` includes only `client/src`, `shared`, `server`, and NOTHING imports the root copy — both importers resolve `./fx5-scanner` inside `server/services/`. The entry-point census of SIX therefore stands.** ⚠️ **BUT IT IS A §15 LINGERING-LEGACY ITEM HOLDING A CLOCK SUBSCRIPTION THAT BECOMES A SEVENTH ENTRY POINT THE MOMENT ANYTHING COMPILES IT** — exactly the accidental-re-entry risk rule 18 exists to prevent. ⇒ **Delete both copies through the workflow, with a blast-radius check, a `DELETED_COMPONENTS_LOG` entry and `_archive/deleted-code/*.removed`.** | **AFTER `3n`.** Owner CC-C. |
> | **3n.b** | **B-ORPHAN-LEVEL-TABLES** — Owner CC-C. **Surfaced by the P3 level census, 2026-09-04; disposition (5) with evidence, not inferred from the names.** | ⛔ **THREE TABLES CARRY TRADE-LEVEL COLUMNS AND ARE BOTH EMPTY AND UNREFERENCED:** paper_sim_ghost_trades (entry_price, stop_loss, take_profit), paper_sim_trades_user_archive (same), paper_sim_open_positions_user_archive (stop_loss, take_profit). **MEASURED: 0 rows each on the live database, and ZERO production references each across server/ and shared/.** ✅ **CONTROL, same query: exit_decision_archive returns 2,685 rows and six production references — so the instrument distinguishes live from dead.** ⇒ **§15 lingering legacy, same class as 3n.a. Delete through the workflow with DELETED_COMPONENTS_LOG + archive.** ⚠️ **They are also a live hazard for THIS batch: any level-basis enforcement that enumerates sinks will keep re-finding them.** | **AFTER 3n.a.** Owner CC-C. |
> | **3n.c** | **B-TRAILING-STATE-DURABILITY** — Owner CC-C. **Surfaced by the P3 in-memory pass, 2026-09-04. NOT folded into 3n — it is a DURABILITY question, not a price-basis one.** | ⛔⛔ **THE STATE THAT GATES EVERY TRAILING EXIT LIVES IN `/tmp`.** `trade-safety.ts:891` — `TRAILING_STATE_FILE = '/tmp/trailing-states.json'`; `persistTrailingStates()` writes it, `loadTrailingStates()` reads it at startup, and `trailing-exit-controller.ts:1581` triggers the exit off that state. **MEASURED live 2026-09-04T12:0xZ: 526,769 bytes, 863 trailing states, rewritten every ~60s.** ⚠️ **SYMPTOM, NOT CAUSE (CONDUCT §8): I have NOT established that anything deletes it.** `/tmp` on staging is disk-backed (`/dev/sda1`), not tmpfs, so it survives a reboot; a systemd rule `D /tmp 1777 root root 30d` governs the directory, and the file’s mtime refreshes every 60s so an age sweep would not reach it while the app runs. ★ **WHAT IS ESTABLISHED: state whose contract is MUST SURVIVE lives in a directory whose contract is DISPOSABLE.** ⇒ **Scope: establish the actual risk, then move it to a durable home or state why /tmp is acceptable.** ➕ **2026-09-24 ITEM ADDED (§9.4 disposition 2, from `B-BOOK-STATE-RESTART-DURABLE` Step 2 §2b): the trailing-exit state loads AFTER the engines resume** (`server/index.ts:510` `resumeActiveEngines()` before `:897` `loadTrailingStates()`), so an exit-loop pass may run before trailing state is back. **A HYPOTHESIS, not a verified defect:** measure the gap on the next restart at this row's Step 1 before calling it one. | **AFTER 3n.b.** Owner CC-C. |
> | **3n.d** | **`B-GRID-REFUSAL-RATE`** — Owner CC-C. **Surfaced by F-G-1's §3a co-denominator at the close read, 2026-09-04. SYMPTOM, NOT CAUSE.** | ⛔⛔ **14,652 `GRID_REJECT` events since 2026-08-28 — 14,649 `stop_distance_after_rounding`, 3 `unorderable_triple` — across 88 distinct symbol/strategy pairs, against 24 crypto opens in the same window.** ⛔⛔ **EVENT COUNT IS NOT SIGNAL COUNT — RESTATED ON DISTINCT SIGNALS 2026-09-04 (Langston).** 88 pairs against 14,652 events is ~166 per pair, the signature of PER-CYCLE RE-EVALUATION. **MEASURED distinct `(symbol, strategy, entry, stop)` triples: `pivot_shift` 620 (6,618 events) · `support_bounce` 328 (3,409) · `morning_star` 220 (2,455) · `inside_bar_reversal` 135 (1,291) · `reverse_impulse` 71 (673) · `defensive_hedge` 23 (244)** — **≈1,397 distinct against ≈14,690 events, a ~10.5× inflation.** ⭐ **`pivot_shift` (620 distinct refused, 0 opened) and `reverse_impulse` (71 distinct refused, 0 opened) do not appear among the seven strategies that traded.** *(Independent cross-check: `rtb_shadow_pairings` in-window holds 51 `pivot_shift` and 4 `reverse_impulse` promoted rows — a few clear the seam and still none open.)* ⚠️ **AND THE PER-STRATEGY SPLIT IS A MONOTONE ACCUMULATOR since 2026-07-14, NOT a window counter — it is usable here ONLY because `GRID_REJECT` postdates the 08-28 deploy, so for that reason-key cumulative ≈ window. Stated as an argument, not assumed by proximity.** ⚠️ **NOT ASSERTED AS A DEFECT: refusing is the seam's designed behaviour when rounding would break the geometry, so a systematically tight stop relative to the tick SHOULD refuse. Whether the RATE is correct or the CHECK is too strict is UNESTABLISHED.** ⚠️ **And no rate is computable yet — two of §3a's four co-denominators (grid evaluations, signals generated) are unavailable on the window; the funnel's 769,693 is CUMULATIVE SINCE 2026-07-14 and dividing them would be a denominator mismatch.** ★ **MATERIALLY RELEVANT TO THE REACHABILITY WORK KYLE PLACED NEXT: a stop-distance refusal and a reachability ceiling are the same geometry seen from two ends.** | **AFTER `3n.c`, BEFORE the reach FIT.** Owner CC-C. |
> | **3n.e** | **`B-CANONICAL-CORPUS-ACCURACY`** — Owner CC-C. **`#733`, open since 2026-08-21; PLACED HERE 2026-09-06 because its entry carried a DUE DATE (`2026-09-04`) instead of a position, which §9.4 forbade on 2026-08-25 — and the date then expired exactly as Kyle predicted.** | ⛔ **FOUR FILES IN `bridge/canonical/` ARE MACHINE-REGENERATED FROM LIVE CONFIG WHILE `CLAUDE.md` §4 DESCRIBES THE WHOLE DIRECTORY AS A FROZEN HISTORICAL RECORD** — so a provenance read of one of those four gets today's config wearing a historical record's clothes, which is confidently wrong rather than absent. ✅ **NARROWED 2026-09-06 at `2fc13111d`: the committed `mapping-regime-strategy.json` now holds the CURRENT taxonomy (zero occurrences of the five old keys), so the stale-content hazard is discharged for that file.** ⛔ **THE RESIDUAL IS PERMANENT AND IS A HABIT RISK, NOT A DATA RISK: the generator writes the wall clock into two self-referential fields every run, so the staging worktree is dirty again seconds after every engine start with byte-identical content — `dt-deploy` refuses on every deploy, about nothing.** ⇒ **a guard that cries wolf every time trains sessions to discard staging's worktree unread, and the August instance proves the diff CAN be substantive.** **Decide one: stop stamping the clock into tracked generated files, or stop tracking them.** ⚠️ **Documentation + generator hygiene — NOT trading-behaviour code, so it does not compete with `F-5` for engineering time.** | **AFTER `3n.d`.** Owner CC-C. |
> | **3n.f** | **`B-DIAG-READ-INTEGRITY`** (`#1014`) — Owner CC-C. **Surfaced 2026-09-06 by rate-limiting my own measurement of `3n`, then reading the status code instead of the body.** | ⛔⛔ **A `429` ON THE DIAGNOSTICS LOGIN RENDERS AS AN EMPTY BODY, SO RATE-LIMITED, UNAUTHENTICATED AND SERVICE-DEAD ALL LOOK IDENTICAL.** The authenticated path takes a fresh token per call; a 150 s poll trips the limiter (**measured `login_http=429`, 3 consecutive samples**), the token comes back empty with stderr discarded, and the harness prints the same `READ_FAILED` it prints for an outage. ⇒ **`#661` leg 1 — an instrument whose silence carries no information, and which a future session will read as an outage or, worse, as a legitimate zero.** ✅ **FIX: log in ONCE and reuse the token; assert the HTTP status before parsing, so a 429/401 can never masquerade as an empty result.** ⚠️ **NOT folded into `3n` — `3n` READS this instrument, and a batch that both depends on a tool and fixes it grades its own dependency.** | **AFTER `3n.e`.** Owner CC-C. |
> | **3n.g** | ⭐ **`B-QUOTE-PEG-DEVIATION-WATCH`** — Owner CC-C. **Placed as Langston’s blocking condition 4 on row `8f`’s predicate, 2026-09-12.** | ⛔ **THE BOUND, STATED AS HE REQUIRED IT: admitting the set-quote pairs (`USDT`, `USDC`) leaves a denomination error bounded by PEG DEVIATION — currently UNMEASURED AND UNMONITORED — and under a depeg it becomes FX-SIZED WITH NO DETECTOR.** ★ **Why `8f` still ships without it: its predicate is SET MEMBERSHIP, a decidable fact at any ref, NOT an assertion that the peg holds** — so a depeg becomes a reason to change the SET, one edit at one SSOT, rather than a silent failure of the test. **This row builds the instrument that would tell us to.** ⚠️ **Both quotes have depegged historically; this is the silent-degradation shape, not a hypothetical.** | **AFTER `3n.f`.** Owner CC-C. ⛔ **NOT a gate on `8f`** — `8f` is safe by construction of its predicate; this closes the monitoring gap that safety leaves open. |
> | **3n.h** | ⭐ **`B-QUOTE-LEG-INTEGRITY`** (`#1050`) — Owner CC-C. **Placed 2026-09-12 as Langston's sibling to `3n.g`, after he BOUNCED my framing of it as an `8f` implementation detail.** | ⛔ **THE QUOTE AND BASE LEGS ARE DERIVED THREE WAYS AND THEY DISAGREE.** Two sites slash-split with an OR-fallback to the whole symbol (`active-execution-engine.ts:4172`, `routes.ts:12958`); the third suffix-strips and returns a TWO-WAY ternary — `routes.ts:5083` reports **every non-`USDT` pair as USD-quoted**, against a population carrying EIGHT distinct quote legs. ★ **Those are exactly the pairs `8f` exists to refuse, so the surface a reader would check cannot show them.** ⚠️ **That OR-fallback is `#546` absent-as-valid: a no-slash symbol stores the WHOLE symbol as the base and the column still reads populated.** `base_currency` is `.notNull()` at four schema sites, so the symmetric nullable quote column is unavailable. **Deliverable: ONE derivation, plus the stored column if it earns its place — a schema change on the trading record, change-class `architecture`, with a migration and a backfill.** | **AFTER `3n.g`.** Owner CC-C. ✅ **The cheap binding half — the writer-side assertion (exactly one slash, both legs non-empty, refuse or flag) — is taken into `8f` NOW and does NOT wait for this row.** |
> | **3n.i** | ⭐ **`B-QUOTE-ADMISSION-LEGACY-SWEEP`** (`#937`) — Owner CC-C. **Placed 2026-09-12: Langston's precondition that `8f` may not ship an admission gate without stating which of the FIVE dispositions `allowedTradingPairs` is.** | ⛔ **`8f`'s gate and this filter ANSWER THE SAME QUESTION ON THE SAME PATH FROM DIFFERENT SOURCES — that is the coupling that bites, not the JSON.** THREE objects, THREE dispositions, stated explicitly. **(i) `kraken.ts:695` `quoteCurrencies?: string[]` and `:724` `allowedQuotes` — DISPOSITION (5), genuinely dead: each occurs EXACTLY ONCE in the file, its own declaration or assignment, never read** (positive control: `blacklist`, a filter that IS used, returns 3). **Delete, rule 18.** **(ii) `active-scan-diagnostic.ts:141` + `:208` — DISPOSITION (3), RECONNECT: a WORKING quote test, defective only in being fed a hard-coded empty local literal built at `:102-107` and reached solely from two HTTP endpoints.** ★ **Once `8f`'s SSOT exists this must read the SAME source, or we ship a gate and a diagnostic screen that disagree — the `#938`/`#921` family again, one layer up.** **(iii) the DB column `allowed_trading_pairs` (`shared/schema.ts:230`, default `ARRAY['USD','USDT']`) — DISPOSITION (5): no reader anywhere in `server/` or `shared/`.** ⚠️ **Its non-empty DEFAULT is the trap — it reads TUNABLE, which is exactly the objection Langston raised against leaving an unread key in the archive JSON, and it applies here with more force because a DB default looks operational.** | **AFTER `3n.h`.** Owner CC-C. ⛔ **NOT a gate on `8f`** — `8f` ships once this disposition is STATED, which it now is; the sweep itself is separable work. ➕ **FOLDED 2026-09-12 (§9.4 (2)): 8f's `refusedQuote` reaches ZERO routes and ZERO client files, so the gate is LOG-ONLY and invisible to Kyle. Same surface, same reason as (ii) — plumb it here or the gate and the screen disagree.** |
> | **3n.j** | ⭐ **`B-HORIZON-GRID-COMPARABILITY`** — Owner **CC-B** (the only non-CC-C member of this cluster). **Placed 2026-09-12 as Langston's disposition on a defect I found in the horizon grid while recovering the reward-to-risk evidence — folded as a PRECONDITION on the geometry leg of `B-EXIT-POLICY-EVALUATOR`, not a standalone investigation.** | ⛔⛔ **THE GRID'S ROWS ARE NOT THE SAME POPULATION, AND ONE CELL PROVES IT WITHOUT ANY NEW MEASUREMENT.** The 2026-09-07 horizon grid reports P(stop) **58.7 % at 48 h** and **50.5 % at 72 h** on a stop fixed at 184 bps. ⭐ **Under first-hit exits on a FIXED origin set, stop-hit probability is NON-DECREASING in horizon by construction** — a longer window can only add hits, never retract one. ⇒ **a fall of 8.2 pp is arithmetically impossible on one population, so the rows describe DIFFERENT origin sets.** ⚠️ **The likely mechanism is the grid's own continuity rule** (≥60 % of minutes present, last bar inside the final 10 %): a longer horizon admits a different, smaller and later-biased set of origins, so the instrument's admission criterion moves with the axis it is measuring — the `wrong-object` class, inside the instrument rather than in the reading. ⛔ **CONSEQUENCE, and it is why this gates rather than informs: the +98 bps at 72 h / 600 and the ≈3.3× interior optimum are the two numbers the proposed baseline leans on, and BOTH are read off the rows this defect separates.** **Deliverable: re-derive the grid on the INTERSECTION origin set — origins satisfying continuity at EVERY horizon — and publish the per-row origin count and the overlap beside every cell, so a reader can see the denominator move.** ✅ **A falsifier is stated in advance: on a common origin set P(stop) must be monotone non-decreasing across all five horizons. If it is not, the defect is not the origin set and the first-hit assumption itself is wrong.** | **AFTER `3n.i`.** Owner CC-B. ⛔ **GATES any horizon number in `B-EXIT-POLICY-EVALUATOR` (2.4h) and in `RR_BASELINE_RECOVERED_EVIDENCE`** — both currently cite the 72 h row. ✅ **It is one re-run of an existing script on a restricted origin set, not a new instrument.** ⚠️ **Langston's binding condition stands on top of it: target and horizon do NOT move in the same step.** |
> | **3n.k** | ⭐ **`B-FAMILY-POOL-REACHABILITY`** (`#1052`) — Owner CC-C. **Placed 2026-09-12 from Langston BLOCKER-2 at OBJ-8 Step 4.** | ⛔ **`addFamilyPoolSurvivors` HAS ZERO PRODUCTION CALLERS** (whole-tree grep: its own definition, an 8f docblock, the 8f test). ✅ **CONTROL: `getFamilyPool` IS read live at `signal-orchestrator.ts:2039`, feeding family-aware strategy selection** ⇒ **the orchestrator reads a set nothing writes.** ⚠️ **NOT ESTABLISHED and the deliverable: whether the family pools were ever written, or whether a refactor moved family routing elsewhere. The PROVENANCE READ comes before any disposition** — deleting a writer without it is what rule 18 warns against. ★ 8f hardens the writer anyway, so a reconnection cannot reopen a quote hole. | **AFTER `3n.j`.** Owner CC-C. ⛔ **NOT a gate on `8f`.** |
> | **3n.l** | ⭐⭐ **`B-REST-SIDES-TO-CACHE`** (`#1056`) — Owner **CC-C**. **PLACED BY LANGSTON, Step-4 rider 2 on row `8c` P1, 2026-09-13.** ⛔ **THE REST ADAPTER PARSES THE BID AND ASK, LOGS THEM, AND THEN STORES ONLY THE MIDPOINT — the sides are discarded ONE LINE before the write.** `live-pricing-adapter.ts:876-877` parses `a[0]`/`b[0]`, `:890` logs them, `:896` calls `updateFromRest(normalized, midpoint, …)`. ★ **THERE ARE TWO REST WRITERS AND THEY DISAGREE:** the `price-cache.ts` poller (`:409-410`) DOES store both sides with `sidesCapturedAtMs`; this adapter path does not. ⇒ **a symbol's sides depend on which writer last touched it.** ⛔⛔ **WHY IT IS PLACED HERE AND NOT LATER: it puts a FLOOR under row `8c`'s rung-2 recovery that is an artefact of OUR OWN WRITE PATH, not of the venue** — so a low recovery may NOT be read as *“the ticker sides are not there.”* ➕ **FIRST ITEM, ADDED 2026-09-26 (Langston ruling on alert `5bfb2af5`; `#1056` amendments 1-2): FIX THE WRONG REST KEYS IN THE STATIC MAP.** `kraken-symbol-map.ts:140` gives `GBP/USD` the altname `GBPUSD`, and Kraken answers keyed `ZGBPZUSD`, so the 2-second poller writes a phantom entry and the paper exit trigger never sees fresh GBP/USD sides. The census found 2 such rows (`GBP/USD`, `ETC/USD` → `XETCZUSD`) and 6 dead rows for pairs Kraken no longer lists (EOS, ICX, MATIC, MKR, REP, WAVES /USD; rule 18). **Rule 23 fix-on-find:** built now in a mini-cycle through Langston, and deployed with the next deploy, which is held for the `8a-P4c` window. Before editing, read what else keys on `krakenRestPair` (the compact map, the WS translator, `fx-conversion-service.ts:151`'s local patch); the resolver beside the map is 🔒 LOCKED. | CC-C | **PLACED 2026-09-13** | **AFTER `8c`, and BEFORE `8a` — Langston's ordering and his reason: `8a` wires the trigger and would INHERIT THE FLOOR SILENTLY.** |
> | **3n.m** | ⭐⭐ **`B-BOOK-SUBSCRIPTION-REACH`** (`#1060`) — Owner **CC-C**. **KYLE DIRECTIVE 2026-09-13, after he challenged the premise of row `8c`.** ⛔ **THE ORDER BOOK IS THE PREFERRED PRICE SOURCE AND WE SUBSCRIBE IT FOR THREE COINS A DAY** — measured, whole-day distinct-symbol count — against a **35-41 symbol survivor pool** and a 337-per-cycle scan. ★ **`0.15%` IS OUR SUBSCRIPTION SET, NOT THE BOOK'S QUALITY.** Nothing in the pipeline asks for a book: the only external subscriber is a HEALTH-CHECK ROUTE (`routes.ts:10134`) covering OPEN POSITIONS ONLY ⇒ we start collecting the good data at the last instant it can inform the trade. ✅✅ **THE CAPABILITY ALREADY EXISTS: `crypto-spot-archiver` runs hash-mod SHARDED MULTI-CONNECTION, live at 2 connections / 406 symbols / sub-second age — subscribing `ohlc`+`ticker` and NEVER `book`.** ⛔⛔ **MEASURED 2026-09-13 (amendment 1 on `#1060`; instrument `scripts/analysis/book-volume-probe.mjs`, two arms × 10 min, out-of-band): the shard arithmetic answers the WRONG QUESTION and the connection count is not the constraint.** **Book frames/sym/min: 1,321.5 on the top-40 (the survivor-pool population) and 290.9 across a stratified random sample of the 416-symbol universe — against 8.4 and 1.0 ticker frames.** ⇒ **40 symbols = 52,860 book frames/min (881/s), 15.3 GB/day raw; 406 = 118,105/min (984/s per connection), 36.1 GB/day — ~290× the frames the two archiver connections carry today** (positive control: their own `rows_persisted_60s` reads 374/min against the probe's 406). ★★ **THE BINDING CONSTRAINT IS EVENT-LOOP TIME IN THE MAIN TRADING PROCESS, NOT Kraken's caps, not Cloudflare, not the REST budget — and MORE CONNECTIONS DO NOT RELIEVE IT because they all land in the same loop.** ⛔ **WS subscriptions are NOT REST calls** — the cost is message volume (~2,500/min today), not call budget; and the book need never be PERSISTED, so storage is a separate, optional decision. ✅ **BOOK MESSAGE VOLUME — DISCHARGED (above).** ⚠️ **STILL NOT ESTABLISHED: (i) ONE 10-min window on a Sunday, and book traffic tracks market activity; (ii) DEPTH 10 WAS ASSUMED, NOT CHOSEN — a shallower book cuts frames and bytes and is the most obvious lever, unmeasured; (iii) subscribe-to-usable warm-up; (iv) the unsubscribe path as symbols leave the pool.** ★ **AND ONE STRUCTURAL FINDING THAT CONSTRAINS THE DESIGN: book chatter is NOT proportional to trading activity — the LEAST-traded third still runs 197 book frames/sym/min against 0.3 ticker frames (584×), because a depth-10 update fires on any change at any of twenty levels. ⇒ a “subscribe only the quiet names” variant is not available.** ⛔⛔ **TWO CLAIMS OF MINE CORRECTED AND FOLDED IN AS SCOPE PRECONDITIONS: (a) “40 needs no new connection, 200 needs one” is UNEVIDENCED — `:269` is a SYMBOL COUNT with NO THROUGHPUT TERM and the binding constraint is FRAMES/SEC, so the volume factor must be MEASURED FIRST (subscribe 3 to `book`, count frames/min vs the ohlc+ticker rate on the same shard); (b) the archiver has NO dynamic membership and NO unsubscribe path — `unsubscribe` occurs 0 times against a control of 5 for `subscribe`, membership is set once and reloaded by DAILY CRON, while FX5 survivors rotate EVERY CYCLE. ⇒ the SHARDING is reuse; the CHURN is a NEW BUILD and may not be scoped as inherited.** | CC-C | **PLACED 2026-09-13** | ✅ **RULED by Langston: AFTER `3n.l`, BEFORE `8a`.** ★ **HIS REASON, not mine: the book is unavailable BY CONSTRUCTION at every job before a position exists, so `3n.l` is the FLOOR under the book, not an alternative to it — and a transactable side must exist WITHOUT the book for ~88% of the scanned population on `3n.m`'s best day.** ⛔ **CONDITION: `3n.m` may not deploy inside an open `3n.l` window without declaring the split at the boundary.** ⭐⭐ **AND OBJECTIVE 1 OF THIS ROW IS NOW KYLE'S RECORDING REQUIREMENT, AHEAD OF ANY SUBSCRIPTION CHANGE (2026-09-13): store THE BOOK AS IT STOOD AT THE MOMENT OF DECISION — one small record per decision, a few thousand a day, kilobytes — NOT every frame, which at 36.1 GB/day would fill the whole 200 GB plan in four days. It audits exactly what he asked (did we take the transactable side, or quietly average it), it is worth doing even if the subscription reach is cut back, and it does not depend on which case wins.** |
> | **3n.n** | ⭐⭐ **`B-DECISION-INSTANT-QUOTE`** (`#1060` am. 8) — Owner **CC-C**. ⛔⛔ **THE SIDE-VS-FRESHNESS RANKING IS UNMEASURABLE FROM THE EXIT-PROVENANCE COLUMNS AT ANY THRESHOLD — NOT "HELD PENDING A BETTER READ".** `exit_decision_price` is DECISION-TIME (`active-execution-engine.ts:1829-1830`, built once per position above the exit evaluation, on every tick); the COLUMN `exit_ticker_bid`/`ask` is the **ARCHIVER'S WITNESS at the PERSIST site** (`:1846`, `#911`). ⇒ **a comparison of the two measures the gap between two capture instants and a transactability defect IDENTICALLY, by construction.** ★ MEASURED on the correctly-named population (crypto maker, all three columns non-null, **n=24 exactly — no xStock row can be in it**, xStock maker checkable is 7): **20 of 24 book ABOVE the ask**, median ≈1.5, max 7.000, producer `kraken_ws_book_mid` on all 24 — **and that figure is WITHDRAWN as bearing on the ranking for the reason above.** ⚠️ **The same limit binds Langston's `14 of 24`, which he has WITHDRAWN rather than restate; the corpus carries TWO distinct `14`s — reconcile to neither.** ⇒ **THE FIX IS A CAPTURE CHANGE ON THE WRITE PATH, NOT A QUERY:** ⛔⛔ **CORRECTED SAME DAY — THAT PREMISE IS PARTLY FALSE AND THIS ROW WAS SCOPED ON IT. THE STAMPS ARE ALREADY PERSISTED, ON BOTH CLASSES.** **xStock:** `metadata.bookState.yields[].inputs` carries `bid`/`ask`/`last` **with `atMs`** (9 closed rows). **Crypto:** `metadata.fg2Shadow` carries `bidFirstExit`/`midFirstExit` **with `atMs`** AND `witnessAtBidExit`/`witnessAtMidExit` **with `capturedAtMs`**. ⭐ **MEASURED ON ONE REAL ROW (DASH/USD): decision `atMs` 1788840806077 vs witness `capturedAtMs` 1788840778019 ⇒ THE WITNESS IS 28,058 ms — TWENTY-EIGHT SECONDS — OLDER THAN THE DECISION.** That is `δ̄`'s window, readable per row, on data already in the table. ⇒ ★ **THE DECOMPOSITION IS COMPUTABLE TODAY on rows carrying these keys; this row's FIRST objective is the QUERY, and the capture change is what GENERALISES it to rows that do not.** ⚠️ **LIMITS: small populations (9 xStock); the `yields`/`fg2Shadow` entries are their own instruments' observations, so `atMs` makes the gap READABLE rather than assumed — CHECK THE GAP before treating either as decision-instant. The recomputation is NOT run.** ★ **Both keys are named in `aee:1848-1850`'s own docblock and neither CC-C nor CC-B followed the pointer — `fix-follows-pointer`, on a pointer already written down.** a decision-instant quote captured on the SAME read as `exit_decision_price`. ⛔⛔ **CORRECTED 2026-09-13 — the clause first written here was FALSE at the ref.** It said the ticker handler *"computes both sides at `:682-683` and DISCARDS them"*; `:675-700` is the MESSAGE ROUTER and has no ticker-side computation. ⭐ **The sides ARE parsed and DO travel** (`:845-865` → `live-pricing-adapter.ts:1171` → `CachedPrice.bid/ask/sidesCapturedAtMs`, `price-cache.ts:44-45`,`:63`) ⇒ **this row is NOT "build a retention path" but "stamp the sides the exit path ALREADY HAS, on the same read that produces `exit_decision_price`" — materially smaller, no new feed, no new subscription.** ✅ **The refusal to fill `exit_ticker_bid/ask` FROM THE BOOK still stands and is not what changes.** ⭐⭐ **AND `pos_in_spread` IS BOUNDED [0,1] FOR ANY SIDE WITHIN THE BOOK (bid 0.0 · mid 0.5 · ask 1.0) ⇒ a value OUTSIDE it CANNOT be a side error.** 20 of 24 sit outside, and CC-B's stop split sits at median −1.300 outside the other side — **3-4× the largest possible side error.** ⛔⛔ **AND ITS STANDING CHANGED 2026-09-13 — READ THIS BEFORE READING THE WITHDRAWN RATIO IN `#1060`.** This row went from *a row worth having* to **THE ONLY INSTRUMENT THAT MAKES THE DECOMPOSITION COMPUTABLE AT ALL.** `pos = 0.5 + δ̄/S_w` needs the spread **AT THE DECISION** (`S_d`) to separate the side term from the drift term, and no existing column carries it. ⇒ ★ **`3n.n` IS THE PRECONDITION, NOT A SALVAGE ATTEMPT** — a reader who meets the withdrawn `2.6-3.6×` first would otherwise read it the wrong way round (Langston's condition). | CC-C | **PLACED 2026-09-13** | **AFTER `3n.l`, BEFORE `3n.m`** — `3n.l` owns what the cache STORES, this owns what the exit path CAPTURES, and `3n.m` (coverage) cannot be ranked until this lands. |
> | **3n.q** | ⭐⭐ **`B-MAKER-FILL-TRANSACTABLE-SIDE`** — Owner **CC-C**. ➕ **STATUS 2026-09-22: BUILT as `8a-P3` (crypto, both lanes, deployed 2026-09-15) and `8a-P4b` X1/X2 (paper xStock, deployed 2026-09-19); the live-defect text below describes the state BEFORE those deploys. Window closed 2026-09-22 12:16:07Z; Step 10 governance landed 2026-09-22; the batch stays OPEN for `8a-P4c` (one batch, two halves). Record: `Batch Completion/B_PRICE_SIDE_BY_JOB_8A_P3_PROGRESS_REPORT.md`.** ⛔⛔ **LANGSTON RULED IT 2026-09-14 RATHER THAN PARK IT A SECOND TIME, AND THE RULING IS THE ROW:** *a resting order FILLING is job 3 — TRIGGERING — so Kyle's rule applies: the transactable side. A SELL fills iff **bid >= limit**; a BUY iff **ask <= limit**.* ⛔ **THE LIVE DEFECT: `evaluatePendingMaker` still fires on the MARK** — `active-execution-engine.ts:2399` passes `currentPrice` for a resting SELL, so **mid >= limit books a maker target fill while the BID sits below it.** ★★ **AND IT IS THE SAME TICK, THE SAME LOOP AND THE SAME POSITION AS `8a-P2`: after that row lands, one position REFUSES A STOP on an untradeable side and BOOKS A TARGET on one.** That asymmetry is the exact shape `8a-P2` BLOCKER-1 was about, surviving one layer out — and it survives on the leg that books a WIN. ⚠️ **NOT A FOLD INTO `8a-P2`, and Langston is explicit about why: the comparator is SHARED WITH ENTRIES** (`isMarketableAtPlacement`, both VTS lanes), so it moves a surface `8a-P2` deliberately did not scope. ⭐ **PROVENANCE: this is `#741` bucket-2 VERBATIM** — *"for a resting SELL the honest comparator is the BID… it needs its own ruling"* — **dispatched to Langston 2026-08-23 and never returned.** The ruling above closes that loop. | CC-C | **PLACED 2026-09-14** | **AFTER `8a-P2`, BEFORE `3n.o`.** ⛔ It must not precede `8a-P2` (same comparator, same loop) and must not follow `3n.o` (whose ceiling work assumes the fill side is settled). |
> | **3n.q2** | ⭐⭐ **`B-PRICE-SIDE-BY-JOB` `8a-P4` — THE xSTOCK HALF of `3n.q`/`8a`** — Owner **CC-C**. ➕ **STATUS 2026-09-22: `8a-P4a` + `8a-P4b` DEPLOYED 2026-09-19 (X3 withdrawn by Step 9 C1 the same night; re-land `3n.q7` after `3n.q8`); Step 8 part 2 confirmed by Langston 2026-09-22 06:38Z; Step 10 governance landed 2026-09-22. DONE from the list below: `8a-P4a`; paper xStock resting fills; the rest-placed line (`MAKER_PLACED`, after the insert); the decision-price comment. ➕ **2026-09-22: `8a-P4c` INCREMENT 1 DEPLOYED `bc199185e` — the telemetry-only instrument (no decision moved, fenced); Step 8 confirmed by Langston 16:08Z; window `2026-09-22T14:38:49.748Z` → `2026-09-30T00:00:00.000Z`, then rules A-D decide increments 2-3 (plan `Scope Files/B_PRICE_SIDE_BY_JOB_8A_P4C_AUDIT_AND_PLAN.md`).** REMAINING = `8a-P4c`: the xStock no-decision instrument, VTS xStock trigger + booking, the VTS hollow-book guard, C8, the no-ask policy (now with the lane asymmetry), the no-decision rail keyed on the trade id; ➕ its epoch migration also writes the REAL boundary instants (never `now()`) into `crypto_spot/vts` and `xstock_spot/vts` (`ADJUSTMENT_FRAMEWORK` epoch rule 7); and the `aee:2806` comment beside the decision-price stamp, which still says xStock triggers on the bid (stale since C1).** Kyle 2026-09-15: *one batch, two halves; nothing is called done until both land* — `8a-P3` (crypto) is at Step 9 (`Batch Completion/B_PRICE_SIDE_BY_JOB_8A_P3_PROGRESS_REPORT.md`). ⭐ **FIRST ITEM `8a-P4a` — the book-state RESEED ESCAPE** (formerly `B-BOOK-STATE-RESEED-ESCAPE`; scope `Scope Files/B_PRICE_SIDE_BY_JOB_8A_P4A_SCOPE.md`): a seed-implausible chain on a RECOVERED book never ends; live 2026-09-18 on all three held xStock positions, LOW past its stop. **Then** (list in the `8a-P3` record §6): the xStock no-decision instrument; paper xStock trigger on the bid; VTS xStock trigger + booking; xStock resting fills; the hollow-book guard for VTS; C8 VTS taker entry booking; the no-ask placement policy on both lanes (➕ 2026-09-22: asymmetric today — paper refuses at the depth gate, VTS rests; the paper line's `?? 'none'` is dead; `8a-P3` §5g); the VTS no-decision rail keyed on the trade id with an automatic re-arm; the `MAKER_RESTED` line after the insert; two comment fixes. | CC-C | **PLACED 2026-09-18** | **AFTER `3n.q` (`8a-P3`), BEFORE `3n.o`.** |
> | **3n.q3** | ⭐ **`B-VTS-NO-DECISION-VALVE`** — Owner **CC-C**. **Langston, `8a-P3` Step 8 C2, 2026-09-18:** a refused VTS exit has no time bound, and the trade ages toward the 7-day max-hold, which books a timeout AT THE MARK. ≥ 4 crypto VTS trades crossed 10 minutes blind in ~79 h. ⛔ **A POLICY DECISION, NOT A FIX.** ➕ **2026-09-19 (alert `4bde6fe1`, Langston): recurrence on AVL/USD, cause MEASURED — its NORMAL spread is 2.8-3.4% against the 0.02 exit ceiling, not feed impairment.** The entry/exit spread asymmetry behind it (VTS entry legs carry no spread ceiling, the exit trigger does) is ALREADY an item on row `3n.o` — ONE home, this is the pointer; the policy here must decide it together with that item. ➕ **SCHEDULED REVIEW (Langston flag 2026-09-19, unconfirmed): per-pass exit COVERAGE.** 3,961 open VTS crypto trades (`vts_open_trades` `state='open'`, read 02:15Z) against ~280 named in one resolve pass. **Bounded, not abandoned:** the oldest open trade is 2026-09-12 02:49Z (inside the 7-day max-hold) and 85,494 are closed, so trades ARE reaching max-hold. **The open question is whether every open trade's stop/target is checked each pass** — read the pass's selection path before calling it a defect. ➕ **2026-09-20 — THE CLASS HAS TWO CAUSES AND THE POLICY MUST SEPARATE THEM (alerts `6569ec15` EGLD, `d3247be2` PONKE, `eed66b94` AVL, all `no_transactable_side`).** Measured on `crypto_spot_ticker_snap`, 9 h to 2026-09-20 02:10Z, per symbol: **AVL** 64.3% of frames wider than the 0.02 ceiling, 92.3% of arrival gaps over the 90 s ceiling (n=14) · **EGLD** 100% wider, 66.7% of gaps over 90 s (n=4, 24 h volume ≈13 k) · **PONKE only 9.6% wider — but 50% of gaps over 90 s, median gap 92.4 s (n=73).** ⇒ **AVL/EGLD are STRUCTURALLY THIN (the spread ceiling binds); PONKE is a CADENCE case (the freshness ceiling binds on a market inside the spread ceiling).** A policy that only moves the spread ceiling leaves the PONKE shape untouched. ⚠️ **Instrument reach, stated:** these are CAPTURED SNAPSHOTS, whose own write cadence bounds the gap measure from below — they do not prove the live cache's `observedAt`; read the cached sides at the refusal instant before any ceiling moves (the alert body's own first instruction). ➕ **2026-09-20, A THIRD SHAPE — A TRANSIENT BLOWOUT ON A NORMALLY-HEALTHY SYMBOL (alert `bcaccdc6`, AKE/USD; Langston measured, CC-C re-derived the hourly profile).** `crypto_spot_ticker_snap`, 12 h to 06:40Z: **0.0% of frames over the 0.02 ceiling every hour from 21:00Z to 04:00Z** (hourly medians 0.146-0.546%, 108-353 frames/hour — well-fed and tight), then **16.9% over at 05:00Z** and **78.4% over at 06:00Z (median 2.558%, n=97 and still accumulating; Langston read 82.6% later in the same hour)**. ⇒ **the three shapes are now: STRUCTURALLY THIN (AVL, EGLD) · CADENCE (PONKE) · TRANSIENT BLOWOUT ON A HEALTHY SYMBOL (AKE).** ★ **This one STRENGTHENS the time-bound-on-refusal framing over any ceiling move:** a per-symbol or standing ceiling calibrated on AKE's normal 0.3-0.4% book cannot reach a morning at 2.5%, and loosening it to reach one would admit every structurally-thin name the ceiling exists to refuse. **No harm on the trade itself** (Langston, 1,284 frames since `opened_at` 02:14:11.938Z: 0 stop touches, 0 target touches; nearest approach 13% above the stop). ⚠️ Same instrument reach as above — captured frames, not the live cache at the refusal instant. Alert left ACTIVE with its three siblings. ➕ **2026-09-22, KII/USD (alert `0bc02856`) — the TRANSIENT shape, not a new one (Langston measured at the venue: 2.1% against the 2.0% ceiling for ~10 min, 0.37% a minute later; he resolved it).** ⛔ *Corrected: my first note here read KII as a candidate fourth shape because its snapshot frames stop at 2026-09-20 20:06Z — that is `#1072` (the archiver's symbol set frozen at the last restart), a MEASUREMENT gap, not an exit-decision cause; exits read the live feed.* ⚠️ Its 7-day max-hold trips ~2026-09-22 16:49:09Z; the booking arm (a `bookedNoBidClamp` increment vs a bid booking) is read after it. ➕ **ITEM ADDED 2026-09-22 (`#1073`): the same question on the PAPER lane — a price refusal skips the resting order's deadline check, and FILL WINS when evaluation resumes; 0 of 320 exit rests past the deadline so far, entry leg unmeasured. Measure first.** ➕ **2026-09-22 (alert `70511ef0`, Langston-triaged and routed here) — EGLD IS THE STRONGEST CASE YET AGAINST LOOSENING THE CEILING.** Measured at the venue (Kraken, 3 consecutive reads): bid 4.15 / ask 4.79 = **14.3% spread**, **1 trade in 24 h** (74 units). On `vts_EGLD_USD_strong_bull_trend_1789808590766` the **mid 4.47 sits ABOVE the 4.45 target while the bid 4.15 sits BELOW the 4.18 entry** — a wider ceiling would book a fabricated +6.5% target hit into the post-epoch VTS corpus on a position sellable only at a small loss. The refusal (`no_transactable_side`, 2% ceiling) is correct and protective. Scale: 1-2 of 293 looks per pass. ⛔ **THE VALVE SITS ABOVE THE GUARD:** the 7-day max-hold fires on this trade at **2026-09-26T09:03:10Z** and books the timeout at the MARK — the very fiction the refusal is holding back. **INSTANCE DISPOSITION — AGREED by Langston 2026-09-22 15:09Z, with four conditions:** no valve build before Saturday — a code change would be a BUILD CHANGE inside `8a-P4c`'s pre-registered window (§B4: a build change splits it), so instances are handled by NAME, not by code, and their closed VTS rows are excluded by id from every outcome-sourced read (never edited or deleted). **(1) It is a SET, not one id**, with an enumeration obligation; **(2) the premise was checked before anything is excluded** — pinned at the code by `b65-tec-parity.test.ts` D7 (the valve at `tec-evaluator.ts` step 2 returns the mark before step 2b refuses, and the booking resolver's no-bid arm books that mark); **(3) ONE home a crypto outcome read consults: `ADJUSTMENT_FRAMEWORK`, calibration epochs, RULE 8** — the register; **(4) evidence captured at the booking instant, and the excluded n and its direction published** — the error is one-signed (the mark sits at or above the bid, so every excluded row OVERSTATES). ➕ **FOLDED INTO THIS ROW (§9.4 disposition 2): the booking ARM must be recorded on each closed VTS row.** Today it is only a per-pass counter (`bookedNoBidClamp`, `out.log`, ~4.5 h reach), so membership cannot be proven after the fact without an instant capture. ➕ **AND THE SAME WRITE LABELS THE CLASS FROM THE TICKER (`#1075`, §9.4 disposition 2):** `vts-runner.ts:3816` re-derives `asset_class` from the symbol with `exchange = 'kraken'`, so xStock closes on tickers that are also Kraken crypto pairs are archived as `crypto_spot` (20 rows since 09-01: 17 on shared tickers, 3 on xStocks delisted while their trades were open). Write the carried class, fix the `pairFriction` fallback, census the same-class sites named at `#1075`, re-run the collision intersection, and give its quarterly re-audit a real trigger (the one five documents cite does not exist). **RIDER (Langston):** if `8a-P4c`'s window splits for an independent reason before 09-30 (increment 2, a hotfix), the valve fix rides that split instead of waiting. The policy for bounding a refused exit in time stays in this row, in order. ➕ **2026-09-24 — A FOURTH CAUSE: THE CROSSED BOOK (Langston triage of alert `af1de12c`, venue-measured).** ETH/GBP, a liquid pair (about 2,000 trades a day): at 14:20Z Kraken's book held a stale ask at 2015.03 **below** bids around 2020-2022. `level-basis.ts:220` refuses `crossed_book` **before** the age check (`:232`) and the spread check (`:238`), so **neither dial this row argues about can reach it.** ⚠️ **It also INVERTS the exclusion's sign:** a crossed mid understates the bid. The one-signed premise in `ADJUSTMENT_FRAMEWORK` rule 8 is corrected. The instance is decided by capture `cc-c-capture-ethgbp` at its 2026-09-28T09:34:34Z max-hold (GBP-quoted, so the frame table cannot reach it). ➕ **2026-09-24 14:43Z — A DESIGN QUESTION ON THE CROSSED-BOOK CAUSE (Langston, from `f706084e`; the second crossed instance, SOL/GBP, came 101 s after ETH/GBP):** an exit is a SELL and transacts on the BID, which was live and about 0.025% tight, but the refusal fired because the ASK was stale and below it. ⇒ Should a sell exit refuse on the ask side's state at all? ⚠️ **Per-side staleness is not available at the `level-basis` layer, so this is a SHAPE change, not a one-liner.** Placed here as an item of this row (§9.4 disposition 2), to be decided with the valve policy, in order. Also added: GBP pairs feed VTS learning while the live lane is dollar-only, which is Langston's corpus-representativeness question with CC-C. ➕ **2026-09-24 15:40Z, the ETH/GBP instance measured (Langston, alert `c04ce79c`, the third mint that day):** one ask about 19 minutes stale sat below a bid about 1 s fresh, so the side a sell would transact on was healthy. **No exposure:** 0 stop or target touches in 385 checks, nearest approach 0.65% above the stop. Both GBP alerts are left ACTIVE by his routing, because resolving mints a fresh copy while the books stay crossed. ➕ **ITEM ADDED 2026-09-24 21:37Z (§9.4 disposition 2, Langston triage of `d007059a`): CARRY THE REAL REFUSAL REASON INTO THE ESCALATION ALERT.** The alert body (`vts-runner.ts:3404-3409`) explains `no_transactable_side` only as a bid outside the age and spread ceilings and points the reader at those two settings, and neither can reach a crossed book. The level-basis reason exists on `_et.selection`, but `_et` is declared inside the crypto block at `:3343` and never reaches the builder at `:3396`, which sees only `decision.noDecisionReason`. Verified at the ref by CC-C. It cost Langston two rounds of manual checking at the venue on 09-24. ➕ **INSTANCE NOTE 2026-09-24 22:10Z (Langston, `c04ce79c`; still crossed at 21:38Z, refusals live, no exposure): THE CAPTURE TIMERS NOW SURVIVE A REBOOT.** The EGLD, ETH/GBP and SOL/GBP captures were transient timers that a reboot would have erased, and for the two GBP trades a capture is the only instrument that can decide membership. They are now persistent units `cc-c-capture-egld|ethgbp|solgbp` (`/etc/systemd/system`, enabled, `Persistent=true`, so a run missed while the box is down fires at boot, with its own timestamp); same schedule, same script, same log files. ➕ **POINTER 2026-09-25 (Langston triage of `d007059a`, 09:37Z):** the alert system has no *held, with a decision date* state, so this row's deliberately-active GBP alerts escalate to Kyle as neglect. **That gap is `#679`'s (CC-B, open since 2026-08-07, a proposed `snoozed-until` state); the instance is recorded there as amendment 1, not re-homed here.** ➕ **ITEM ADDED 2026-09-26 (§9.4 disposition 2, Langston's Class B ruling, rider 1): COUNT AND PERSIST THE `clamp_no_mark` ARM.** EGLD's max-hold closed with no live mark and was booked at its entry price (arm `clamp_no_mark`, now `ADJUSTMENT_FRAMEWORK` rule 8 Class B). The pass line counts only `clamp_no_bid` (`vts-runner.ts:3534`), so a `clamp_no_mark` booking leaves no trace in any counter. Add a `bookedNoMarkClamp` field beside `bookedNoBidClamp` on the same `[8a-P3][VTS_TOUCH]` line, and carry this arm in the per-row arm write this row already owes. Until then Class B membership is the code-derived predicate (timeout AND exit price = entry price), which can overcount but never miss. | CC-C | **PLACED 2026-09-18** | **AFTER `3n.q2` (`8a-P4`).** |
> | **3n.q4** | ⭐ **`B-EXIT-LINE-IDENTITY`** — Owner **CC-C**. The trade id and asset class on the `[11.6][Exit]` line (the only close-reason carrier) **and on the VTS no-decision streak line** (Langston, `8a-P3` Step 8 C2), so a close or a streak is read by identity, never by a symbol-and-second join. | CC-C | **PLACED 2026-09-18** | **AFTER `3n.q` (`8a-P3`) — not inside it: changing the emitter mid-window splits the window.** |
> | **3n.q5** | ⭐ **`B-BOOK-STATE-RING-INDEPENDENT-BOUND`** — Owner **CC-C**. Candidate C of `8a-P4a`: a release bound for a seed-implausible book-state chain that does NOT depend on the retained spread ring (K two-sided frames with BOTH sides moving, under a DB-governed class ceiling). ⛔ **The PERMISSIVE direction the `8a` r5 BLOCKER-3/4 rulings fought, and the "separate gated decision" `book-state-tracker.ts:296-297` names.** **Homed, not built:** the pre-registered rule returned A + the named arm (PRIMARY 4 of 468 stranded, 0 held; SENSITIVITY 0), `Scope Files/B_PRICE_SIDE_BY_JOB_8A_P4A_AUDIT_AND_PLAN.md` §A7. **REOPENS if** the named arm's logged real ratios show a held symbol stranded. | CC-C | **PLACED 2026-09-18** | **AFTER `3n.q2` (`8a-P4c`).** |
> | **3n.q6** | ⭐ **`B-EXIT-DECISION-RUNG-STAMP`** (`#1064`) — Owner **CC-C**. **Placed under `3n`, runs AFTER `8a-P4c`** (inside `3n.q2`), so one fix serves both classes. **Langston, `8a-P4b` Step 4 CONDITION-3, 2026-09-18:** the ENTRY seam self-identifies its decision basis (`entryPriceSource` `kraken_equities_ws:raw_ask` vs `raw_ask_unguarded`); the EXIT seam does not: `_exitProvenanceBase` keeps `kraken_equities_ws_mid` / `kraken_equities_ws` while the xStock rest fill and trigger now decide on a raw BID, and crypto has carried the same asymmetry since `8a-P3`. ⇒ a post-deploy exit row cannot be cut by whether its trigger was judged. **Build:** stamp the exit decision rung (guarded / unguarded / ladder rung) on the exit provenance, both classes. ⚠️ **Until it lands:** `book_state/xstock_spot/enabled = 1` on staging (read 2026-09-18); if it is flipped mid-window the xStock exit population splits at that flip and nothing on the row says so. | CC-C | **PLACED 2026-09-18** | **AFTER `8a-P4c` (inside `3n.q2`).** |
> | **3n.q8** | ⭐ **`B-BOOK-STATE-RESTART-DURABLE`** (`#1066`) — Owner **CC-C**. The xStock book-state guard's outside datum — the retained spread ring (SIM S25b `_retainedSpreads`) — survives a restart. **Why:** a restart empties it, so every held xStock cold-seeds VACUOUSLY; during the 2026-09-19 post-close blowout that validated ANET's 32%-spread book against itself and stopped it out 12 s after the C1 deploy (`#1066`). Same class as the AMR EV-gap window (in-memory rolling state that reports its cold behaviour as normal). ⛔ **DESIGN CONSTRAINT (Langston): persistence REUSES the r5 write gate — only a ring from a chain that was not `seedImplausible` AND set `observedMovement` — it never re-derives it.** A ring persisted from the wrong chain is a DURABLE CONTAMINATED ring, and per BLOCKER-4 that fails OPEN (median 1.986 ⇒ threshold 5.958 admits the seed median 0.004 refuses): durable + wrong provenance is strictly worse than absent. **The scope names its relation to `3n.q5`:** q5 manufactures genesis WITHOUT a ring, this preserves genesis ACROSS a restart — the same question, and they must not be built in contradiction. ➕ **2026-09-22 (Langston, `8a-P4` Step 8 part 2): this row is also what makes `8a-P4a`'s own escape legs (§A8 `SEED_ESCAPED`, §A9 `ringAfter`, `seedImplausible=true`) observable at all** — on 09-21 every seed was vacuous (`retainedMedianNow=none`) because the 21:19Z restart re-vacated the ring, so those legs COULD NOT OCCUR and no amount of waiting discharges them. | CC-C | **PLACED 2026-09-19** | **BEFORE `3n.q7`** — the bid re-land depends on it. ➕ **2026-09-24: STARTED — Step 1 APPROVED by Langston (scope `6e2c5ec20`), Step 2 in progress. Deploy after the `8a-P4c` window closes (2026-09-30T00:00Z).** |
> | **3n.q7** | ⭐ **`B-XSTOCK-BID-TRIGGER-RELAND`** — Owner **CC-C**. The xStock stop/target TRIGGER back on the transactable bid, after `8a-P4b` Step 9 C1 took it off (a symmetric post-close blowout's stub bid fired a false stop, `#1065`; record `8A_P4B_AUDIT_AND_PLAN` §L). ⛔ **This row IS the absolute plausibility test `book-state.ts` names as "a separate, gated decision"** (the relative arms cannot see a symmetric widening). **Langston's three conditions (2026-09-19):** (1) the would-refuse rate at the proposed threshold across held xStock names in RTH and at the 13:30Z open, from the C1 instrument's real 1.5 s frames (`X3_BID_DIVERGENCE_START/END` + the `EXIT_TRIGGER` frame tag), each rate with its per-leg denominator and an RTH/off-hours split; (2) a false-HOLLOW instrument; (3) the reseed → `8a-P4a` escape chain driven through the decision site to a terminal row. ⚠️ **The first cut already prices the cost (2026-09-17 replay, 68 symbols): a spread gate anchored to daytime spreads refuses every captured frame for more than 1 h overnight on 25 of 68 symbols.** A decision that holds a live, wide, transactable book unmanaged overnight goes past Kyle's 2026-09-03 ruling (a price we cannot see) ⇒ **that hold policy is KYLE'S call, brought to him with the numbers, before any verdict half ships.** ➕ **FOUR INSTRUMENT NOTES FROM THE C1 APPROVAL (Langston 00:53Z) — they bind condition (1)'s rate:** (a) `endedBy=converged` is wrong for a run that ends because the frame stopped being capturable (`bid=none`) — add `frame_lost` before any rate is computed; (b) `ticks` and `durMs` count different populations (a guard-refused tick adds to `durMs`, not `ticks`), so `durMs` overstates the interval a re-landed trigger would have been live — state the convention beside every rate; (c) the static stop/target is exact TODAY (`trailing_enabled_active=false`, `moonbag_qualifying_strategies=[]` on `xstock_spot`, read 2026-09-19) — ⚠️ **tripwire:** if either flips mid-window the stop leg becomes a LOWER BOUND; the break-even ratchet's independence from those locks is an OPEN check; (d) never cite `_xsBidDivergence.size` as a count of open runs (entries leak on closes that skip the exit check). **Depends on `3n.q8`:** a blown book is refusable only against an outside datum, and a restart destroys it. | CC-C | **PLACED 2026-09-19** | **AFTER `3n.q6`; its measurement window runs from the C1 deploy.** |
> | **3n.s** | ⭐ **`B-PRICE-DOC-CONSOLIDATE`** — Owner **CC-B**. **Combine `PRICE_FEED_MAP.md` with `PRICING_DATA_ARCHITECTURE.md` into ONE canonical price-feed governance document** (Kyle 2026-09-15: *"that needs to happen"*). Content per the map's §5.3 Langston ruling: import the architecture doc's §2 provenance, §5 cost and §5b operational reach; retire its §3.1; re-derive its §6; trace the six `UNTRACED` no-price cells. ⚠️ **Named in the map since 2026-09-14 as "placed after `3n`" — but NO ROW EXISTED HERE until this one. Naming was not placing.** | CC-B | **PLACED 2026-09-15 — held, not started (Kyle: weekly budget)** | **AFTER `3n.q`.** The audit in `3n.t` reads this document, so it comes first. |
> | **3n.t** | ⭐⭐ **`B-FEED-BY-SITUATION-AUDIT`** — Owner **CC-B**. **Kyle 2026-09-15: for EVERY situation — path (paper mode · VTS · live mode) × asset class (crypto · xStock) × the job the price does (level-setting at signal birth · ranking · trigger · maker fill · booking) — which feed is wired today, and which is the correct feed per the consolidated document?** Output = one table, every cell filled, every mismatch cited `path:line` at the ref. ⚠️ **Live mode has no running path yet (Phase 21) — its cells are read from the code it would run, and labelled so.** Read-only investigation; each mismatch becomes an item in `3n.u`. | CC-B | **PLACED 2026-09-15 — held** | **AFTER `3n.s`.** |
> | **3n.u** | ⭐⭐ **`B-FEED-MISMATCH-FIX`** — **the fix batch for every mismatch `3n.t` finds.** ⛔ **Overlaps `3n` (`B-PRICE-SIDE-BY-JOB`, CC-C + Langston own the side decision)** — so ownership and whether a mismatch folds into `3n` are settled with CC-C + Langston at `3n.t` close, not assumed here. ⭐ **DETERMINED 2026-09-18 (Langston + Coltrane; record `B_FEED_BY_SITUATION_AUDIT_FIRST_PASS.md` §DETERMINATIONS) — owner CC-B, TWO items:** **(1) CLOSE-FILL FRESHNESS CONTRACT** — the close is the only fill site that skips the existing warmth gate (`warmth_max_age_ms`); three arms (warm walk / stale walk + stamp / cold); flatten paths stop requesting at ENTRY (slippage column = gross P&L today) and stop accepting an unbounded-age `last_known_good_reserve`; manual close-all ROUTES through `forceClosePosition` (keeps the button; fixes a silent position deletion). **(2) xSTOCK LEVEL-VALIDITY GATE** — `active-dispatch:181-186` checks tick age while levels come from a 15-minute bar; add a bar-age term, default hold; measure downstream ask re-validation first; alert `404e978a` folds in. **NOT MINE:** every xStock side cell → CC-C `8a-P4`; bar-close levels stay (`8c`); VTS xStock clamp re-arm → `3n` OBJ-7. | CC-B | ⏳ **DEPLOYED `323ae2776` 2026-09-19 00:02:43Z — Langston Step-8 CONFIRMED; OBSERVATION (four of six close arms unobserved). Split out: `3n.u2`, `3n.u3`, `3n.u4`.** | **AFTER `3n.t`**, and after `8a-P4` is placed (Langston: three cells hand their xStock half to it). |
> | **3n.u2** | ⭐⭐ **`B-ENTRY-LEVEL-RECHECK`** — Owner **CC-B**. **Langston Q4 ruling on `B-FEED-MISMATCH-FIX` Step 2, 2026-09-18: split out as its own subject.** ⛔ **NOTHING RE-CHECKS A SIGNAL'S LEVELS AGAINST THE CURRENT PRICE BEFORE THE FILL, ON EITHER CLASS** — no entry-overshoot check, no reward-to-risk at the fill price (the one min-RR check, `normalizeAndGateTarget`, runs at birth), no stop-already-breached check at open, and no signal age carried (the xStock dispatch input has no timestamp); RTB has had no TTL since R9.3-C, so a signal can wait in the pool unbounded. Evidence: `B_FEED_MISMATCH_FIX_PRE_AUDIT.md` A-5. Constraints already ruled: RR through the EXISTING `getPerClassTargetGate` (no second resolver, `#1052`); unknown-token arms fail closed; does NOT touch CC-C's flat tick gate (`3b.f-c`). Needs its own Step 1. | CC-B | **PLACED 2026-09-19 — not started** | **IMMEDIATELY AFTER `3n.u`** (Langston's placement). |
> | **3n.u3** | ⭐ **`B-CLOSE-WRITER-COSTS`** — Owner **CC-B**. **Langston Step-4 findings on `B-FEED-MISMATCH-FIX`, 2026-09-18, homed not folded:** (1) `closePosition` still DELETES a position whose open trade row cannot be found — now loud (log + alert) but the honest disposition is to WRITE the `closed_trades` row from the position (symbol, quantity, entry are all on it; stranded-clear already proves the pattern); (2) the stranded-clear writer books `fees: '0', slippage: '0'` — a cost claim no fill model produced. Both are close-row honesty in writers `3n.u` touched but did not own. Frequency today: 0 rows of either kind in `closed_trades` (07-15 → 09-18). | CC-B | **PLACED 2026-09-19 — not started** | **AFTER `3n.u2`.** |
> | **3n.u4** | ⭐ **`B-ENGINE-STOP-DURATION-COLUMN`** (`#1067`) — Owner **CC-B**. **Found 2026-09-20 by EXERCISING the stop path at `3n.u` Step 7 (Kyle-approved).** ⛔ **AN ENGINE STOP CANNOT BE RECORDED ONCE THE SESSION HAS RUN > 24.85 DAYS:** `active-engine-service.ts:954-964` writes the elapsed run duration into `run_for_ms` (`integer`, `shared/schema.ts:2074`), declared as the REQUESTED run length; the live session was 5,648,048,403 ms old and the write overflowed. **The flatten completed and the session row still says `running`** — the stop looks failed and is done, the row looks running and is not. Two defects: the overflow, and the wrong-object reuse of an input column for an output. | CC-B | **PLACED 2026-09-20 — not started** | **AFTER `3n.u3`.** |
> | **3n.v** | ⭐⭐ **`B-REACH-BASELINE-ADJUST`** — Owner **CC-B**. ⛔⛔ **KYLE RULED 2026-09-15: the one-way lock at `ADJUSTMENT_FRAMEWORK.md:18` ("no per-strategy row looser than its class default without realised-excursion evidence") GOES.** His reasons: the class defaults are self-described placeholders; the evidence already gathered (strong_bull_trend, crypto, 1,288 maker trades replayed on raw 1-minute bars: 2R −0.059 · 4R +0.052 · 6R +0.108) is enough to TRY a change; no other variable carries a rule this hard. ⇒ **Adjust the `(target, reach ceiling, min_rr)` triple for `strong_bull_trend`, `vwap_pullback` and the other blocked strategies as a NEW BASELINE PLACEHOLDER, still calibrated in Phase 25, and watch for signs of life.** Carries: rewrite `ADJUSTMENT_FRAMEWORK.md:18`; correct row `2.4g-3`'s false premise (VTS DOES hold refused trades — 2,906 closed strong_bull_trend tagged `unreachable`); delete dead `target_floor_pct`. ⚠️ **Still stated with the values: the 48 h horizon was chosen, and a minute holding both target and stop was scored a win.** | CC-B | **DEPLOYED `40f22a1bb` 2026-09-20T21:19:40Z — Step 10 of 11, OBSERVATION OPEN.** CI 4/4 per job, run `35538205688`. ⏳ **The record is a PROGRESS report: a 7-day rollback window with its trigger and both pre-deploy baselines registered BEFORE the deploy, and the gates still UNEXERCISED at deploy+15 min.** ⭐ **HOLD RELEASED by Kyle 2026-09-20** (*"strong bull trend and VWAP pullback settings aren't the only settings that should be adjusted"*), and the `8a-P2` gate is discharged. **Langston: Step-1 APPROVED w/ six conditions, Step-2 PROCEED, Step-4 CHANGES-NEEDED → RULED the xStock ceiling ships.** Ships SIX rows across five cells; **five proposed cells WITHDRAWN on evidence.** | **AFTER CC-C's exit-side fix (`8a-P2`).** Kyle: *"I want Claude Analyst to finish its work on the exit, then we work on this."* Does NOT wait for `3n.q`. |
> | **3n.v2** | ⭐ **`B-VTS-CLASS-LABEL-INTEGRITY`** (`#1068`) — Owner **CC-B**, cross-referenced to **CC-C** (the price-side lane owns the writer). ⛔⛔ **ROWS LABELLED `asset_class='xstock_spot'` ARE CARRYING CRYPTO SYMBOLS, AND THEY BOOK INSTANT WINS.** Found at `3n.v` Step 2 while checking a result that was too clean: the xStock `strong_bull_trend` cohort is **89 of 89 rows exiting `TP_target_hit`, zero stops, zero time-stops, median hold 1.0 MINUTE** against a target six ATRs away — on **STX/USD and STRK/USD**, which are crypto pairs. **CONTROL: the same query on crypto returns `SL_hit` 1,002 / `TP_target_hit` 715 / `time_stop` 51 at 855–1,022 minute medians.** ⭐ **Langston measured the breadth: SEVEN symbols carry BOTH class labels, and `STX/USD` under `xstock_spot` spans 0.1272 → 1127.25 — two price regimes under one key.** ⚠️ **IT CONTAMINATES ANY PER-CLASS VTS STATISTIC OVER THIS WINDOW, including ones other batches may already have drawn.** ⛔ **And it relabels `xstock_spot/strong_bull_trend` to n=0, not to a readable number** — all 256 rows in that cell, all-time, are three symbols priced ≤ $7.43. | CC-B | **PLACED 2026-09-21 — not started** | ⛔⛔ **RE-SEQUENCED 2026-09-22 (Langston, §13 disposition FOLD — do NOT mint a key batch, one exists): AFTER `3b.h-4` `B-SYMBOL-CLASS-IDENTITY` (`#1024`, owner CC-C, KYLE-RULED 2026-09-09 — *"An xStock and a crypto coin sharing a ticker are DIFFERENT ASSETS; both trade; identity becomes `(symbol, asset_class)`"*), which is itself blocked behind `3b.h-2` `B-UNIVERSE-REFRESH-ACTS`.** ⇒ **THIS ROW IS THE BACKFILL HALF ONLY** — the key must be unique first or a cleaned corpus re-contaminates at the next collision. ⚠️ **AND `3b.h-4`'s OBJ-1 IS A CENSUS OF EVERY SYMBOL-KEYED STORE, NOT A MIGRATION — do not duplicate it here**, and `2.4c` `B-RTB-SIGNAL-IDENTITY` (`#1006`, mine) is forbidden from minting its own encoding: it takes `assetSymbolKey` from `#1024`. |
> | **3n.v3** | ⭐ **`B-GATE-WILDCARD-REFUSE`** (`#1069`) — Owner **CC-B**. **Langston's C-1 at `3n.v` Step 4.** `3n.v` moved the unresolved-asset-class fail-hard from the deleted `target_floor_pct` onto the per-class `min_rr` row — **and that rests on a property of the DATA, not of the code: the resolver scores a wildcard `asset_class` row 0 and MATCHES ANY KEY, so ONE global `min_rr` row would silently satisfy an unresolved class and the throw would vanish.** ⚠️ **THE PRECEDENT IS IN THE SAME CONSTANT FAMILY, TWICE — `min_rr_unknown_floor` and `reach_atr_max_unknown_floor` each already carry a global row.** `3n.v` ships a migration invariant (4b) and a test that PINS THE GAP rather than closing it; **the durable fix is a resolver read that refuses a wildcard-asset-class match for a key that named a concrete class.** | CC-B | **PLACED 2026-09-21 — not started** | **AFTER `3n.v2`.** |
> | **3n.v4** | ⭐ **`B-SILENT-STRATEGY-CENSUS`** (`#1070`) — Owner **CC-B**. ⛔ **`abcd_long`, `breakout` and `orb` are WIRED ON BOTH LANES AND HAVE NO BUCKET IN THE GUARD TRACKER AT ALL — zero evaluations in 89 days, not zero passes.** Something stops them before the geometry guard, and it is neither gate `3n.v` touches. ⚠️ **TWO LIVE HYPOTHESES AND THE CENSUS MUST DISCRIMINATE THEM (Langston): the guard call sits BELOW each detector's early returns, so "something upstream refuses them" and "their detectors simply never fire" both fit the same silence.** ✅ **Cheapest first cut: whether each appears in the regime→strategy map at all.** *(`liquidity_trap` is NOT a member — deliberately disabled since Batch 45, long-only-incompatible, and correctly absent.)* | CC-B | **PLACED 2026-09-21 — not started** | **AFTER `3n.v3`.** |
> | **3n.v5** | ⭐ **`B-OPEN-OBLIGATION-SWEEP`** (`#1071`) — Owner **CC-B**. ⛔⛔ **A HOMED OBLIGATION THAT NOTHING WATCHES — the `#1005` / `#754` shape, found the expensive way at `3n.v`.** The 2026-08-17 migration wrote its own tripwire (*"sma_trend_ride crypto 'unreachable' share 0% -> ~9%; >15% means the VTS geometry did not transfer and the row is revisited"*); it FIRED, `#696` was opened inside 48 hours with Langston's HOLD ruling and two ordered discriminators — **and four weeks then passed with nobody re-reading it, so a fresh audit rediscovered the tripwire as if it were new and nearly overruled a criterion that had been written down in advance.** ⇒ **Homing an item is not watching it.** Scope: what re-surfaces an open obligation that has no alert behind it. | CC-B | **PLACED 2026-09-21 — not started** | **AFTER `3n.v4`.** |
> | **3n.w** | ⭐ **`B-EXIT-MAKER-VS-TAKER-REVIEW`** — Owner **CC-B**. **Kyle's observation 2026-09-15: in paper mode many maker exits are profitable and taker target exits all look negative.** Analyse paper outcomes by exit fill type (stops excluded — a stop must stay taker), and assess the option **"a target exit that does not fill maker does NOT fall back to taker"**, in simulation first. Read with `3n.v`'s results: if raising targets does not rescue taker target exits, that option goes to Kyle. | CC-B | **PLACED 2026-09-15 — held** | **AFTER `3n.v`** (its result is an input) **and `3n.q`** (maker fill honesty). |
> | **3n.x** | ⭐⭐ **`REVIEW — NECESSARY VS NICE-TO-HAVE FOR LIVE MODE`** — ⭐⭐⭐ **WIDENED AND STARTED BY KYLE 2026-09-23 — this row IS the pre-live inventory, and the working file is `Scope Files/PRE_LIVE_INVENTORY_WORKING.md`.** *"generate a complete list of what needs to be done in Phase 19 (what is left) and what is planned for phase 25, 16, and 20 … and 21 … going through every session's task list, the phase 19 plan, the roadmap, and Langston's archives … slotted and not slotted … and dedupe that list … categorized … must be done before live, would be extremely helpful to do before going live, and wait until after we have gone live. From there we pick the items we will do before going live and reorganize our roadmap and session task lists — assigning each item to a session including trialing Coltrane as an implementor instead of a reviewer."* **Now covers phases 19, 25, 16, 20 AND 21** (it read 19 and 25 only), **three buckets** (it read two), and **ends in a reassignment**, not a list. ⭐ **LANGSTON'S FRAMING, 2026-09-22, adopted as the standing test:** the run order is **19 → 25 → 16+20 → 21, strictly sequential (Kyle 2026-06-08)**, so every item in 19/25/16/20 is ALREADY scheduled before live — **the burden sits on a DEFERRAL past live, not on a MUST**, and any re-order is Kyle's. **MUST is anchored to the Phase-21 hard-blocker list** (`21.1.a` #953 · `21-3c` #734 · `21-3d` `B-MODE-DELETE-SCOPE` · `21-3a` #401 · `21-3b` · `19-17b`) and to what Kyle needs to judge *"comfortable in paper"* honestly, and lands as a **Phase-21 entry gate table in the §6 switch-on form**. ⚠️ **The row was homed here on Langston's §13 challenge — *"the inventory has no home"* — and it did: this one, from 2026-09-15.** — **Kyle 2026-09-15: list what is left in Phase 19 and Phase 25, split into what is NECESSARY to judge whether paper mode trades at a good success rate (and then go live) versus what can come after go-live.** Kyle decides the split. | CC-B drafts, Kyle decides | **SCHEDULED REVIEW 2026-09-15** | **AFTER `3n.v` and `8a-P2`** — Kyle: *"after we get a few more of these things done."* |
> | **3n.r** | ⭐⭐ **`B-TSC-COVERS-TESTS`** — Owner **CC-C**. ⛔⛔ **A REQUIRED FIELD DOES NOT COMPILE-FORCE A TEST FIXTURE, BECAUSE `tsconfig` EXCLUDES TEST FILES — AND IT FIRED TWICE IN ONE BATCH, SO IT IS A CLASS, NOT AN INCIDENT.** (1) `LevelBasisRungKey.stage` — the first run after `stage` landed minted a key literally named `active:crypto_spot:undefined:book`; the fix was a RUNTIME throw, because the type could not reach the fixture. (2) `8a-P2`'s `triggerPrice` — tsc correctly forced all THREE production callers and reached **none** of 38 fixtures across three files, so CI went red on the very parity contract the changed module's docblock names as mandatory. ⛔ **DISPOSITION 4 IS NOT AVAILABLE: "note it in the header" is what was done the first time, and the note did not stop the second.** (Langston, 2026-09-14.) ⇒ **THE ROW: a CI typecheck pass that INCLUDES test files**, so that *required* means what every author already assumes it means. ⚠️ **NOT a change to the build `tsconfig`** — tests are excluded from the BUILD for good reason; this is a separate check with its own config. ★ **WHY IT IS WORTH A ROW RATHER THAN CARE: the failure is SILENT AND DELAYED — the author sees green locally, the reviewer sees a required field and reasonably infers the census is closed, and the truth arrives from CI minutes later or from production later still.** | CC-C | **PLACED 2026-09-14** | **IMMEDIATELY AFTER `3n`** (Langston's slot, confirmed). ⛔ Not a gate on `8a-P2` — that row's fixtures are already repaired; this stops the NEXT one. |
> | **3n.o** | ⭐⭐ **`B-CRYPTO-MARK-AGE-GATE`** — Owner **CC-C**. ⛔⛔ **PREMISE REWRITTEN 2026-09-14 (Langston BLOCKER on `8a` r3, re-derived by me at `6b9155e9e`) — THE ROW SURVIVES, ITS ORIGINAL PREMISE DOES NOT.** It was placed reading *"a crypto stop can be evaluated against an arbitrarily old mark and nothing refuses it."* **THAT IS FALSE. The crypto mark IS age-bounded, structurally, at 2,000 ms** — `aee:1652` `getPriceWithFallback(symbol, 2000)` → `live-pricing-adapter.ts:1332` serves only `age <= WS_CACHE_FRESH_MS` **AND** `isKrakenVenueSource`, else REST-fetches, else re-serves as `last_known_good` (`:1404`) which fails the venue predicate at `aee:1672` and skips the tick. ⛔ **`RUNNING_ISSUES.md:7278` ALREADY RECORDED THAT MECHANISM** (*"flat 2,000 ms, hardcoded … + the `isKrakenVenueSource` actionability gate"*) — the finding was filed against a ledger that refutes it. ⇒ ★★ **THE REAL GAP, AND IT IS NARROWER AND SHARPER: THE BOUND IS AN EQUALITY BETWEEN TWO INDEPENDENT LITERALS, AND THE ONE THAT READS LIKE THE KNOB IS INERT WHERE IT BINDS.** The venue branch tests the adapter's PRIVATE `WS_CACHE_FRESH_MS` (`lpa:368`); the engine's `2000` ARGUMENT IS NOT READ THERE — it governs only `lpa:1346` `age <= staleThresholdMs`, which re-serves the cached entry **under its original source**. ⚠️ **Raise the call-site literal and you do not widen the venue window — you OPEN that branch, and a STALE entry still tagged `kraken_ws` passes the venue predicate into `evaluateTECExit`.** ⛔ **AND EIGHT OTHER PRODUCTION CALLERS ALREADY SIT ON THE FAR SIDE OF THE EQUALITY:** `routes.ts` ×5 (`:10728`, `:12264`, `:12799`, `:12888`, `:13088`), `aee:917`, `active-portfolio-manager.ts:308`/`:631` pass **5000**; `verification-test-protocol.ts:331` passes **30000**. ⚠️ **Whether any of them DECIDES anything is UNTRACED — that census IS this row's first job, not a claim made in advance.** ⇒ **THE ROW'S SUBJECT: the bound is a per-call-site literal, not class-keyed, not DB-governed, not risk-derived, and never counted as an age refusal.** **Folded in on Langston's surfacing:** `aee:917` computes `slTriggered`/`tpTriggered` for the diagnostics endpoint off a 5,000 ms window with **no venue predicate** — display only, same question. ⛔ **NOT a reflex port of the xStock ceiling: that one is RISK-DERIVED PER SYMBOL from a σ cache, and whether crypto wants that form or a flat one is the scope question, not a formality.** ⭐⭐ **INPUT DELIVERED 2026-09-14 BY `8a-P2` r6/r7 — THE RISK DERIVATION THIS ROW'S SCOPE QUESTION WAS WAITING FOR, plus the residual that argues for the per-symbol form.** **DERIVED BOUND: >= 2.50 s** (held-name crypto, `f`=0.10, worst threshold of a `trade_count` sweep; the pool p90 is **41% HIGHER** and is WRONG-POPULATION - ~400 thin names we never trade drove it up, and held-name sensitivity across the sweep is 1.77x against the pool's 4.8x). **`8a-P2` SHIPS 2,000 ms as a TIGHTENING below that bound**, matched to `aee:1891`'s mark-loop constant - ⛔ **the two are NOT coupled: changing `aee:1891` does not automatically move the trigger ceiling, re-derive first.** ⚠️ **AND THE RESIDUAL IS THIS ROW'S, BECAUSE `8a-P2` CANNOT FIX IT:** at 2,000 ms the effective `f` is 0.051 at the held p90 but **0.165 at the held p99** - holding 0.10 there needs **730 ms**, which no feed delivers. ⇒ ★ **A FLAT CEILING CANNOT BE SIMULTANEOUSLY RIGHT FOR A 0.26% NAME AND A 0.84% NAME - that is the argument FOR the per-symbol form, and it is now a measurement rather than a preference.** *(Langston condition 3 on `8a-P2` r6: the per-symbol ceiling needed a HOME, not 'named as follow-on'. It is homed HERE rather than as a new row - this row's scope question already IS it.)* ⭐⭐ **SECOND INPUT DELIVERED 2026-09-15 BY `8a-P2` B-4 — AND IT IS A SCHEDULED RE-DERIVATION, NOT A WARNING.** `EXIT_TRIGGER_MAX_SPREAD_FRACTION = 0.02` was derived from `spread <= 2·D·(1+f)` and calibrated against a held-name tail. ⛔⛔ **THE TAIL MOVED WITHIN ONE DAY: 2026-09-14 p99.9 1.6012% / max 4.407% (n=90,833) -> 2026-09-15 p99.9 2.0095% / max 5.5319% (n=91,226, Langston re-derived).** ⇒ the shipped 2.0% sat ABOVE p99.9 for ONE DAY and now sits just BELOW it — **ON THE TAIL KNEE.** The caveat fired before the ink dried. ★ **THAT IS THE ARGUMENT FOR THIS ROW, MEASURED RATHER THAN ASSERTED: a POOLED tail drifts with the traded universe and a flat ceiling must chase it; a PER-SYMBOL bound DISSOLVES the drift.** ⚠️ **AND `D` IS DOING DOUBLE DUTY IN THAT FORMULA** — the true `D` is the LIVE mid-to-stop distance at the trigger instant, calibrated here at the entry-time distance, the LOOSEST member. As the mid walks to the stop, `D_live -> 0` and residual overbooking -> `spread/2`, up to **1.0%** at the ceiling. **That residual is unbounded by any flat number and is this row's to close.** ➕ **ITEM ADDED 2026-09-19 (Langston triage, alert `d457e5e4`) — THE VTS OPEN-BUT-CANNOT-EXIT GAP:** VTS crypto ENTRY legs take `ENTRY_LEG_NO_SPREAD_CEILING` (`vts-runner.ts:3220`, and `:2261`) while the VTS exit trigger takes `VTS_EXIT_TOUCH_MAX_SPREAD_FRACTION` = 0.02 (`:3325`) ⇒ VTS can open a name whose book is too wide for its own exit rail. First live instance: AVL/USD, opened 00:10:39Z, spread 1.97-2.01% straddling the 2.00% ceiling; isolated (`openNoTriggerStreaks` ≤ 1 of 3,926 open crypto trades, Langston's read). **The per-symbol ceiling this row derives must bind the ENTRY leg too, or the gap survives the derivation.** ➕ **ITEM ADDED 2026-09-24 (Langston triage, alerts `d007059a` SOL/GBP + `c04ce79c` ETH/GBP) — A HYPOTHESIS, NOT A FINDING: ENTRY-SIDE SELECTION TOWARD ANOMALOUS BOOKS.** The only two open GBP-quoted VTS trades (2 of 3,192 open, `vts_open_trades` `state='open'`, read 2026-09-24) sit on the only two crossed GBP books, while 5 other GBP pairs read clean (Langston, at the venue). ⚠️ **A RIVAL THE ITEM MUST EXCLUDE FIRST — EXIT-SIDE RETENTION:** a crossed book refuses the exit (`level-basis.ts:220`, row `3n.q3`), so a trade on one cannot leave the open set, and the open set is then biased toward currently-refusing books whatever entry did. Both opened 2026-09-21 between 09:34 and 09:37Z, three minutes apart, and have been held about 78 h; closed GBP trades since 09-01 average 14-64 h per pair (47 opens across six pairs, XRP/GBP the most at 22). **The discriminator is whether each book was already crossed at its own entry instant, and no entry-time book measurement exists** (the frame table does not capture GBP pairs, per row `3n.q3`). ⇒ Not decidable from history; decide what entry records before claiming either cause. | CC-C | **PLACED 2026-09-14; TWO DERIVATION INPUTS LANDED (age 09-14, spread 09-15)** | **AFTER `3n.n`.** ★ `8a-P1`'s measured exit-population age distribution is the input this row needs, so it cannot precede it. |
> | **3n.p** | ⭐ **`B-EXIT-TICKER-LEG-ADAPTER-SIDES`** — Owner **CC-C**. **PLACED 2026-09-13 AS LANGSTON'S CONDITION 5 ON THE `8a-P1` TICKER-LEG RULING: he struck my "it is a P2 question" as a NON-DISPOSITION (§9.4) and he is right.** ⛔ **THE EXIT PATH'S OWN PRICE ROW ALREADY CARRIES THE TWO SIDES AND THE EXIT CANNOT SEE THEM, BECAUSE THE RETURN TYPE DROPS THEM.** `live-pricing-adapter.ts` `CachedPrice` carries `bid` (`:290`), `ask` (`:291`), `sidesCapturedAtMs` (`:293`), `venueObservedAtMs` (`:299`) — and `PriceQuote` (`:232-255`) carries NONE of them, with none of `getPriceWithFallback`'s four return sites (`:1333`, `:1351`, `:1379`, `:1400`) projecting them. ⭐ **AND `CachedPrice`'s OWN DOCBLOCK (`:283-289`) NAMES A CONSUMER THAT IS NOT THERE:** *"`getPriceWithFallback` returns this object, and `active-execution-engine.ts:1485` consumes it"* — **`aee:1485` at the ref is a `console.error` in the xStock hollow-yield catch.** ⛔ **AND THE ROW CARRIES BOTH HALVES, BECAUSE THE PROJECTION ALONE WOULD SHIP A WORSE INSTRUMENT:** three writers put **null** sides on that row with **no carry-forward** — `fetchPrice:592`, `seedLastKnownGoodPrice:1253`, and **`aee:1760`, which is the exit loop nulling the sides for its OWN position's symbol on the direct-REST leg.** ★ **WHAT IT BUYS, AND IT IS THE REASON IT EXISTS RATHER THAN A TIDY-UP: the adapter row carries a FINE-GRAINED `producer` (`PriceProducer` includes `kraken_ws_book_mid`), which is the field that would settle F2's ambiguity — today a rung-2 acceptance labelled `kraken_ws` is EITHER a genuine ticker write OR the book's own top, and `level-basis.ts:980-982` says outright *"the field that would settle it does not exist."*** ⚠️ **`8a-P1` was RULED onto the shared-cache leg instead (Langston 2026-09-13), so this is NOT a blocked dependency — P1 ships without it and names the residual as a stated limit.** | CC-C | **PLACED 2026-09-13** | **AFTER `3n.o`.** ⛔ **NOT a gate on `8a-P1`.** ★ Its value rises once the age ceiling is load-bearing at `8a-P2`, because the projection is what puts the sides INSIDE the 2,000 ms venue gate the shared-cache read sits outside of. |
> | **4** | **`F-5` — per-strategy reach structure** — ⭐⭐ **PULLED FORWARD BY KYLE 2026-09-03: *"I would like to see if we can squeeze the reachability in just after that"* (after `3n`). HIS COMPLAINT IS THAT IT HAS WAITED AGES, AND THE PLAN'S OWN TEXT SAYS HE IS RIGHT.** | **BUILD ONLY, behaviour-neutral**, seeded at today's value. ⛔ **The FIT is gated on F-E's clean fills and MUST NOT close as "reach calibrated"** — but the BUILD does not wait for F-E, which is why it can sit here. ⛔⛔ **AND THE FIT IS NOT WAITING ON AN OBSERVATION WINDOW EITHER — RE-DERIVED AT THIS PLAN 2026-09-03 AFTER I TOLD KYLE THE OPPOSITE.** I classed reachability as "tuning, needs a clean window". **FALSE, and this row and row 8 say so:** the FIT is gated on **`F-E`**, and row 8 states `F-E` ★ **NEEDS NO NEW TRADES** — it grades the **547 closed trades already in hand** against retained venue bars. `F-E`'s own gate is *"#1 live and #3 done"*: **#1 `B-EXIT-PROVENANCE` CLOSED 2026-08-30**, **#3 `F-G-1` deployed `56ac8067a` with its criterion (alert `2093a98a`) reading 2026-09-04.** ⇒ ★ **NEITHER HALF OF THE REACH WORK REQUIRES A FRESH WINDOW. It has been treated as blocked on data that was never required** — the structure was explicitly unblocked in this very cell, and the fit runs on history. ⇒ **SEQUENCE: `3b.b` closes → `3n` (four jobs) → `F-5` structure → `F-E` on existing history → the reach FIT → only then the paper-trading reset and the clean window.** ⚠️ **THE RESET COMES AFTER, NOT BEFORE** — a fresh window opened before the price changes land would measure the old ruler again, which is the loop this whole sequence exists to break. | **2nd — immediately after `3n`.** Owner CC-C. ➕ **2026-09-24: the STRUCTURE is DELIVERED** by `B-GEOMETRY-REACH-BASELINE` OBJ-A.1 (CC-B, closed 2026-09-13): `reach_atr_max` resolves per (strategy × class), with no per-strategy rows seeded. **What remains of this row is the FIT, gated on `F-E` (row 8).** |
> | **4.b** | ⛔ **`B-KILLSWITCH-DENOMINATOR`** (`#618` remaining legs) — Owner CC-C | ⛔⛔ **THE DAILY-LOSS KILL SWITCH'S RATIO IS NOT YET SOUND — `#618`'s own status line says it: *"NUMERATOR CLOSED, DENOMINATOR OPEN."*** ✅ **AND THE PART EVERYONE WAS POINTING AT IS ALREADY DONE — measured 2026-08-30 at the code and the live DB, because both the work ledger and my memory said "build pending / highest priority and UNTOUCHED" and both were STALE.** `B-BALANCE-TRUTH` shipped the cap fix: `storage.ts:3156` now requires `limit: number | 'all'`, **the `|| 100` default is GONE**, and `closed_trades.mode` is live `NOT NULL` on all 665 rows with `getClosedTradesGlobal` filtering on it (`// Step F (#618)`). ⛔ **WHAT REMAINS: (1) the DENOMINATOR leg — the risk-envelope half; (2) the guardrail-set adjudication before the $150 sizing change, which `#618` records as *"scoped, NOT started"*; (3) a NAMED, STILL-LIVE residual — Langston's rider at `storage.ts:3164-3168` says the required-`limit` contract is *"strong against accident, not airtight"* because an `any`-typed options bag satisfies it and is flagged by NOTHING, and `routes.ts:12120` `const options: any = {}` is exactly that site.** | ➕ **2026-09-23: `B-BALANCE-TRUTH` (`#618` legs 1-2) CLOSED — `Batch Completion/B_BALANCE_TRUTH_COMPLETION_REPORT.md`; this row carries what remains.** ⛔ **PLACED HERE BY KYLE, 2026-08-30: *"put the kill switch activity batch, whatever you wanna call it, AFTER the F series of fixes"*** — so it sits immediately after `F-5` (row 4), the last F-series item, and ahead of the BTC/DOGE egress fix at row 5. ⚠️ **It reaches the RISK ENVELOPE, so it does not get quietly re-ordered forward again without Kyle.** |
> | **4.c** | ⛔ **`B-MODE-PREDICATE-SWEEP`** (`#736`) — Owner CC-C | **Three raw-SQL readers of `closed_trades` do not filter on its `mode` column**, so the moment live mode writes its first row they mix live and paper: `ready_to_buy_service.ts` (the RTB stop-out cooldown — a live stop would hold a symbol out of the paper queue) and `exploration-lane.ts` (the daily exploration quota and the anneal count). `B-BALANCE-TRUTH` Step F partitioned every STORAGE reader (fenced); raw SQL outside storage bypasses that. **Also carries: delete `getClosedTradesGlobal`** (zero callers, `limit || 1000` — rule 18) with its `DELETED_COMPONENTS_LOG` entry. **Closing condition:** every raw-SQL reader filters `mode = ${mode}` and the partition fence is extended to raw SQL. **Failure condition:** any live row visible to a paper read, or the reverse. ⚠️ **Why it is placed only now:** its home was written 2026-08-21 as *"due 2026-09-11, immediately after B-BALANCE-TRUTH closes"* and never entered the plan; found at that batch's close, 2026-09-23. **Pre-live:** the no-op property that makes it verifiable expires when live writes its first row. | **PLACED 2026-09-23** | **AFTER `4.b`** — the same `closed_trades`-by-mode reader family. |
> | **5** | **`B-SCANNER-EGRESS-NORMALISE`** | **BTC and DOGE are not being traded at all** — the scanner's Kraken egress uses symbol forms the venue rejects. Normalise at the egress (3 un-normalised sites + the shared cost-cache key + 2 dedupe joins). | ✅ **CLOSED 2026-08-30, same-day functional verification, NO observation window.** Deployed `fd81ce18c` 16:36:32Z; Langston approved at the ref; CI 4/4. **Verified two-sided at the deploy boundary: `XBT/%` 65,268 rows → 0 with `BTC/%` absent → 126; `XDG/%` 8,738 → 0 with `DOGE/%` absent → 16 across all 8 wsnames; `%/XBT` 42,098 → 0 with `%/BTC` absent → 94; `ETH/%`+`SOL/%` controls unchanged.** Zero history rejections; `BTC/USD` reaching strategy evaluation. ⛔ **A GUARD SHIPPED WITH IT** — the normalisation sits above the B63.3 prefetch and would have handed 31 BTC-quoted pairs the strong-DBS bypass, whose live `minVolume` is **0**; ≥8 clear its price floor and `TBTC/XBT` carries ~$110/day. Bypass now denied to BTC quotes, **verified at zero**. Homed out: `#965` (3b.j) · `#966` (5.a) · `#967` (5.b) · `#968` (3b.k). |
> | **5.a** | **`B-NONFIAT-QUOTE-DENOMINATION`** (`#966`) — Owner CC-C | ⛔⛔ **31 BTC-QUOTED PAIRS (`AAVE/XBT … ZRX/XBT`) BECOME TRADABLE FOR THE FIRST TIME AS A SIDE EFFECT OF ROW 5 — AND NOTHING HAS EVER CHECKED WHETHER OUR MONEY ARITHMETIC IS CORRECT WHEN THE QUOTE CURRENCY IS NOT A FIAT.** Position sizing, the friction model, Net Expectancy and the guardrails all assume a quote unit that does not itself move. **A BTC-quoted position is two exposures wearing one price.** ⛔ **Excluding the quote slot is NOT the alternative — it would re-emit a venue-rejecting form.** | **IMMEDIATELY AFTER ROW 5, and it is the reason row 5 is not finished when it deploys.** **Deliverables: (a) decide whether BTC-quoted instruments are IN or OUT of the tradable universe — a Kyle call, put to him with a recommendation; (b) if IN, audit every sizing/friction/EV path for a non-fiat quote; (c) if OUT, exclude them at ADMISSION with the reason stated, never at the symbol layer.** |
> | **5.b** | **`B-PRICE-FLOOR-REVIEW`** (`#967`) — Owner CC-C, **DECISION IS KYLE'S** | ⛔ **THE ACTIVE PATH REFUSES ANY COIN UNDER $0.25, AND THAT EXCLUDES DOGECOIN *AND* CARDANO.** Measured 24h: `XDG/USD` 545 rows rejected `low_price` at 0.0851, `ADA/USD` 546 rows at 0.2013, both against a 0.25 threshold (live `screener_filters`: `min_price` 0.25 on every active profile but `strong_trend`; VTS uses 0.05). ⇒ **both coins are studied in the learning population and can never be traded by the active path** — the two populations disagree about the same asset by construction. ⛔ **NOT A DEFECT — bug-taxonomy outcome (2), working as designed with the decision missing.** A price floor is a proxy for *avoid sub-penny illiquid tokens*; $0.25 no longer draws that line, and nobody chose it against today's universe. | **AFTER 5.a.** ⛔ **This is the half of Kyle's *"fix for Bitcoin and Doge"* that has NO defect under it — Bitcoin is a real bug fixed at row 5; Dogecoin is a threshold nobody has revisited.** **Deliverables: (a) put the three options to Kyle — keep, lower, or replace with a liquidity/tick-size test that measures what the floor was proxying for; (b) if it moves, it is a risk-envelope change and goes through the full workflow, never a config edit.** |
> | **6** | **`F-C` — staleness bound** (**#743**, and **#913** rides here) | derive the max-age threshold **from #1's stamped data** rather than choosing it. #913 = a log line calls the inter-tick cadence `ageMs=`, the exact value an implementer would wrongly reach for. | queued — **hard dependency on #1 producing data** |
> | **7** | **`F-D` — VTS accessor + isolation** | VTS uses the shared price accessor **zero** times, against an invariant saying all consumers must. | queued |
> | **8** | **`F-E` — fill-integrity detector + disposition** | tiers the existing history (A clean / B contaminated / C unassessable). ★ **NEEDS NO NEW TRADES** — it grades the **547 closed trades already in hand** against retained venue bars. | queued — needs #1 live so tiers are provable, and #3 done so BTC/DOGE are in the population |
> | **9** | **`F-F(b)` — THE RESET GATE** | the decision to reset the record. Gated on: monitor clean ≥N days · stamp on **100%** of closes · **≥50 assessable** · **zero contaminated on BOTH legs**. **The epoch is already set; this is the GATE, not the act.** | queued — last |
> | **10** | **`B-ALERT-LIFECYCLE`** (**#912** + #443 + #508 RIDER-2) | the governance alarms **can be raised and can never clear themselves** — one re-fired for two days on finished work. Three issues, one defect class, one batch. | queued — **not blocking the trading sequence**; slot where convenient |
>
> ✅ **CLOSED IN THIS SEQUENCE:** `B-MBIM-SWITCH-ON` (the order-book alarm built 2025-12-30 that had **never once run**) · `B-OBSERVATION-EPOCH` · `B-EPOCH-KEYING-PARITY` · `B-PHANTOM-FILL-RECONSTRUCT` · the balance-curve chart fix.
>
> ⚠️ **NO DATES ON ANY ROW, PER `§9.4` AS AMENDED BY KYLE 2026-08-25.** A home is a **name and a place in this queue**, never a calendar date — *"Who knows what we're gonna be doing on September fifth?"* A date on a batch is a commitment nothing enforces, and it expires into a stale record that reads as a missed deadline rather than as work correctly re-ordered. **Rows 1 and 2 are additionally held by a GATE, which is stronger than any date: batch 1 cannot be marked complete while #911 is open.**

> ## ★★ CC-A RUN ORDER — SET BY KYLE 2026-07-28. Work it top-down; do not re-order without him.
> **His instruction, verbatim in substance:** *"Book the removal for Phase 16. Put the storage fix behind the batches you were just about to work on and close, but before we raise the questions for debate on fixing our open trades drought problem."*
>
> ★★ **KYLE ANSWERED 2026-07-30 — THREE DECISIONS, RECORDED VERBATIM IN SUBSTANCE:**
> - **"The pool-scanning batch is closed."** ⇒ **`B-ARM-REMOVAL` IS FORMALLY CLOSED** (§2 step 11 satisfied — it was complete and verified, awaiting only his acknowledgement).
> - **"You can leave the pool columns in."** ⇒ **the `pool` I/R column STAYS on all six trade tables. DECIDED — do not re-raise.** Rationale on the record: the pools themselves survive (only the dynamic split was deleted), so the column still carries genuine provenance; removing it would have destroyed real information for no gain.
> - **"Langston needs (you all need) to be able to read diagnostics. Add this to the list before the scheduled debates."** ⇒ **#613 SEQUENCED BEFORE THE SCHEDULED DEBATES, owner CC-A.** ★ **HIS PARENTHESIS IS THE SCOPE: ALL FOUR PARTIES, not a reviewer-only credential.** Today no sanctioned read-only diagnostics credential exists at all — the reviewer's only route to live state would be forging a token from a secrets file, which he declined to do. ⇒ every claim about LIVE state currently reaches him as reported fact.
> - **"Fix the learning gap issue just before we run the debates we have scheduled in our quick fix list."** ⇒ **#602 (the active path has never fed the outcome-learning store) is SEQUENCED IMMEDIATELY BEFORE the scheduled quick-fix-list debates.** ⚠️ **This SUPERSEDES my earlier proposal of the Phase-19 runtime pipeline audit — he placed it earlier and more precisely than I did.**
>
> ⚠️ **AND A NAMING CORRECTION HE PROMPTED, recorded because the ambiguity was mine: "the storage fix" and "the learning gap" are TWO DIFFERENT ITEMS and I had not distinguished them plainly enough for the distinction to survive.**
> - **THE STORAGE FIX = `B-TRADE-RECORD-RETENTION`** (`B-STORAGE-CATALOG` part 2). Trade records are HARD-DELETED at 90 days with no archive. **About LOSING data. Date-bound: first irreversible loss ≈ 2026-08-09.**
> - **THE LEARNING GAP = #602.** The active path's closed trades never reach the outcome-learning store, because the write is gated on a `regime` field an active position cannot carry. **About NOT GAINING something. Nothing is lost or corrupted; not date-bound.**

> ★★ **STORAGE FIX — LEG 1 SHIPPED + DEPLOYED + VERIFIED 2026-07-30 (`e3cacb56e`; migration at `0b8583010`, CI run `30501091630` all-4-green ON THAT SHA).** `data_lifecycle.vts_open_trades.closed_gc_retention_days` **90 → 365** ⇒ **the ~2026-08-09 first-deletion date is off the clock and legs 2-3 are designed on merit, not against a date.**
> ★ **THE INSIGHT WAS LANGSTON'S AND IT DISSOLVED A FALSE TRICHOTOMY: the deadline is a CONFIG ROW, not a date.** CC-A had framed three options (retroactive archive INSERT / per-cohort retention / accept the loss) and was about to take one to Kyle. **None were needed — raising the row loses nothing, so it moves neither risk nor authority and required no Kyle decision.**
> **VERIFIED BY STATE, not exit code:** value `365`, `updated_by=b-trade-record-retention-leg1`, `updated_at=2026-07-29T23:58:42Z`. ★ **AND EFFECT-SIDE: `would_delete_at_90 = 0` and `would_delete_at_365 = 0` across 41,459 closed rows (oldest `closed_at` 2026-05-11 00:06:25) ⇒ NOTHING WAS EVER DELETED; the change landed BEFORE the first row became eligible.**
> ⚠️ **RUNTIME READ NOT YET PROVEN, and deliberately not forced:** the last boot (22:03:13Z) PREDATES the migration and logged `retention=90d`, so the value is STORED not demonstrably READ. Restarting live trading for a log line was not proportionate with nothing eligible for ten days. ⇒ alert **`vts-gc-retention-365-runtime-proof`** fires 2026-07-31T12:00Z. ★ **Its method carries Langston's correction: ESTABLISH THE BOOT FROM `pm2` UPTIME FIRST — stdout rotates ~2-daily against a 14-FILE cap, so "no `GC_SWEEP` line" is NOT evidence of "no boot," and the two would otherwise be indistinguishable.**
> ★ **THE EXPIRY IS A MECHANISM, NOT A COMMENT** (Langston: *"a stated expiry with nothing enforcing a revisit is exactly the fix-it-later §13 forbids"*) ⇒ alert **`vts-gc-retention-365-expiry-revisit`** fires 2026-09-01T09:00Z. **365 chosen to match the existing `default_warm_retention_days`, not invented.** Cost measured: table is 52 MB / 41,591 rows = **0.030% of a 169 GB DB** — free, and NOT the table driving disk pressure.
> ⚠️ **MAGNITUDE, per Langston — do NOT read leg 1 as an emergency response: at 90 days this was a GRADUAL BLEED, not a cliff** (~804 rows by 08-17, 3,326 by 08-30, of 41,459). **The reason to act now was that it is free, not that it is urgent.**
> **SCOPE CORRECTIONS BANKED IN LEGS 2-3:** the at-risk population is **~10,098 non-shadow rows, NOT 39-41k** (31,358 are shadow, out of scope by the reorg-B4 allowlist); **`pool` is already in the VTS `stateSnapshot`** ⇒ five keys to add, not six; **`calibration_state` is OUT** (preserved independently — the alternates row does not share a key with the parent, so there is no join to break); and ★ **candidate (B) CANNOT cover May — 0% archive coverage there vs 100% June / 93.5% July — so the pre-June backfill is leg 3 and its boundary must be MEASURED, not assumed at 06-01.**
> ⏭ **NEXT: leg 2 (forward field preservation) then leg 3 (pre-June backfill). #619 (skip-marked data-seed census, owner CC-A) and #617 (the discarded linking id) are scoped ALONGSIDE, not inside.**

> **1. CLOSE WHAT IS IN FLIGHT.** `B-ARM-REMOVAL` (shipped + verified; awaits Kyle's acknowledgement to formally close) and the `B-STORAGE-CATALOG` catalogue legs — **part (1) tables + non-table stores DONE** (`e1fc2f9f7`, `74fb7e3e9`) and **part (3) doc cross-referencing DONE** (`09b32f212`).
> **2. THEN THE STORAGE FIX** — `B-TRADE-RECORD-RETENTION` (= `B-STORAGE-CATALOG` part 2). Langston CHANGES-NEEDED discharged, cleared to Step 2. ⏳ **date-bound: `MIN(closed_at)` 2026-05-11T00:06:25Z ⇒ first irreversible delete 2026-08-09T00:06:25Z.** Residual **six** fields. **May resolve as a STORAGE_POLICY documentation gap + a recorded chosen-loss rather than a batch** — run the bucket-2 taxonomy first (Langston).
> **3. THEN — AND NOT BEFORE — RAISE THE OPEN-TRADES DROUGHT DEBATE.** Kyle-owned discussion; **not yet booked as an issue** (searched: no entry exists), so it is named here as the gate that item 2 must precede.
>
> ⚠️ **MY READING OF "the batches you were just about to work on and close" IS STATED SO IT CAN BE CORRECTED IN ONE LINE:** I have taken it as **`B-ARM-REMOVAL` + the `B-STORAGE-CATALOG` catalogue legs** — the CC-A work already done or awaiting only a close. **If Kyle meant something else, this line is the one to fix.** I have NOT silently assumed it.
> **NOT in this order (deliberately):** the Phase-16 removal (booked `POST_AUDIT_ROADMAP` **16.8**, a later phase) and **#602** the active-path learning-store gap (proposed for the Phase-19 runtime pipeline audit; awaiting Kyle) — neither is CC-A's next work.



> **⏭ BOOKED 2026-07-28 (Kyle-directed) — `B-STORAGE-CATALOG` (CC-A) — **now unblocked: `B-EVIDENCE-GATE` closed without build (below), so nothing precedes it.**** Kyle: *"we need to catalog everything that we're storing and where it can be found… that needs to become a canonical document that is referenced in our system manual."* **THREE PARTS:** (1) **CATALOG EVERY STORE IN `STORAGE_POLICY.md`** — every table, log dir, archive bucket and state file, WHAT it holds, WHERE it lives, WHICH tier, and **whether it is registered in the b75 sweep** (a retention key alone does NOT tier — the trap that produced *trade-record retention*). (2) **FIX THE UNARCHIVED TRADE RECORD** — `vts_open_trades` is hard-DELETEd at 90d and is in no tier; the OUTCOME survives in `exit_decision_archive` (warm+cold) but `position_size`, `quantity`, `stop_loss`, `take_profit`, `signal_type`, `pool`, `chosen_entry_mode`, `entry_fee_rate`, `maker_limit_price`, `maker_deadline`, `calibration_state`, `opened_at` and the raw `context` do not. **First loss ≈2026-08-09 — this part is date-bound.** (3) ★ **MAKE THE REFERENCE DOCS DISCOVERABLE — Kyle's actual point:** `STORAGE_POLICY.md`, `BUILD_METHOD_PLAYBOOK.md` and the adaptive-trading-path-flow doc must be **cross-referenced from `SYSTEM_MANUAL.md` AND `SYSTEM_IMPACT_MAP.md`**, so they are found, used and updated instead of existing unreferenced. **A canonical doc nobody is pointed at is not canonical.** ★★ **PART (3) IS DONE — landed `09b32f212`, 2026-07-28.** Measured BEFORE editing: **0 references from `SYSTEM_MANUAL.md` and 0 from `SYSTEM_IMPACT_MAP.md`** to any of the three ⇒ Kyle's complaint was exactly right, they were canonical in name only. Now referenced from **both** master docs, each **with its own caveat rather than a bare link**: `STORAGE_POLICY.md` carries the *a retention key alone does NOT tier — sweep registration is a SECOND act* warning at the point of use; `BUILD_METHOD_PLAYBOOK.md` is marked **descriptive, not binding** (`CLAUDE.md` binds this project); `ACTIVE_PATH_FLOW.md` is marked **IN CONSTRUCTION (owner CC-C)** and carries its own *freshness forces ATTENTION, not TRUTH* warning — a passing freshness check is not a correctness certificate, which is the failure that doc exists to prevent. Placed into the existing **Companion Documents** / **does NOT contain** / **Usage** blocks rather than appended as dated sections, per each file's own maintenance convention (§9). ★ **PART (1) IS DONE FOR TABLES — `STORAGE_POLICY.md` §9, landed `e1fc2f9f7`, 2026-07-28.** Measured at the read sites (`b75-retention-sweep.ts` registries + the `data_archive_manifest` + 31 live `data_lifecycle` constants), not inherited. **13 tables registered: 12 exported+verified BEFORE drop (6 market-data, 6 analytics), 1 deleted outright by design** (`xstock_qd_probe_history` — now recorded as a CHOSEN loss, not a discovered one). ★★ **`vts_open_trades` is in NONE of them** — it has its own retention key AND its own boot-time hard `DELETE` with no archive, which is the *key-is-not-tiering* trap stated as the section's headline rule. **Measured `swept=0` on every logged run ⇒ NOTHING LOST YET; the risk is ahead, not behind.** ⚠️ **Also recorded: REGISTERED ≠ EXERCISED — only 5 source tables have EVER produced an archive object (72 objects), so 8 of the 12 registered have never archived anything.** Benign explanation fits (partitions younger than their window) and it is NOT asserted as a defect — but *a safety claim should cite a manifest row, not a config key.* ⏭ **PART (1) REMAINDER: log DIRECTORIES, archive BUCKETS and STATE FILES are not yet catalogued — tables only.** ⏭ **REMAINING: (1) the non-table stores and (2) the unarchived trade record — (2) IS THE DATE-BOUND ONE, first irreversible loss ≈2026-08-09**, and per Langston it should be scoped to the fields with a NAMED CONSUMER (`chosen_entry_mode`/`entry_fee_rate`/`maker_limit_price` = the only record of maker-vs-taker entry policy; the `pool` tag) rather than to everything that disappears.

> **✖ `B-EVIDENCE-GATE` — CLOSED WITHOUT BUILD 2026-07-28. NO LONGER NEXT UP. Nothing was written.** ★ **KYLE DECIDED THE PRIOR QUESTION: `ml-calibration.ts` is REMOVED (legacy from the predictive-learning / ML era), homed to PHASE 16 — see `RUNNING_ISSUES` #174.** ⇒ **the gate's subject is being deleted, so the gate evaporates. #591 limb (b) (`B-CALIBRATION-QUALITY-WEIGHT`) evaporates with it.** **Why it was ever scheduled here:** it had NO home at all — Langston approved it and it was then orphaned, which Kyle caught. **Why it is not being built:** the keep/remove decision registered by B-NEW-54 on 2026-06-08 (#174, *"decorative"*, Langston-signed at Step-4 AND Step-8) was **logically prior and had sat unmade for seven weeks** — both CC-A and Langston walked past it and scoped a careful statistical gate onto a component already booked for possible deletion. ★ **THE LESSON, recorded because it generalises: search the ledger by the FILE PATH, not by the symptom.** The symptom search returned nothing; the filename returns #174 immediately. **The scope file is RETAINED as the record** — including the corrected live measurement (window 28 not 50; `ENGULFING` n=1 → INCREASE and `ABCD` n=1 → DECREASE firing today) and the WITHDRAWN drift claim, so the removal batch inherits real numbers rather than my first ones.

> **✅ 2026-07-28 — B-ARM-REMOVAL SHIPPED (CC-A): the Adaptive Ratio Manager and its pool-aggregate limb are DELETED; the scanner uses the fixed config-SSOT split (`IDEAL_RATIO=0.6`).** Kyle-authorised on the condition that the improvement path be tested first — it was, and it failed on arithmetic. **Behaviour-neutral BY MEASUREMENT:** `actualIdealCount = min(ceil(300×ratio), available)` and `Available` never exceeded **16** against a target of **151** in any observable history, so the clamp bound every cycle. Both its evidence sources were unusable (a table with zero rows ever, live-mode-gated; a damper saturating at 100 samples). ★ **Decisive: the PURPOSE TEST — it allocated scan attention while the binding constraint is the net-EV qualification drought (#570).** Deployed restart#536; (a) target=180 ✅ (c) RatioManager ceased ✅ (b) **PASS** (`Available=3` is an ordinary draw from the pre-cut distribution — avg 5.6, #597; the "≈16" comparator was mis-specified and withdrawn). **Opened #594 (6 of 19 strategies have NEVER traded) · #596 (outcome-corpus representativeness — BLOCKS outcome-sourced ranking) · #597 (ideal pool at ~4-5% of slots vs a nominal 70%; membership still outcome-blind).** ⚠️ **This batch did NOT fix membership** — the pools survive and still rank on four pre-trade estimates off a single observation.

> **✅ 2026-07-22 — B-RTB-REFRESH-CONSOLIDATE OBJ-1 SHIPPED (CC-A): THE DUPLICATE RTB REFRESH SCHEDULER IS RETIRED (#532 core defect closed; issue stays OPEN for OBJ-2b/3/4/5/6).** `d2306518e` + `373d73612`, Langston Step-4 APPROVED (all three asks re-derived at the ref), CI 4-green (29960947657), deployed + verified. **Two independent schedulers had been running over one ready-to-buy queue for ~7 months, double-processing every signal into the SQE. There is now exactly ONE: the bucketed `RTBRefreshService`** (15s micro / 120s macro / 8 buckets), started unconditionally at boot — not engine-gated (Langston confirmed at `server/index.ts:348`). **★ DECISION-LOG ENTRY (Kyle 2026-07-22, and it corrected CC-A):** the retirement gate was discharged by **PROVENANCE**, not by the planned staging observation. The bucketed service was built at `7a029f390` (2025-12-23) to decouple the refresh from the FX5 scan loop **because the single-path ~30s refresh was under strain and could not keep up — and the longer refresh gap was WEIGHED AND ACCEPTED as part of that switch.** `bridge/canonical/` documents only the bucketed mechanism, so A was never meant to coexist. CC-A had been treating the 30s→120s change as an open risk needing a new measurement; that was **re-litigating a settled 2025-12 decision.** **⚠️ STILL OPEN AND LEGITIMATE — Kyle explicitly left it live: should the 120s macro cycle be TIGHTENED for today's system?** Not foreclosed by the retirement (intervals are config). Blocked on evidence: see the OBJ-4 finding below. **Measured:** per-signal staleness 5–20s (both live) → p50 73s / max 77s post-cut; zero Mechanism-A ticks after restart. **⚠️ FINDING FILED (OBJ-4): `rtb_signals.promoted_at`/`.promoted_trade_id` are NEVER populated (0 of 101 rows, all time) while promotions demonstrably occur — rows are deleted on promotion, so dwell-to-promotion is UNMEASURABLE from the queue's own record. That instrument must exist before the tightening question can be answered with evidence.**

> **★★ REORG 2026-06-19 (post-Active-Trading-Pipeline-Audit + July-9 fee reality + Kyle both-classes directive). Authoritative reorg detail: `Claude Comms and Packages/Scope Files/P19_REORG_BOTH_CLASSES_PLAN_2026-06-19.md` (decisions D1–D7 + build board B1–B9). Approved on CC-B + Langston agreement (Kyle 2026-06-19 released the CC-A concur dependency; Langston APPROVED the hybrid).**
>
> **Governing decisions (locked):** **D1 Both in code, one live at a time** — build all SHARED pieces for BOTH classes now (don't crypto-restrict → no false second pass); only live activation / debug focus / calibration values are sequenced. **D2** shared bugs fixed for BOTH on the spot during crypto-first debug. **D3** BOTH classes paper-active ON by Phase-19 close (wired+working bar, not calibrated). **D4** Phase-25 calibrates both (crypto comfortable first; xStock may spill into Phase 16/20 in parallel). **D5** both ready by Phase 21 → launch live TOGETHER (days-not-weeks safety stagger only if needed). **D6** fee ladder: rung-1 bigger targets (~3.5-4%, RR≥2.5-3) at TAKER opens trades NOW (low-build); rung-2 maker build + asymmetric-stop EV kernel + shared maker/taker service (active+VTS); rung-3 raise pWin ceiling only on measured win-rates (Phase 25). **D7 HARD GATE: only ONE asset class in active-trading during the validation window** — xStock in shadow/VTS until crypto's template is validated.
>
> **Build order from here (replaces the strict B6.5f→B6.5g→B7 tail; folds into the rows below):** B6.5f recognition (RE-SCOPED both-class) → rung-1 target-floor + liquid-volatile universe selector (NEW; opens crypto at taker) → #233 EV-input plumbing (ONE site, accuracy — Langston: NOT the opener) → **B8 shadow-trade layer PULLED FORWARD** (data engine + selection-quality; telemetry-only) → **ranking fix** (NEW; the make-or-break — wire a correlate-with-winning score into the live picker) → Filter Diagnostics tab → rung-2 maker build → turn crypto paper-active ON (template) → xStock paper-active ON → both ON by Phase-19 close. **Audit (3-way APPROVED): `1-system-manual/ACTIVE_TRADING_PIPELINE_AUDIT_AS_OF_2026-06-18.md`.** §13 homes (dead-code, two-fee-source, regime-flip-exit) in RUNNING_ISSUES.
>
> **★ FORMALIZED INTO BATCH NUMBERS (Kyle renumber 2026-06-30 — resolves the two collided "B7" labels):** the run-up tail above maps to **P19-B7 = ranking fix (B7.1) → maker/taker shared service (B7.2) → crypto gate-10 lifecycle proof** (the "rung-2 maker build" / informal "reorg-B7" gets this clean home; absorbs B6.5g) → **P19-B8 = paper monitoring screens (BOTH classes) + THE SWITCH-ON (crypto then xStock) + xStock lifecycle proof** (folds old B7a UI-shells + old B7b switch-on; flip lives inside B8) → **P19-B9 = sustained paper run + audit** (B8 = make-it-work; B9 = run-it-for-real + audit for Phase-25). **Both-classes EXPLICIT:** the scan→filter→evaluate→signal→RTB→promote pipeline runs for crypto AND xStock together (shared, D1); only active EXECUTION is one-class-at-a-time during validation (D7); both paper-working by phase close (D3). See §1 rows P19-B7/B8 + §5 (2026-06-30 decision).

| Seq | Batch | Roadmap items absorbed | Status | Completion report |
|---|---|---|---|---|
| P19-B1 | Test-suite cleanup | §16.7 + TEC.b strict-throw restore (parked 2026-06-09) | **✅ DONE 2026-06-13** — 12-fail/141-skip → **0/0 both environments** (1880/1880, 161/161 files); TEC.b strict LIVE (#141 closed, deploy-verified 4-class boot 29ms); bench=CI parity via Docker; #226 opened (tier separation); Langston APPROVE Steps 2+4, CONFIRMED Step 8 | `P19_B1_COMPLETION_REPORT.md` |
| P19-B2 | Live-mode build-approach decision | 19-18 (reshaped — see §3.2) | **✅ DONE 2026-06-13** — **Option A (live reuses paper engine by extension) ratified** (CC + Langston + Kyle). Paper execution target DECIDED: Kraken has NO spot paper-fill system (verified exhaustively — futures-only demo; validate=true is validation-only; institutional spot env is onboarding-gated) → **Kraken-vetted high-fidelity internal fill** (validate=true on every paper order + real Kraken prices/fees + honest slippage/partial-fills). Legacy `live-trading-service` stub **DELETED** (commit 59d501fc4, Langston Step-4 APPROVE, tsc+vitest green). New homed items → B3/B4/B7 (§5). | `P19_B2_COMPLETION_REPORT.md` |
| P19-B3 | Known-broken active-path repairs | 19-6 (#137) + 19-10 (#139) + **FIRST DELIVERABLE (P19-B2 §9 Q3): typed `OrderPlacer` execution port** (open-order→fill-result, close-order→fill-result) with a **partial/delayed/rejected-capable fill-result type from day one** — so paper's fill handling is written against a shape that already holds live's reality (Langston) | **✅ DONE 2026-06-14 (both halves).** Step-1 scope ACK'd by Langston 2026-06-13. **SUB-BATCHED:** B3a (OrderPlacer port + #139 classify hardening) → B3b (triage gate + #137 fixes); land B3a first. **OrderPlacer port (OBJ-1) DONE + VERIFIED** — `execution/types.ts` + `execution/order-placer.ts` + 6 engine edits (thin port wrapping the open/close fill seams), tsc baseline clean (no regressions), 6 new unit tests + full suite 1886 green. Blast-radius audit (Kyle directive) found the classifier regex is DUPLICATED (`asset-classes.ts:405` + `symbol-normalize.ts:74`) + the throwing-resolve surface is ~21 active-path sites + 4 symbol modules — #139 approach revised: widen BOTH regexes + centralize alarm in `safeResolveAssetClass` + switch the 9 vts-runner sites; HOME the other ~12 sites→B4 + 4-module consolidation→Phase 20. **B3a CODE COMPLETE + BENCH-VERIFIED 2026-06-13** — #139 widen (SSOT constant `CRYPTO_SPOT_BASE_MAX_LEN=15` shared by asset-classes + symbol-normalize per Langston C1) + centralized alarm (counter + escalation hook in `safeResolveAssetClass`) + all 9 vts-runner throwing sites → safe-with-alarm; tsc baseline clean (no regressions); full suite **1896 green** incl. 16 new tests (6 port + 10 classify). **✅ B3a SHIPPED + CLOSED 2026-06-13** — commit `027d2ddcc`, CI `27477509296` all-4-green, deployed + staging-verified (HTTP 200, ZERO classify fall-throughs/throws, engine healthy); full suite 1896 green (+16); Langston Step-1/2/4 cleared (parity-confirmed). Governance: SIM §9.13/§9.14 + System Manual §3.7 + RUNNING_ISSUES #228/#229/#230 + BATCH_CATALOG + `P19_B3a_COMPLETION_REPORT.md`. **✅ B3b SHIPPED + CLOSED 2026-06-14** — commit `d9b312780`, CI `27481700174` all-4-green, deployed (HTTP 200, clean boot, engine healthy 14ms event-loop). #137 triage: 66 files/474 errors split (SIM Layers 3–6 + 2 reachability sweeps), Langston split-gate APPROVE Q1–Q5; active-path FIX 13 files/~59 errors + 2 landmines (474→404 tsc, baseline gate green, suite 1899/1899); ~390 off-path homed (#234). **Landmine #2 (CRITICAL):** orchestrator built SQESignalInput w/o riskScore/profitRate → queueSQESignal threw → catch swallowed → EVERY signal silently dropped at active-paper turn-on; fixed + dead ngc-column write removed + observable drop-counter. **Landmine #1:** Phase10TradeRecord += assetClass, VirtualSignal += netEV (revived a dead Net-EV floor → #232). Homes: #231 ablation integer-id→B4, #232 netEV-threshold + #233 driftScore/volZ-defaults → P19 pre-go-live. Langston Step-4 APPROVE. **P19-B3 BOTH HALVES COMPLETE. NEXT: P19-B4.** | `P19_B3_SCOPE.md` |
| P19-B4 | xStock wire-in (merged) | 19-1 + 19-7 (#92) + 19-8 residual (latency **+ WS staleness/reconnect fitness for execution** — Langston) + B3.2 active-path strategy gates + #153 0.50-cap gate + **Kraken `validate=true` round-trip smoke test** (reframed from "paper-order smoke" — Kraken has no spot paper-fill; validate-only is the real-venue contact) + B11-tagging decision (§5) **+ P19-B2 paper-execution-target items: (a) Kraken-vetted HIGH-FIDELITY fill model — validate=true on every paper order + real Kraken tiered fees + slippage vs real L2 book depth + partial-fill realism (Langston's #1 fidelity must, NOT optional — frictionless paper = false EV → bad live promotion); (b) paper gets its OWN credential/rate-limit lane so its validate traffic can never throttle a real live order at Phase-21 co-run; (c) shared-singleton SPLIT-BRAIN isolation audit (trading-mode global, RTB cooldown/dedup/portfolio-heat, tradingEngines map, TEC per-mode caches) — design per-mode isolation in here, MUST pass before any Phase-21 live+paper co-run (distinct from + earlier than the Phase-21 strain re-eval)** | **B4a ✅ DONE 2026-06-14** — xStock active wire-in + feed-safety (DORMANT til B7b): C1 stamp-at-source, C2 dispatch seam, C3 freshness+liquid-fill-window(RTH)+stall-watchdog (DB fail-closed; #236→B6.6), C4 classify hardening+hook+#230, C5 DB strategy gate + dispose hardcoded allowlist (default-open; #238), C6 calibration_state, C8 cap 0.50→0.15, C7 SET-NOT-NULL deferred #237. 20 commits head `1523ff7ac`, CI `27517345398` green, deployed+verified. Langston Step-4 APPROVE-W-COND (3 ratified). **B4b queued** (fill fidelity + paper/live split-brain isolation). | `P19_B4a_COMPLETION_REPORT.md` |
| P19-B4b (D5) | Paper/live split-brain isolation | Phase-21 co-run precondition | **✅ DONE 2026-06-15** — S1/S4/S6 per-mode + DB-SSOT liveness (H1/H2 + `LIVENESS_SPLIT` witness); S3 deferred (#296). | `P19_B4b_D5_COMPLETION_REPORT.md` |
| P19-B4b.1 | Paper fill fidelity (depth-walked fill + partials + #295 gate) | B4b D2+D3+D4 (fill-fidelity half) + #295 | **✅ DONE 2026-06-16** — crypto-first depth-walked fill (replaces flat 0.05% slippage) + partial-open down-sizing + close always-full-fill w/ DB beyond-depth penalty + the 24/5 **book-depth-sufficiency + warmth gate** (`fill_depth_gate`, per-class fail-closed) REPLACING the B4a-C3 RTH clock (#295 CLOSED). xStock gets a top-of-book gate (R1); closes never partial (R2). Commit `b74526dc3`, CI `27586477175` all-4-green, deployed restart#393 (8 `fill_depth_gate` rows seeded, 2 watchdog keys retained), bench tsc-no-regression + vitest 1979. Langston Step-1/2/4 APPROVE; **D4 validate-round-trip + credential rate-limit lane DEFERRED to #296** (locked `kraken.ts`). DORMANT til B7b. | `P19_B4b_1_COMPLETION_REPORT.md` |
| **P19-B4b.2** | **Dead paper-fill machinery sweep** (#300) | **✅ DONE 2026-06-16** — DELETED `realtime-paper-executor.ts` (whole file; `executeTrade` 0 callers) + the dead `/api/execution/metrics` endpoint + the dead order-book read sub-path (`market-data-coordinator` map/handler/`getLatestOrderBook` + `market-data-ws` `'orderbook'` emission). `system-health-monitor.getExecutionMetrics()` rerouted value-identically (Langston-verified line-by-line). **★Framing correction:** the "dup 2nd WS book path" was NOT a clean module cut — `market-data-coordinator` + `market-data-ws` are LIVE shared infra (feed-integrity/health/parity); only the order-book SUB-PATH was dead, the `book` subscription + midpoint tick KEPT. `calculatePriceImpact` KEPT (golden-test ref); `pre-execution-validator` LEFT pending #297. NEW vestigial-subsystem finding → **#301**. Commit `977f3be08`, CI `27598725568` all-4-green, deployed restart#394 (root HTTP200, `/api/execution/metrics` → 404), bench tsc-no-regression + vitest 1979. Langston Step-1/2/4 APPROVE. DORMANT-path-only, no fill behavior change. | `P19_B4b_2_COMPLETION_REPORT.md` |
| **P19-B5a** | Data-capture completion — reject/admit hooks (§19.0.5; the HARD 19-3 precondition) | 11 pre_filter hooks (market-scanner + fx5 family/pattern IMF, gated) + SQE/RTB/TCL reject hooks (capture REAL scores) + paper-engine terminal admit, reusing `signal_eval_archive` schema 1:1; ZERO migration; ships DORMANT (live-verified 0 rows on staging). **✅ CLOSED 2026-06-16** — commit `1e119531a`, CI `27614885978` green, deployed, vitest 1987/1987; Langston Step-4 APPROVE (dormancy traced in code). 19-3 paper-active capture precondition SATISFIED. | ✅ done | `1e119531a` |
| **P19-B5b** | Data-capture completion — #94 xStock macro snapshot + #86 home decision | #94 capture precondition (Kyle 2026-06-13): every xStock decision record carries the equity-macro snapshot (VIX + DXY z + raw + freshness, feed = `amr-equity-feed`) so Phase-25 item 25-7 has build material — CAPTURE only, build stays Phase-25. **NOT DORMANT — writes on merge** (rides the xStock eval-cycle archive writes). #86 home = B5c CONFIRMED (Langston Q3). **✅ CLOSED 2026-06-16** — commit `c9f2e8285`, CI green, deployed, vitest 1992/1992; live-verified macro on new xStock rows (vixZ + ageSeconds + raw vix populated); new `macro-snapshot.ts` helper threaded into all 4 archive sites; ZERO migration. | ✅ done | `c9f2e8285` |
| **P19-B5c** | Continuous Q-D probe (#86) — `xstock_qd_probe_history` | **NEW (Langston Q2 2026-06-16): carved OUT of B5 because a continuous quote-depth probe is ALWAYS-ON (not dormant) — own write-volume/cadence/retention/dedup story.** Own near-term batch (capture-now/build-later); home FINALIZED with Langston before B5b closes. Gates downstream friction-extraction. | **✅ CLOSED 2026-06-16** — NEW `xstock_qd_probe_history` table + every-5-min `xstock_qd_probe_cron` + B75 plain-table retention pass; cadence/retention/freshness `module_constants`-resolved; A1 degenerate-quote classification; D5 fire-grid `bucket_start` dedup; D7 weekend-vs-breakage fire-evidence meta. **CAPTURE-ONLY** (friction-extraction → B81/Phase-25; R-D2 basis candidate homed there). Commit `f521f6d6b` (Step-7 fix: per-symbol indexed read after the DISTINCT-ON-no-WHERE full-scan hit the 30s statement_timeout — Langston's index-served caveat realized), CI all-4-green, deployed restart#398; **live-verified** 486 rows/fire, all quote_quality=ok, spread p50 ~15 bps, duration 3.1s. Langston Step-1/2 ACK + Step-4 APPROVE + Step-8 CONFIRM (runtime+data; Condition B SIM registry landed at Step-10). | `f521f6d6b` |
| P19-B6 | Daily loss-budget kill switch | 19-4 (§19.0.B) — **RESTORE the deleted Phase-8 auto-trip** (`checkKillSwitch`+`calculate24hPL` recovered from `594aad717^`; Kyle: "it was working as of Phase 8") + flatten-on-kill + 2 warning tiers (50/75) as PER-MODE guardrails + circuit-breaker; force-trip mandatory | **✅ DONE 2026-06-17** (pending Langston Step-8 + Kyle ack) — RESTORED the deleted Phase-8 auto-trip (re-homed `checkKillSwitch`+`calculate24hPL` → `daily-loss-budget.ts`) + flatten-via-existing-stop (no double-close) + 2 warning tiers as PER-MODE guardrails (migration) + circuit-breaker. Langston Step-4 CHANGES (RULE_011 string-compare + thrown-doKill latch) → both fixed → **re-APPROVE**. **Gate-7 force-trip PROVEN** (5-case integration test). Bench tsc-clean + vitest 23/23; CI green `43601300f`; deployed restart#399 HTTP200. **DORMANT til B7b** (gated `isEngineActive`); live close-driven exercise = B7b. | `P19_B6_COMPLETION_REPORT.md` |
| **P19-B6.5** | **Crypto active-pipeline RESURRECTION** (Kyle 2026-06-15, #235) | **NEW (CC+Langston consensus).** Crypto active-paper hasn't run since Phase 8 (Nov 2025). (1) **accretion-delta audit** of the shared active crypto chain (FX5→activeFilterPool→signal-orchestrator→SQE→RTB→TCL/TEC→paper-execution-engine→open→trailing→close) vs the Phase-8 baseline (asset-class awareness + multi-asset pair processing = prime suspects); (2) **crypto-only dry-run watching ≥1 FULL closed-trade lifecycle** (open→exit→close→cooldown→telemetry, not just signal emission); (3) **fill-path parity** through the high-fidelity INTERNAL fill (validate=true vetting — rule-20: Kraken has no spot paper-order system). **B7b HARD-GATED on B6.5 green (§6 gate 10).** **★Step-1 ACK 2026-06-17 + SPLIT (Langston ruling): B6.5a + B6.5b.** Q1→**Option C** (Langston overrode CC's lean to A): build a **first-class per-asset-class active gate** because B7b is staged crypto-first w/ xStock dormant → B7b ITSELF can't flip the shared per-mode `isEngineActive` without co-activating incomplete xStock → the per-class gate is **mandatory B7b infra that was missing**, not scope-balloon. Harness (A) bypasses the turn-on it must prove; fail-closed suppression (B) is a silent fallback (rule#10). | **Step-1 ACK / split → B6.5a next** | `P19_B6_5_SCOPE.md` (Rev-2) |
| **P19-B6.5a** | **Per-asset-class active gate** (the Option-C infra) | **NEW (B6.5 split).** First-class per-asset-class active toggle so crypto can be activated WITHOUT co-activating the dormant xStock active path (today only the per-MODE `system_context.isEngineActive` exists — confirmed `fx5-scanner.ts:543-555`; the xStock `active-dispatch.ts:123` gates on the same flag). Durable B7b machinery (B7b's crypto-first flip needs it too) + tests + the xStock-isolation acceptance test. | **✅ DONE 2026-06-17** — additive fail-closed default-OFF JSONB `active_asset_classes` on `system_context` + typed single-read accessor + H1 setter + AND-gates at fx5 crypto scan + xStock active-dispatch + per-class witness + RTB upstream-guarantee comments (#320); 11 new tests incl the gate-10 xStock-isolation acceptance test. Langston Step-1/2/4 ACK→PROCEED→APPROVE + Step-8 CONFIRMED (staging: `{}` both modes fail-closed, engine dormant, clean boot). CI green `500127614`; deployed. DORMANT (both classes default OFF). #321 homes the uncalled witness + dual-active edge to B6.5b. | `P19_B6_5a_COMPLETION_REPORT.md` |
| **P19-B6.5b** | **Crypto audit + dry-run + fill-parity** (proof) | **NEW (B6.5 split).** Obj-1 accretion-delta audit + Obj-2 crypto-only time-boxed reverted dry-run watching ≥1 FULL closed-trade lifecycle THROUGH the B6.5a gate (the exact mechanism B7b uses) + Obj-3 fill-parity via the internal validate-vetted depth-walk fill. Uses the B6.5a gate. | **✅ CLOSED 2026-06-17** — shipped the per-class crypto active gate F1-F5 (commit `d56a0cc1e`, CI `27693018813` all-4-green): F1 fx5-scanner per-class crypto gate propagation (#320) + F2 witness wire (#321) + F3 paper cooldown read fix + F5 ATR-zero exit floor + rule-18 dead-code removal. Langston Step-4 APPROVE. The crypto-only reverted dry-run PROVED the front half works (scanner→pools→orchestrator→SQE all fire for crypto, no crash) but surfaced TWO ready-to-buy-insert breaks blocking every crypto signal → fixed in B6.5c. Closed on its own scope; gate-10 (≥1 FULL closed lifecycle) carried to B6.5c then B6.5e. | `P19_B6_5b_COMPLETION_REPORT.md` |
| **P19-B6.5c** | **Crypto signal→ready-to-buy repair** (the 2 B6.5b-dry-run breaks) | **NEW (B6.5b dry-run finding).** Fixes the two breaks that blocked 100% of crypto signals from ready-to-buy: (1) the leftover NOT-NULL `cwqi` column DB-drift on `rtb_signals` (the code already removed it; staging schema still had it → every Drizzle insert rejected); (2) the pattern recognizer stamping invalid `pattern_*` strategy names (not valid `strategy_type` enum values) → DB-rejected at ready-to-buy. | **✅ CLOSED 2026-06-17 (objectives 1-3; objective 5 / gate-10 CARRIED to B6.5e)** — commits `52adcdf6f` + `0b5bb206d`, CI `27720080156` all-4-green, deployed staging. (1) DROPPED the `cwqi` column via migration (reconciles the DB-drift anywhere) + tested rollback + MANIFEST + DELETED_COMPONENTS_LOG; (2) NEW `resolvePatternConsumingStrategy` resolves each detected pattern to its consuming CANONICAL strategy (regime + class aware, exact-match-or-drop — patterns are TRIGGERS not strategies; the 19 canonical strategies are fixed) at the `patternToTradeSignal` SOURCE, removed the redundant duplicate-emitter loop. PROVEN LIVE in a reverted crypto dry-run: crypto signals reach ready-to-buy with canonical names (`reverse_impulse`, `inside_bar_reversal`), zero `pattern_*`, zero `cwqi` errors, xStock isolated. **Objective 5 (gate-10: ≥1 FULL closed crypto lifecycle) NOT met** — a NEW downstream break surfaced (sized crypto signal won't OPEN; `[8.8.3-I3]` shows attempts=11/opened=0/blocked=0/reasonSum=0 — #325) → per Langston, gate-10 carried to NEW batch B6.5e. | `P19_B6_5c_COMPLETION_REPORT.md` |
| **P19-B6.5d** | **Asset-class stamp integrity** (carry-the-stamp across all 35 resolve sites; clears the live A/EUR classify alert) | **NEW (B6.5c sibling-finding; the live `classify-fallthrough-active` CRITICAL alert 58367b27 — `A/EUR@kraken` dropped on the active path — surfaced a STRUCTURAL gap: the asset class was re-derived from the symbol string at 26 of 35 resolve sites instead of being carried with the pair, and re-deriving hardcodes `exchange='kraken'` → mishandles the 9 USD / 8 EUR collision tickers + single-letter bases).** | **✅ CLOSED 2026-06-18** — commit `3bd3deedc`, CI `27728040408` all-4-green, deployed staging (HTTP200, pm2 restart#403, pure code / no migration). Enforces **one `SizingContext` = one asset class = one pipe**: the class is STAMPED at pipe entry and CARRIED with the pair, never re-derived downstream. OBJ-1 widened the resolver base-length FLOOR from an implicit 2 → 1 via one SSOT constant `TICKER_BASE_MIN_LEN=1` (`shared/asset-classes.ts`) feeding all 3 resolver regexes (CRYPTO_SPOT_CANONICAL / XSTOCK_SPOT_DISPLAY / XSTOCK_PERP_RAW) — clears the A/EUR alert (A=Vaulta, ex-EOS rebrand, a real Kraken-spot crypto); collision precedence preserved (resolver order DISPLAY→COLLISIONS→SYMBOLS→CANONICAL→raw, so the widened single-letter canonical never shadows the xStock collision map — verified live: A/EUR=crypto_spot, T/USD=crypto_spot, Tx/USD=xstock_spot). OBJ-2 per-pair alert dedupe key; OBJ-3 prefer-the-carried-stamp at ~12 sites (SQE gate + signal-orchestrator ×4 + routes P/L ×2 + paper-engine AMR ×2 + pre-exec-validator + RTB AMR-shadow); OBJ-4 threaded `assetClass` through `evaluateTradeExpectancy` + `feePercentFor`; OBJ-5 fail-closed active defaults + logged passive defaults; OBJ-6 2 throwing-variant swaps (market-context-engine:1442, vts-service:341). 14 new tests pass. **Alert 58367b27 RESOLVED post-deploy (the headline gate — MET).** Langston Step-4 APPROVE; Step-8 in flight. Carried side-items: N1 sig-orch:1151 dead-import → RUNNING_ISSUES home; JC#4 STAMP_MISSING_ACTIVE disposition → B6.5e completion report; the §B instrument is intentional for the B6.5e dry-run. | `BATCH_P19_B6_5D_ASSET_CLASS_INTEGRITY_SCOPE.md` |
| **P19-B6.5e** | **TCL→paper-execution-engine open-path silent-failure repair + gate-10 closed-lifecycle owner** (#325) | **NEW (B6.5c gate-10 dry-run finding 2026-06-17; Langston ruling).** A sized crypto active-paper signal never opens a paper trade — it falls out of the TCL→paper-execution-engine open handoff BEFORE any reason/block is recorded (`[8.8.3-I3]` invariant monitor: attempts>0, opened=0, blocked=0, reasonSum=0 — likely un-awaited promise / swallowed throw / early-return-no-telemetry; untested since Phase 8). **OWNS gate-10** / the ≥1 FULL closed-trade-lifecycle proof (open→exit→close→cooldown→telemetry) that B6.5b/B6.5c proved up to but not through. **May promote to a full `P19-B7` if the open-path surface is large.** **B7b stays HARD-GATED on the closed-lifecycle proof, now owned by B6.5e (§6 gate 10).** Scope with a DEEP SIM + System-Manual read first (Kyle directive 2026-06-17). | **✅ CLOSED 2026-06-18 (Phase A / #325 + diagnosis; gate-10 MOVED to B6.5g)** — commit `dbd0a2283`, CI `27731034448` all-4-green, deployed staging (HTTP200, pm2 restart#404, pure code / no migration), bench tsc-baseline-clean + 30 tests (6 new). non_architecture; Langston Step-1 PROCEED + Step-4 APPROVE. Made the post-guardrail open stage OBSERVABLE: typed `OpenOutcome` + a NEW `openFailedTotal`/`openFailedByStage` 3rd term in `rtb-metrics-service` (I3 invariant now `attempts === opened + blocked + openFailed`; was a silent void-return / trade-count-delta inference). PROVEN LIVE: the contained crypto dry-run reconciled + stage-named the invariant — `[8.8.3-I3][INVARIANT_CHECK][OK] attempts=8 opened=0 blocked=0 openFailed=8 [EV_REJECT:8]`. **Diagnosis (#325 was NOT an execution break):** the open path runs correctly to the Net-Expectancy gate (11.8B), which HONESTLY rejects every crypto signal because the EV kernel runs on DEFAULT pWin=0.60/DI=50/VolNoise=0.300 inputs (#233) — RawEV positive, friction exceeds it; the EV gate is working. **gate-10 (≥1 full closed crypto lifecycle) MOVED to NEW P19-B6.5g** (EV-input integrity); B7b stays hard-gated, now on B6.5g-green (§6 gate 10). +#327 dead-import removed. JC#4 dry-run measured CLEAN ZERO `[STAMP_MISSING_ACTIVE]`. NEW canonicalizer-quote batch **P19-B6.5f** ships first. | `P19_B6_5e_COMPLETION_REPORT.md` |
| **P19-B6.5f** | **Canonicalizer quote-currency completeness** (B6.5e dry-run finding) | **NEW (B6.5e crypto dry-run — ships FIRST, before B6.5g).** The dry-run fired 6 critical `classify-fallthrough-active` alerts (ETH/EUROP, ETH/PYUSD, XBT/EUROP, XBT/PYUSD, XRP/RLUSD + earlier A/EUR) from two QUOTE-side root causes (DISTINCT from B6.5d's single-letter BASE fix): (a) `symbol-canonicalizer.ts:151` `knownQuotes` missing newer Kraken stablecoin quotes; (b) `shared/asset-classes.ts` `CRYPTO_SPOT_CANONICAL` quote group `[A-Z0-9]{3,4}` caps at 4 chars while EUROP/PYUSD/RLUSD are 5. Fix = enumerate Kraken's full current quote set (verify live) + widen the quote-length bound + a loud named alert on any unrecognized quote. **RE-SCOPED both-class (reorg-B1, D1/D2).** | **✅ CLOSED 2026-06-19 (reorg-B1)** — code `b06eb9e5c`, CI `27837279938` all-4-green, deployed staging, bench tsc-baseline-clean + vitest 128/128. Shared `QUOTE_LEN` SSOT (MAX=5, live-confirmed 23 legs) widened CRYPTO_SPOT_CANONICAL + XSTOCK_SPOT_DISPLAY in lockstep (closes the 6 slash-form alerts) + complete curated `KNOWN_QUOTE_CURRENCIES` + self-healing `setDiscoveredQuotes()` slot (fed from dynamicQuotes every refresh; SIM §17/S17) + storm-safe `classify-unknown-quote` alert. PROVEN LIVE: 6 pairs→crypto_spot, xStock complete (490-sym), recognition≠eligibility (allowedQuotes unchanged). Langston Step-1/2/4 PROCEED. | `P19_B6_5f_REORG_B1_COMPLETION_REPORT.md` |
| **reorg-B2** | **Rung-1 — per-class target-floor + universal RR gate + reachability gate, BOTH classes** (the plumbing for crypto opening; reorg build-board B2) | **NEW (reorg D6 rung-1).** A central pure normalizer (`signal-target-normalizer.ts`) lift→universal-RR→reachability, wired at BOTH convergence points (active `buildSizedSignalForStrategy` + VTS `vts-runner`) so sim-to-live gate identically; per-class ROI/EV gate (assetClass threaded through `expectancy.ts`; migration seeds per-class `expectancy_gates`/`roi_gating` + DELETES the global `'*'` rows; fail-closed boot assertion). Surfaces by-reason (not a hidden gate). | **✅ DEPLOYED 2026-06-20 (architecture; CC-B + Langston)** — head `250761929`, CI `27883194231` all-4-green, migration applied + clean boot. **🚨 RUNG-1 PLUMBING — does NOT make crypto OPEN at taker** (EV finding: at the Tier-1 ~1.8% taker fee wall + pWin 0.60 ceiling, NetEV is net-negative even at 4%/1.6% → the EV gate honestly refuses; real opener = maker build reorg-B7 + Phase-25 pWin). Step-7 + Langston Step-8 verified: 11 per-class rows EACH for crypto_spot+xstock_spot, 0 global ROI rows (roi_gating + the 6 per-class expectancy_gates knobs) + 1 deliberate global `friction_safety_buffer` (uniform margin on the per-class friction model; Phase-25 revisit #337), fail-closed boot assertion PASSED (HTTP 200). Langston Step-4 APPROVED after catching 2 real blockers (active atr=0 → carry-on-SizingContext + loud invalid_atr; VTS invalid_atr silent-mislabel → distinct reason). §13 #335 (win-rate→B9)/#336 (xStock floor→Phase-25)/#334 (V4#2→pre-go-live). | `P19_REORG_B2_COMPLETION_REPORT.md` |
| **reorg-B2.1** | **Gates → strategies' shared guard; DROP the floor-lift; ONE per-class RR SSOT + per-class reachability** (Kyle 2026-06-21 placement question — "set targets right in the strategy module, not a post-hoc normalizer") | reorg-B2 follow-on (normalizer-placement fix) | **✅ DEPLOYED 2026-06-21 (OBJ-1/2/3/4; architecture; CC-B + Langston) — CLOSING.** Moved floor-lift/RR/reachability OUT of the post-hoc `signal-target-normalizer.ts` INTO the strategies' shared guard `applyGlobalGuards` at signal-gen (all 18 guard-eligible strategies wired; liquidity_trap skipped — disabled/inverted-geometry); **DROPPED the floor-lift** (a target mutation redundant with the Net-Expectancy gate); **ONE per-class RR SSOT** (killed the live 1.5-vs-2.5 split-brain); per-class reachability; NEW `guard-eval-tracker` suppression instrumentation (#372) on `/api/diagnostics/guard-eval-stats`. Normalizer KEPT as a net-neutral bridge (retires in reorg-B2.2 OBJ-C, #373-gated). Production code deployed at `8beb34181`; sanity 3 strategies/23 evals @90s. **⚠️ HONEST CI NOTE: OBJ-4b (`8beb34181`) wiring exposed 2 strategy unit tests (b79-0d-orb, b63-item12-geometry-override) that did not warm/mock the guard's `expectancy_gates` sync read → CI was RED from `8beb34181` until the TEST-ONLY fix `7bef81fd7` (mislabeled "green" mid-batch; caught at close). CI 4-green CONFIRMED on `7bef81fd7` (run 27901621481).** Langston Step-4 APPROVE all objectives. **OBJ-5 (retire) + OBJ-6 (VTS-tab visibility) SPLIT → reorg-B2.2 (#374) — window-gated.** | `P19_REORG_B2_1_COMPLETION_REPORT.md` |
| **reorg-B2.2** | **Tracker PERSISTENCE (unfreeze/crash-proof) + VTS-tab RR-gate VISIBILITY + normalizer-RETIRE** (the window-gated reorg-B2.1 OBJ-5/6 split out; #374) | reorg-B2.1 OBJ-5+OBJ-6 + Kyle 2026-06-21 persistence + UI directives | **✅ OBJ-A+B DEPLOYED + CLOSED 2026-06-23 (architecture; CC-B + Langston, autonomous)** — head `283bf4d6a`, CI `28052594844` all-4-green, pm2 restart#409 clean HTTP 200 (no migration). OBJ-A persistence (atomic checkpoint + reload + `getGuardEvalStartedAt`, `cf9e26d07`) + OBJ-B per-`(strategy,assetClass)` re-key (FLAG-2 sum-raw-re-derive #372 aggregate preserved; FLAG-1 keySchema discard incl. unversioned-legacy; new `getGuardEvalStatsByClass`/`PerClass`; v3 raw endpoint `statsByClass`) feeding per-class `guardDrops` into both VTS Filter-Diag tabs (`/api/vts/filter-diagnostics` v1.5, `/api/xstocks/filter-diagnostics` v2.1) + the shared `FilterDiagnosticsPanel` Reward-vs-Risk/Reachability card (by-reason + meanRR + suppression, distinct no-evaluations state — FLAG-3). New test 6/6. Langston Step-1/2/4 PROCEED + Step-8 APPROVED. **§9.3 UI-navigated BOTH tabs:** crypto mean_reversion 2/2.54/0%; xStock morning_star 40/0.73/100% + vwap_pullback 12/2.00/100% + sma_trend_ride 3/2.00/100% — distinct per-class. The durable per-class window now feeds reorg-B2.3 baselines + the #371 OBJ-C divergence read. Latent rrMin/rrMax Infinity-over-JSON homed #376. **OBJ-C (normalizer retire) still OPEN — #373-gated.** | `P19_REORG_B2_2_COMPLETION_REPORT.md` |
| **reorg-B2.3** | **Per-strategy × per-class minRR BASELINE — set from each class's OWN per-class data, NOT inherited** (Kyle directive 2026-06-23) | reorg-B2 global `min_rr=2.5` → per-`(strategy × asset_class)` | **✅ CLOSED 2026-06-27 (architecture; CC-B + Langston Step-1/2/4/8 APPROVED, Step-8 independent PASS). Head `47286ccfd`, CI `28283869359` all-4-green, deployed (HTTP 200, migration applied — 18 `module_constants` rows; 48h guard-eval window SURVIVED).** 13 per-strategy floors from each class's OWN 48h tracker (spread `max(1.0,mean×0.90)` / fixed-RR `max(1.0,mean−0.05)`; thin→`*`=2.0); single-chokepoint canonicalization (REUSED `normalizeStrategy` + fail-closed `resolveCanonicalStrategy`; `getPerClassTargetGate(assetClass,strategy)` REQUIRED — CF-1 closed the silent fail-open); unknown→`min_rr_unknown_floor` (max-per-class) + §13 tripwire; `rrSumSq`+`rrSumSqEvals` for 25-20 σ (CF-2, #399). **(D) RESOLVED** (Kyle KEEP ACTIVE → 25-20; zero floors moved). `P19_REORG_B2_3_COMPLETION_REPORT.md`. ORIGINAL SCOPE: Migrate the minRR knob from a single global `'*'` value to per-`(strategy × asset_class)` `module_constants` rows; set each baseline a notch BELOW that strategy's natural meanRR (Langston refinement) from a per-class VTS window — crypto AND xStock EACH from their OWN data (Kyle: NOT xStock-inherits-crypto; a thin-data xStock strategy gets a conservative default, not a crypto borrow). The 48h read (#372) is the crypto-side distribution baseline; the per-class split needs OBJ-6 first. **SETS the baseline (Phase-19); the win-rate RECALIBRATION is Phase-25 25-20 (clustered with 25-17/25-18, the reorg-B2 gate-knob family); the sub-1.0 EV-survival audit is folded into 25-19.** ↔ RUNNING_ISSUES #372/#375. | — |
| **reorg-B3** | **#233 EV-input thread — DI + dbsScore to the open-gate kernel** (the reorg-board form of B6.5g's #233 core; audit-reshaped: accuracy + strong-trend parity, NOT a crypto opener) | reorg build-board B3 (D6 accuracy half) | **✅ DEPLOYED + CLOSING 2026-06-24 (architecture; CC-B + Langston, autonomous)** — head `99887f90e`, CI 4-green (`28063641366` core + `28063929575` OBJ-4 followup), migration applied (di_at_queue + dbs_score_at_queue), restart#411. Threads DI+dbsScore from the routing-time FX5 survivor snapshot (Option B — the pool already carried dbsScore; DI was being dropped at `addSurvivors`) → typed `rtb_signals` columns → open-gate (NO metadata fallback / NO coerce → kernel documented defaults on null). **🚨 NOT a crypto opener (§9.1):** H1 real-DI gives ZERO EV lift (default DI=50 already at the 0.60 ceiling); H2 dbsScore is the lone EV-relevant thread (lifts strong-trend pWin off the 0.40 floor). **★FIND 1 (latent, dormant active path): the promote-conversion DROPPED `sourcePool` → the kernel strong-trend branch NEVER fired → the dbsScore thread would itself have been INERT; threaded it — CHANGES_AND_FIXES FIX-2026-06-24-A.** OBJ-4 rtb-metrics `evInputThreadProof` surface (forward-instrumentation; deploy-verify §9.3 caught the endpoint subset-map silently dropping it → instance fix `99887f90e` + §13 #379); OBJ-2 friction clean; OBJ-3 no-op; OBJ-5 HF9-DI provenance documented (recompute untouched — live VTS filter). Integration test 8/8 (strong-trend branch FIRES with non-default dbsScore). Active path DORMANT → live proof gated on paper-active turn-on. Langston Step-4 APPROVED. §13: #377 (xStock dbs source → reorg-B8), #378 (di_at_open const-50), #379 (rtb-metrics verbatim refactor). **VolNoise/prices DROPPED from #233 scope (not kernel EV inputs). gate-10 stays B7b's predecessor, NOT reorg-B3.** | `P19_REORG_B3_COMPLETION_REPORT.md` |
| **reorg-B3.1** | **#378 di_at_open reads carried at-queue DI + #379 rtb-metrics verbatim-minus-redaction** (the two small reorg-B3 §13 follow-ups) | reorg-B3 §13 | **✅ DEPLOYED 2026-06-24 (CC-B + Langston).** #378: di_at_open reads the promoted signal's carried `diAtQueue` (real routing-time DI) instead of the dead `metadata.DI ?? 50` that was always 50 → trailing-exit coherence. #379: rtb-metrics endpoint returns `getSummary()` VERBATIM-minus-redaction (spread + override `byReason→byBlockReason` + count-gate `bySymbol`/`recentEvInputSamples`) — kills the subset-map defect-class that silently dropped `evInputThreadProof` + `openFailedByStage`. Deployed staging `c077faa7f` restart#412. Langston retro-Step-4 confirmed sound (rode up via the shared-tree interleave on `b88d3cdac`). Forward-note (#379): allowlist→denylist flip means any future heavy `getSummary` field auto-surfaces → gate new heavy fields like `bySymbol`. | close note in `P19_REORG_B3_COMPLETION_REPORT.md` |
| **reorg-B3.2** | **VTS gate = TAG-DON'T-DROP for quality gates (un-strangle the VTS learning engine)** (Kyle 2026-06-24 — VTS opens collapsed 95-97% from the reorg-B2 gate) | reorg-B2 RR gate strangling VTS | **✅ DEPLOYED 2026-06-24 (architecture; CC-B + Langston + Kyle consensus).** VTS opens collapsed ~150/day → 3-5/day (staging-confirmed) from the reorg-B2 `min_rr=2.5` gate (suppresses 62-100% per #372) wired into vts-runner — strangling the learning engine AND making reorg-B2.3 CIRCULAR (can't calibrate a floor from data the floor already filtered). Fix: VTS QUALITY/EV gates (`rr_below_min`, `unreachable`) now LABEL (`vtsGateVerdict`) + simulate-to-close with the NATIVE target; only DATA-VALIDITY (`invalid_atr`, `invalid_geometry`) DROP. ACTIVE+LIVE keep SUPPRESSING — same gate eval, different disposition per path. Restores volume + captures the counterfactual outcomes that un-circularize reorg-B2.3 (calibrate from realized outcomes on the FULL population). Langston Step-4 caught + I fixed a real TS2322 (flat `TargetNormalizeResult`, `!ok` doesn't narrow `reason`; masked by baseline headroom) via positive-narrow. Head `c077faa7f`, CI 4-green (`28099133186`), restart#412. Step-7: VTS now simulating quality-gated trades live. `SkipReason += Target_Invalid_Geometry`. No migration (verdict re-derivable). | close note in `P19_REORG_B3_COMPLETION_REPORT.md` |
| **reorg-B3.3** | **Strategy-level VTS tag-don't-drop — the CORRECTED un-strangle** (reorg-B3.2 was inert: the 18 strategies hard-drop at signal-gen, upstream of the vts-runner normalizer B3.2 fixed) | reorg-B3.2 correction (Kyle-confirmed 2026-06-24) | **✅ IMPLEMENTED — pushing (architecture; CC-B + Langston Step-4 CONCUR 2026-06-24).** Root cause (staging-proven: 1 signal / 136,779 evals, rr+reach drops ~95%+): all 18 strategies `if (!_gr.pass) return null` at signal-gen, so `callStrategyDetect` returns null BEFORE the B3.2 normalizer → B3.2 dead on the live path. Fix: SSOT `guardForcesDrop(gr, disposition)` in `strategy-helpers.ts` (`VTS_TAGGABLE_GUARD_REASONS = {rr_below_min, unreachable}`); trailing-optional `gateDisposition='enforce'` threaded through all 18 detect sigs + the strategy-engine wrappers + the vts-runner dispatch; the crypto VTS path (`vts-runner:1182`) opts into `'tag'` → quality guards don't drop, flow to the existing B3.2 normalizer which tags `vtsGateVerdict` + simulates. **Active/live untouched BY CONSTRUCTION** (omit the arg → 'enforce' → byte-identical). Validity (invalid_atr, stop_distance) still drops on every path. Bench: tsc baseline OK, new test 6/6. Langston Step-4 all-6-conditions CONCUR. Step-7 proof = `signalsGenerated`/opens climb from 1 (NOT guardDrops — `recordGuardEval` is path-blind). §13: xStock → reorg-B3.3x (#382); known-benign clamped-vs-raw reach-ATR divergence logged on #382. **Step-10 owes: SYSTEM_MANUAL VTS-gating content + SIM applicability note (Langston §16).** | `P19_REORG_B3_3_COMPLETION_REPORT.md` |
| **reorg-B3.3x** | **xStock VTS un-strangle — UNIFY onto the shared normalizer (Langston Option B)** (#382) | reorg-B3.3 Option A (xStock deferred) | **✅ DEPLOYED 2026-06-24 (architecture; CC-B + Langston Step-1 + Step-4 CONCUR). Staging `53f601b93` restart#415, CI 4-green `28131918663`, no migration.** xStock VTS (`eval-cycle.ts`) had NO `normalizeAndGateTarget` (build-history gap, B79.0m). **Option B (NO-PATCHES): UNIFY** — eval-cycle:526 opts into `'tag'` + runs the SHARED normalizer after detect, before the Net-EV floor (orthogonal+sequential): quality TAG+simulate (native target); validity DROP (xStock GAINS the B3.3y `target<=entry` backstop). **★Langston Step-1 catch (fixed):** `RegisterOpenVtsTradeInput` lacked `vtsGateVerdict` (my field type-checked via excess-property bypass → silently ignored, inert); added it to the input + threaded onto the `OpenVirtualTrade` record (crypto-parity in-memory surface; in-memory-only, no DB col → #384→reorg-B4). Active/orchestrator path untouched. So the normalizer's tag-don't-drop is now the ONE shared VTS gate for BOTH classes. Step-7 ✅: xStock TAG_NO_DROP markers fire (KR/MRNA/MRVL, rr_below_min+unreachable). New test 5/5; tsc baseline OK. Governance: SYSTEM_MANUAL §11 + SIM §1.2a-2 + RUNNING_ISSUES #382 RESOLVED/#384 + BATCH_CATALOG + PHASE_HISTORY. | `REORG_B3.3X_SCOPE.md` → `P19_REORG_B3_3X_COMPLETION_REPORT.md` |
| **B-DIAG-387** | **xStock filter-diagnostics Net-EV-floor reject counter fix (#387)** — the "Net EV Below Floor" dashboard tile read 0 forever (the #386-misleading bug); pulled in before reorg-B4 because it gates the reorg-B7 maker/taker baseline readability | Kyle directive 2026-06-25 (fix #387 first) | **✅ CLOSED 2026-06-25 (non_architecture/observability; CC-B + Langston Step-1/2 + Step-4 APPROVED). Staging `1c451f5b5` restart#416, CI 4-green `28161070581`, no migration.** Endpoint sourced the tile from permanently-empty `byReason` scaffold; eval-cycle reject site recorded the reason in no counter the endpoint read. FIX (display-only): reject site writes `net_ev_rejected` into the aggregates → endpoint reads it from the lifetime accumulator (crypto-parity); surfaced 3 invisible pre-open gate reasons (OBJ-2, no-hidden-gates); excised the dead scaffolding (§18, DELETED_COMPONENTS_LOG). **PROOF: live endpoint == `signal_eval_archive` = 25 == 25 exact.** New test 5/5, tsc baseline OK. Governance: RUNNING_ISSUES #387 RESOLVED + DELETED_COMPONENTS_LOG + SIM + BATCH_CATALOG + PHASE_HISTORY. | `B_DIAG_387_SCOPE.md` → `B_DIAG_387_COMPLETION_REPORT.md` |
| **B-TEC-SELFHEAL** (re-scoped from ~~B-XSTOCK-TEC-WARMUP~~) | **#349 — TEC config staleness fence self-heal (refresh-before-throw) + VTS exit-loop per-trade isolation.** The reopen-warm framing was DROPPED at the deeper diagnosis: the fail-closed fence is correct, but it (1) LATCHED-until-restart (throw before the lazy-refresh trigger) and (2) one stale-class trade aborted the WHOLE VTS cycle (no per-trade isolation). General defect (not weekend-only — crypto stuck too), so a general self-heal beats the reopen-warm. | CC-A 2026-06-25 (B-NEW-40 soak finding); §9.4 named home | **✅ CLOSED 2026-06-25 (architecture; CC-A + Langston Step-1/2/4/8, Kyle-greenlit). DEPLOYED staging `5b9180120` restart#417, CI 4-green `28181014927`, no migration.** OBJ-1 = single-call-site `scheduleBackgroundRefresh` at the TOP of `resolveTECConfig` (before both throws; coalescer + B-NEW-40 45s fence preserved; fail-closed throw byte-identical; explicit unprimed-class guard). OBJ-2 = per-trade try/catch in the VTS exit loop (mirror `paper-execution-engine.ts:794`). OBJ-3 (periodic timer) OMITTED (Langston — gold-plating; #349 considered-and-declined). 48 TEC tests green; restart#417 fresh, ZERO TEC_STALE post-deploy; 06-28-reopen self-heal proof scheduled (§10.5 `tec_selfheal_verify`). SIM S18 (weekend pause state) was added during the diagnosis (`73d4b90b6`). Governance: RUNNING_ISSUES #349 RESOLVED + SIM TEC content + System Manual fence-semantics + BATCH_CATALOG + GOVERNANCE_EXCEPTIONS. | `B_TEC_SELFHEAL_SCOPE.md` | `B_TEC_SELFHEAL_COMPLETION_REPORT.md` |
| **reorg-B3.3y** | **Symmetric `invalid_geometry` — drop `target<=entry` / `reward<=0` longs (validity, not quality)** — the validity gap reorg-B3.3 EXPOSED (RUNNING_ISSUES #383) | reorg-B3.3 Step-8 (CC-B surfaced + Langston call) | **✅ DEPLOYED 2026-06-24 (non_architecture; CC-B + Langston Step-4 CONCUR). Staging `2136c74fe` restart#414, CI 4-green `28130496306`, no migration. Pulled AHEAD of B3.3x (Kyle + Langston OK — it was polluting).** `normalizeAndGateTarget`'s geometry guard nets `stop>=entry` but MISSED `target<=entry`, so a reward≤0 long fell into the `rr_below_min` quality-tag bucket and got VTS-simulated (live: `USDT/GBP/volatility_edge rr=-0.00` ~1/min). FIX = added `nativeTarget <= entryPrice` (valid long needs `stop < entry < target`) → `invalid_geometry` DROP on every path. Active unchanged (orchestrator `:1232` already dropped it as `rr_below_min`; now `invalid_geometry` — label-only). VTS tag→validity-drop; keeps the `rr_below_min` cohort to geometrically-valid-but-low-RR (the reorg-B2.3 population). New test `reorg-b3-3y` 5/5; tsc baseline OK. Governance: SYSTEM_MANUAL §11 + SIM §1.2a-2 + RUNNING_ISSUES #383 + BATCH_CATALOG + PHASE_HISTORY. | RUNNING_ISSUES #383 (scope) |
| **reorg-B4** | **Shadow-trade telemetry layer — selection-quality data engine (feeds reorg-B5)** — open a counterfactual shadow trade for EVERY RTB-pool member each promotion cycle, isolated telemetry-only | Kyle-directed run order 2026-06-25 (after B-DIAG-387) | **✅ CLOSED 2026-06-25 (architecture; CC-B + Langston Step-1/2/4/8 APPROVED — full 1413-line diff byte-verified + independent staging reader-sweep). Staging `f3cbe2d14` restart#418, CI 4-green `28201765444`, migration applied (`rtb_shadow_pairings` + 3 indexes).** Hook in `getRankedSignals` (sole live caller `checkRtbPromotion`) AFTER the FinalScore sort + BEFORE the slice → opens a shadow per pool member (promoted + non-promoted) recording decision-time ranking inputs + realized outcome into the NEW isolated `rtb_shadow_pairings` sink. Exit math REUSED (same `evaluateTECExit`, drift-guard test); only `maxHoldMs`(6h)+close-action differ. 4-layer by-construction isolation: separate `openShadowTrades` Map (S19) + allowlist `shadowClose` + shared `VTS_OPEN_TRADES_EXCLUDE_SHADOW` predicate on every non-shadow `vts_open_trades` read + rehydration split. **★Langston Step-4 LOAD-BEARING catch (closed this batch):** persisted-table reader contamination (factor-replay/ablation feed + xStock 24h count + bootstrap gate) → fixed with the shared predicate + 2 BEHAVIORAL tests. DORMANT (rtb_total=0 → §9.1; verified live row_count=0, no boot errors; 22/22 unit suite = behavioral proof). §13 B9-gated: #388 (rehydration fail-direction) + #389 (capture-path guard). Governance: SIM (callout+S19+GC-DELETE/cycleKey) + System Manual §19.8 + RUNNING_ISSUES #388/#389 + BATCH_CATALOG + PHASE_HISTORY. **NEXT: B6.6/B6.7/B6.8.** | `P19_REORG_B4_SCOPE.md` → `P19_REORG_B4_COMPLETION_REPORT.md` |
| **reorg-B4.1** | **Shadow-trading VISIBILITY tab + per-cycle pool-membership record** — the tab to see shadow results (trading page, after Trade History) + the per-cycle-capture fix Kyle's mechanics question exposed | Kyle-directed 2026-06-26 | **✅ CLOSED 2026-06-26 (architecture; CC-B + Langston Step-1/2/4/8 APPROVED — incl. a Step-4 CHANGES-NEEDED round F1+F2 fixed). Staging `153cc4235` restart#419, CI 4-green `28204773605`, migration applied (`rtb_shadow_pool_members` + 3 indexes). STAGING-UI-VERIFIED via Claude-in-Chrome (§9.3).** Additive two-table: KEEP `rtb_shadow_pairings`=resolving TRADE+outcome (deduped 1/signal); ADD `rtb_shadow_pool_members`=EVENT grain (1 row/cycle×signal: rank, promoted, pool_size STAMP, scores, logical FK→pairings.id). captureShadowPool writes a member row every cycle; registerOpenShadowTrade dedupe-return null→existing-id (verified safe). NEW read-only `GET /api/shadow-trades/by-cycle` + "Shadows" tab (per-cycle ranked-pool comparison + best-trophy + open-shadows + selection-quality summary). **★Step-4 F1 (premature scoring) + F2 (page-scoped) FIXED:** summary = ONE SQL aggregate over ALL FULLY-CLOSED cycles (`bool_and(p.closed)` + `percentile_cont` median). **OBJ-4 UI verify (Claude-in-Chrome):** empty-state clean + seed-then-clean populated proof (promoted-#1-not-best → Promoted=Best 0%/Median 100%; "resolving…" on open-member cycle) → seed DELETED → dormant. DORMANT (rtb_total=0 → §9.1). §13: #390 (member-row retention sweep → B9, in words). Bench: tsc OK; reorg-b4-1 9/9; full unit 1873 pass. Governance: SIM fold + System Manual §19.8 extension + RUNNING_ISSUES #390 + BATCH_CATALOG + PHASE_HISTORY. **NEXT: B6.6/B6.7/B6.8.** | `P19_REORG_B4_1_SCOPE.md` → `P19_REORG_B4_1_COMPLETION_REPORT.md` |
| **P19-B6.5g** | **EV-input integrity + gate-10 lifecycle proof** (B6.5e diagnosis successor; OWNS gate-10) | **NEW (B6.5e diagnosis).** The open path is sound; crypto never opens because the EV gate honestly rejects every signal on DEFAULT EV inputs. Core = **#233** (thread real `DI`/`VolNoise`/`prices`/`dbsScore` into `evaluateTradeExpectancy`) + a friction-decomposition audit (rule out double-counted spread / wrong fee tier) + a ranking/sizing normalization check (absolute price-unit NetEV is not cross-symbol comparable) + surface the `openFailedByStage`/`byReason` breakdown in `GET /api/diagnostics/rtb-metrics` + **gate-10 (≥1 full closed crypto lifecycle: open→exit→close→cooldown→telemetry) as the exit gate**, re-run on real inputs. **gate-10 MOVED here from B6.5e** (does not vanish). **B7b stays HARD-GATED on B6.5g-green (§6 gate 10).** | queued (after B6.5f) | — |
| **P19-B6.6** | **Price-discovery-liveness fill gate** (P19-B4a C3 / #236) | **NEW (CC+Langston Step-4 consensus 2026-06-14).** Closes the holiday/half-day hole in the C3 clock-only liquid-fill-window: on a US-equity holiday/half-day the clock says in-window AND the 24/5 token feed keeps `captured_at` fresh, but `last` is frozen on a closed book → both the clock gate AND the freshness gate pass → a fill would land on a stale reference price. Durable fix = a **price-discovery-liveness gate** requiring `last` to have actually MOVED within the last N min/M snapshots before a fill (immune to holidays, half-days, DST/clock bugs; piggybacks the freshness read). Threshold set from evidence. **B7b HARD-GATED on B6.6 green (§6 gate 11).** | **✅ CLOSED 2026-06-26 (architecture; CC-B + Langston Step-1/2/4 APPROVED). Staging `2398702e8` restart#420, CI 4-green `28208230871`, migration applied (5 `price_discovery_liveness/xstock_spot` rows seeded/verified), HTTP 200.** Gate at the open seam AFTER the depth gate (depth-first), xStock-only, fail-closed; requires this token's `last` to have CHANGED ≥`min_moves` within the **45-min window PINNED** (3 weekday sessions — passes active names ≥2× / excludes deep-but-slow ETF tokens whose stale cadence = stale-reference fills; justified by **type-II frozen-but-quoted-book detection**, not p99 margin; single, NOT tiered — the upstream `lq_min` gate removes the unseparable names; GOTU residual #391). Reason codes flat_last/no_data/sparse_snapshots/liveness_timeout (feed/config outage distinguishable from holiday); index-bounded windowed read + Promise.race timeout-fail-closed. 22 unit tests; full suite no-regress (9 fail = pre-existing env-only, proven on base). **DORMANT (§9.1 forward-instrumentation — no live fill).** §13: #391 + #392. Governance: SYSTEM_MANUAL §7 + SIM S20 + RUNNING_ISSUES #236 RESOLVED + BATCH_CATALOG + PHASE_HISTORY. | `P19_B6_6_SCOPE.md` → `P19_B6_6_COMPLETION_REPORT.md` |
| **P19-B6.7** | **Vestigial 2nd-WS market-data subsystem cleanup** (#301) | **NEW (P19-B4b.2 finding; Kyle pinned 2026-06-16).** The `MarketDataCoordinator` + `MarketDataWebSocket` 2nd Kraken WS connects but subscribes to ZERO pairs (no `subscribeToPair` caller) → ticker+book streams dead in production, superseded by `kraken-websocket-adapter`. Audit the 4 live readers' real liveness (`feed-integrity-monitor` / `health-monitor` / `parity-gate` / the B4b.2-rerouted `system-health-monitor.getExecutionMetrics`) — esp. whether the 2 safety monitors give a false all-clear off the idle connection — then remove if confirmed dead. Also answers the deferred Q2 "is the health `execution` block itself dead?". **Sequenced after B6.6, before B7a/B7b** (pre-switch-on; no live impact, does NOT hard-gate B7b). | **✅ CLOSED 2026-06-26 (architecture; CC-B + Langston Step-1/2/4/8 APPROVED — Step-8 independent staging PASS + governance-CONTENT verify). Staging code commit `b337ede36` restart#421, CI `28266067266` all-4-green, migration applied (5 `module_constants` rows `module_name='feed_health'`), HTTP 200.** Confirmed the 2nd WS vestigial via runtime probe (`MD-WS_TICK`=0/`Sub OK`=0 since April) + blast-radius trace (4 status-only consumers, 0 tick-output, 0 `subscribeToPair`) → **DELETED** `market-data-ws.ts`+`market-data-coordinator.ts` (§15 archive+log, tsc-zero-dangling). Re-pointed all 4 at the PRIMARY `krakenWebSocketAdapter` via new pure `market-data/feed-health-aggregate.ts` (27 tests). **3-axis aggregate (opposite by design):** ALARM=per-class freshest-symbol age (xStock `isXstockMarketOpenUTC` per-symbol gate + post-open warmup grace + market-closed suppression; crypto 24/7); go-live GATE=conservative proportion-fresh (kills the dead-feed false-PASS); display=freshest. The Q2 `execution` block is LIVE (not dead) — re-sourced. **Phase-19 landmine REMOVED** (feed-integrity would false-CRITICAL once dormant-suppression lifts) + **30s `[MD-WS] Data stale` spam structurally GONE** (Langston independently verified). **#396 split:** WS-error half = the dead 2nd-WS (removed); scanner-underfill half → PURE telemetry (OBJ-4), FIX homed **P19-B6.9**. DORMANT alarm (deploy+structural+bench proven, NOT live-runtime — §9.1). §13: #396 + #398 (parity-gate uptime-cumulative-reconnects trap) → P19-B6.9. Governance: SIM (S21+2.1.1) + SysManual (§16+§8) + RUNNING_ISSUES (#301 RESOLVED) + BATCH_CATALOG + PHASE_HISTORY. Resolves #301. | `P19_B6_7_SCOPE.md` → `P19_B6_7_COMPLETION_REPORT.md` |
| **P19-B6.9** | **Scanner short-universe fix (#396) + parity-gate uptime-trap fix (#398)** — pre-Phase-21 go-live-path hardening | **NEW (P19-B6.7 §13 homes).** (1) #396: after B6.7's OBJ-4 telemetry attributes the live cause of the Kraken universe fetch coming back short (transient REST hiccup vs `pairInfo`-join drop), fix it so the scanner stops occasionally scanning ~43/300 pairs. (2) #398: re-base `parity-gate.assessWsReadiness` uptime off CUMULATIVE `reconnectAttempts`/fixed-120-denominator (a long-lived process spuriously fails the Phase-21 go-live WS gate) → interval/rolling-window. Both are go-live-path hardening, not urgent (parity-gate dormant till Phase-21). | **✅ CLOSED 2026-06-30 (non_architecture; CC-B + Langston Step-1-consensus/Step-4-APPROVE/Step-8). Head `c1e5ef80c`, CI green, deployed (HTTP 200, no migration).** **#398 FIXED:** re-based `parity-gate.assessWsReadiness` WS-uptime off the adapter's CUMULATIVE lifetime reconnects (which drifted a healthy long-lived process below the 99% floor → spurious go-live-gate fail) onto a ROLLING 1h window — new pure `computeRollingWindowReadiness` (denominator = snapshots present in the feed-integrity ring, NOT since-boot → self-heals) fed by new `getRollingWindowReadiness()`; cumulative formula removed (§15); `MIN_READINESS_SAMPLES=6` fail-closed warm-up. `reconnectsSinceLastCheck` confirmed a true per-interval delta (Langston's load-bearing check). **#396 PARKED (§13):** B6.7's OBJ-4 `[#396] SHORT UNIVERSE` telemetry SILENT across 12k staging lines (not reproducing) → parked in RUNNING_ISSUES with a concrete re-trigger (reopen when the marker fires). Bench tsc-baseline GREEN + 37/37. **Phase-21 calibration flag (NOT changed): 99%-floor vs 12-snapshot granularity → roadmap §3.5 item 21-3b.** Governance: RUNNING_ISSUES (#398 RESOLVED/#396 PARKED) + SIM §2.1.1 + SysManual §16 + roadmap §3.5 21-3b + BATCH_CATALOG + PHASE_HISTORY + completion report. | `P19_B6_9_SCOPE.md`/`P19_B6_9_PRE_AUDIT.md` → `P19_B6_9_COMPLETION_REPORT.md` |
| **P19-B6.8** | **Per-mode guardrail completeness — paper-specific guardrail set** (Kyle directive 2026-06-16) | **NEW.** Audit EVERY user-settable guardrail / trading-setting → find the ones NOT yet separated per-mode (still in the legacy single `trading_settings` table or otherwise global/mode-blind) → migrate + wire them into the per-mode `guardrails_v2` so **paper has a COMPLETE working paper-specific guardrail set**. Live gets the same DB structure but is explicitly NOT required to be fully wired/functional (Kyle: live internals unknown, out of scope here). Extends the B6 per-mode-warning pattern. **Sequenced after B6.7, before B7a** (paper's full set ready before the UI shells + the flip). Paper-only **B7b pre-flight gate** (§6 gate 14). | **🔄 IN PROGRESS 2026-06-29 — REFRAMED after verify-everything pre-audit (CC-B + Langston Step-1 consensus).** The #302 "migrate mode-blind `trading_settings`" premise is OUTDATED: `trading_settings` purged (41F-L), old `guardrails` table bypassed, and the LIVE guardrails tab (`CoreFourGuardrails`→`/api/guardrails-v2`) ALREADY delivers complete per-mode (paper+live) guardrails (DB-verified: distinct paper/live rows). So #302's functional core is **already satisfied**; B6.8 = (1) surface the 50/75 daily-loss WARNING TIERS as 2 per-mode user controls in CoreFourGuardrails (the one residual + #323 fold — they were DB-backed+firing but not user-settable; UI + PUT allow-list/validate/persist, RULE_011 per-mode coherency already server-enforced); (2) §15-delete the stranded-dead `guardrails-tab.tsx` + orphaned `copy-to-live-modal.tsx` (unrendered; archived+logged, tsc-clean). **✅ CLOSED 2026-06-29 (architecture; CC-B + Langston Step-1-consensus/Step-4-APPROVE/Step-8). Head `ffb737b0c`, CI `28357436217` all-4-green, deployed (HTTP 200, NO migration — UI+endpoint).** Surfaced the 50/75 daily-loss warning tiers as 2 per-mode user controls in CoreFourGuardrails (the one #302 residual + #323 fold); §15-deleted the stranded-dead GuardrailsTab + orphaned copy-to-live-modal (archived+logged, tsc-clean). **★ Step-8 DB-cross-check caught + fixed a real bug:** the valid PUT validated+returned-ok but didn't persist — `upsertGuardrailsV2` UPDATE merge-map omitted the warning tiers (the 4th wiring place after extraction/validation/updatePayload); fixed `ffb737b0c`. RE-VERIFIED: PUT paper 55/80 persists, DB paper-updated + live-untouched (per-mode), coherency 80/55 RULE_011-rejected, restore clean. §9.3 UI (Claude-in-Chrome): all 8 controls render incl the 2 new warning tiers, PAPER mode, GuardrailsTab gone. #302 closes as functionally-satisfied + delta-surfaced. §15 follow-up = P19-B6.10 (old-table retirement + 2 latent bugs #400). Governance: SysManual Ch4 + SIM + DELETED_COMPONENTS_LOG + BATCH_CATALOG + PHASE_HISTORY + completion report. **↳ FOLLOW-UP P19-B6.8a (✅ CLOSED 2026-06-30, non_architecture; CC-B + Langston Step-4): Kyle UI directive — renamed the tab "Guardrails"→"Paper Guardrails", replaced the small mode badge with a big bold "Core Guardrails — Paper Mode" header, and PINNED the tab to paper via a required `mode` prop (the ambient toggle defaults to `'live'`, so a "Paper" label on a toggle-following tab would mislabel live risk-limits). Head `240b7eec7`, CI green, deployed. Live tab deferred → §13 home RUNNING_ISSUES #401 + roadmap §21.3 (21-3a). `P19_B6_8a_COMPLETION_REPORT.md`.** | `P19_B6_8_SCOPE.md` → `P19_B6_8_COMPLETION_REPORT.md` |
| **P19-B6.10** | **Retire legacy guardrails-v1 (old `guardrails` table + `PUT /api/guardrails` + `upsertGuardrails` throw-stub)** — §15 cleanup + caller migration | **NEW (P19-B6.8 §13/§15 home — Langston consensus 2026-06-29).** The old per-mode `guardrails` table + its deprecated endpoint are superseded by `guardrails_v2` but NOT cleanly removable: live callers `reasoning-orchestrator.ts:500` reads `from(guardrails)` directly (compare_guardrails → STALE data) + `intent-executor.ts:418` calls `upsertGuardrails` (→ THROWS). Migrate BOTH to v2 (fixes the 2 latent bugs), then §15-dispose the table+endpoint+stub. Split from B6.8 per §15(b) — folding AI-reasoning/intent migration into a guardrails-UI batch = cross-blast-radius. Homes RUNNING_ISSUES latent-bug entries. **Not urgent** (both callers are AI-path, rarely/never fired today). | queued | — |
| **P19-B7** | **Ranking fix + maker/taker shared service + crypto lifecycle proof** (the opener/engine work) | **★ Kyle renumber 2026-06-30** — gives the colliding "reorg-B7" / "rung-2 maker build" a clean home + absorbs gate-10. Run as sub-batches, **ranking FIRST** (prerequisite). **B7.1 ranking fix:** wire a genuinely-predictive (correlate-with-winning) score into the LIVE picker — today `rankingScore` is inert + `finalScore` is anti-predictive (the make-or-break for picking the right signal). **B7.2 maker/taker shared service:** per-signal maker-vs-taker entry decision (post-only spread-capture @ 0.40% maker vs cross @ 0.80% taker) + asymmetric-stop EV kernel, **SHARED by active + VTS** (D6 rung-2 — the real crypto opener: at the taker fee wall the EV gate refuses crypto). **gate-10 exit proof:** watch ≥1 FULL **crypto** trade open→exit→close→cooldown→telemetry on real inputs (**absorbs B6.5g**; B7's acceptance test — needs the maker build to clear the fee wall first). | **B7.1 ✅ DONE 2026-06-30** (the R-multiple ranking fix — head `0dacd34f2`, CI `28478843721` 4-green, deployed restart#427, Langston Step-4 APPROVED + CC-A 2nd-eyes); **B7.2 ✅ DONE 2026-07-01** (maker/taker best-of-both entry decision — the STRUCTURAL crypto opener; head `c595d987e`, CI `28486308234` 4-green, deployed staging (16 `maker_taker` rows + 8 `rtb_signals` cols verified live), Langston Step-4 APPROVE-to-push + Step-8 PASS, CC-A landmine-catch; #330 SPLIT→P19-B7.2a, calibration/pattern-vol homes #410/#411); **B7.2b ✅ DONE 2026-07-01** (complete the shared maker/taker service — VTS now gates on best-of-both + active-path decision runs before the SQE + RTB refresh re-decides + the wrong-stage in-queue maker_pending STRIPPED + fee-mode carried onto all 4 trade stores + uniform "Entry Fee Mode" UI column on 4 surfaces; commits `a9480e4f8`+`8dc63772a`, CI 4-green, restart#429, Langston Step-4 APPROVE + confirm-B fix + Step-8 PASS, VTS Open surface live-verified Taker 0.80%); **B7.2c ✅ DONE 2026-07-02** (post-promotion PENDING maker-fill lifecycle, Kyle-SIMPLIFIED mid-batch: pending-till-honest-trade-through-fill OR hard-drop ~1h NO convert + marketable→stored-taker check + VTS maker/taker TWINS for learning-by-comparison + never-filled visible-≠-counted records + shared pure `pending-maker-logic.ts` paper/VTS parity module + paper storage-chokepoint typed guard; head `b48aef51f`, CI `28612927459` 4-green, restart#432 w/ db:migrate DB-verified, Langston Step-4 APPROVE-for-push + 4 conditions ALL dispositioned + Step-8 PASS "cleared for close"; #412 RESOLVED, #414 closed-overtaken; first-occurrence soak evidence = #433); **B7.2a ✅ DONE 2026-07-02** (#330 fee-resolver consolidation — the cost-cache's second fee road removed: `CostMetrics` drops fee, `resolveCryptoTakerFee` DELETED §15, every consumer composes at read time from the B-4.5 merge site with per-site class context, the governed fee un-clampable + TTL-free by construction, drift monitor strictly truthful; head `4b9d62fc9`, CI `28617134643` 4-green, restart#435, Langston Step-1rev2/2/4 iterated + APPROVE, live proof 642 symbols all 0.008; #330 RESOLVED w/ B81 forward-pointers; **B-RENAME ✅ DONE 2026-07-03** — see the §5 row + P19_B_RENAME_COMPLETION_REPORT.md; B8 gains the closed_trades growth gate + Q5 design decision + #404 check); **B7.2d ✅ DONE 2026-07-03** (#434 — the xStock VTS lane wired: shared decision at the eval-cycle seam, floor on chosenNetEV, fee-mode stamp, pending lifecycle via the shared registry, twins via the extracted shared `maybeOpenTwin` under the narrow B79.0m.b lock-lift; commit `127c6d845`, CI `28626488749` 4-green, deployed no-migration, Langston Step-1/2/4 PROCEED); **P19-B8-AC1 (was "gate-10") — the crypto lifecycle acceptance proof — queued, RUNS AT the B8 crypto switch-on** (needs paper-active) | `P19_B7_1_COMPLETION_REPORT.md`, `P19_B7_2_COMPLETION_REPORT.md`, `P19_B7_2b_COMPLETION_REPORT.md`, `P19_B7_2c_COMPLETION_REPORT.md` |
| **GOV-ARC (#668)** | **The governance-standardization arc — THE TRACKED HOME (Kyle-directed 2026-08-07 after it drifted unnoticed).** Five pieces, CC-A: rules-file slimming (**1a+1b CLOSED**; 1c path-scoped rules / 1d skills incl. the `STORAGE_POLICY.md` CONTENT refresh / 1e ordering OPEN) · **DECISION RECORDS (#671)** (Langston’s ~3,000 rulings live on one box OUTSIDE git — scoped, NOT started; ⛔ the DURABILITY fix is SEPARABLE and not sequenced behind the harvest) · governance tier list · catalogue work (`B-CATALOG-1`) · measurement hooks (rule-29 leg 2). **Runs ALONGSIDE Phase-19 trading work, not inside it** — tracked here because Phase 19 is the live phase and an arc with no phase-plan row is an arc only Kyle can see drifting. | **IN PROGRESS (CC-A)** |
| **B-TEC-REGIME-PARAM-REMOVAL** | Dead `regime` read removed from the active exit path (`evaluateTECExit` context). Homed by #602 as "one dead cast"; **the required census found THREE dead reads**, and the two with `??` fallbacks were **deliberately LEFT as the reconnect site** for #677. Five stale `paper-execution-engine` doc sites judged per site. | **✅ CLOSED 2026-08-07 (CC-A).** Deployed `8bf25630e`, restart #554, CI 4/4 per-job; Langston Step-1/2/4/8 ALL PASS. **Spawned #676 / #677 / #678 / #683.** |
| **B-VOLATILITY-CACHE-RETIRE** | **Retire the dead volatility cache + `0.015` fallback the regime-gate fix left behind** — `B-REGIME-INPUTS-LIVE`'s OBJ-4, never done (#543 residual; rule 18). | **PLACED 2026-09-02, owner CC-B, Backlog.** At the ref: `market-metrics.ts` still holds `volatilityCache` (no writer), `updateVolatilityData` (no production caller) and `return 0.015`; `ready_to_buy_service.ts:74` still imports `getVolatility` marked *unused, OBJ-4's retirement target*. **TWO live route sites survive (Langston census, 2026-09-02): `routes/dse.ts:56` READS the cache on `/dse/metrics-cache`; `routes/dse.ts:78` `clearVolatilityCache()` MUTATES it.** Third orphan limb `getVolatilityClassification` (`market-metrics.ts:77`, zero production callers) needs an explicit disposition. Tests split subject-vs-probe: `dynamic_sizing.test.ts:267-273` (the cache) vs `:277-281` (the classifier). **Also carries:** the live fail-loud exercise `B-REGIME-INPUTS-LIVE` criterion 3 never ran (starve the MCE in the rig, observe the refuse), and `metadata.regimeWeight ?? 0.5` at `ready_to_buy_service.ts:1014` checked as a HYPOTHESIS. Deletion per rule 18: blast-radius pass on both route sites + tests, `DELETED_COMPONENTS_LOG`, `.removed` archive. Langston's 07-20 approval condition (*"OBJ-4 has its named home before this is closed"*) is met by this row. | after `B-ALERT-ACTOR-ALLOWLIST`; Langston Step-1 |
| **P19-B8** | **Paper monitoring screens (BOTH classes) + THE SWITCH-ON + the acceptance proofs P19-B8-AC1 (crypto) / P19-B8-AC2 (xStock; was "gate-10"/"gate-10x")** | **★ Kyle renumber 2026-06-30** — folds old B7a (UI shells) + old B7b (switch-on); **both-classes made EXPLICIT** (D1/D3). **Screens (crypto + xStock):** Filter-Diagnostics (crypto + xStock tabs) · Ready-to-Buy queue · Open + Closed trade tables (mirror VTS) · Surviving-pairs table · NEW Results & Outcomes dashboard · VTS-indicator placement (§4 item 7) — built vs fixture data, populate at turn-on (double as §9.3 visual verify; §6 gate 9). **THE SWITCH-ON (staged, §6 pre-flight checklist):** flip crypto paper-active ON → troubleshoot screens + functionality until trades flow clean open→close → flip xStock paper-active ON → **prove ≥1 FULL xStock trade completes (scan→filter→evaluate→signal→RTB→promote→open→close)** — the xStock equivalent of B7's crypto gate-10, made EXPLICIT so xStock can't slip. Both classes ON + working by B8 close (D3: both paper-working by phase close; D7: one class actively EXECUTING during the validation window — the pipeline up to promotion runs for BOTH together). **★ Intra-B8 order (RESTRUCTURED at the Kyle design discussion 2026-07-03, supersedes the 06-30 sketch; design record = `Langston Design Asks/P19_B8_DESIGN_INTENTIONS_v1.md` + consensus addendum, Kyle decisions A/A/A + field research archived): B8.1 = three-mode-page UI reorganization → B8.2 = balance policy (Kraken-mirror start, auto physics-based re-anchor, launch snap, dollar-agnostic fence; §4 LOCKED) → B8.3 = per-mode dashboards + metrics-strip move + three-balances labeling → B8.4 = THE SWITCH-ON (crypto→xStock per D7) + AC1/AC2, HARD-GATED on the AC1 restart-with-open-positions proof BOTH legs (CI rehydration on a fresh instance + deliberate live pm2 restart with byte-identical-state/zero-double-count assertions). The 06-30 principles carry: crypto flip isolated + attributable; xStock proves its OWN gate behavior + lifecycle, never riding crypto's proof.** | **B8.1 ✅ DONE 2026-07-04** (three mode pages Live/Paper/VTS via ONE shell + tab manifests; toggle+modals into the Paper page; ML→calibration home; Pattern Scanning + Filter Insights deleted rule-18 w/ the re-trace-corrected KEEP set; 4 diagnostics defects fixed at true sources incl. the module-constants refresher race → swap-on-success+SWR; head `111b9d349`, restart#441, CI 4-green, Kyle full-visual-audit walked, Langston Step-1×2/2/4/8 ALL PASS — Step-8 independently reproduced 150|145|3; xStock counter visual proof → 2026-07-06 system-alert, owner CC-B); **B8.2 ✅ DONE 2026-07-05** (balance policy — Kraken-mirror start w/ read-only confirm + fail-hard refusal + continue-never-fetches; 7+2 ghost deletions incl. the hardcoded $10k exposure/drawdown DENOMINATORS — RISK-029 resolved, ~11× understatement at the real balance; $25k ghost row + orphaned `dawntrader_v2` schema-copy deleted; `portfolio_anchor_events` ledger + `anchor_version` + auto friction-divergence re-anchor w/ 24h cooldown, 12 seeded knobs; balance-ratio-at-open stamp — ratio+anchor stamped TOGETHER, honest NULLs, VTS by-absence; resume refusal both seams; dollar fence static-scan w/ red-proof — ZERO live-path $ violations, Kyle sign-off list EMPTY proven; #410 RESOLVED w/ shim retired; **★ Step-7 pin revision on LIVE evidence: the real account = 100% USDC (824.11) + zero free ZUSD → mirror widened to allowedQuotes parity, #435**; synthetic re-anchor proven end-to-end on the live-mode row — learning counts byte-identical, Kyle's paper row untouched; heads `3ac778d0c`+`b07683c43`+`71c9d81bd`, migration applied+live-verified, restart#443, CI 4-green ×3, Langston Step-1/2/4+delta ALL PROCEED; #436 throwing-upsertGuardrails trio → B6.10; C4/C6 soak riders → B8.4); **B8.3 ✅ DONE 2026-07-06** (per-mode dashboards + mode-honest FD tabs — Dashboard landing tab on all 3 mode pages w/ Kyle's widget template + realized-balance chart + breakdowns; analytics mode-parameterized w/ 6 new metrics in pure unit-tested math; netPnlPercent always-0 ROOT-FIXED against the B8.2 anchor balance; profitFactor null-on-no-losses w/ "∞ (no losses)"; NEW vts/analytics + balance-curve + pipeline-tail endpoints; metrics strip re-homed top-bar→pages w/ three-balance labels + both WS listeners moved; FD disposition model — VTS shows Dropped/Tagged, Paper/Live one true Rejected=Evals−Passed identity test-pinned + verified exactly on live data, "Rejected" structurally unreachable on VTS; shared-scanner banner; OBJ-8 visible fetch-failure states everywhere; head `6ba933708`, restart#446, CI 4-green, Langston Step-1×2/2/4×2+delta/8 ALL PASS — Step-8 independently reproduced all 5 endpoints; #415/#416/#417 → B8.3b); **B8.3b ✅ DONE 2026-07-07** (the fast-follow, re-declared `non_architecture` — Option A: the fx5-scanner is MODE-MULTIPLEXED so it becomes the active funnel at switch-on, no dual-funnel build needed. `scanDiag.destinationCount`+`totalDestinationCount` RETIRED (mislabeled serialized-but-unrendered; survivor `routes.ts:7809/:7853` untouched); the FD panel's #417 display separation done — all 3 VTS-runner blocks gated to 'tag' + honest enforce placeholders, the "→ VTS Destination" rows relabeled "→ Survivors (shared scan feed)" on enforce; headline netPnl → canonical net basis #415; balance-curve carrier seeded #416. **The §9.3 visual walk caught a THIRD (24h-rolling) VTS-runner block the diff review couldn't see** (ungated block ≠ in diff) → fixed in follow-on `23047f291`. Heads `9e91245ab`+`23047f291`, restart#447-448, CI 4-green ×2, Langston Step-1/2/4/8 ALL PASS (Step-8 re-verified the 2nd-block fix on live staging); #415/#416/#417 RESOLVED; **#418 → B8.4 gate** (the switch-on closer MUST populate `closed_trades.net_pnl` — the `default('0')` bridge would silently zero a real trade). ON-state enforce funnel render → B8.4 §13); **B8.3c ✅ DONE 2026-07-07** (Kyle-directed via OLD Claude — restored the open/closed trade COUNT atop the trade tabs, dropped in the B8.1 restructure; VtsOpen/VtsClosed show `(data.trades.length)`, ActiveTradesV2 "Active Trades (N)" mode-scoped; TradeHistoryTab was NOT dropped — already showed "{totalCount} total trades", left unchanged; 3 components/2 files; head `315e9b680`, CI 4-green, Paper "Active Trades (0)" §9.3-confirmed live, VTS counts source-verified (VTS-tab nav = Claude-in-Chrome limitation); non_architecture, SIM/SysManual N/A) ****B8.4 ✅ DONE 2026-07-08** (Filter-Diagnostics active-path instrumentation, BOTH classes — ★ Kyle RENUMBER 2026-07-07: B8.4 is now this instrumentation batch, THE SWITCH-ON moved to B8.5. Part-1 display + Part-2/B8.4b engine emits into the NEW S22 `active-funnel-tracker`; signalsGenerated denominator + pre-SQE + SQE-per-gate + POST-SQE distinct bucket + strategyAttrition-as-own-upstream-bucket (Langston anchor-b/family_imf fix) + RTB-refresh cyclesRun/reconfirmed/rejected + SQE-at-refresh phase-split + promoted; envelope `active-funnel/v3`; ALL DORMANT until switch-on; head `5058b8644`, CI 4-green `28903563905`, restart#454; Langston Step-4 APPROVED + Step-8 PASS independent; #419 refreshAndRank error-bucket → B8.5); **B8.4c ✅ DONE 2026-07-08** (Filter-Diagnostics tab STANDARDIZATION — the six FD tabs, Kyle UI feedback, REV-1/2/3, non_architecture display + read-only telemetry. REV-3 head `623064821`, CI 4-green `28954002287`, restart#465, NO migration. **OBJ-6** split the mislabeled "Scanner Capacity" → **Per-Cycle Target** (crypto 300 / xStock 75) + live **Total Universe** (crypto krakenUniverseSize ~1,500 / xStock ~481) surfaced read-only onto fx5 ScanDiagnostics (vts whole-object lastScan passthrough) + the xStock scanner diag; ScannerCard 5→6 fields; **#421 RESOLVED** (crypto universe in the VTS feed — §9.3-verified live 1,509 on VTS + Paper cards). **OBJ-7** one shared width all six tabs; **OBJ-8** Paper/Live mirror the VTS 3-table structure DORMANT (never bare-0); **OBJ-9** per-table blue/purple/teal filled bold headers; **OBJ-10** per-table horizontal-scroll + frozen first column. §15 DELETED ActiveDownstreamFunnel + ActivePipelineTail (superseded by DormantPipelineTables; writers persist, endpoints client-unconsumed → B8.5). Langston Step-4 APPROVED (his crypto-OBJ-6 blocker RETRACTED — wrong-endpoint grep, verified the vts.ts:1629 whole-passthrough himself) + §9.3 Chrome walk (crypto VTS live + Paper dormant). #422 (scan-24h orphan) → B8.5. SIM/SysManual N/A (display + read-only telemetry); GOVERNANCE_EXCEPTIONS class-override + N/A rows resolve alert 970f9467) **B8.5a ✅ DONE 2026-07-13** (switch-on prep — scoring de-contamination + gate restructure; head `dbfb14460`, restart#468, CI 4-green; ranker de-tinted to measured per-class flat-pWin base rates (DB knobs), net-EV admission moved INTO the SQE (11.8B → STILL-BLOCKS drift backstop), confidence→sizing severed, finalScore gate retired→shadow-log, honest VTS capture fields; Langston Step-1/2/4/8 + OLD Claude dual Step-4/8 ALL PASS; behavioral gate proofs on live traffic = named B8.5 obligation per §9.1); **B8.5b ✅ DONE 2026-07-13** (the pre-flip CAPTURE micro-batch — the old "B-NEW-53.3", pulled BEFORE the flip by Kyle's 2026-07-13 §9.2 resequencing; heads `3aab09d99`+`fe5040d47`, CI 4-green ×2, migration-first deploy. Step-1 decided BOTH+substitution: the six decision-time scalars (avg-volume ZERO scalar consumers — proven by-construction at both lanes' indicator objects → CURRENT volume substituted) + `settled_window_hash` (`swh1:`, settled-only preimage) as typed columns on `signal_eval_provenance`, both lanes fed from the SAME indicators objects the strategies read; **#500** real-DI carry + proxy-bridge DELETE both lanes (honest-absent → kernel's documented default; the two-DI-formulas divergence → **#502**, homed 25-4a, NOT silently reconciled); **#498** sourcePool fed at both refresh sites (regimeStability ruled intentional-deferred → 25-4b). **Step-9 catch:** the hash silently wrote 0/20,588 live rows while unit tests passed — `require()` in the ESM bundle, eaten by the never-throw catch; static-import fix, Langston-approved pre-push; LESSON: the Step-7 LIVE-POPULATE check gates every capture batch, bench green is not sufficient. Langston Step-1/2/4/8 ALL PASS (Step-8 independent: partitioned parent relkind='p'/13 partitions; honest-NULL via the partial-triple test = 0; 3,731 live hashes/120min). #206 post-accrual alert `83afc970` fires 2026-08-03T12Z; crypto-capture-density watch-item on record) **B8.5c ✅ DONE 2026-07-14** (the #503 units fix from Kyle's opens-drop investigation — BOTH standalone VTS kernel calls DELETED (fraction-as-dollars friction; one computation site = the shared decideMakerTaker), five consumers re-pointed, three-regime cohort stamped (CLEAN VTS-EV calibration cohort starts 2026-07-14 w/ #500's DI boundary); head `a8242a3`, CI 4-green, no migration; Langston Step-1/2/4/8 ALL PASS — Step-8 clincher: 938/963 impossibility violations pre-restart vs 0/22 post, split at the deploy second; taker-dominance ruled by-design pFill=0.50) **→ NEXT: B8.5 THE SWITCH-ON (carries #418 net_pnl-population gate + AC1/AC2 + the ON-state enforce funnel render + the B8.5a behavioral gate proofs + #419/#422 + the SEALED no-floors seed consensus + Kyle EXPLICIT-GO precondition), gated on: OLD Claude's shadow-evidence sink ✅ DONE (B-EVIDENCE-SINK, 2026-07-14 — durable tiered `switch_on_shadow_evidence`, deployed+wired 0-rows-pre-flip, Langston Step-1/2/4/8 ALL PASS) + #501 ✅ done (harness run, consensus sealed) + #499 ✅ resolved-as-corrected (rotation done; the premise was a wrong-location read, owned) — #500 + #498 SEEN via B8.5b. **★ ALL FOUR PRE-FLIP GATES + the sink now SEEN — B8.5 is unblocked on the build side; the FLIP HOLDS for Kyle's EXPLICIT go (not gate-completion; recorded constraint 2026-07-14).** | `P19_B8_1_COMPLETION_REPORT.md`, `P19_B8_2_COMPLETION_REPORT.md`, `P19_B8_3_COMPLETION_REPORT.md` |
| ~~P19-B8 (orig)~~ | ~~Shadow-trade layer~~ → **✅ SHIPPED EARLY as `reorg-B4`** | The original "B8 = shadow/learning layer" (19-17) was PULLED FORWARD in the 2026-06-19 reorg and shipped as `reorg-B4` (done). The B8 number is REUSED above for the paper-screens + switch-on batch (Kyle renumber 2026-06-30). | ✅ done | `P19_REORG_B4_COMPLETION_REPORT.md` |
| P19-B9 | Paper trading run + audit | 19-11 + 19-12 (AMR shadow→active ~1 week into the RUN — §5 re-anchor) + **opening task: first-48h capture-completeness audit** (every B5 hook landing rows, esp. paper-engine admit — Langston req., #206 failure class) + #138 first-confluence watch | queued | — |
| P19-B10 | Performance + exit verification | 19-13 + 19-14 (§19.3.5; absorbs exit-protection proving — see §3.4) | queued | — |
| P19-B11 | xStock safety additions | 19-9 (entry-side failure modes) + 19-16 (sector clustering) | queued | — |
| P19-B12 | Diagnostics + internal-health monitoring ➕ **ITEM ADDED 2026-09-11 (`#1041`, owner CC-B): the go-live parity gate and the health snapshot average an execution-timing buffer that has had NO writer since P19-B4b.2 deleted its last caller (2026-06-16)** — the gate can never pass and its latency/slippage checks pass on zeros. Rewire or replace the source. ⛔ **Do not re-encode `fees > 0`: an xStock maker fill books a rebate.** ➕ **ITEM ADDED 2026-09-11 (`#1042`, owner CC-B, Langston's Step-2 ruling on `B-XSTOCK-FEE-CONTRACT`): `calibration_ledger.decision_grade` has no reader and is not rendered** — it read `true` on wrong fee rates from 2026-06-02 and has never discriminated anything. CC-B's call: the column goes, unless a consumer that needs it is found. | §19.6 + §19.6.6 (long-tail spills to Phase 20 §20.4.5) | queued | — |
| P19-B13 | Stock-characteristics data feeds | 19-15 (low urgency — slot flexibly late) | queued | — |
| P19-B14 | Boot Readiness Coordinator | 19-5 / 19.x — **CONDITIONAL** (boot-ordering half only; runtime-monitor half lives in B12 per §19.6.6) | conditional | — |
| **P19-B15** | **Test-in-paper / bypass-in-live capability gating** (Kyle directive 2026-06-17, #322) | **NEW — CONFIRMED Phase-19 slot (Kyle: schedule it in Phase 19; placed LAST as it doesn't pair with any existing batch).** Live + paper share the SAME pipeline except at the final order-placement seam (P19-B2 Option-A), and paper runs CONTINUOUSLY alongside live as the permanent calibration/new-functionality testing ground (the both-modes steady state — #321). So new/experimental functionality must be runnable in PAPER but **BYPASSABLE in LIVE** until proven: a per-capability feature-gate keyed by mode (the same mode-keyed-gate pattern B6.5a introduced for the per-class active gate), so unproven logic never touches live orders. Design with Langston at scope time. Sequenced LAST in Phase 19 (after B14); no dependency on the paper turn-on, but a prerequisite for the Phase-21 live launch. | queued (last) | — |

| **B-FILTER-DIAG-STANDARDIZE** | **All six Filter-Diagnostics tabs standardized (VTS / paper / live x crypto / xStock)** - Kyle-directed after he rejected B-FILTER-DIAG-PAPER's central objective. Same displayed metrics, same organization, on every tab; the ONLY legitimate difference is that the VTS has no SQE (its Net-EV check sits elsewhere in that pipeline). The active path now READS the per-strategy decline taxonomy it was already computing and discarding, so the Paper tables carry real data rather than an honest placeholder. change-class **architecture** (re-declared mid-batch when Kyle's override pulled orchestrator hot-path instrumentation in). | **deployed `cf395a71c` 2026-08-07; Langston Review HOLDING on 3 owed items** | #662 |

| **B-TEC-STATE-DURABILITY** | **Engine trailing-state durability — the silent-zero-states defect (#678) and the rehydrate seed gap (#677).** On restart the engine reads every open position's ladder progress and ratcheted stop from one file. The read, the parse and the load sit inside a single catch that **logs and carries on**, so a corrupt file boots the engine with **zero** trailing progress on every position while it reports itself healthy — measured live **568 states, 347 KB** (`trade-safety.ts:909-922`). The file also lives in `/tmp`, which a reboot can clear. #677 compounds it: two of the three fields that carry stop provenance have no carrier on rehydrate, so **together, one reboot is unrecoverable**. | **PLACED VERY LAST — behind `P19-B15`, Kyle 2026-08-26.** Scoped 2026-08-07 (`8bd129232`); **no code written and the defect is live in the deployed build.** Deliberately last: it is a recovery-path exposure, not an active-trading blocker, and nothing ahead of it in Phase 19 depends on it. | queued (very last) | #678, #677 |

| **B-TOKEN-WATCH** | **Capture-only observation feed over new Solana token launches — births recorded at creation, then observed on a fixed grid, so that at 90 days we hold the survivors AND the far larger population of failures, recorded identically from the same starting line.** The durable output is **case-control survival machinery built where a published answer key exists**, before pointing the same machinery at `#594`/`#596`/`#597`, which are the same statistical problem with no answer key. **No trading, no wallet, no execution, no strategy, no orchestrator contact** — the fence is §0 of the scope. **Hosted on Helsinki, explicitly NOT the trading box.** **OBJ-10 (Kyle 2026-08-28): a tracking page on the staging site** — launched/alive/died aggregates with an aging tracker, plus the oldest 100 survivors — built only after the collector is proven. | ★ **PLACED IN PARALLEL, NOT IN THE QUEUE, and the reason is structural rather than convenient: different host, different owner, different lane. Nothing in Phase 19 depends on it and it blocks nothing** — so a queue position would misstate the dependency. **Positioned alongside the Phase-19 tail, running from now; the 90-day read-out lands well after Phase 19 closes.** Step 1 APPROVED by Langston with three conditions, all discharged (`8c5412f38`); **Step 2 audit + plan complete** (`B_TOKEN_WATCH_PRE_AUDIT.md`), two gates open. | Step 2, awaiting Langston | #920 |

| **B-TOKENWATCH-OBSERVED-AT** | **The timestamp every observation is indexed by is a CALLER'S CLOCK, not the moment of the reading.** A caller reads the clock once and stamps it on every row it writes, so one function call can emit several rows carrying the same instant. **Measured: 5 duplicate (mint, observed_at) pairs in 47,093 rows, one pair holding genuinely different values.** ⛔ **The 0.011% is a LEVEL whose driver is an EVENT RATE** — it scales with discovery lag and sweep lag, so one outage plus a catch-up buys as many collisions as the backlog holds; quoting the level as the exposure would be wrong. ⛔ **And it is a statement about the MEASURAND, not plumbing:** the code claims a token's true age is recoverable from its creation and observation times, but on a catch-up sweep the row is stamped with the run that consumed it rather than the moment it was read — in a study whose measurand is *token state at age checkpoints*. **Cause diagnosed to the line by Langston** (`store.py:284`/`:309-315`, `follow_up.py:130-155`/`:195`, `:185-191`). ⚠️ **TAXONOMY OUTCOME (2) — working-as-designed but unaddressed, so the remedy is a SCOPE DECISION and is not taken unilaterally.** Direction when it is taken: stamp from the actual read and give the row an id, so uniqueness holds by construction rather than by guessing which fields separate rows. | **PLACED IMMEDIATELY AFTER `B-TOKEN-WATCH` AND BEFORE `B-TOKENWATCH-PAIR-SELECT`** — ahead of the pair-selection batch deliberately, because this one touches the age measurand that every other reading is indexed by. The correction read path already defends against a wrong correction being applied, so nothing is urgent; the record itself is what stays unrepaired. Owner CC-INFRA. ⚠️ **No date, per §9.4.** | queued | #986 |
| **B-TOKENWATCH-PAIR-SELECT** | **A freshly-graduated token is observed through its DEAD pool.** The aggregator is asked for a token's pair and returns the one with the most 24-hour volume; on graduation the liquidity moves to a new pool while the day's volume still sits on the old bonding curve, so the pair we observe is the drained one. **Measured 2026-09-02: CERNEY's live pools held $5,703 and $2,991 while the pair returned for it was its emptied curve; EGGS the same at $20.** The on-chain liquidity field is already fixed and now names a drained curve rather than reporting it as zero (pre-registration AMENDMENT 10) — **price, volume and buy/sell counts still come from the dead pool and are unexamined.** ⛔ **Graduation is the study's SECONDARY OUTCOME, so this error lands on the outcome rather than across the sample: a validity problem, not a precision one.** Scope: decide the selection rule (candidate — prefer a pair with a live liquidity figure over one with more stale volume, the account's own state being authoritative over the aggregator's label), then measure the affected share forward. ⚠️ **The share CANNOT be recovered from the store** — only the chosen pair is kept — **so no figure is quoted until a forward measurement runs.** | **PLACED IMMEDIATELY AFTER `B-TOKEN-WATCH` AND BEFORE `B-HELSINKI-MOUNT-WATCH`.** It is a defect in that batch's own instrument, so it belongs against it rather than at the phase tail; it does not block the collector, which keeps recording. Owner CC-INFRA. ⚠️ **No date, per §9.4.** | queued | #983 |
| **B-HELSINKI-MOUNT-WATCH** | **Nothing detects a wedged Google Drive mount on Helsinki.** The remedy is documented in `COMMS_BRIDGE_RUNBOOK.md`; **the detection is not.** Measured 2026-08-28: the mount had been hung for **20+ days** with three file-searches stuck on it in uninterruptible I/O, and it was found **only by accident** because `B-TOKEN-WATCH`'s Step-2 audit happened to need the host's load figure. Its only casual symptom is a load average that reads as plausible. **RE-AIMED 2026-08-28 (Kyle), then REHOMED AS DECIDE-THEN-ACT (Langston, same day) — three parts, order not free: (a) one `CONDUCT.md` line banning whole-filesystem scans, UNCONDITIONAL AND IMMEDIATE, routed to CC-A's rules arc since that file is over cap and mid-rewrite; (b) REMOVE the mount — Kyle's call, blocked on the root-crontab gap; (c) detection survives ONLY if (b) says the mount stays, and then aimed at the CLASS not this mount. ⛔ As first placed this was a watcher for a hazard we may be about to delete, sequenced ahead of the decision that determines whether it should exist.** Original framing: nothing on Helsinki references `/mnt/gdrive` and Kyle confirms nothing is meant to. Scope: remove the mount, then ship a detector aimed at the SYMPTOM that actually hid for 20 days — any process stuck in uninterruptible I/O on that host — which is not specific to this mount and therefore survives its removal. | **PLACED IMMEDIATELY AFTER `B-TOKEN-WATCH`, and the ordering is deliberate: that batch puts a second long-running service on that host**, which doubles what a silent wedge takes down. Owner CC-INFRA. | queued | #921 |

| **B-SSH-KEY-CENSUS** (investigation only) | **Two ungoverned keys can log in as the account that owns the trading application, and the System Impact Map cites a mitigation that covers only a third one.** Measured on staging: three keys authorise `deploy` — one **unlabelled**, one `langston-dt-agent` with **no options at all**, one source-restricted. `#110` is scoped to *"Langston's pubkey"* and does not reach the other two; `langston-dt-agent` appears **once in the whole repo**, inside a pre-audit sentence written the same day. **Nobody is asserting they are orphans — that is the point.** Scope: identify the two fingerprints and their owners. **Investigation only; no removal, no restriction.** | **PLACED AT THE END OF PHASE 19 — Kyle 2026-08-28**, who homed the *remediation* to the cleanup phases and offered Phase-19-end *if the investigation is urgent enough*. **It is: we do not know what can log in as the account that owns the application, Phase 21 turns that box into the live path, and resolving it is an hour.** ⛔ **REMEDIATION IS NOT HERE — it goes to `POST_AUDIT_ROADMAP` §20.4 beside `#110`.** Phase 16 rejected on this roadmap's own precedent (`:1038`): protective/security work is not cleanup. Phase 25 rejected by Kyle. | queued (Phase 19 end) | #924, cross-ref #110 |

| **B-TSC-GUARD-CWD** | **The push guard runs from the shell's working directory, so pushing from a subdirectory fakes the broken-parse alarm and refuses the push.** Measured same-commit/same-minute: from a subdirectory it reported **all 214** baseline triples dropped and REFUSED; from the clone root, **0 drops, exit 0, push succeeded** — on a Python-only change that cannot move a TypeScript count. Ruled out as a parse failure using the baseline file's own recorded control (141 `routes.ts` errors). `check-tsc-baseline.mjs` already fixed this class once for `BASELINE_PATH` (`:52-53`); the same fix was not applied to the tsc run or the `<ROOT>` normalisation at `:117`. ⛔ Its own advice points at `--regen-acknowledged`, which from a subdirectory would bake a mis-keyed reading into the SHARED baseline. | **PLACED AT THE PHASE-19 TAIL beside `B-SSH-KEY-CENSUS`.** Blocks pushes today but has a reliable workaround (push from the clone root), so **not a hotfix**. Owner CC-B, who holds `#680`. | queued (Phase 19 end) | #926, cross-ref #920, #680 |

| **B-REVIEWER-LOOP-AVAILABILITY** | **The fresh-context reviewer loop is mandated in four workflow skills and could not fire in the session those skills govern.** Kyle's standing approval was recorded in the skill text 2026-08-27; the harness still required per-case permission, so it fired **zero times** — four skills asserting a safeguard that could not run, which is the "reads as covered" failure the safeguard exists to prevent, one level up. Unblocked for the current session by Kyle on 2026-08-28 and **immediately productive**: three rounds on `B-TOKEN-WATCH` produced a published-figure retraction, a month-boundary ledger defect, a 198 ms-per-observation cost invisible to every test, and **four documented "fixes" that could be reverted with all 116 checks still green**. ⛔ The permission lives in one conversation, not in anything a cold session loads. | **PLACED AT THE PHASE-19 TAIL, immediately after `B-TSC-GUARD-CWD`** — same class as `#920` and `#926`: an instrument that gives a confident wrong answer, or silently does not run at all. Owner CC-INFRA. ⚠️ **No date, per §9.4.** | queued (Phase 19 end) | #931, cross-ref #920, #926 |

| **B-BURN-THRESHOLD-BASIS** | **The credit-burn alarm's thresholds are fractions of the monthly cap, while the budget is designed to consume 99.3% of that cap** — so a month spending exactly its protected floor and nothing else projects `warning`, and the full planned allocation projects `critical`. The alarm separates *spending* from *not spending*, never *on plan* from *off plan*. ⚠️ Directionally pre-existing, but **r3 is the first version where the never-shed floor ALONE crosses the warn line**: the reserve was re-derived 776,000 → 803,000 and its dependent constant was not re-checked. The suite cannot see it — the flat control runs at 0.24% of cap. **Remedy is a scope decision, not a fix:** raise the fractions · project against the plan pro-rata (recommended) · or alert on the shed transition. | **PLACED AT THE PHASE-19 TAIL beside `#931`, `#920`, `#926`** — same class, an instrument that answers confidently without discriminating. **Does not hold the push:** it does not touch the spend gate, which is independently bounded and injection-tested. Owner CC-INFRA. ⚠️ No date, per §9.4. | queued (Phase 19 end) | #932 |

**Already done before kickoff (pulled forward into the Interphase 24→19 plan):** 19-19 AMR body (✅ 2026-06-12, in shadow; brain = Phase 25) · 19-20 decision-provenance capture (✅ 2026-06-08) · 19.0.C tiered fee model (✅ 2026-06-11) · per-asset-class regime 4.7 (✅ 2026-06-11) · scan-stall 4.6 (✅ 2026-06-12).

**Parked (not Phase 19):** 19-17b `live_engine_enabled` numeric-1 flip note → Phase 21 go-live checklist.

---

## §2 — THE AGREED SEQUENCE (locked with Kyle 2026-06-12; Langston review pending)

Ordering rationale: clean test background first (B1), then the early design decision that shapes paper plumbing (B2), then de-mine the known-broken path (B3), then connect xStock (B4), then the two hard switch-on preconditions (B5 capture, B6 safety net), then the flip with its displays (B7), then the learning multiplier (B8), then the long run that IS the phase (B9), with verification (B10) and xStock safety (B11) proving inside it, diagnostics hardening at the end (B12) to protect the Phase-25 calibration run, B13 slotting wherever convenient late, B14 only if triggered.

Exit criteria (roadmap §3.2 expected outcome): stable, debugged paper-trading system generating real decision data for Phase 25 calibration; diagnostics layer live; all components validated.

---

## §3 — RECONCILIATION RECORD (2026-06-12 kickoff walk — what changed vs the roadmap catalog)

Kyle directive: walk the draft sequence item-by-item against ACTUAL completion state, code-verified, not roadmap-trusted. Findings:

### 3.1 STALE — already done, removed from the plan
- **19-8 xStock real-time pricing — DONE in substance.** The roadmap line ("stale REST polling") is outdated: B74's `equity-spot-archiver.ts` holds a persistent WS to `wss://ws-equities.kraken.com` (ohlc interval-1 + ticker channels → `xstock_spot_ohlc_1m` + `xstock_spot_ticker_snap`); no REST polling in the live flow. **Residual folded into P19-B4:** verify the ingest→DB→scanner-read hop is fast enough for active execution (direct-stream read if not).

### 3.2 RESHAPED — smaller than written
- **19-18 live-mode build approach.** The Item-4 separation (2026-06-10) already cleaved VTS/paper/live into standalone systems with live scaffolding hard-gated 409 until Phase 21. Remaining question is narrower: how much of paper's engine the live build reuses. Still early (B2) because it shapes paper plumbing care.

### 3.3 MERGED (Kyle approved 2026-06-12)
- **19-1 + 19-7 are the same work** → single batch P19-B4. The B79.0n umbrella (16 sub-batches, all CLOSED 2026-05-27) did ALL per-class plumbing but deliberately deferred the final connection (umbrella items #14 WIRE-IN / #16 flip). Code-verified gap: xstock scanner routes only to VTS (`eval-cycle.ts` → `registerOpenVtsTrade`, never RTB); `signal-orchestrator.ts` evaluates the FX5/crypto pool only. P19-B4 contents: scanner→orchestrator dispatch with `assetClass='xstock_spot'` (#92), activate the deferred canary log + outcomeFeedback EMA store (EXECUTION/RTB/ORCHESTRATOR deferrals), RTB Phase-4 `SET NOT NULL` migration (zero-null gate), **xstock 0.50 pattern-pool-cap placeholder validation (HARD pre-flip gate, RUNNING_ISSUES #153)**, pricing-latency check (3.1), B3.2 active-path strategy gates (pending task — config now, calibrate Phase 25).

### 3.4 STALE-ish — built, only proving remains
- **Paper exit protections (draft item 7) — built.** `paper-execution-engine.ts:953` consumes `evaluateTECExit` with per-asset-class config (B79.0n.TEC per-class `break_even_enabled`, trail multipliers, moonbag caps); paper loop independent of VTS. Survives only as the prove-on-real-fills half of P19-B10 (19-14/§19.3.5).

### 3.5 HALF-DONE — reduced scope
- **19-3 data capture (§19.0.5).** EXISTS: SQE rejects (`vts-runner.ts:3755`, xstock `eval-cycle.ts:657`), strategy_internal, admitted hooks (orchestrator:1047 dormant; VTS; xstock). MISSING (= P19-B5): fx5 pre-filter reject rows, RTB TTL/stale rejects (currently silent deletes), proper TCL hook at trading-bootstrap, **paper-engine admit hook (engine writes nothing to `signal_eval_archive` today)**.

### 3.6 CONFIRMED REAL — as written
- **B1:** 59 known stale test failures (~12 files: cost_telemetry, net_expectancy, dynamic_sizing, b73-exit-replay, b72-dbs-routing, b70-run-mode, b79-0m-b2-pattern-filter set…) + TEC.b strict-throw fold-in (~15-20 stale TEC mocks).
- **B3:** #137 = 54 files / 231 Phase-19-tagged errors + routes/storage majority share; baseline `phase_tag` audit itself unfinished (all 66 files still "TBD") → B3 opens with a triage pass. Named landmines code-confirmed: Phase10TradeRecord builder leaves ~13 archive-read fields unset (`vts-runner.ts:1543` vs ~1882-1921); SQESignalInput `ngc`/`riskScore`/`profitRate` read-never-set with the throw swallowed by `.catch()` (`ready_to_buy_service.ts:1595+`). #139 = 9 throwing `resolveAssetClass` sites remain in vts-runner (4 already safe).
- **B6:** confirmed no daily-loss service exists; `tripKillSwitch` manual-only (`guardrail-policy.ts:442`).
- **B8, B9, B10, B11, B13:** stand as cataloged. **B14:** conditional, boot-ordering half only.

---

## §4 — KYLE UI/DISPLAY DIRECTIVES (2026-06-12 — binding; land with **P19-B8** — Kyle renumber 2026-06-30, was "P19-B7")

The trading page's pipeline tabs are rebuilt for PAPER mode (same pattern later for live) — the three systems are now separate, so paper needs its own equivalents of the VTS-tracking tabs:

1. **Filter Diagnostics — crypto** (paper)
2. **Filter Diagnostics — xStock** (separate tab)
3. **Ready-to-Buy queue**
4. **Open trades + Closed trades tables — MIRROR the VTS equivalents** (Kyle 2026-06-12 evening: "pretty much just mirror what we have in the VTS")
5. **Surviving-pairs table — KEEP** (all pairs that passed filters)
6. **Results & outcomes dashboard (NEW — Kyle 2026-06-12 evening):** paper trades nonstop unless intentionally stopped → needs a cumulative results/outcomes view; mine the current dashboard's tables for ideas at design time.
7. **VTS-indicator UI placement** (pending Kyle item) — folds into this same design pass.

Timing decision (Kyle 2026-06-12, renumbered 2026-06-30): tabs/dashboard built **inside P19-B8 with the switch-on** — shells built first vs fixture data, then the flip; they populate the moment trading flips on, doubling as visual verification (§9.3 UI-verify applies). The two filter-diagnostics tabs cover crypto AND xStock.

---

## §5 — DECISION LOG

| 2026-08-28/29 | **⛔ F-G-1 — B-GRID-REPRESENTABILITY: DEPLOYED `56ac8067a`, NOT CLOSED. The batch is in a pre-registered OBSERVATION WINDOW and its record is a PROGRESS REPORT** (Kyle: *“the patch report remains a progress report, and the batch remains open because we're still in the middle of this observation window”*). CI 4/4, Langston Step-4 APPROVED, both Filter Diagnostics tabs UI-verified, board card in `Observation`. **DECISION 1 — THE CRYPTO/xSTOCK ASYMMETRY IS DELIBERATE:** a crypto tick is the venue's own published statement so an absent tick **REFUSES**; an xStock grid is our GCD inference from our own archive so an absent grid **PASSES THROUGH UNROUNDED** — refusing on our own coverage gap would drop valid signals. **DECISION 2 — THE CRITERION IS READ ON INTENT-SIDE COLUMNS ONLY** (`intended_entry_price`/`stop_loss`/`take_profit`): `entry_price` is the FILL and is on the venue grid by construction, so grading it grades Kraken and can never fail. **DECISION 3 — `never_filled` ROWS COUNT** (Langston: they are **not independent of the property under test** — an unfillable price is a plausible signature of an off-grid entry, so excluding them would be selecting on the outcome). **DECISION 4 — KYLE AMENDED THE WINDOW to count xStock opens on their own separate counter**, after he spotted that the original counted crypto only; decoupled per Langston so both legs gain rather than trade off. ★ **All nine amendments were APPENDED, never edited, so the record shows the bar did not move to fit a result** — and the window is armed as self-firing alert `2093a98a` rather than depending on a session staying alive. |


| 2026-08-24 | **★ B-OBSERVATION-EPOCH — CLOSED LATE, and the lateness is the record.** Code `8088b49be` shipped before its governance; the checker's `Governance overdue` and `Missing pre_audit` alerts were **both correct** and are discharged rather than silenced. **DECISION 1 — the reset is a `module_constants` row, NOT a schema change**, because Kyle had already ruled the balance-change and score-reset decoupled and `getLifetimeScoreboard` already implemented that ruling; the anchor stays untouched at 824.11/v4. **DECISION 2 — the straddler rule is STATED, not inherited** (Langston): a trade opened pre-epoch carries a contaminated entry, and close-time keying would have admitted them for days (measured 11 closes / 4 both-leg / 3 of 7 open positions pre-fix). **DECISION 3, the one that cost a follow-on batch — A BATCH INTRODUCING A NEW SHARED VALUE MUST CENSUS ITS CONSUMERS BEFORE WRITING THE FIRST ONE.** No census was run; the predicate went inline; three of four readers never got the rule; and the four tests that pinned the decision tested the FUNCTION rather than the PARITY, so all four passed while the card showed three answers. **DECISION 4 — retroactive governance is written as retroactive.** The pre-audits for this batch, `B-MBIM-SWITCH-ON` and `B-EPOCH-KEYING-PARITY` are all labelled written-after with a table separating analysis done BEFORE the code from analysis done after: **backdating would silence the only independent signal that the step was skipped.** |

| 2026-08-24 | **★★ B-EPOCH-KEYING-PARITY — SHIPPED + DEPLOYED `30808c6c0` (CC-C; Langston APPROVED, five conditions discharged).** **DECISION 1 — A DECIDED RULE GETS ONE HOME, AND "we tested it" IS NOT EVIDENCE WHEN THE RULE HAS COPIES.** Both-leg keying was decided, argued and pinned with four tests, and still reached one reader of four because the predicate was inline; the tests asserted the FUNCTION, not the PARITY. **DECISION 2 — extraction is NECESSARY, a row-level SQL↔TS parity fence is SUFFICIENT** (Langston, citing the shape already built at `b-phantom-fill-reconstruct-fence.test.ts:147`): a second implementation in a second language is not covered by a TS-to-TS assertion. Homed `#900`. **DECISION 3 — the rule having one home does not mean the VALUE does:** SQL resolves the epoch as the explicit row, TS as `explicit ?? first_trade` — a measured 4-row/0.75% divergence, inert today, **propagated by this batch from one reader to three.** Homed `#901`. **DECISION 4 — OBJ-5 reworded rather than defended:** the fence proves the `epoch === null` branch, which production reaches only with zero trades; it does not prove "no-epoch behaviour unchanged." **STANDING METHOD NOTE: Step-7 means the page the batch is about** — this was found only because Kyle redirected me off the Dashboard tab. |

| 2026-08-24 | **★ B-MBIM-SWITCH-ON — SHIPPED + DEPLOYED `afb7d326c` (CC-C, Kyle-directed provenance read; Langston pairwise, r1→r3, APPROVED with three conditions landed in the deployed ref).** **DECISION 1 — the drift branch ships LOG-ONLY, and remediation is NOT flagged-off but HOMED:** a flag defaulting off is config surface on a `non_architecture` batch and a switch someone flips later without re-reading the depth-gate interaction; `triggerSoftResubscribe` is retained, deliberately uncalled, with `#507` carrying a reciprocal rider so a later grep cannot read it as a missed call site. **DECISION 2 — an expectation is PRE-REGISTERED before any monitor's first deploy** (Langston's condition: a monitor finding nothing reads identically to a monitor not running), including the discriminating case — substantial MBIM drift with `crossedDetections` still 0 is the **non-crossing ghost**, which the crossed-detector is structurally blind to. **DECISION 3 — `crossedDetections` is declared NOT COMPARABLE across this deploy**: the checksum mismatch arm `continue`s above the crossed detector, so a zero after is not the same measurement as a zero before. **THE GENERAL RULE ADOPTED: switching a dormant component on introduces every action it takes** — *"pre-existing behaviour"* is not a disposition, and a limb written before a gate existed must be re-asked against today's architecture. Opened as `pre-existing-therefore-fine` in `MISTAKE_PATTERNS.md`. |
| 2026-08-08 | **★★ CRYPTO NET-EV INVESTIGATION (CC-C, Kyle-directed; Langston pairwise, ONE BOUNCE) — the answer changed FOUR times and the final one splits paper crypto in two.** Kyle asked whether crypto's near-universal negative Net EV is *"just the nature of crypto"* or *"incorrect ranges / a calculation designed for a different asset class."* **FINAL MEASUREMENT (canonical predicate `closed_at IS NOT NULL AND close_reason IS DISTINCT FROM 'never_filled'`, n=241): the two admission lanes are nothing alike — `organic` 12 trades / **83.3% wins** / **+6.87% avg** / **+$133.52**, vs `exploration` 229 trades / 45.0% wins / −0.56% avg / **−$144.52**. They near-cancel, which is why the headline read −$11 and looked like nothing was happening.** ⇒ **crypto is NOT uniformly broken: the signals that clear the bar honestly make good money; the ones admitted below it lose.** **★ AND NET EV HAS NO ORDERING POWER BELOW ZERO — exploration WINNERS were admitted at a WORSE median netEV (−0.047) than LOSERS (−0.011).** ⇒ Kyle's *"maybe allow a small negative for crypto"* is **not supported**: it is not that a small negative is acceptable, it is that **the score stops meaning anything once it goes sub-zero.** **FOUR SELF-CORRECTIONS, all caught by measurement or by Langston, recorded because the method is the lesson:** (1) I claimed the FEE was the binding constraint — a zero-fee control showed the signals lose **before fees**; (2) I recommended bigger targets, then **refuted it myself** on the unbiased population; (3) I retracted the pWin-ceiling refusal on a 25-row survivor sample, then **restored it** when the real population showed 35% against a 0.40 floor; (4) **Langston BOUNCED my live numbers — I had averaged over `closed_trades` including at-open and never-filled rows, the exact error MY OWN B-EXPLORATION-ANNEAL-CLOSED-FIX fixed eleven days earlier.** **METHOD RULINGS THAT SURVIVE:** the **VTS cannot answer target-sizing questions** (no SQE, no RTB ranking, and its geometry is ~2× live — Kyle's objection, upheld); the 2026-06-03 bar study **rejected FINER bars, not coarser**, and prices a frequency change as a foundation batch; **maker fill rates are SIMULATED**, so every EV figure netting off unfilled orders inherits that model's optimism. **CARRIED INTO THE BATCH:** nine hardcoded strategy buffers → `module_constants` (Kyle-approved); **volNoise carry-through — the LIVE filter is healthy (`fx5-scanner.ts:1065`, really rejects pairs); only the downstream STAMP is a constant 0.3, so carry the real value rather than delete it (Kyle's ruling, better than my delete proposal)**; crypto-perp feed **gated on rate-limit + storage arithmetic not yet done** (Kraken lists 284 perps, we take 10); hot→warm acceleration first. **OPEN:** stop price recorded on only **49/241 (20%)** crypto closes — the same provenance gap #677 names, and it is what caps any replay at n=25. | CC-C + Langston |
| 2026-08-07 | **★ B-TEC-REGIME-PARAM-REMOVAL CLOSED, and its DECISIONS matter more than its diff.** (1) **Disposition (3) RECONNECT, not (4) REMOVE, for the two `??` reads** — *a behaviour-neutral deletion that erases where the fix goes is not a cleanup*; they stay as #677’s reconnect site pending **Kyle’s scope call** (wiring them changes post-restart exit behaviour ⇒ re-declares as architecture). (2) **#677’s trigger corrected from RESTART to STATE LOSS** — TEC state IS file-persisted and restored (measured 279–283, zero dropped; live-confirmed on the deploy restart itself), which retired a wider claim another session had already acted on. (3) **`/tmp` durability split OUT as #678** rather than ridden on #677 — *coupling would park the higher-severity half behind a Kyle-pending decision*. (4) **Typing the exit parameter split OUT as #676** — it would force a same-commit tsc-baseline sync inside a batch whose verification leans on a minimal provably-neutral diff. | CC-A + Langston |

| 2026-07-29 | **★★ B-COST-MATH-CONSOLIDATION — SHIPPED + DEPLOYED (CC-C, Kyle-directed; Langston pairwise, Step-4 signed off). Report pending §9.3.** Kyle asked for the three copies of the cost arithmetic to be scoped "including looking into the history and historical intent" — **the first batch under the new §2 1.b provenance rule, and the read changed the batch's shape twice.** Shipped one pure `core/math/trade-pnl.ts`, all three copies re-pointed, **bit-identity proven with `toBe` against verbatim transcriptions written BEFORE the re-point**; four broken self-checks repaired; **verified live — zero MISMATCH in 32 min post-deploy while 60 OBSERVED fired**, against 339+17 historically. **★ THE THREE DECISIONS WORTH CARRYING.** **(1) The census was scoped by PATTERN, not QUANTITY** — re-scoped to *"computes **or ASSERTS** a relationship among MONETARY quantities"* it went **3 → 9 sites**; Langston's two fences each found sites mine could not, and **neither closes the census alone.** **(2) A repair can invert a check's polarity** — re-anchoring the P/L reconciliation fixed 17 false alarms **and made it unfalsifiable in the same stroke**, turning always-RED into always-GREEN, the exact failure the batch had flagged an hour earlier; demoted to OBSERVED, real invariant homed **#620**. ⇒ **polarity, not class, decides which direction of a check's proof is load-bearing.** **(3) It ended at the RISK ENVELOPE, not the display** — `getPortfolioBalanceV2` feeds the **daily-loss kill-switch denominator** and `SizingContext`; **Langston voided his own P19-B6 approval**, which had rested on a shrinking denominator the cap and session-scope prevent. ⚠️ **Direction certain, MAGNITUDE UNMEASURED; paper mode, no capital exposed.** Restated as **#618** (retitled to the mechanism, consumers table, risk paths marked). Also closed a governance gap — **`c5-financial-diagnostics.ts` had NO SIM entry at all** despite three checks on the live money path (now **SIM 9.15**, absence recorded as the likely reason they survived). **★ AND THE STANDING CHECK THIS BATCH EARNED: before quoting any two financial quantities together, STATE THE SET EACH IS COMPUTED OVER** — two of the night's wrong numbers were that one mistake on different axes. | CC-C + Langston |
| 2026-07-28 | **★ B-COST-ACCOUNTING-HONESTY CLOSED (CC-C, Kyle-directed; Langston pairwise + CC-A peer check).** Kyle asked for the slippage/negative-cost bug to be fixed; the investigation found **the premise was false** — net P&L was correct on every trade (293/293, incl. all 57 negative-cost rows) — and that the obvious fix would have **broken** it, since the two presentation distortions cancel exactly. Options went to Kyle (rule-24 outcome 2, never unilateral code); he chose gross-off-actual-fills. Shipped: gross on actual fills, cost line = explicit fees only, slippage retained as signed telemetry, % denominator → actual entry value (§9.2, cutover 11:57Z). **METHOD NOTES worth carrying:** (a) the §9.5 census found the formula at **THREE** sites where the scope's first draft said one — a forward trace would have stopped at the engine; (b) Langston found **asymmetric persistence** the census missed, which measurement then showed was latent with **zero** live instances; (c) a shape-independent test guard **disproved my own written carve-out reasoning within a minute**. Industry-grounded (Perold/Harris/Zipline/GIPS/PRIIPs/SEC-605) at Kyle's explicit request. §9.3 homed to a dated task; delay-vs-execution-cost split homed to `B-IMPLEMENTATION-SHORTFALL`. | CC-C + Langston + CC-A |
| 2026-07-28 | **★ B-PROMOTION-RACE-FIX CLOSED (#508 RESOLVED) — CC-C, Langston pairwise.** The orphaned-trade-record hunt (Kyle-driven) ended in a real bucket-1 concurrency defect, not the three causes first proposed: `checkRtbPromotion` carried THREE unlocked triggers so two passes could promote+open one signal, and `createActiveOpenPosition` silently returned the winner's row on the `unique(symbol)` 23505 — stranding the loser's record. Fixed (single-flight latch, `isRunning`-guarded per Langston's Step-4 blocker; discriminated `{position,created}` + caller compensation; in-pass duplicate Set; `deleteClosedTrade` restricted to unclosed rows). Head `46cba2b9c`, CI 4-green, deployed restart#535; 18 tests incl. a mutation-proved source fence; 3 legacy rows deleted Kyle-authorized → **zero true orphans**, balance verified unchanged. **METHOD NOTE (rule 24.a):** three successive causes were each proposed and each killed by Kyle's data challenges — the fix only became correct after the census, not after the story. | CC-C + Langston |
| 2026-07-27 | **★ Two Kyle-scratch-list cleanup batches CLOSED (CC-C; Langston pairwise per §27), deployed together in one window (head `1c63c07da`, CI 4-green, one engine restart, HTTP 200).** (A2) **B-EXPLORATION-LANE-MARKER** — display-only "Lane" column on the paper Open + Closed trade tables surfacing the already-stored `admissionBasis` (amber **EXPL** badge on exploration-lane trades, em-dash otherwise); pure-adapter + the shared tables' default-OFF append props (VTS tabs untouched); **§9.3-verified both tabs** (ONDO EXPL on Open; UNI/LINK/AAVE/USELESS/NEAR/WLD EXPL on Closed). change-class non_architecture. (A4) **B-EXPLORATION-ANNEAL-CLOSED-FIX** — bug-taxonomy bucket-1: the anneal driver `closedExplorationCount` counted `closed_trades` exploration rows without a `closed_at` filter, but `closed_trades` holds an at-open (closed_at NULL) row per position → still-open + orphaned rows over-counted, tightening the exploration floor early; fix adds `AND closed_at IS NOT NULL` + a test poison-guard (measured crypto 191→187: orphans MET/ETH/AVAX + open ONDO); Langston **ruled change-class non_architecture** (restores his 2026-07-15 two-denominator intent, direction is toward *more* exploration). Both surfaced in the Kyle scratch-list working session; A5 (crypto orphan close-path) is next. | CC-C + Langston |
| 2026-07-24 | **★ B-REPO-RELOCATE SUBSTANTIVELY COMPLETE — completion report written; awaiting Kyle's acknowledgement to CLOSE (CC-C; Langston pairwise per §27).** Landed: `CLAUDE.md` §7.1 rewritten (Kyle-confirmed, `e54c5ff7b`) with the ONE-DIRECTION rule + Langston's DR carve-out, a 4-check batch-close sync gate (the 4th catches NEVER-COMMITTED work — CC-A's file hid 07-08→07-23 because the gate only asked whether *committed* work was pushed), and the review-timing contradiction fixed (§2 step 4 / §5 rule 9 said "before push"; he reads at a ref). **Three scope objectives SUPERSEDED by measurement, not drift:** worktrees→independent clones on one shared branch (worktrees force per-session branches ⇒ silent revert at merge); dual-push-URL→separate remotes (a dead backup leg BLOCKS the normal push); and Langston's clone→**no working copy at all** (Kyle: read off the review branch — measured, a bare repo serves reads *and* whole-tree search; only execution needs a checkout, and he never runs tests). **The scope's own open question — "bare mirror or GitHub raw/API?" — is answered BOTH, split by task.** Backup gate is REPRODUCTION not ref-comparison (ref-equality certified an EMPTY backup 4×). `main` renamed + protected. Open: two Kyle-only folder deletions. Report: `Batch Completion/B_REPO_RELOCATE_COMPLETION_REPORT.md`. |

| 2026-07-23 | **★ B-REPO-RELOCATE — D-1 RE-RULED, §6/D-4 DISSOLVED, and D-5 REDEFINED AFTER IT GAVE A FALSE PASS (CC-C + Langston; Kyle-authorized, pairwise by his instruction).** **(1) D-1 overturned — separate CLONES on the shared review branch, not worktrees on per-session branches.** Kyle found the cost neither CC-C nor Langston had weighed: per-session branches mean the failure mode is not a visible conflict but a **silent revert at merge** off a stale edit, and they actively HIDE staleness (your own branch is never behind itself). On a shared branch **git REJECTS a push from a clone that is behind**, making sync structural rather than remembered (#568 / board-honesty principle); index isolation, the whole reason worktrees were endorsed for #557, is preserved. Built: 4 independent clones, 442 pkgs / 0 zero-byte each (vs the Drive tree at **405 of 452** zero-byte — #567 resolved by relocation). **(2) §6 + the D-4 gate DISSOLVE, not satisfied** — research confirmed no supported migration of conversation history for a moved project, and that storage-folder junctions resolve to their physical path (documented split/orphan risk); 4 junctions created then REMOVED, store verified intact (101 transcripts / 4 memory files / 3.9 GB). Since read/write is path-agnostic the benefit was always *working in* the tree, never *opening* there ⇒ **no session relocates.** **(3) ★ THE HEADLINE — D-5 ref-equality CERTIFIED AN EMPTY BACKUP FOUR TIMES.** The Drive mirror's initial push reported success and `ls-remote` returned the right sha, so the explicit-ref check PASSED ×4 — while the repo held **2.7 MB, a `.idx` and `.rev` but NO `.pack` file at all**, `cat-file` unable to resolve the ref, `fsck` = `invalid sha1 pointer`. A ref is a 41-byte pointer; FUSE wrote *that* correctly. **The gate compared the one artifact that survived and was structurally blind to whether the objects existed — #568 inside the control built to prevent silent divergence. The next push failing is what caught it, not the gate.** **All four PASS results WITHDRAWN in writing** (plan §13.5) rather than quietly re-run. **Mechanism → a single `git bundle` file** (one sequential write is what the mount survives) — but Langston's caveat is binding: *the bundle only lowers the odds, it is not the reason to trust the backup; treating it as such would be a patch.* **Gate → REPRODUCTION, not verification:** `bundle verify` proves internal consistency, NOT byte-identity to source, so ref-equality + verify are cheap pre-checks only; PASS is earned solely by cloning from the backup and matching a known path's object hash to source. First earned PASS: reproduced commit `845b40f65b41` + blob `21cd9ce24e5a` @ 25,007 bytes, identical to source. **(4) Per-clone "doesn't carry" sweep** (Langston: the identity bug generalizes) — git identity (set *locally* in the Drive repo, global empty ⇒ first commit in a new clone failed outright), dual-remote push URLs, `http.postBuffer` 500 MB, `core.pager`, and untracked `.claude/launch.json`: all fixed in 4 clones. `core.hookspath` deliberately NOT replicated (absolute Drive path; no active git hooks — the real guards are the tracked `.claude/hooks/`). **Rule 28 added to CLAUDE.md** (no cross-session play-by-play in Kyle's chat). Plan §13 is authoritative; §3/§6/D-1/D-4 banner-marked SUPERSEDED. | Kyle + CC-C + Langston |

| 2026-07-21 | **★ B-REGIME-REFRESH-PIPE CLOSED (CC-A + Langston; Kyle-delegated to completion, URGENT) — the RTB refresh now computes its OWN live regime input for queued pairs; the ~6-month-dead regime gate is finally live.** Root cause (code + provenance): the refresh passively read the MCE survivor-cache, but the scanner EXCLUDES queued/traded pairs from the survivor set → their regime cache went cold → 54/55 refreshed signals rejected; the refresh's regime inputs had been unfinished placeholder constants (0.015/0.5, data-filler never wired) since it predated the MCE. FIX (Langston-ruled A1): a NEW PURE MCE method `computeRegimeInputsOnly` calling `calculatePairRegime` directly, carrying queue-time DBS (regimeWeight-safe), with ZERO of `computeContext`'s five side-effects. **LIVE-VERIFIED both classes:** rejections 58→0, 0 compute-misses, 33 distinct regimeWeight values, xStock computing fresh (OKTA 0.49/ADBE 0.65/DDOG 0.47/PYPL 0.46). Two owned misses (an over-strict sparse-bars guard → `adxPeriod+1` hotfix; a #441 premature conclusion caught by a diagnostic + warm re-measure). #552 RESOLVED; #441 falsified as a blocker here (its 60m-snapshot stall stays homed to B-XSTOCK-FRESHNESS-MONITOR). Commits `86d39e00d`+`c4010f538`+`eaf0d98cf`, CI green; SIM + System Manual content-updated. | Kyle + CC-A + Langston |

| 2026-07-17/18 | **★ P19-B8.7 Step-9 FINAL PIECE SHIPPED + the #530 pattern-lane revival (CC-B; head `a8dc548de`, CI 4-green, deployed 13:31Z, engine self-resumed #502).** The paper Open/Closed tabs now MOUNT the shared VTS tables through the pure 13-test adapter (Langston ruling B, both conditions verified in his GO) — layout identity + the frozen symbol/header Kyle asked for land by construction; `active-trades-v2.tsx` rule-18 DELETED (#528 executed; the hardcoded 0.10%/0.15% client P/L recompute quoted verbatim — rule-23 find 1). Cost 5-col split everywhere + friction-COMPONENT capture at VTS open (no backfill, Langston-ratified) incl. xstock (#527 discharged in-push). **Rule-23 find 2:** xstock lane's hardcoded crypto spread → `costMetrics.spread` (measured; VTS friction rises ≥2bps, admission tightens; SysManual Ch5). **THE HEADLINE — Kyle's morning missing-DBS question + his ordered review found the pattern-pool signal lane silently DEAD since B63:** intake dropped the scanner's DBS → MCE hard contract threw → per-symbol catch swallowed it (592 violations 07-17 alone; ZERO post-deploy — lane REVIVED, admission gates unchanged, VTS clean; #530 RESOLVED; catch-site audit → #522). Kyle RTB round shipped same push (header/cell alignment, S.Wgt removed → #529 its own batch BEFORE #522, Duration added). Langston Step-4 GO after a 3-round citation-discipline iteration (2 stale-paste blockers owned, resolved via object hashes — the process caught its author twice and was right both times); Step-8 runtime half PASS (independent at-ref); governance half = this commit. **Kyle decisions banked: #529 sequenced immediately BEFORE the #522 audit (not inside it); the pattern/quant per-path filter architecture re-verified in code at his order.** | Kyle + CC-B + Langston + CC-A |

| 2026-07-20 | **★ #531 xSTOCK WEEKEND POSTURE — CONSENSUS LOCKED + 4 BATCHES HOMED (Kyle delegated to crew; CC-B + Langston + CC-A unanimous).** PRIMARY = calendar entry-cutoff (no admission inside a fixed window N before a scheduled closure; NO per-signal hold prediction — CC-B's first wording smuggled one and was caught). N interim from VENUE STRUCTURE, never from today's hold data (the #543 gate is inert ⇒ contaminated); real N = governed ONE-SHOT later, never a recalibration job. Tail-handler SPLIT + evidence-gated on measured reopen-gap distribution for the actual names held. Inoperative-stop-across-a-closure ruled NOT a defect (inherent) ⇒ risk-posture choice, not a bug hunt. **BATCHES: (1) ship-now `B-XSTOCK-WEEKEND-POSTURE` incl. #526 folded in (causally coupled); (2) tail-handler fast-follow; (3) post-#543 N-set; (4) fix D split (Langston-approved — disjoint code + unsettled scope).** | CC-B + Langston + CC-A |
| 2026-07-19 | **★ P19-B8.12 CLOSED (CC-B) — pool-marking carry (the 3rd transit-drop) + dashboard hover icons + window chip.** Kyle's challenge to B8.11 was CORRECT: the scanner stamps every pair `ideal|rotational`; it was dropped at the activeFilterPool intake. Carried end-to-end; B8.11's `hidePoolColumn` DELETED per rule 18. Landing commit `d090178d6` (swept — Langston stands-as-shipped, both conditions discharged: 18-line proof + #539/#540 homes). Deployed at head `bfb365fa7`, CI 4-green, §9.3 all four legs verified incl. behavioural chip test + pre/post-deploy pool carry proof. **OBSERVATION (not a finding, rule 24): 5/5 post-deploy signals `rotational`, 0 `ideal` — evidence bar set at a full weekday re-sample before it earns a home.** | CC-B + Langston |
| 2026-07-19 | **★ P19-B8.11 CLOSED (CC-B, same-day) — dashboard rolling earnings + Pool (I/R) off the paper tabs.** Earnings CALENDAR→ROLLING (rule-18 rename, honest fields, §5.13-aligned); hidePoolColumn default-OFF prop (VTS byte-identical, live-verified). Langston Step-4 GO; head `a561ce5eb`, CI 4-green, deployed + §9.3 walked. Rode with the Kyle metric-explanations round (incl. negative ranking score = live-mode structural blocker, twice over). | CC-B + Langston |
| 2026-07-18 | **★ P19-B8.10 CLOSED (CC-B) — trade-table truthfulness: the blank columns captured at genesis, the Rank leak fixed, the Phase-8 metrics stack purged.** Kyle's blank-column list (regime/glbl-context/pattern-name/liquidity) = ONE cause: the active path never captured them at signal birth (VTS always did). Genesis capture now stamps all of them from the SAME shared helpers (per-pipe SizingContext carriers; pattern pass re-stamps; xStock threads; pairFriction formula extracted+shared) — live-proven on the first post-deploy signal (AAVE/USD: regime + INSIDE_BAR + frictions + both DBS + liquidity). Rank cell had shown the RETIRED finalScore via a hidden queue-enrich fallback — removed; open trades now show the promote-frozen B7.1 R-multiple under the distinct "Promote R" header; dead getTopSignal/checkForPromotion deleted (June park discharged). ExecutionMetricsPanel + SLAL fully purged (generateSignalId relocated + format-pinned). RTB freeze panes; Slot to position 2; "pending"→em-dash; Regime Wt off closed (0.5-constant → #529 rider). Langston Step-4 r2 APPROVED (his archive-integrity catch on record); head `8b13fe0b8`, CI 4-green, deployed, §9.3 Chrome pass. Entry-slip = OLD Claude. | CC-B + Langston |
| 2026-07-17 | **★ P19-B8.9 (parent) SERVER-HALF DEPLOYED (CC-A) — venue-only AT-SOURCE.** The display price chain now matches the execution venue: the Binance + CoinGecko fetchers are DELETED (`fetchLivePrice` = Kraken-REST-or-nothing; source unions dropped `'binance'`/`'coingecko'`), so the UI can no longer show a third-party number for a position Kraken prices+exits. xstock symbols skip the structurally-doomed Kraken-REST ask (no carrier quotes tokenized equities) and answer venue-quiet — LIVE-CONFIRMED firing on BSX + UBER (`XSTOCK_REST_GATE rest_ask=skipped serving=last_known_good`). Reconciled Langston's OBJ-5 item-2 drift onto ONE server-side `priceVenueQuiet` boolean shipped to BOTH Open Trades + RTB payloads (age-blind client helper removed). Fifth broadcast-ternary mislabel fixed (same B8.9a family). 5 drifted restFallbackSources lists → 1 predicate. Langston Step-4: OBJ-1/fold/OBJ-2 APPROVED + pushed `b28cf7074`, CI 4-green `29582668953`, deployed HTTP-200, zero third-party fetches. **OPEN sliver:** the venue-quiet DISPLAY CELL renders via CC-B's B8.7 shared-table rewire (carry obligation) → §9.3 UI walk + Langston OBJ-5 sign-off close it once CC-B's push lands. SIM §2.1.2 + SysManual §3.5 content landed. | CC-A + Langston + CC-B |

| 2026-07-16 | **★ P19-B8.9a CLOSED (CC-A) — source-tag honesty: FOUR mislabels + one stale-price safety hole, one micro-diff.** The "one label fix" became four: the REST-as-WS cache badge (load-bearing as a call-saver — fixed with the venue PREDICATE so honesty doesn't raise REST volume), the equities-feed ticks wearing the crypto tag (union extended, CC-B-blessed), the lying method name (updateFromWebSocket→updateCache), and a ternary inside it stamping everything else 'binance' (caught by the batch's own test). Plus: the last-resort stale re-serve kept its venue tag on REST failure — a stale price could pass the actionable gate in the exact dark-venue case the skip-rail exists for; now honestly last_known_good → skip-rail engages. Langston Step-1/amendments/Step-4 APPROVED (independently verified at ref); 28/28 tests; deployed 21:05Z; §9.3 walk PASS (Active Trades (15), cap full at $145); corrected baseline: REST avg 14.7/min peak 0.28/sec (was 23/46, 0.77/sec). FIX-ON-FIND (rule 23) working as designed. Parent B8.9 retirement cuts next. | CC-A + Langston + CC-B |

| 2026-07-16 | **★ P19-B8.5d — sizing tune-3 (CC-A; Kyle-specced 100%/6.67%/$2,250 → ~$145.58 buffered, ratified) — the $209 mystery SOLVED + applied.** Root cause: the per-trade CLAMP binds (exposure%×position%×buffer on portfolioValue), risk% never engages — tune-1/2 turned the wrong lever, and tune-2 accidentally RAISED new sizing to $347.84. Second mechanism: rtb rows carry queue-time sizing (promotion opens at stored size) → one-time paper clearQueue (5 flushed). Applied: guardrails 6.67% + pattern_max_position_pct 0.0667 both classes + $2,250 anchor (v3). PROVEN live: fresh signals size $144.52 (= live $2,233.79 portfolio × 6.67% × 0.97); §9.3 UI walk confirms $2,250 strip + Open Trades tab rendering 11 rows incl. pending-maker (CC-B's d1742f0a1 crash fix verified live same walk). Langston Step-1 PROCEED + rev-2 APPROVED + (a)-(d) PROCEED. `P19_B8_5_SIZING_TUNE3_COMPLETION.md`. | Kyle + CC-A + Langston |

| 2026-07-14 | **★ B-EVIDENCE-SINK CLOSED — the last pre-flip build gate on CC-A's side is SEEN; the durable home for the switch-on behavioral proofs.** The three B8.5 switch-on proofs (FINALSCORE_SHADOW verdict / EV_REJECT open-stage backstop / maker-taker pick+haircut) now write to a dedicated durable tiered store `switch_on_shadow_evidence` (partitioned, 22 superset-nullable-by-type cols, 90d hot, tiered via b75+b70), so they survive the weeks-long paper-validation window instead of aging out of the now-rotating (#499) stdout. Fire-and-forget through `enqueueArchiveRow` inside a degrade-never-throw guard (Flag-A isolation, bench-proven: a throwing enqueue → `.not.toThrow()`). §9.1: DEPLOYED+WIRED, **0 rows pre-flip (correct)** — emitters are on the ACTIVE path; live proof rows are a named B8.5 switch-on obligation, not this batch's claim. Langston Step-1/2/4/8 ALL PASS (Step-8 independent: 13 partitions / 22 cols / all 5 named cols / retention=90 / 0 rows / registration wired / tiering+partition members genuine). Review-caught: asymmetric asset_class NOT-NULL guard (fixed uniformly `?? 'unknown'`); Step-7 self-caught the retention-key-alone-doesn't-tier gap (hardcoded sweep/partition lists — table added to both). **★ ALL FOUR PRE-FLIP GATES (#499/#501/#500/#498) + the evidence sink now SEEN; B8.5 THE SWITCH-ON is build-unblocked. The FLIP HOLDS for Kyle's EXPLICIT go — a recorded standing constraint, NOT gate-completion.** | Kyle + CC-A + Langston |

| Date | Decision | Source |
|---|---|---|
| 2026-06-12 | Plan doc created as running Tier-1 (during Phase 19) governance doc; CLAUDE.md §3 tier list updated | Kyle directive |
| 2026-06-12 | Merge 19-1 + 19-7 into P19-B4; retire draft item 7 into P19-B10; 19-8 marked done-in-substance with latency residual | Kyle approved CC reconciliation |
| 2026-06-12 | Paper pipeline tabs + results dashboard ride with P19-B7 (switch-on) | Kyle |
| 2026-06-12 | Open/Closed paper trade tabs mirror VTS equivalents; results dashboard sourced from current dashboard ideas | Kyle |
| 2026-06-12 | Loss-budget kill switch (19-4, roadmap-optional) WILL be built pre-flip | Kyle ("cheap insurance") |
| 2026-06-12 | First batch = P19-B1 test-suite cleanup | Kyle |
| 2026-06-12 | Langston review: APPROVE as ordered + 4 required additions (disposition sweep §7, Kraken paper-order smoke, B9 first-48h capture audit, AMR re-anchor below) + B7a/B7b split + staged per-class flip + B6 force-trip test — ALL ACCEPTED by CC, folded same-day | CC ↔ Langston consensus |
| 2026-06-12 | **AMR shadow-window RE-ANCHOR ratified:** roadmap 19-19 says shadow "first ~5-7 days of Phase 19"; correct reading = first ~week **of the paper-active RUN** (B9). Shadow against VTS-only flow proves nothing about the dials; the debug-confounder concern applies to the active run. Plan reading is canonical; recorded so roadmap/plan divergence is a decision, not drift | Langston (his original amendment) + CC |
| 2026-06-12 | **B11-timing contamination guard:** entry-side halt/earnings guards land AFTER xstock paper trading starts → either pull a minimal halt-check into B4 or tag all pre-B11 xstock trades for Phase-25 exclusion. **DECIDE AT B4 SCOPE**; record outcome here | Langston flag |
| 2026-06-13 | **#80 exit-strategy ablation xstock extension → Phase 25** (needs real fills; calibration-flavored) | **Kyle decision** |
| 2026-06-13 | **Phase-19 working-style directive:** every pre-implementation audit = thorough CODE-LEVEL audit; SIM dug through for upstream + downstream impacts of every touched component + System Manual consulted; NO dismissiveness toward small errors — certainty about the issue before any deletion or cut; CC + Langston iterate autonomously per batch until verified complete AND verified correct, escalate to Kyle only on no-consensus | **Kyle directive** (P19-B1 turn) |
| 2026-06-13 | Bench DB: **Docker Desktop install APPROVED + executed** (winget, pgvector/pg17 compose to follow in P19-B1 Bucket A); reboot timing Kyle's call | Kyle |
| 2026-06-13 | **#94 macro-modifier HOMED (Kyle confirmed):** build = Phase-25 item 25-7; capture precondition = P19-B5. Recorded in roadmap 25-7 + B5 row above. (CC recommendation accepted over Langston's earlier B11 lean.) | Kyle |
| 2026-06-13 | **#80 exit-strategy ablation xstock extension → Phase 25** (Kyle confirmed 2026-06-13; already in §7) | Kyle |
| 2026-06-13 | **NEW STANDING RULE (Kyle directive): surfaced-issue scheduling** — every agreed "fix later" gets a concrete named home (batch / roadmap phase+item / dated task) AT the moment of agreement; vague deferral not acceptable. Written into CLAUDE.md §9.4 + Langston's CLAUDE.md. | Kyle |
| 2026-06-13 | **#226 home DECIDED → Phase 20.3.1** (CC+Langston consensus, Kyle-delegated): test-tier DB-isolation re-plumbs every test → don't swap the foundation mid-debug; parity bench's always-on DB covers the interim. Optional ≤20min CI write-time guard against new unmocked-DB unit tests. Written to roadmap §20.3.1 + RUNNING_ISSUES #226. | CC+Langston |
| 2026-06-13 | **Langston model: Fable 5 retired (no access) → Opus 4.8 [1m]** — bridge flipped + verified, CLAUDE.md §6/§8 + dispatch snippet updated, rollback backup `*.pre-opus48-backup-20260613` | CC (Fable retirement) |
| 2026-06-13 | **P19-B1 CLOSED.** Headline: the "59/12 pre-existing failures" story was FALSE — CI was already zero-tolerance green; all bench red was environment + 2 latent bugs (db-migrate Windows path doubling; regime-scan stateful-regex guard weakener — both fixed). TEC.b strict restore SHIPPED with blast radius MEASURED (exactly the predicted +50) before repair; obsolete defaults-backfill test REWRITTEN to lock the strict contract; 7 parked skips DELETED with replacement coverage verified first; #141 closed, #226 opened (unit/integration tier separation — Langston cond-2 + notes A/B). Mock-mechanism decision: cache seeder (real resolver exercised; sibling vi.mock pattern proven non-viable). All four Langston gates clean (Step-1 ACK, Step-2 PROCEED, Step-4 APPROVE, Step-8 CONFIRMED) | CC + Langston (Kyle autonomy directive) |
| 2026-06-13 | **P19-B2 Option A RATIFIED — live reuses the paper engine by extension, NOT a separate build.** The active engine (`PaperExecutionEngine`) is already mode-parametric (`mode:'live'\|'paper'` threaded throughout); only 2 order-placement seams (open `:2196`, close `:1104`) diverge. A separate live engine would fork unproven copies of the exit/RTB/sizing/monitoring code Phase 19 exists to harden (NO-PATCHES violation). Isolation already provided by the 409 gate + the seam boundary + per-mode storage partition. | **Kyle sign-off** + CC + Langston consensus |
| 2026-06-13 | **Divergence surface reframed (Langston): 2 order seams + 2 shared-path invariants** that must stay live-swappable through B3–B6: (i) fill-confirmation lifecycle (paper's synchronous atomic fill vs live's async/partial/rejectable — #1 named seam), (ii) balance/equity source for sizing (paper-simulated balance vs real Kraken equity). | CC + Langston |
| 2026-06-13 | **`OrderPlacer` execution port homed to P19-B3 FIRST deliverable** (Langston Q3) — typed open/close port with a **partial/delayed/rejected-capable fill-result type from day one**; P19-B2 stays code-free, port is B3 code. | CC + Langston (§9.4 home-at-agreement) |
| 2026-06-13 | **PAPER EXECUTION TARGET DECIDED (Kyle): Kraken-vetted high-fidelity internal fill.** EXHAUSTIVELY verified Kraken has NO spot paper-fill system for ordinary users — futures-only demo (`demo-futures`); spot `validate=true` is validation-only (no fill); institutional spot test-env is onboarding-gated + unconfirmed-to-fill; even Kraken's own March-2026 CLI does spot paper LOCALLY. Kyle's Sept/Oct-2025 memory = the futures demo (heavy 2025 derivatives marketing). DECISION: keep internal fill but route every paper order through Kraken `validate=true` (real-venue vetting) + real prices/fees + honest friction (Langston: real fees + L2-depth slippage + partial fills — the #1 fidelity must, else paper EV runs hot). Institutional onboarding = **non-blocking RUNNING_ISSUES inquiry**, NOT a gate. **Governance rule 20 wording corrected** (paper→"Kraken paper order system" is futures-only; spot is Kraken-vetted internal fill). | **Kyle** (pre-authorized: "if the investigation confirms, go with the recommendation") + CC + Langston |
| 2026-06-13 | **Paper/live SIMULTANEITY reqs (Kyle): paper always-on (full pipeline, unlike VTS) running ALONGSIDE live once live is fixed; per-mode labels; each mode its own page (filter-diag, RTB pool, surviving-pairs, open/closed tables); no mutual interference; manageable strain.** Does NOT reopen Option A. Surfaces (Langston): paper needs its OWN rate-limit lane (validate traffic can't throttle live orders) + a shared-singleton SPLIT-BRAIN audit (RTB cooldown/portfolio-heat sharing is the worst leak) — both homed to B4, audit must pass before any Phase-21 co-run (strain re-eval stays Phase 21). | **Kyle** + CC + Langston |
| 2026-06-13 | **Legacy `live-trading-service` stub DELETED now (Kyle directive + Langston Step-4 APPROVE).** 593-line removal (stub + 4 routes + dead approval branch + harness scenario + stale archiver source-type); verified tsc+vitest green, modern gated live path + UI untouched, zero client refs. Gate #8 (§6) RESOLVED by physical deletion. **NEW STANDING POLICY (Kyle, CLAUDE.md both files): never leave legacy lingering** — on discovery, discuss+delete on the spot OR schedule a concrete dated deletion; maintain `DELETED_COMPONENTS_LOG.md` + `_archive/deleted-code/`. Supersedes rule-18 "mark, don't delete in-flight → Phase 16 sweep". | **Kyle directive** + CC + Langston |
| 2026-06-13 | **P19-B3 Step-1 ACK'd by Langston; SUB-BATCHED B3a/B3b.** B3a = OrderPlacer port (`execution/order-placer.ts` + `execution/types.ts` for the `FillResult` union/port iface) + #139 classify hardening; B3b = #137 triage (Step-2 pre-audit, Langston-gated split) → active-path fixes. Key consensus: (a) triage is pre-audit work not code; (b) #139 alarm cuts on **active-vs-passive** (WARN+counter passive, **system-alert** active/live — bare WARN = silent skip, forbidden); (c) #137 tag = "reachable on active path INCLUDING shared-with-VTS modules"; paper-mode-control UI → B6 home; (d) L2 durable fix = align `SQESignalInput` type AND fix the swallowing `.catch()` (rule-11). | CC + Langston (Step-1 consensus) |
| 2026-06-14 | **P19-B3b split-gate + Step-4 decisions (Langston APPROVE both).** (a) Landmine-#2 ngc: go **3b** (read `confidence`, retire `ngc`) — and during impl found `rtb_signals` has NO ngc column → the write was dead (suppressed TS2353) → REMOVED entirely (cleaner 3b; nothing SELECTs it). (b) Swallowing `.catch()` → **observable counter** (`recordQueueFailure`/`getQueueFailureStats`), not just a louder log (rules 10/11). (c) Landmine-#1 = **substrate subset** (assetClass + netEV); 18 VTS telemetry errors homed to a later VTS cleanup. (d) `netEV` revival reactivates a dead Net-EV floor → pre-go-live threshold verify (#232); `driftScore/volZ` explicit defaults (no real source on the active path) → pre-go-live verify (#233) — both wake in Phase 19. (e) **orchestrator:1051** active-signal ablation needs an integer signalId (string SLAL id today) → refusing to force a string into an integer FK is the correct rule-15 call → homed #231→B4 (stays within baseline TS2322:1). (f) `lockedByUser` is a LIVE jsonb column (NOT dropped legacy as first-spec'd) → real fix = null-guard (Langston "best catch" — trace-every-caller before cutting). The ~390 off-path errors homed to named future batches (#234). | CC + Langston (split-gate Q1–Q5 + Step-4 APPROVE) |
| 2026-06-15 | **Crypto active-pipeline validation made EXPLICIT (Kyle directive) → new dedicated batch P19-B6.5 (CC+Langston consensus, #235).** Crypto end-to-end was only IMPLICITLY covered (B3 shared-path de-mining — landmine-2 would have dropped every crypto signal at turn-on; B4-C2 reuses the shared crypto pipeline; B7b staged crypto-first; B9 run). Now an OWNED step: accretion-delta audit + crypto-only FULL-closed-trade-lifecycle dry-run + fill-path parity, with **B7b hard-gated on B6.5-green** (§6 gate 10). Langston: separate proof/repair (B6.5) from the flip (B7b) so repair can't balloon and pressure an early "good enough" flip (§11). | Kyle + CC + Langston |
| 2026-06-15 | **P19-B4b D5 — paper/live SPLIT-BRAIN ISOLATION shipped (S1 + liveness + S4 + S6); S3 deferred.** The Phase-21 co-run precondition. **S1** portfolio-manager cluster (holds the heat ceilings) single-slot → `Map<mode,Manager>` via mode-aware accessors; **liveness** consolidated to the DB `isEngineActive` SSOT with H1 (write-then-broadcast ON COMMIT — kills the `setTimeout(,0)` deferred-write that was the 5-reader divergence root) + H2 (settling-guarded per-mode invariant-check emitting an observable **`LIVENESS_SPLIT` witness**); **S4** risk-concentration → `Map<mode,Map>` (required mode threaded, no silent default); **S6** RTB refresh latch key mode-prefixed. Vestigial busy-flag/operation-lock mechanism REMOVED (rule-18; DELETED_COMPONENTS_LOG). Bench: tsc no-regression + vitest 1952/1952 (incl 7 new isolation/witness tests). **★NUMBERED HARD-BLOCKER for Phase-21 (paper+live co-run): the `LIVENESS_SPLIT` witness counter MUST read 0 for every key during a co-run dry-run before the flip — `tradingStateSync.getLivenessSplitStats()`. Any non-zero entry blocks the co-run.** **★C3 AMENDED (R2, Langston AGREE):** the paper rate-limit lane is keyed by **CREDENTIAL IDENTITY (API-key-derived), NOT `${userId}:${mode}`** — paper+live share ONE Kraken key so the account-wide lockout is correctly shared; mode-keying would deepen a real lockout. **★S3 DEFERRED out of D5** (kraken.ts is a 🔒 LOCKED module + S3 is a fragmentation fix not a hard blocker) → named follow-up batch carrying the locked-module directive (RUNNING_ISSUES #296). R1 paper-default caveat for the routed #297 dormant sites recorded. S13/S8 left shared (S13 witnessed by H2 + VTS only needs any-active; S8 a load-knob shadowed per-mode for decisions). | Kyle (autonomy directive) + CC + Langston (Step-2 PROCEED + Step-4) |
| 2026-06-14 | **P19-B4a CLOSED — xStock active wire-in + feed-safety (DORMANT til B7b).** Kyle pivot **STAMP-AT-SOURCE** (asset class stamped at the pipe entry, never re-derived from symbol — the 17 collision tickers are wrong-by-construction under resolve-from-symbol; Langston independently re-derived the same fix). Langston Step-4 APPROVE-W-COND; **3 decisions RATIFIED:** (a) **C5 strategy gate DEFAULT-OPEN** — explicit-allowlist would black out all crypto until a seed migration; the resolver throwing on cold cache is the real fail-hard → crypto_spot seed + a B7b non-empty-assertion homed **#238**; (b) **C7 RTB `SET NOT NULL` DEFERRED** to B7b post-activation real soak (**#237** — nothing writes rtb_signals while dormant → vacuous soak; the C1 resolver-backed write is the shipped precondition); (c) **C8 xStock pattern cap 0.50→0.15** (crypto-aligned interim — xStock is less liquid so its concentration cap must be ≤ crypto's, not 3.3× higher; final per-class calibration stays Phase-25/#153). C3 liquid-fill-window = US RTH as a fill-QUALITY liquidity gate (token still 24/5 for scan+VTS — Kyle morning-confirm). Holiday/half-day hole in the clock-only window → **NEW batch P19-B6.6** price-discovery-liveness gate (**#236**, hard-gates B7b). C5 disposed the hardcoded `enabledStrategies` allowlist + re-pointed the reb-2-12F source-text-regex diagnostic (fragile coupling removed). | Kyle + CC + Langston |
| 2026-06-16 | **P19-B4b.1 CLOSED — depth-walked paper fill + partial-open + the 24/5 book-depth-sufficiency gate (#295 CLOSED).** The active paper fill REPLACES the flat 0.05% slippage with an honest book-walk (crypto: live WS book depth=10; xStock: top-of-book `askDepthUsd`). OPEN may `partial` → the engine sizes the position down to filled qty (single-point `quantity` reassign; partial-open exposure split-brain closed). CLOSE always full-fills (R2 — never a stuck position) with a DB-resolved `beyond_depth_penalty_bps` (no magic %). The B4a-C3 RTH liquid-fill-window CLOCK is RETIRED as a fill gate (#295) → replaced by `_evaluateOpenDepthGate` (per-class, DB-resolved `fill_depth_gate`, fail-closed); the clock predicate + keys RETAINED for the silent-stall watchdog (verify-before-cut catch — the archiver uses them). **★R1:** xStock gets a real top-of-book depth gate (not freshness-only) — strictly better than the Step-1 scope. **★R2:** closes never partial → Langston's partial-close double-count hold N/A by design. RNG-free (Box-Muller micro-move dropped). **★D4 (validate-round-trip) + the credential rate-limit lane DEFERRED to #296** (locked `kraken.ts`); paper fill is NOT yet venue-vetted until #296 (§9.1). **★#300 / P19-B4b.2** (dead paper-fill machinery sweep) homed as a numbered §1 row before B7b. **★B7b gate-#13** written (§6 — per-class fill-quality gate required: crypto depth + xStock depth + #236 liveness). Commit `b74526dc3`, CI `27586477175` all-4-green, deployed restart#393 (8 `fill_depth_gate` rows + 2 watchdog keys verified), bench tsc-no-regression + vitest 1979. Langston Step-1/2/4 APPROVE; SIM §9.14 + System Manual §7 content updated. DORMANT til B7b. | Kyle (autonomy directive) + CC + Langston |
| 2026-06-16 | **P19-B4b.2 CLOSED — dead paper-fill machinery sweep (#300).** DELETED `realtime-paper-executor.ts` (whole file — `executeTrade` 0 callers; `recordPaperTrade` never finished) + the dead `/api/execution/metrics` diagnostic endpoint (0 client consumers, stale `killSwitch`) + the dead order-book read sub-path (`market-data-coordinator` `latestOrderBooks`/`orderbook`-handler/`getLatestOrderBook` + `market-data-ws` `'orderbook'` emission). `system-health-monitor.getExecutionMetrics()` rerouted **value-identically** to mdCoordinator/rateControl/executionTiming (Langston-verified line-by-line — the executor's `getStatus()` was a dead pass-through over exactly these three). **★Framing correction (the original #300 plan was inaccurate in 2 places):** (i) `realtime-paper-executor` had TWO live `getStatus()` consumers + a constructor tick-listener, not "only a diagnostic endpoint"; (ii) the "dup 2nd WS book path" is NOT a clean module cut — `market-data-coordinator` + `market-data-ws` are LIVE shared infra (feed-integrity-monitor / health-monitor / parity-gate), so only the order-book SUB-PATH was dead; the `book` subscription + the midpoint-tick emission are LIVE and were KEPT. **KEPT (per directive):** `calculatePriceImpact` (depth-walk golden-test ref; parent module still imported by pre-execution-validator + routes.ts); `pre-execution-validator` (LEFT pending #297). **NEW finding → #301:** the whole `MarketDataCoordinator`/`MarketDataWebSocket` subsystem is likely vestigial (zero `subscribeToPair` callers → idle 2nd Kraken WS) — homed to a separate liveness-then-remove audit (NOT folded in — certainty-before-cutting); the Langston-Q2 "is the health `execution` block itself dead?" question rides into #301 too. Commit `977f3be08`, CI `27598725568` all-4-green, deployed restart#394 (root HTTP200, `/api/execution/metrics` → 404), bench tsc-no-regression + vitest 1979. Langston Step-1/2/4 APPROVE. DORMANT-path-only, no fill behavior change. | Kyle (autonomy directive) + CC + Langston |
| 2026-06-16 | **#301 (vestigial 2nd-WS market-data subsystem) given a CONCRETE home — Kyle flagged "decide at next touch" as too vague.** Decision: NUMBERED batch **P19-B6.7** (sequenced after B6.6, before B7a/B7b — pre-switch-on), NOT Phase-20 — because two of its 4 live readers are health/safety monitors (`feed-integrity-monitor` + `parity-gate`) that may be giving a false all-clear while watching the idle connection, worth confirming before paper turn-on. Does NOT itself hard-gate B7b (no live impact); runs in the pre-turn-on sequence. Added as a §1 board row. | Kyle (flagged vagueness, "just get it done, before is fine") + CC |
| 2026-06-16 | **P19-B5 SPLIT (Langston Step-1 ACK).** B5a = the §19.0.5 reject/admit capture hooks (FX5 pre-filter + SQE/RTB/TCL + paper-engine admit) — the HARD 19-3 precondition, lands isolated since it gates paper-active; reuse the admitted-path `signal_eval_archive` schema 1:1 + the SAME paper-active gate (no parallel flag). B5b = #94 xStock macro snapshot (capture-only; build stays Phase-25) + the #86 home decision. **#86 Q-D probe carved OUT to its own batch B5c** (Langston Q2 — continuous/always-on, NOT dormant; doesn't share B5's zero-live-risk profile). Proceeding to B5a Step-2 pre-audit first. | CC + Langston |
| 2026-06-16 | **P19-B5b CLOSED — #94 xStock VIX+DXY macro snapshot on EVERY xStock decision record.** New `server/asset_classes/xstock_spot/macro-snapshot.ts` `buildMacroSnapshot()` (extracted from eval-cycle for testability — Langston-ratified) threaded into all 4 xStock archive `features` objects; captures all 8 fields incl RAW vix/dxy (baseline-independent ground truth) + freshness (`ageSeconds`, Infinity→explicit-null) + `partialFeed`; straight-copy null-preserving (explicit null ≠ 0). CAPTURE-ONLY — modifier build stays Phase-25 item 25-7. **NOT DORMANT** (writes on merge; declared §9.1). ZERO migration (`features` JSONB). Commit `c9f2e8285`, CI green, deployed restart#396; **live-verified** macro present + populated on post-deploy xStock rows (vixZ=-1.16, ageSeconds=57s, raw vix). #86 home = B5c CONFIRMED. Langston Step-1 ACK + Step-2 PROCEED (+§14 PHASE_19_PLAN mandatory — this entry) + Step-4 APPROVE. System Manual N/A (telemetry enrichment, justified). 5 new tests, vitest 1992/1992. Next: B5c. | CC + Langston (autonomy directive) |
| 2026-06-16 | **P19-B5a CLOSED — active-path reject/admit capture hooks (the §19.0.5 / 19-3 HARD precondition).** 11 pre_filter hooks (market-scanner global filters + fx5 family/pattern IMF) via new centralized `capturePreFilterReject`, gated on the existing `isEngineActive`/`!isPassiveLearning` SoT (no new boolean); SQE/RTB/TCL reject hooks capturing REAL scores (NO-PATCHES: failing finalScore / confidence_modulated); terminal paper-engine admit. ZERO migration. New `SignalEvalSource` = market-scanner/fx5-scanner/ready-to-buy. **★Step-3 verify-before-edit corrected the signed site list** (DROP vestigial core_imf_lq_vn, MOVE pattern_imf to the real :1228 filter, ADD pattern_high_price — Langston re-blessed). **★TCL = duplicate_position ONLY** (max_open_trades is a cycle-level DEFER, not a reject → skipped). **★Two-admit-milestone double-count rule** documented in SIM (orchestrator-admit vs paper-engine-admit, source-disambiguated). Commit `1e119531a`, CI `27614885978` green, deployed (HTTP 200), vitest 1987/1987; live dormancy proof = 0 rows from new sources on staging. Langston Step-2/chunk-A-resign/Step-4 all APPROVE. System Manual N/A (observe-only, justified). **The 19-3 paper-active data-capture precondition is now MET.** Next: B5b, then B5c. | CC + Langston (autonomy directive) |
| 2026-06-16 | **P19-B5c CLOSED — continuous Q-D (quote-depth) friction probe (#86).** NEW always-on `xstock_qd_probe_cron` (every 5 min) records a compact derived on-venue friction row per active xStock symbol (bid/ask spread + top-of-book depth + freshness) into the NEW plain table `xstock_qd_probe_history`, read from the internal `xstock_spot_ticker_snap`. **Design (Langston D1-D11/A1-A2):** core = derived on-venue series (NOT the redundant raw bid/ask, NOT the B79.0a basis); **D5** `bucket_start`=fire-grid-floored (NOT captured_at) so a feed gap yields one honest stale row per bucket; **A1** raw stored + derived NULL on degenerate (`crossed/zero_bid/zero_ask/nonpositive_mid/zero_depth`); **D7** fire-evidence meta `{market_open,universe_size,rows_written,symbols_skipped_no_snap,symbols_stale}` distinguishes weekend (rows still written stale=true) from breakage; **D9** retention folded into the B75 sweep as a plain-table age-delete pass (single owner, no partitioning, no cold-offload, 90d); cadence/retention/freshness `module_constants`-resolved (fail-loud). **R-D2** basis LEFT OUT (RTH-only-clean; Finnhub-sourceable) → candidate homed B81/Phase-25. **★Step-7 live-verify caught a real 30s timeout** (DISTINCT-ON-no-WHERE full-scanned the multi-GB tick archive — Langston's Step-4 index-served caveat) → fixed to per-symbol indexed seeks (`f521f6d6b`), next fire 3.1s. Live-verified: 486 rows/fire (490 universe), all ok, spread p50 ~15 bps. CI all-4-green; Langston Step-4 APPROVE + Step-8 CONFIRM. CAPTURE-ONLY (consumption B81/Phase-25). | CC + Langston (autonomy directive) |
| 2026-06-16 | **P19-B6 reframed build→RESTORE (Kyle catch + Langston ACK to Step-2).** Kyle: the daily-loss auto-trip worked as of Phase 8 — re-check, do not assume. Git archaeology recovered the deleted `risk-manager.ts::checkKillSwitch`+`calculate24hPL` (mode-aware, already wired to the modern `tripKillSwitch`; deleted 2026-01-01 in `594aad717` as collateral of a remove-legacy sweep — §9/§15 failure mode; F2 = trigger wiring already cut before deletion → logic proven, trigger is what B6 re-establishes; F3 orphan `paper-metrics.calculate24hPL` still in live tree → delete in B6). B6 = RESTORE+reconnect, not build-from-scratch. **Kyle decisions:** flatten-on-kill (close ALL open positions then halt); TWO warning tiers 50%/75% of kill threshold as PER-MODE user guardrails (migration adds `dailyLossWarning1/2Pct` to guardrails_v2 both modes); circuit-breaker (restart rebaselines — hard-lockout = named follow-up); defaults paper-kill **15** / live-kill **7**, warns 50/75. **Langston ACK + refinements:** latch-first-then-flatten + synchronous in-memory `killInProgress` guard (closes the TOCTOU window where 2 same-tick deferred evaluators double-flatten); strict bounds `0<warn1<warn2<100` enforced in the coherency-rule engine (RULE_007-style, not raw DB CHECK); de-dup epoch == loss-window `engineSessionStart` (one anchor — divergence = split-brain) + ratchet (highest-tier-crossed wins) + the KILL ALSO fires a critical system-alert (trip critical / warn2 warning / warn1 info). Step-2 owns 3 code-truth traces: engineSessionStart advances on `/api/trading/start`, stopPaperSimulation closes-vs-leaves open positions, close-path re-entrancy. | Kyle + CC + Langston |
| 2026-06-16 | **NEW BATCH P19-B6.8 — per-mode guardrail completeness (Kyle directive).** Kyle: at some point in Phase 19, separate out the REST of the user-settable guardrails so **paper has its OWN working set specific to it**; live needs the same DB structure but NOT full wiring/functionality ("I don't even know what's sitting over there in live mode"). `guardrails_v2` is already per-mode for the Core Four + (B6) the warning tiers; B6.8 sweeps every remaining user-settable guardrail/trading-setting (legacy `trading_settings` or other global/mode-blind config) into per-mode separation, completing paper's set. **CC recommendation (Kyle asked where to home it): dedicated batch sequenced after B6.7, before B7a; paper-complete + working; live structure-only; a paper-only B7b pre-flight gate (§6 gate 14).** Placement adjustable by Kyle. | Kyle directive + CC recommendation |
| 2026-06-17 | **P19-B6 CLOSED (Langston Step-8 CONFIRMED; Kyle ack pending) — daily-loss auto-trip RESTORED.** Kyle caught that the auto-trip "worked as of Phase 8"; git archaeology recovered the deleted `risk-manager.ts::checkKillSwitch`+`calculate24hPL` (`594aad717^`) → B6 = RESTORE not build. Shipped: `daily-loss-budget.ts` (rolling-24h realized loss %, session-anchored, `≤0`→breach) auto-tripping the EXISTING `tripKillSwitch` (which already flattens via `stopPaperSimulation→forceCloseAllOpenPositionsOnStop` — no double-close); 2 warning tiers (`dailyLossWarning1/2Pct`, % of kill, per-mode migration, RULE_011 strict `0<w1<w2<100`); ratchet + hysteresis re-arm; alerts on BOTH the operational `.jsonl` queue AND the user-facing dismissible website banner (Kyle directive); circuit-breaker (restart rebaselines). **Langston Step-4 CHANGES→re-APPROVE:** caught 2 real blockers — RULE_011 was a decimal-STRING lexicographic compare (`"9"<"80"` false → legal config rejected) + a thrown `doKill` permanently latched the evaluator OFF; both fixed (parseFloat; latch-rollback + critical TRIP-FAILED alert). **Gate-7 force-trip PROVEN** by a 5-case integration test (auto-trip+latch+both-alerts, idempotent, recovery, gated-off, already-tripped). Bench tsc-clean + vitest 23/23; CI green `43601300f`; deployed restart#399. **DORMANT til B7b** (gated `isEngineActive`; live close-driven exercise = B7b — Langston Step-8 obs). F3 orphan `paper-metrics.calculate24hPL` deleted. Realized-only (faithful to Phase-8) → unrealized gap homed #303→Phase-25. ★Kyle setting decision pending: kill is paper-10/live-15 (pre-existing) vs recommended 15/7. | Kyle + CC + Langston |
| 2026-06-17 | **P19-B6 Kyle-ACKNOWLEDGED + CLOSED.** Langston Step-8 independently CONFIRMED on staging (HEAD `43601300f`/restart#399, all 4 objectives PASS: warns 50/75 both modes, clean boot + RULE_011 live incl NaN-fail, engine passive so auto-trip dormant-correct, no active alerts). Kyle acknowledged + directed proceed. | Kyle + CC + Langston |
| 2026-06-17 | **P19-B6.5 Step-1 ACK + SPLIT (Langston ruling) — crypto active-pipeline resurrection.** Q1 dry-run mechanism → **Option C** (Langston OVERRODE CC's lean to A). The decisive finding (Step-1.a): there is **no per-asset-class active toggle** — the active path gates only on the per-MODE `system_context.isEngineActive` (`fx5-scanner.ts:543-555`), and the xStock `active-dispatch.ts:123` gates on the SAME flag → **B7b (staged crypto-first, xStock dormant til B6.6) CANNOT flip the shared flag without co-activating the incomplete xStock path either.** So a **first-class per-asset-class active gate is mandatory B7b infrastructure that was missing**, not scope-balloon. Harness (A) bypasses the very turn-on B6.5 must prove + leaves B7b's mechanism unproven; fail-closed suppression (B) is a silent fallback (rule#10). → **SPLIT: B6.5a (per-class active gate + xStock-isolation acceptance test) + B6.5b (accretion-delta audit + crypto-only time-boxed reverted dry-run THROUGH the gate + fill-parity)**, both under the single B7b-green gate 10. Q2 agree (break-fix in-batch / calibration deferred §13-homed); Q3 agree + gate-10 adds the C-gate verification; Q4 agree + xStock-isolation proof = the gate's acceptance test; Q5 agree (gate 13 depth + gate 7 daily-loss confirmed wired before the dry-run). Scope Rev-2 + this board. ★Surfaced to Kyle: the per-class gate is a B7b prerequisite (changes the turn-on story). | Kyle + CC + Langston |
| 2026-06-17 | **P19-B6.5a CLOSED (Langston Step-8 CONFIRMED; Kyle ack pending) — per-asset-class active gate SHIPPED.** Additive fail-closed default-OFF JSONB `active_asset_classes` on `system_context` as an ADDITIONAL AND-gate on the per-mode `isEngineActive` master (class trades iff `isEngineActive(mode) && isAssetClassActive(mode,class)`). Single typed read (`isAssetClassActiveInContext`, `=== true` no coercion) called at the 2 class entry gates with the context already in hand; H1 setter (`setAssetClassActive`, await-DB-first then broadcast, read-merge-write); per-class witness (gate allow/skip counter + a `LIVENESS_SPLIT` hard-breach hook). Gates: crypto `fx5-scanner` (OFF→passive/VTS), xStock `active-dispatch` (OFF→classDormantSkip); RTB entry-protected (defense-in-depth #320→B6.5b). **xStock-isolation acceptance test (gate-10 precondition) GREEN.** Langston Step-1 Option-C + split, Step-2 PROCEED + Q-A..Q-E, Step-4 APPROVE (2 non-blocking: static-contextBridge cleanup + #321 homes the uncalled witness/dual-active edge to B6.5b), Step-8 CONFIRMED. CI green `500127614`; deployed; staging-verified `{}` both modes fail-closed + engine dormant + clean boot. DORMANT (both classes default OFF). **B7b crypto-first now expressible: master ON + crypto ON + xStock OFF.** Next = B6.5b (audit + crypto-only dry-run THROUGH this gate + fill-parity). | Kyle + CC + Langston |
| 2026-06-17 | **Two Kyle directives recorded.** (1) **Both-modes-active is the INTENDED STEADY STATE, not an edge case** — once live launches, paper keeps running continuously alongside it as the calibration/new-functionality testing ground (full pipeline, one best signal/cycle, no real trade); this was the driving force behind the paper/live cleave (between-Phase-24-and-19 / B4b D5). #321 reframed: the `fx5` single-`activeMode` scan + the per-class gate's single-context read must handle concurrent paper+live → re-pointed to the Phase-21 co-run (B6.5b records, does not fix). (2) **`test-in-paper / bypass-in-live` capability gating SCHEDULED as a CONFIRMED Phase-19 item = P19-B15** (Kyle: give it a confirmed Phase-19 home; CC placed it LAST as it pairs with no existing batch) — a per-feature mode-keyed gate so new/experimental functionality runs in paper but is bypassed in live until proven (#322). (3) Daily-loss failsafe philosophy (auto-trip vs alert-only vs user-selectable) captured as a revisit (#323; 20% paper kill stands; revisit if it over-trips during paper testing; natural home = B6.8). | Kyle + CC |
| 2026-06-17 | **P19-B6.5b CLOSED — the per-class crypto active gate F1-F5 shipped (its governance was deferred to this joint close).** Commit `d56a0cc1e`, CI `27693018813` all-4-green, deployed: F1 fx5-scanner per-class crypto gate propagation (#320) + F2 witness wire (#321) + F3 paper cooldown read fix + F5 ATR-zero exit floor + rule-18 dead-code removal; Langston Step-4 APPROVE. The crypto-only reverted dry-run proved the front half (scanner→pools→orchestrator→SQE all fire for crypto, no crash) but surfaced two ready-to-buy-insert breaks → repaired in B6.5c. B6.5b closed on its own scope; the closed-lifecycle proof (gate-10) is NOT B6.5b's deliverable. | CC + Langston |
| 2026-06-17 | **P19-B6.5c CLOSED on objectives 1-3; objective 5 (gate-10) CARRIED to a NEW batch B6.5e (Langston ruling).** Commits `52adcdf6f` + `0b5bb206d`, CI `27720080156` all-4-green, deployed. Fixed the two breaks the B6.5b dry-run surfaced — (1) DROPPED the leftover NOT-NULL `cwqi` column on `rtb_signals` via migration (the code had already removed it; the staging schema drift was rejecting every ready-to-buy insert), (2) the pattern recognizer was stamping invalid `pattern_*` strategy names → fixed by resolving each detected pattern to its consuming **canonical** strategy (regime + class aware, exact-match-or-drop) via new `resolvePatternConsumingStrategy` + removed the redundant duplicate-emitter loop. **★Kyle correction (recorded):** patterns are TRIGGERS that feed strategies, NOT strategies — the 19 canonical strategies are FIXED; never invent strategies, never map-to-nearest (pollutes that strategy's stats). PROVEN LIVE in a reverted crypto dry-run: crypto signals reach ready-to-buy with canonical names, zero `pattern_*`, zero `cwqi` errors, xStock isolated. **Objective 5 / gate-10 (≥1 FULL closed crypto lifecycle) NOT met** — blocked by a NEW downstream break: a sized crypto signal won't OPEN (TCL→paper-execution-engine open path falls out silently; `[8.8.3-I3]` invariant monitor = attempts=11/opened=0/blocked=0/reasonSum=0 → #325). Per Langston, gate-10 is carried to a NEW batch rather than ballooning B6.5c. | Kyle + CC + Langston |
| 2026-06-17 | **NEW BATCH P19-B6.5e created — TCL→paper-execution-engine open-path silent-failure repair + gate-10 closed-lifecycle owner (#325; Langston ruling).** Owns the ≥1 FULL closed-trade-lifecycle proof (open→exit→close→cooldown→telemetry) that B6.5b/B6.5c proved up to but not through. The downstream break: a sized crypto active-paper signal never opens a paper trade — it falls out of the TCL→paper-execution-engine handoff before any reason/block is recorded (`[8.8.3-I3]`: attempts>0, opened=0, blocked=0, reasonSum=0 — likely un-awaited promise / swallowed throw / early-return-no-telemetry; this path has been untested since Phase 8). **May promote to a full `P19-B7` if the open-path surface is large.** Scope with a DEEP SIM + System-Manual read first (Kyle directive — CC had conflated pattern-vs-strategy, so likely more unknowns; scope thoroughly, don't assume). **B7b stays HARD-GATED on the closed-lifecycle proof, now owned by B6.5e (§6 gate 10 re-pointed).** Also surfaced (separate, non-fatal, NOT folded into B6.5c): a B63 DBS-not-propagated hard-contract warn on some crypto pairs in the pattern-pool eval (#326) → own B63-DBS-lineage home. | Kyle + CC + Langston |
| 2026-06-18 | **P19-B6.5d CLOSED — asset-class stamp integrity (carry-the-stamp across all 35 resolve sites).** The live `classify-fallthrough-active` CRITICAL alert (id 58367b27, `A/EUR@kraken` dropped on the active path) surfaced a STRUCTURAL gap: the asset class was being re-derived from the symbol string at 26 of 35 resolve sites instead of being carried with the pair — and re-deriving hardcodes `exchange='kraken'`, mishandling the 9 USD / 8 EUR collision tickers + single-letter bases. NO-PATCHES: fixed all of it, enforcing **one `SizingContext` = one asset class = one pipe** (stamped at pipe entry, carried with the pair, never re-derived downstream). OBJ-1 widened the resolver base-length FLOOR from an implicit 2 → 1 via one SSOT constant `TICKER_BASE_MIN_LEN=1` (`shared/asset-classes.ts`) feeding all 3 resolver regexes → clears the A/EUR alert (A=Vaulta, the ex-EOS rebrand, a real Kraken-spot crypto); collision precedence preserved (DISPLAY→COLLISIONS→SYMBOLS→CANONICAL→raw — verified live A/EUR=crypto_spot, T/USD=crypto_spot, Tx/USD=xstock_spot). OBJ-3 prefer-the-carried-stamp at ~12 sites; OBJ-4 threaded `assetClass` through `evaluateTradeExpectancy`+`feePercentFor`; OBJ-2/5/6 dedupe key + fail-closed-active/logged-passive defaults + 2 throwing-variant swaps. 14 new tests; commit `3bd3deedc`, CI `27728040408` all-4-green, deployed (HTTP200, restart#403, pure code/no migration). **Alert 58367b27 RESOLVED post-deploy — the headline gate, MET.** Langston Step-4 APPROVE; Step-8 in flight. Carried side-items each given a home: N1 sig-orch:1151 dead-import → RUNNING_ISSUES; JC#4 STAMP_MISSING_ACTIVE disposition → B6.5e completion report; the §B instrument is intentional, kept for the B6.5e dry-run. | Kyle + CC + Langston |
| 2026-06-18 | **P19-B6.5e CLOSED on Phase A (#325 repair) + diagnosis; gate-10 + EV-input integrity PROMOTED to a NEW batch B6.5g; canonicalizer quote completeness = a NEW batch B6.5f that ships FIRST (CC + Langston + Kyle consensus).** The B6.5c-surfaced silent failure (#325) is FIXED: `executeSimulatedTrade` was `void`-returning with ~7 bare-`return` post-guardrail early-exits invisible to the I3 invariant, and `executePromotedSignal` inferred success by a trade-count DELTA → any post-guardrail open failure was silent. Fix = a typed `OpenOutcome` return + a NEW `openFailedTotal`/`openFailedByStage` 3rd term in `rtb-metrics-service` (I3 invariant now `attempts === opened + blocked + openFailed`; `recordOpenFailed(symbol,strategy,stage,reason)`). PROVEN LIVE on the deployed code (`dbd0a2283`, CI `27731034448` all-4-green, restart#404): the contained crypto dry-run reconciled + stage-named the invariant — `[8.8.3-I3][INVARIANT_CHECK][OK] attempts=8 opened=0 blocked=0 openFailed=8 [EV_REJECT:8]`. **★The diagnosis (OBJ-2) is the decisive finding:** the open path is NOT broken in execution — it correctly reaches the Net-Expectancy gate (11.8B), which HONESTLY rejects every crypto signal (NetEV<0 after friction) because the EV kernel runs on DEFAULT pWin=0.60/DI=50/VolNoise=0.300 inputs (#233). RawEV is positive (genuine edge) but friction exceeds it on default inputs; the EV gate is working and must NOT be weakened. So gate-10 is blocked by decision-grade EV-input math, NOT the open path — and the lever for a real open is fixing the EV inputs. **Decisions:** (1) **gate-10 (≥1 full closed crypto lifecycle) MOVED B6.5e→B6.5g** (a NEW batch = EV-input integrity: #233 thread real DI/VolNoise/prices/dbsScore into `evaluateTradeExpectancy` + friction-decomposition audit + ranking/sizing normalization check + gate-10 re-run on real inputs); B7b stays HARD-GATED on B6.5g-green (§6 gate 10 re-pointed B6.5e→B6.5g). (2) **NEW batch B6.5f = canonicalizer quote-currency completeness, ships FIRST** (before B6.5g): the dry-run fired 6 `classify-fallthrough-active` quote-side alerts (ETH/EUROP, ETH/PYUSD, XBT/EUROP, XBT/PYUSD, XRP/RLUSD + A/EUR) from `knownQuotes` missing newer Kraken stablecoin quotes + the `[A-Z0-9]{3,4}` quote group capping at 4 chars (EUROP/PYUSD/RLUSD are 5) — distinct from B6.5d's single-letter BASE fix. (3) **#327 dead import removed**; JC#4 dry-run measured a CLEAN ZERO `[STAMP_MISSING_ACTIVE]` (tripwire left as-is); #297 guard-inversion note + an rtb-metrics API field gap logged. Langston Step-1 PROCEED + Step-4 APPROVE (Phase A); non_architecture. SIM S16 registry row content-updated; System Manual N/A (open-path observability + telemetry invariant; EV-gate behavior unchanged). | Kyle + CC + Langston |
| 2026-06-19 | **B-XSTOCK-GLOBALS CLOSED + B-GOV-2 activated-in-shadow (CC-A governance close; Claude-New-cleared shared-doc window).** (1) **B-XSTOCK-GLOBALS CLOSED** — xStock VTS opens now stamp the at-open per-class global regime/friction/DBS (`a93e274c8`, CI green, deployed restart#405, UI+DATA verified: same-minute-open integrity = 1 distinct value, values track the live market-wide series; global friction = measured xStock spread, not the fixed fee). Root cause = B-4.7 made `registerOpenVtsTrade` caller-pass-only and the xStock eval-cycle caller never passed the 4 globals → blank; fix mirrors the crypto per-class stamp. Langston Step-4 APPROVE; the blank-while-LIVE guardrail tripwire SPLIT to a §13 follow-up. (2) **B-GOV-2 activated-in-shadow** (governance-checker activation, `ec37b2990`, CI green, 35/35 tests) — installed on staging + proven (it correctly flagged the real xStock-governance gap + its own missing change-class) but **PAUSED in shadow** after the first tick flooded the §10.5 queue with 88 backfill entries (all resolved); 2 §13-homed follow-ups gate live-paging (closed-batch exception seeding + the shadow-surfacing fix). #324's two original gates ADDRESSED. | CC-A + Langston |
| 2026-06-21 | **reorg-B2.1 DEPLOYED (OBJ-1/2/3/4) + CLOSING — the gate-PLACEMENT fix (Kyle's question).** Kyle asked why the floor-lift/RR/reachability live in a post-hoc normalizer instead of the strategy modules where the signal is made. Investigation across all 19 strategies → consensus: move the gates INTO the strategies' shared guard `applyGlobalGuards` at signal-gen (18 wired; liquidity_trap skipped — disabled/inverted geometry), **DROP the floor-lift** (a target mutation redundant with the Net-Expectancy gate), **consolidate RR to ONE per-class SSOT** (killed a live 1.5-vs-2.5 split-brain), add per-class reachability, + NEW `guard-eval-tracker` suppression instrumentation (#372). Normalizer KEPT as a net-neutral downstream bridge. Langston Step-4 APPROVE all objectives (caught + fixed: meanRR skew → rrEvals denominator; active reachability atr=0 → carry-on-context; VTS invalid_atr mislabel; orb import-ext). **⚠️ CI was RED from OBJ-4b (`8beb34181`) — the guard-wiring exposed 2 unit tests not warming the `expectancy_gates` read; mislabeled green mid-batch, caught at close, fixed TEST-ONLY at `7bef81fd7` → CI 4-green confirmed (run 27901621481).** Sanity 3 strategies/23 evals @90s. **OBJ-5 (retire normalizer) + OBJ-6 (VTS-tab visibility) are window-gated → SPLIT to reorg-B2.2 (#374) so B2.1 closes now** (Langston's close gate: home it in RUNNING_ISSUES + this plan — done). | Kyle + CC-B + Langston |
| 2026-06-21 | **reorg-B2.2 created (#374) + the 48h soak FREEZE decision.** The split-out window-gated work + two Kyle directives: (a) the `guard-eval-tracker` must SURVIVE restarts (PAUSE+RESUME the suppression window, not reset) — CC-B built tracker PERSISTENCE (checkpoint+reload, OBJ-A, `a2a870da9`, bench-green, UNDEPLOYED); (b) the reward-vs-risk gate must be SURFACED in BOTH VTS Filter-Diag tabs (confirmed NOT there today, only the raw endpoint — OBJ-B). **Kyle FREEZE ruling (after weighing pause/resume vs reset):** rather than deploy persistence mid-soak, HOLD all staging deploys for the ≥48h window so the fixes soak under ONE unchanged system; everyone keeps building (scope/pre-audit/code/Langston-review) WITHOUT deploying, queued to push at window close. CC-A's B-GOV-3 is a SEPARATE service (own clone/timer) → does NOT restart the dawntrader app → does NOT touch the window (CC-A deploys freely; only dawntrader-app deploys cost the window). Window alert `b0ba8e56` fires 2026-06-23T01:09:31Z. | Kyle + CC-B + CC-A + Langston |
| 2026-06-23 | **48h window READ + the minRR calibration HOMED with concrete batches (Kyle directive — "say WHERE, not just homed").** Window closed clean (pm2 48.2h, restart#408, no wipe). The global `min_rr=2.5` wildcard suppresses 62–100% of every recorded strategy (meanRR 0.85–2.38). **Kyle agreed the baseline-now → calibrate-from-win-rates path; Langston refined (set minRR BELOW each meanRR not at it; key per-(strategy×asset_class) from day one; win-rate calib gated behind Phase-19 outcomes).** **CONCRETE HOMES (replacing the vague "→ Phase-25"):** (1) **reorg-B2.3** (NEW Phase-19 batch, §1 board) SETS the per-strategy × per-class minRR baseline from each class's OWN per-class VTS data (after reorg-B2.2's OBJ-6 re-key); (2) **roadmap 25-20** RECALIBRATES it from paper-active win-rates (post-Phase-19); (3) the sub-1.0-RR EV-survival audit (morning_star 0.97 / support_bounce 0.85 — RUNNING_ISSUES #375) is **FOLDED INTO roadmap 25-19** (its per-strategy cut, one-home convention — placement re-checked per Kyle, not tacked-on), Phase-25 (needs outcomes; the 2 still trade in the paper run, flagged-for-watch not pre-disabled). **★Kyle xStock decision: do NOT have xStock inherit crypto's baseline — set xStock's OWN baseline from its OWN per-class data (reorg-B2.3), for Phase-25 calibration; a thin-data xStock strategy gets a conservative default, not a crypto borrow.** Per-class retro-split of the closed window is NOT possible (guard drops are class-blind counters; the symbol-carrying skip log doesn't capture them) → the split needs OBJ-6 captured going forward. ↔ RUNNING_ISSUES #372/#375; POST_AUDIT_ROADMAP 25-20/25-21. | Kyle + CC-B + Langston |
| 2026-06-23 | **reorg-B2.2 OBJ-A+B DEPLOYED + CLOSED — the per-class re-key + durable tracker + VTS-tab visibility (freeze lifted, window closed).** With the 48h window read, deployed the A+B bundle (head `283bf4d6a`, CI `28052594844` all-4-green, restart#409, no migration): OBJ-A tracker persistence (atomic checkpoint + reload + wipe-stamp) + OBJ-B the `(strategy, assetClass)` ADDITIVE re-key — `getGuardEvalStats` SUMS raw across classes + RE-derives the ratios so the #372 strategy aggregate is byte-preserved (FLAG-2), a `keySchema` checkpoint guard discards stale-cardinality incl. unversioned-legacy (FLAG-1), and the two VTS Filter-Diag tabs + the shared `FilterDiagnosticsPanel` now show the reward-vs-risk/reachability gate by-reason **per class** (Kyle's no-hidden-gates, both classes) with a distinct no-evaluations state (FLAG-3). Langston Step-4 PROCEED (v2→v3 comment nit folded) + Step-8 APPROVED (independently verified HEAD + restart#409 + routes; UI evidence trusted). §9.3 UI-navigated both tabs (crypto mean_reversion; xStock morning_star/vwap_pullback/sma_trend_ride — distinct per-class). The durable per-class window now ACCUMULATES the data reorg-B2.3 baselines + the #371 OBJ-C divergence read need. **OBJ-C (normalizer retire) stays OPEN — #373-gated on the #371 divergence, now measurable on the per-class window.** Latent rrMin/rrMax Infinity-over-JSON homed #376 (→ OBJ-C diff). | Kyle + CC-B + Langston |
| 2026-06-24 | **reorg-B3 DEPLOYED + CLOSING — the #233 EV-input thread (DI+dbsScore to the open-gate) + the FIND-1 `sourcePool` latent bug.** The Net-Expectancy kernel inputs DI+dbsScore were DROPPED at the orchestrator→RTB boundary → the open-gate always used kernel defaults (#233). Threaded both from the routing-time FX5 survivor snapshot — **Option B (Langston-endorsed over the original MCE approach): the active-filter-pool already carries the SAME dbsScore that drove strong-trend routing, so sourcing it there is more coherent + avoids a 3rd DI compute** → typed `rtb_signals.di_at_queue`/`dbs_score_at_queue` columns → open-gate (no metadata fallback / no coerce → kernel documented defaults on null). **DECISION (audit-confirmed, §9.1): NOT a crypto opener** — H1 real-DI gives ZERO EV lift (default DI=50 already at the 0.60 ceiling), H2 dbsScore lifts strong-trend pWin off the 0.40 floor (the lone EV-relevant half); **VolNoise/prices DROPPED from #233 scope (not kernel EV inputs).** **★FIND 1 (latent, dormant active path): the promote-conversion `checkRtbPromotion` DROPPED `sourcePool` → the kernel strong-trend branch NEVER fired → the dbsScore thread would itself have been INERT; threaded it (one fix repairs the EV branch + the trade-record sourcePool persists + the sizing reads at once).** Deploy-verify §9.3 caught the rtb-metrics endpoint subset-map silently dropping the OBJ-4 proof surface → instance fix `99887f90e` + the verbatim-minus-redaction refactor defect-class homed #379 (Langston NO-PATCHES). head `99887f90e`, CI 4-green, migration applied, restart#411. Langston Step-4 APPROVED. Active path dormant → OBJ-6 live proof gated on paper-active turn-on; integration test 8/8 is the proof now. §13: #377 (xStock dbs source) / #378 (di_at_open) / #379 (rtb-metrics refactor). | Kyle + CC-B + Langston |
| 2026-06-24 | **reorg-B3.1 + reorg-B3.2 DEPLOYED — the two small B3 follow-ups + the VTS un-strangle (Kyle-surfaced).** B3.1: #378 di_at_open reads the carried `diAtQueue` (was const-50); #379 rtb-metrics returns `getSummary` verbatim-minus-redaction (kills the subset-map silent-drop defect-class). **B3.2 (Kyle-surfaced; CC-B + Langston + Kyle consensus):** VTS opens collapsed 95-97% (~150/day → 3-5/day, staging-confirmed) from the reorg-B2 `min_rr=2.5` gate wired into vts-runner. **DECISION:** on the VTS learning path the QUALITY gates (`rr_below_min`, `unreachable`) TAG-AND-SIMULATE (capture the counterfactual outcome) while DATA-VALIDITY (`invalid_atr`, `invalid_geometry`) still DROP; ACTIVE+LIVE keep SUPPRESSING (same gate eval, different disposition per path). Un-strangles the learning engine AND un-circularizes reorg-B2.3 (calibrate the floor from realized outcomes on the FULL population, not the gate-filtered one — Langston's circularity catch). Answers Kyle's pollution concern: real signals → real outcomes = good data; only garbage dropped. Langston Step-4 caught a real TS2322 (masked by baseline headroom) → fixed via positive-narrow. Head `c077faa7f`, CI 4-green (`28099133186`), restart#412, no migration. | Kyle + CC-B + Langston |
| 2026-06-24 | **reorg-B3.3 — the CORRECTED VTS un-strangle (reorg-B3.2 was inert) + reorg-B3.3x §13-homed for xStock.** B3.2 (the vts-runner normalizer tag-don't-drop) turned out INERT: the 18 strategies hard-drop the signal at signal-gen via the reorg-B2.1 in-line guard (`if (!_gr.pass) return null`), UPSTREAM of the normalizer — so `callStrategyDetect` returns null and B3.2 is dead code on the live path (staging-proven: 1 signal / 136,779 evals, rr+reach drops ~95%+). **FIX = move the un-strangle to the actual drop site:** an SSOT `guardForcesDrop(gr, disposition)` (`strategy-helpers.ts`; `VTS_TAGGABLE_GUARD_REASONS={rr_below_min, unreachable}`) + a trailing-optional `gateDisposition='enforce'` threaded through all 18 detect signatures + the strategy-engine wrappers + the vts-runner dispatch; the crypto VTS path opts into `'tag'` (quality guards don't drop → flow to the existing B3.2 normalizer → tag `vtsGateVerdict` + simulate). **Active/live unchanged BY CONSTRUCTION** (omit the arg → 'enforce' → byte-identical; the safety property — zero orchestrator edits). Validity (invalid_atr, stop_distance) still drops on every path. **Fork = Option A (Langston's call): crypto fully this batch; xStock = reorg-B3.3x, the IMMEDIATE next batch (named home RUNNING_ISSUES #382 + §1 board), because eval-cycle:526 is a structurally different gate layer (Net-EV floor, no B3.2 normalizer).** Langston Step-4 CONCUR on all six conditions (unreachable=far-but-valid proven; caller exhaustiveness grep-confirmed default-enforce; normalizer clamped-vs-raw ATR divergence examined → benign + intentionally-left, logged on #382; downstream-of-guard verified; telemetry = `signalsGenerated`/opens climb from 1, NOT guardDrops; #382 home). Bench: tsc baseline OK, new test 6/6. **Step-10 owes (Langston §16, batch-close requirement): SYSTEM_MANUAL VTS-gating content update + explicit SIM applicability note (`gateDisposition` = threaded param, not SIM-registry shared state).** | Kyle + CC-B + Langston |
| 2026-06-24 | **reorg-B3.3 CLOSED (Langston Step-8 PASS, Kyle ack) + reorg-B3.3y shipped (the validity gap B3.3 exposed) — pulled AHEAD of B3.3x.** reorg-B3.3 closed on all gates: Langston independently verified staging (head ed9b8b626, 10 TAG_NO_DROP markers) + Kyle acknowledged. SYSTEM_MANUAL §11 + SIM §1.2a-2 landed (gov 6fc8d9d03/6d2384382). **reorg-B3.3y:** Step-8 surfaced (CC-B) that `normalizeAndGateTarget`'s geometry guard was ASYMMETRIC — caught `stop>=entry` but not `target<=entry`, so a reward≤0 long (live: `volatility_edge rr=-0.00` ~1/min) fell into the `rr_below_min` quality-tag bucket and got VTS-simulated. Langston's call: RECLASSIFY to validity-DROP. FIX (one condition): added `nativeTarget <= entryPrice` → a valid long needs `stop < entry < target`; reward≤0 → `invalid_geometry` DROP everywhere. Active unchanged (already dropped as rr_below_min; now invalid_geometry — label-only; both callers traced). VTS tag→validity-drop. **Kyle directed the run order (ack turn): B3.3y FIRST → B3.3x → reorg-B4 → B6 series; Langston OK'd pulling B3.3y ahead** (polluting + trivial). New test 5/5, tsc baseline OK, no migration. Staging `2136c74fe` restart#414, CI 4-green `28130496306`. Governance: SYSTEM_MANUAL §11 + SIM §1.2a-2 + RUNNING_ISSUES #383 + BATCH_CATALOG + PHASE_HISTORY. **NEXT: reorg-B3.3x (xStock VTS un-strangle, #382).** | Kyle + CC-B + Langston |
| 2026-06-24 | **reorg-B3.3x DEPLOYED — xStock VTS un-strangle, UNIFIED onto the shared normalizer (the reorg-B3.3 family is now complete across both classes).** xStock VTS (`eval-cycle.ts`) had NO `normalizeAndGateTarget` call (build-history gap, B79.0m) — so none of B3.2's tag-don't-drop, B3.3y's validity, or the verdict. **Langston Option B (NO-PATCHES, "not close"): UNIFY onto the ONE SSOT, not a hand-rolled second copy.** eval-cycle:526 opts into `'tag'` + runs the shared `normalizeAndGateTarget` after detect, before the Net-EV floor (orthogonal+sequential): quality (rr_below_min/unreachable) TAG+simulate (native target); validity (invalid_atr/invalid_geometry — xStock GAINS the B3.3y `target<=entry` backstop) DROP; positive-narrow. **★Langston Step-1 catch:** `RegisterOpenVtsTradeInput` lacked `vtsGateVerdict` — my `xOpenTrade` field type-checked only via excess-property bypass on a variable, so the sink silently ignored it (inert). Fixed: added the field to the input + threaded it onto the `OpenVirtualTrade` record (crypto-parity in-memory surface; in-memory-only, no DB col → #384 homed to reorg-B4). NOT "zero sink change" (scope claim corrected). Active/orchestrator path untouched (own normalizer :1227). Langston Step-1 + Step-4 CONCUR (5 catches addressed; he independently verified link (b) end-to-end on staging HEAD). Step-7 ✅: xStock TAG_NO_DROP markers fire (KR/MRNA/MRVL, rr_below_min+unreachable) — real xStock signals that were hard-dropped now tag+simulate. New test 5/5, tsc baseline OK. Staging `53f601b93` restart#415, CI 4-green `28131918663`. Governance: SYSTEM_MANUAL §11 (now the ONE shared VTS gate for both classes) + SIM §1.2a-2 + RUNNING_ISSUES #382 RESOLVED/#384 + BATCH_CATALOG + PHASE_HISTORY. **The reorg-B3.3 family (B3.3 crypto + B3.3x xStock + B3.3y validity) is COMPLETE. NEXT: reorg-B4 (shadow-trade layer) → B6.6/B6.7/B6.8.** | Kyle + CC-B + Langston |
| 2026-06-25 | **B-DIAG-387 CLOSED — xStock filter-diagnostics Net-EV-floor reject counter fix (pulled in before reorg-B4 at Kyle's direction, because it gates the reorg-B7 maker/taker baseline readability).** The xStock "Net EV Below Floor" dashboard tile read 0 forever — the #386-misleading bug. ROOT CAUSE: the endpoint (`routes.ts:7846`) sourced it from a permanently-empty `byReason` "reference shape" scaffold; the eval-cycle reject site (`eval-cycle.ts:716`) bumped only `signalsRejectedBySQE`, recording the reason in no in-memory counter the endpoint read. **FIX (observability-only — NO gate/strategy/regime/signal-pipeline/EV-math change):** (OBJ-1) reject site writes `net_ev_rejected` into `nullReasonAggregate` + per-lane aggregates; endpoint sources `rejectedReasons.netEvBelowFloor` from the lifetime accumulator (crypto-path parity). (OBJ-2, no-hidden-gates) surfaced the 3 invisible pre-open/TCL gate reasons (`reentry_cooldown`/`price_past_stop`/`price_past_target`) in structured `nullReasons` + 3 panel rows. (§18) excised the dead `byReason`/`totalRejected`/`byRegime`/`total*`/`byStrategy` scaffolding + the always-empty `signalRejections` field (DELETED_COMPONENTS_LOG). **Langston Step-1/2 + Step-4 APPROVED; 4 conditions met** (dead-scaffold fully excised; single key `net_ev_rejected` cross-layer-mapped + guard-commented; single reject site 1:1 with archive; 15% band → EXACT cross-check). **PROOF:** live endpoint `netEvBelowFloor` == `signal_eval_archive` count over the matched since-restart window = **25 == 25, exact.** Staging `1c451f5b5` restart#416, CI 4-green `28161070581`, no migration. New test 5/5 + 30 adjacent green, tsc baseline OK. Governance: RUNNING_ISSUES #387 RESOLVED + DELETED_COMPONENTS_LOG + SIM + BATCH_CATALOG + PHASE_HISTORY. **NEXT: reorg-B4 (shadow-trade layer) → B6.6/B6.7/B6.8.** | Kyle + CC-B + Langston |
| 2026-06-25 | **reorg-B4 CLOSED — the shadow-trade telemetry layer (selection-quality data engine; feeds reorg-B5).** Attaches a telemetry-only layer to the promotion boundary: inside `getRankedSignals` (sole live caller `checkRtbPromotion` → 1 capture/cycle), AFTER the FinalScore sort + BEFORE the slice, open a counterfactual "shadow" trade for EVERY RTB-pool member (promoted picks AND non-promoted alternatives) → record decision-time ranking inputs + realized outcome into the NEW isolated `rtb_shadow_pairings` sink. **Exit math REUSED (not forked):** shadows resolve through the SAME `evaluateTECExit` service (drift-guard test pins it); only `maxHoldMs=SHADOW_MAX_HOLD_MS`(6h)+close-action differ. **By-construction isolation (4 layers):** separate `openShadowTrades` Map (SIM S19); allowlist `shadowClose` (writes ONLY the sink + its own backing row + clearTrailingState, never a learning store); shadow rows in the shared `vts_open_trades` (`context.shadow=true`) excluded from every non-shadow read via the single shared `VTS_OPEN_TRADES_EXCLUDE_SHADOW` predicate; boot rehydration split. **★Langston Step-4 LOAD-BEARING catch (closed this batch):** the persisted-table reader contamination (the factor-replay/ablation feed + xStock 24h count + bootstrap gate didn't exclude shadows) was missed by the original isolation proof → fixed with the shared predicate + 2 BEHAVIORAL tests (shadow ABSENT from the HIGH ablation feed via an emitted-SQL-keyed mock; dedupe key survives persist→rehydrate round-trip). 3 decisions ratified (hook-in-getRankedSignals; `(mode,signalId)` dedupe; `promoted=ranker-selected`). Langston Step-1/2/4/8 APPROVED (full 1413-line diff byte-verified + independent staging reader-sweep). **DORMANT** (rtb_total=0 → §9.1 forward-instrumentation; verified live: table row_count=0, 3 indexes+pkey, no boot errors; the 22/22 unit suite is the behavioral proof). Staging `f3cbe2d14` restart#418, CI 4-green `28201765444`, migration applied. §13 B9-gated homes: #388 (rehydration fail-direction → `rtb_shadow_pairings` authoritative on restart) + #389 (capture-path guard). Governance: SIM (reorg-B4 callout + S19 + GC-DELETE/cycleKey notes) + System Manual §19.8 + RUNNING_ISSUES #388/#389 + BATCH_CATALOG + PHASE_HISTORY. **The reorg-B4 layer is wired + proven + dormant; it lights up at paper-active turn-on (~B9). NEXT: B6.6/B6.7/B6.8.** | Kyle + CC-B + Langston |
| 2026-06-26 | **reorg-B4.1 CLOSED — the Shadows visibility tab + per-cycle pool-membership record (Kyle-directed: a tab to see the shadow results + the per-cycle-capture fix his mechanics question exposed).** reorg-B4 stamped rank/promoted only at a signal's FIRST appearance → the pool-at-cycle-N + later-cycle-promotion weren't reconstructable. FIX (additive two-table, Langston-approved): KEEP `rtb_shadow_pairings`=resolving TRADE+outcome (deduped 1/signal); ADD `rtb_shadow_pool_members`=EVENT grain (1 row/cycle×signal: rank, promoted, **pool_size STAMP**, scores, **logical FK→pairings.id NOT NULL, no DB REFERENCES/CASCADE** — integrity by resolve-first ordering). captureShadowPool writes a member row every cycle; `registerOpenShadowTrade` dedupe-return widened **null→existing-id** (sole caller discards, verified). NEW read-only `GET /api/shadow-trades/by-cycle` + "Shadows" tab (after Trade History): per-cycle ranked-pool comparison (promoted marked + best-trophy + outcomes) + open-shadows + selection-quality summary. **★Langston Step-4 CHANGES-NEEDED → FIXED this batch:** F1 (the summary scored partial cycles → premature/flipping headline) + F2 (page-scoped) collapsed into ONE fix — a SQL aggregate over ALL FULLY-CLOSED cycles (`bool_and(p.closed)` + `percentile_cont` median). F3 (logical-FK wording) folded into governance. **OBJ-4 STAGING-UI-VERIFIED via Claude-in-Chrome (§9.3):** empty-state clean + seed-then-clean populated proof (a demo cycle where the promoted #1 was NOT best correctly scored Promoted=Best 0%/Median 100%; net_pnl-as-fraction confirmed; "resolving…" on the open-member cycle) → seed DELETED → back to dormant. Langston Step-1/2/4/8 APPROVED. **DORMANT** (rtb_total=0 → §9.1; verified live table 0 rows + 3 indexes + endpoint serves the empty state cleanly). Staging `153cc4235` restart#419, CI 4-green `28204773605`, migration applied. §13: #390 (un-deduped member-row retention sweep → B9/paper-active turn-on, in words). Bench: tsc baseline OK (client+server); reorg-b4-1 9/9; reorg-b4 20/20+2/2; full unit 1873 pass. Governance: SIM (reorg-B4 callout fold + logical-FK wording) + System Manual §19.8 extension + RUNNING_ISSUES #390 + BATCH_CATALOG + PHASE_HISTORY. **NEXT: B6.6/B6.7/B6.8.** | Kyle + CC-B + Langston |
| 2026-06-26 | **P19-B6.6 CLOSED — xStock price-discovery-liveness fill gate (#236), the FIRST of the B6 series.** Closes the holiday/half-day hole in the retired clock-only liquid-fill-window: a 24/5 token feed keeps `captured_at` fresh + depth quoted while the underlying is closed, so `last` is frozen and a fill would land on a dead-but-quoted book. NEW `xstock_spot/price-liveness.ts` at the open seam, AFTER the depth gate (depth-first), xStock-only, fail-closed — requires this token's `last` to have CHANGED ≥`min_moves` within the window. **★Key Step-2 decisions (3-way consensus):** (1) PLACEMENT = open seam beside the depth gate (Option B), not dispatch — liveness is a fill-time property + the threat is a FILL on a dead book; (2) **WINDOW = 45 min PINNED, single, NOT tiered** — the raw per-symbol in-RTH inter-trade p99 spans secs→59m (would overlap holiday-flatness), BUT that unseparable shallow tail is removed upstream by the LIVE `lq_min` gate (38/33); among names that actually reach the seam (depth ≳$2K), the worst in-RTH p99 (3 weekday sessions) is EWN 68m/EWP 50m = deep-but-slow ETF tokens correctly EXCLUDED (stale-price fills), every genuinely-active name ≤20m (≥2× margin); **justified by type-II frozen-but-quoted-book detection, NOT p99 margin** (Langston's two replies diverged 45-vs-60; resolved — 60 is a dead middle, doesn't rescue EWN's 68m, only slows type-II detection); (3) GOTU (33m) = lone accepted residual, bounded by the open-seam depth-sufficiency cap (#391); (4) SEMANTICS = this token's KRAKEN-book price-discovery cadence, NOT "ARCA open" (a 24/7 liquid token passes on a US holiday, correctly). Reason taxonomy (flat_last vs no_data/sparse/timeout) keeps a feed/config outage distinguishable from a holiday; index-bounded windowed read + Promise.race timeout-fail-closed (the depth gate has NO timeout → liveness strictly more defensive). 22 unit tests; full suite no-regress (9 fail = pre-existing env-only, proven on base). **DORMANT (§9.1 forward-instrumentation).** §13 homes: #391 (post-B7b flat_last block-rate tuning + null-config/spike alert + GOTU watch + the Promise.race orphaned-query residual) + #392 (deterministic holiday-calendar defense-in-depth, Phase-19 fill-safety follow-up — the stopgap that became a complement). Staging `2398702e8` restart#420, CI 4-green `28208230871`, migration applied. Governance: SYSTEM_MANUAL §7 fill-safety CONTENT + SIM S20 + RUNNING_ISSUES #236 RESOLVED/#391/#392 + BATCH_CATALOG + PHASE_HISTORY. **Gate-11 GREEN + gate-13 xStock-liveness half GREEN. NEXT: B6.7 (#301) → B6.8 (#302).** | Kyle + CC-B + Langston |
| 2026-06-26 | **P19-B6.7 CLOSED — vestigial 2nd-WS market-data subsystem removed + the feed-health alarm re-pointed onto the real feed (#301).** Pre-audit proved the `MarketDataCoordinator`+`MarketDataWebSocket` 2nd Kraken WS DEAD (runtime probe: `MD-WS_TICK`=0/`Sub OK`=0 across ALL logged history since April; blast-radius: 4 status-only consumers, 0 tick-output, 0 `subscribeToPair`) — yet its still-open TCP socket made every health/safety monitor read it "connected/healthy." **Two graded hazards removed:** (a) `feed-integrity-monitor` (boot-started, 5-min cron) graded the dead feed → would fire false CRITICAL `feed_health` alerts every 5min the moment Phase-19 lifts dormant-suppression (the LANDMINE); (b) `parity-gate` false-PASSED the Phase-21 go-live WS gate off it. **DELETED both files** (§15 archive+DELETED_COMPONENTS_LOG+tsc-zero-dangling); re-pointed all 4 consumers at the PRIMARY `krakenWebSocketAdapter` via new pure `market-data/feed-health-aggregate.ts`. **★3-axis aggregate, OPPOSITE by design (Langston Step-2 consensus):** the ALARM grades per-class FEED-LEVEL freshest-symbol age (won't cry wolf on one quiet illiquid pair) with the xStock class HARD-GATED on `isXstockMarketOpenUTC` per-symbol + a post-open WARMUP GRACE (so a reopened market's stale-at-close age doesn't false-fire at the bell) + market-closed suppression; the go-live GATE uses the conservative complement (proportion-of-symbols-fresh — kills the dead-feed false-PASS); display uses freshest. Per-class thresholds+grace = DB `module_constants 'feed_health'` (defensive — unwarmed→skip, never false-fire). **★Langston Step-4 (2 folded):** latency source → smoothed `avgHeartbeatLatency` proxy (honest "NOT true RTT") + null-driven-critical staleness sentinel. **30s `[MD-WS] Data stale` spam structurally GONE** (Langston Step-8 independent: zero matches over entire out.log). DORMANT alarm (deploy+structural+bench-proven, NOT live-runtime — staging dormant; FeedIntegrity:auto correctly skips; §9.1). **#396 SPLIT:** WS-error half = the dead 2nd-WS (removed); scanner-underfill half → PURE telemetry (OBJ-4) + FIX homed **NEW P19-B6.9** (with #398, the parity-gate uptime-cumulative-reconnects trap Langston flagged). Staging code `b337ede36` restart#421, CI `28266067266` 4-green, migration applied (5 `module_constants` rows). Langston Step-1/2/4/8 APPROVED (independent staging + governance-CONTENT verify). Governance: SIM (S21+2.1.1) + SysManual (§16+§8) + RUNNING_ISSUES (#301 RESOLVED / #396 split / #398 NEW) + BATCH_CATALOG + PHASE_HISTORY + completion report. **NEXT: B6.8 (#302 per-mode guardrail completeness), then B6.9 (the #396/#398 hardening) per sequence.** | Kyle + CC-B + Langston |
| 2026-06-27 | **reorg-B2.3 CLOSED — per-(strategy×class) minRR baseline + single-chokepoint canonicalization (#372/#375).** Replaced the flat global `min_rr=2.5` (over-suppressing the ~2.0-RR suite) with 13 data-derived per-strategy floors, each a notch below that strategy's OWN-class measured mean RR from the live 48h tracker (xStock from xStock's own data, Kyle 2026-06-23). `getPerClassTargetGate(assetClass,strategy)` made **REQUIRED** (Langston Step-4 CF-1 — tsc enforces all 21 callers, closing the §8#10 silent fail-open); single-chokepoint canonicalization REUSING the SSOT `normalizeStrategy` + a new fail-closed `resolveCanonicalStrategy` (no duplicate canonicalizer — the ★check-for-existing-component rule paid off); unknown token → fail CLOSED to `min_rr_unknown_floor` (max-per-class) + §13 tripwire; `rrSumSq`+`rrSumSqEvals` for Phase-25 25-20 σ (CF-2 restore-seam homed #399). **★ DECISION (D) — Kyle 2026-06-27: KEEP ACTIVE.** The three structurally-low-mean strategies (xStock morning_star, crypto volatility_edge/support_bounce) stay active at their data-derived 1.0-clamp floors; the trade-vs-shelve call is deferred to **Phase-25 25-20**, judged on realized win-rate × RR − friction (NOT reward-size — Langston's methodology catch: mean-RR ≠ Net-Expectancy). (D) changed ZERO floors this batch. Head `47286ccfd`, CI `28283869359` 4-green, deployed (HTTP 200, 18 rows, 48h window survived). Langston Step-1/2/4/8 APPROVED (Step-8 independent PASS). Governance: SYSTEM_MANUAL (reorg-B2.3 subsection) + SIM (singletons callout) + RUNNING_ISSUES #399 + BATCH_CATALOG + PHASE_HISTORY + completion report. **NEXT per plan: B6.8.** | Kyle + CC-B + Langston |
| 2026-06-29 | **P19-B6.8 CLOSED — per-mode guardrail completeness; the #302 premise was OUTDATED, reframed to surface-residual + cleanup (#302+#323).** Kyle directed a verify-everything pre-audit; it found #302's "migrate mode-blind `trading_settings`" premise stale — that table was purged (41F-L), the old `guardrails` table bypassed, and the LIVE guardrails tab (`CoreFourGuardrails`→`/api/guardrails-v2`) ALREADY delivers complete per-mode (paper+live) guardrails (DB-verified). **Kyle clarified the anchor (2026-06-27): the guardrails TAB must have a complete per-mode set; #323 folded in (50/75 alerts → auto-trip → manual restart).** Langston Step-1 consensus reframed B6.8 to: (1) surface the 50/75 daily-loss WARNING TIERS as 2 per-mode user controls (the one residual; DB-backed+firing since B6 but not user-settable); (2) §15-delete the stranded-dead GuardrailsTab + orphaned copy-to-live-modal; (3) verify the failsafe (already built). **★ Step-8 DB-cross-check caught a real persistence bug** — `upsertGuardrailsV2`'s UPDATE merge-map dropped the warning tiers (the 4th wiring place after extraction/validation/updatePayload); fixed `ffb737b0c`. Verified API/DB (per-mode persist + RULE_011 coherency) + §9.3 Claude-in-Chrome (2 new controls render, PAPER, GuardrailsTab gone). Head `ffb737b0c`, CI `28357436217` 4-green, deployed (no migration). #302 RESOLVED (already-satisfied + delta). §15 follow-up = **P19-B6.10** (old-table/endpoint retirement + 2 latent bugs RUNNING_ISSUES #400). Langston Step-1/4/8; governance: SysManual Ch4 + SIM + DELETED_COMPONENTS_LOG + BATCH_CATALOG + PHASE_HISTORY + RUNNING_ISSUES + completion report. **NEXT per plan: B6.9 (#396/#398 hardening) then B7a.** | Kyle + CC-B + Langston |
| 2026-06-30 | **P19-B6.8a CLOSED — guardrails tab → "Paper Guardrails", pinned-to-paper, big bold "Core Guardrails — Paper Mode" header (Kyle UI directive).** Kyle directed relabeling the guardrails tab as paper (big bold header instead of the small badge; tab button renamed) with a separate "Live Guardrails" tab to come later. Kyle clarified the architecture: paper active-trading, live active-trading, and VTS now run as SEPARATE CONCURRENT systems — each mode needs its own dedicated guardrails tab, not one tab on a shared toggle. **Decision (Langston Step-4 "right call, not just acceptable"): PIN the tab to paper via a REQUIRED `mode` prop** rather than the ambient global trading-mode toggle — `trading-mode-context` defaults to `'live'`, so a static "Paper" label on a toggle-following tab would render LIVE risk-limits under a Paper header on fresh load (a mislabel-of-risk, not cosmetic). Required (not defaulted) prop → tsc enforces every call site. UI-only (no server/endpoint/DB/migration). **Interim:** live guardrails UI-uneditable until the Live tab is built — DELIBERATE + acceptable (live dormant until Phase 21; live row persists; prop makes the live tab a one-liner). **§13 home for the deferred Live tab: RUNNING_ISSUES #401 + POST_AUDIT_ROADMAP §21.3 item 21-3a.** Head `240b7eec7`, CI green, deployed. Langston Step-4 APPROVED. **NEXT per plan: B6.9 (#396/#398) then B7a.** | Kyle + CC-B + Langston |
| 2026-06-30 | **P19-B6.9 CLOSED — parity-gate WS-uptime rolling-window fix (#398) + scanner short-universe §13-park (#396); pre-Phase-21 go-live-path hardening.** B6.7 §13 homed both here. **Verify-everything Step-2 (Kyle standing rule) reshaped the batch:** #398 is a real code-verified bug (the go-live WS-readiness gate's uptime derived from the adapter's CUMULATIVE lifetime reconnects / fixed denominator → a healthy long-lived process drifts below the 99% floor and spuriously fails the gate) — FIXED by re-basing onto a rolling-1h window (pure `computeRollingWindowReadiness`, denominator = snapshots present in the feed-integrity ring, self-healing; `MIN_READINESS_SAMPLES=6` fail-closed warm-up for the cold-start swing Langston flagged). #396, by contrast, is NOT reproducing — B6.7's deployed `[#396] SHORT UNIVERSE` telemetry is silent across 12k staging lines — so per §13 (CC-B + Langston consensus) it was PARKED with the telemetry-evidence + a concrete re-trigger (reopen when the marker fires + attributes the cause), NOT given a speculative fix. Langston's load-bearing Step-4 check (that `reconnectsSinceLastCheck` is a true per-interval delta, not cumulative) confirmed at `feed-integrity-monitor.ts:424`. The 99%-floor-vs-12-snapshot granularity (1 reconnect/hr → 91.7% → fails) is a Phase-21 THRESHOLD-calibration item (roadmap §3.5 21-3b), deliberately NOT re-tuned (gate dormant). Head `c1e5ef80c`, CI green, deployed (no migration), 37/37 tests. Langston Step-1/4/8; governance: RUNNING_ISSUES + SIM §2.1.1 + SysManual §16 + roadmap §3.5 + BATCH_CATALOG + PHASE_HISTORY + completion report. **NEXT per plan: B7a.** | Kyle + CC-B + Langston |
| 2026-06-30 | **★ PHASE-19 TAIL RENUMBER + BOTH-CLASSES MADE EXPLICIT (Kyle directive).** Resolved a real naming collision: the plan had TWO things numbered "~B7" — the §1-board "B7a/B7b" (paper UI shells + the switch-on) AND the reorg build-board's "rung-2 maker build" (informally "reorg-B7" — the maker/taker shared service). Kyle clarified "B7 = the maker/taker system" and directed the renumber. **NEW canonical tail:** **P19-B7 = ranking fix (B7.1, the make-or-break) → maker/taker shared service (B7.2; D6 rung-2, the real crypto opener) → crypto gate-10 lifecycle proof (absorbs B6.5g).** **P19-B8 = paper monitoring screens (BOTH classes) + THE SWITCH-ON (the flip folds INTO B8, staged crypto→xStock) + an EXPLICIT xStock closed-lifecycle proof** (folds old B7a + old B7b). **P19-B9 = sustained paper run + audit** (the B8-vs-B9 line: B8 = make-it-work/turn-on+debug; B9 = run-it-for-real + audit for Phase-25). The old "B8 = shadow layer" already shipped as reorg-B4 (its number reused). **Both-classes EXPLICIT (Kyle concern — the plan under-surfaced xStock):** the scan→filter→evaluate→signal→RTB→promote pipeline runs for crypto AND xStock TOGETHER (shared pieces, D1 — not gated); only active EXECUTION is one-class-at-a-time during the validation window (D7); both paper-working by phase close (D3). xStock's own end-to-end proof is now a NAMED step in B8 (was implicitly assumed to ride crypto's template). Updated: §1 board (B7/B8 rows + retired-B8 breadcrumb), build-order banner, §4 (lands in B8), §6 pre-flight (switch-on = B8; gate-9 → B8; gate-10 crypto → B7), POST_AUDIT_ROADMAP. **NEXT: begin P19-B7 (B7.1 ranking fix first).** | Kyle directive + CC-B |
| 2026-06-30 | **★ P19-B7.1 (ranking fix) DESIGN LOCKED + Langston Step-1 SIGNED OFF ("green to implement").** Kyle directed a field-first design (don't scope from internal plumbing availability; ground in industry prior art; **rank best opportunity REGARDLESS of asset class**). Two sourced external surveys + Langston + CC-A 2nd-eyes + internal deep-trace **all converged** on: **rank by the expected R-multiple `R = netEV ÷ risk_price` DESC** — risk-normalized, net-of-cost, cross-asset comparable (Van Tharp R-multiples / CTA vol-scaling / Kelly growth-optimal for sequential single-bets; the field-standard cross-asset normalization). Code-verify of 3 load-bearing facts: (1) sizer is fixed-fractional-RISK (`paper-position-sizing.ts:179`) → R-rank coherent; (2) netEV is PRICE-delta (`net-expectancy-kernel.ts:114-115`, friction ×entry) → `R = netEV÷(entry−stop)` dimensionally clean, NOT double-normalized (CC-A's inverted suspicion disproven in source — flag earned its keep by forcing the unit-test); (3) geometry gate is sign-only, no min-stop floor. **★ OBJ-2 simplified (CC-A finding):** the R-multiple ALREADY EXISTS as the kernel's `netRewardToRisk` (`:117`, with built-in `distStop>0` ∞-guard), just dropped at the wrapper — so OBJ-2 = a ~3-touch-point PASS-THROUGH (destructure + return literal + `TradeExpectancyResult` interface) NOT a new compute (reuse-over-recompute kills the unit-trap class); rank-time calls the PURE kernel directly = the no-double-EV-sample design. **5 OBJs:** pluggable ranker (default R-multiple, no hidden default) · surface kernel R-multiple · degenerate-geometry REJECT-primary + microstructure floor (min-ATR-fraction primary, capital-independent — Langston Q1 final: NOT the same number as the clamp-bind) · shadow R-multiple + per-cycle cross-sectional selection-IC (Phase-25 GO/NO-GO) · sizing-coherence invariant (≤R upper-bound) + effective-risk-fraction telemetry (absorbs notional clamp AND the `correlationScale` covariance modulator — CC-A A.2b). pWin calibration + fractional-Kelly + xStock dbsScore-gap + selection-IC go/no-go + per-class haircut = **5 dated Phase-25 §13 homes** (each in RUNNING_ISSUES). change-class=architecture (SysManual Ch1+Ch4 + SIM content gates). **NEXT: implement B7.1.** | Kyle + CC-B + Langston + CC-A |
| 2026-06-30 | **★ P19-B7.1 (ranking fix) CLOSED — shipped + deployed.** Head `0dacd34f2`, CI `28478843721` all-4-green, deployed staging restart#427 (2 migrations applied+self-verified; HTTP 200, boot clean; `active_ranker='r_multiple'` resolves on staging DB). All 5 OBJs landed (pluggable R-multiple ranker default · surfaced kernel `netRewardToRisk` + reuse-wrapper sample-free · degenerate-geometry reject + microstructure floor · shadow predicted-R/pwin_floored/cross-class + selection-ic.ts harness · sizer ≤R invariant + clamp-bind telemetry). **Langston Step-4 = CHANGES-NEEDED → APPROVED** after 3 real fixes landed (CHANGE-1 sample placement inside opened-gate; CHANGE-2 pwin_floored flipped from an INCOMPLETE strong-trend&&null-dbs derivation to a kernel-output read `pWin≤minPWin` — complete across all floor paths incl DI≤0; CHANGE-3 selection-IC point estimate period-weighted). CC-A 2nd-eyes clear. Q3 confirmed (rank-R/gate-R share the null-target `entry*1.02` default). Q1: found emit-stage GUARD-1 (`MIN_STOP_DISTANCE_BPS=30`) — dispositioned as defense-in-depth (different stage/basis) + a Phase-25 single-source-consideration home, not a same-gate split-brain. Bench tsc-baseline GREEN + 20 new + 51 affected tests. DORMANT (active trading OFF). 5 Phase-25 §13 homes = RUNNING_ISSUES #399. Governance: SYSTEM_MANUAL Ch1+ch11 + SIM + BATCH_CATALOG + PHASE_HISTORY + ADJUSTMENT_FRAMEWORK + RUNNING_ISSUES #399 + completion report. **NEXT: P19-B7.2 (maker/taker shared service) → crypto gate-10.** | CC-B + Langston + CC-A |
| 2026-07-01 | **★ P19-B7.2 (maker/taker best-of-both entry decision — the STRUCTURAL crypto opener) CLOSED — shipped + deployed.** Head `c595d987e`, CI `28486308234` all-4-green, deployed staging (migration via `db:migrate`: 16 `maker_taker` module_constants rows + 8 `rtb_signals` columns verified live in Supabase; b72-warmup passed, HTTP 200). Per-signal maker-vs-taker ENTRY decision computed ONCE at the shared build convergence `buildSizedSignalForStrategy` (F1-verified pattern bypasses `[HF9]`), snapshotted, read by the `[11.8B]` open-gate + the B7.1 ranker (single-consistent-number) → a taker-unprofitable/maker-profitable signal now survives to rank+open. **6 OBJs:** best-of-both via the SAME kernel (fee delta single-sourced per Langston Q1) · per-class DB haircut `pFill·(makerNetEV−A) − (1−pFill)·C` (non-fill an opportunity-cost LOSS, START TIGHT, urgency endogenous via continuation/reversal prior) · single-consistent-number invariant + named test · make-then-take + convert-safety in the RTB refresh (KERNEL not computeNetGeometry — CC-A landmine; atomic re-snapshot; slot-free-while-waiting + S1/S4-at-fill + mutual-exclusion) · #330 SPLIT→P19-B7.2a · maker-pick-rate monitor + data-fence. **Langston Step-4 = APPROVE-to-push (3 riders) → Step-8 = PASS** (deploy + Q1 structural close + Q3 SQE-dormant all verified live on staging). CC-A landmine-catch (kernel-vs-computeNetGeometry name collision). Bench tsc-baseline GREEN + 13 new tests. **DORMANT** (active trading OFF; §9.1 — real Kraken post-only resting-order lifecycle = Phase-21; haircut calibrates only on Phase-21 live fills → Phase-25). Q3: active SQE ROI gate DORMANT (guarded `if(entry&&target&&regime)`, unset on the active sqeInput `signal-orchestrator.ts:669`; Langston-confirmed `signal_quality_evaluator.ts:326`) → `[11.8B]` sole active EV gate; if ever activated, best-of-both moves before it (SysManual + report). New homes: #330→P19-B7.2a, #410 (haircut calibration Phase-21/25), #411 (pattern-vol at B8). Governance: SYSTEM_MANUAL + SIM (incl. `maker_pending` liveness registry) + BATCH_CATALOG + PHASE_HISTORY + PHASE_19_PLAN §1/§5 + ADJUSTMENT_FRAMEWORK + RUNNING_ISSUES + completion report. **NEXT: crypto gate-10 lifecycle proof (needs paper-active) → P19-B8 switch-on.** | CC-B + Langston + CC-A |
| 2026-07-01 | **★ MAKER/TAKER ARC RESHAPED + EXPANDED (Kyle directives) — master plan `Scope Files/P19_B7_2b_AND_BEYOND_PLAN.md` (SSOT for the arc).** **P19-B7.2b (in flight):** VTS now calls the shared maker/taker decision (before its Net-EV gate) + the active-path decision moved before the SQE (SQE stays calc-free); the RTB refresh RE-DECIDES maker/taker each cycle (load-bearing — feeds the B7.1 ranker queue order + the [11.8B] gate); the B7.2 in-queue `maker_pending`/convert-safety was the WRONG STAGE and was STRIPPED (§15 delete + 4 rtb_signals cols dropped) — a queued signal carries a DECISION only. Remaining B7.2b: score-timing fix, DELETED_COMPONENTS_LOG, fee-mode carry-through (4 stores) + UI column (4 surfaces), diff→Langston Step-4→deploy→gov. **★ NEW P19-B7.2c (BUILD NOW, not deferred — Kyle emphatic): the post-promotion PENDING maker-order lifecycle for PAPER + VTS** — a promoted maker order OCCUPIES A SLOT + shows PENDING until filled; PAPER/VTS fill = honest trade-through of the REAL Kraken price (fill only if the market trades through the limit); TIERED diminishing-returns timeout (fill-fast→high profit% … final→DROP; DB-governed; calibrate Phase-25; initial ~1h max-age) → on timeout convert-to-taker-if-[11.8B]-positive else drop. LIVE real-Kraken resting-order lifecycle = Phase-21. **Sequence:** B7.2b → B7.2c → B7.2a (#330) → **P19-B-RENAME** (files + DB tables: paper→active for the SHARED active pipes, VTS de-paper, delete dead old paper files, keep only true paper-only/live-only divergence; sign-off list first; AFTER the B7.2 arc) → crypto gate-10 → B8 → B9. Phase-21 homes: live maker resting-order lifecycle + the fill-timeout profitability calibration. §1 board + POST_AUDIT_ROADMAP + RUNNING_ISSUES updated to reference the plan. | Kyle + CC-B + Langston |
| 2026-07-01 | **★ P19-B7.2b (complete the shared maker/taker service — VTS wiring + fee-mode visibility + model correction) CLOSED — shipped + deployed.** Commits `a9480e4f8` + `8dc63772a`, CI `28526892658`+`28527184200` all-4-green, deployed staging restart#429 (migration via db:migrate; fee-mode cols on 3 trade tables + 4 maker_pending cols dropped from rtb_signals — Langston queried Supabase independently: 5 post-deploy crypto opens stamped `taker/0.008000`, legacy null; HTTP 200). **OBJ-A** VTS gates on best-of-both `chosenNetEV` + active-path decision runs standalone before the SQE (calc-free) · **OBJ-E** RTB refresh re-decides AFTER the decayed score, SINGLE atomic write (distStop invariant → ranker r atomic) · **maker_pending STRIP** (methods + refresh-branch + mutual-exclusion + promotion-POST + 4 cols; zero refs/readers, forward-only migration + nullable down; DELETED_COMPONENTS_LOG §15) · **OBJ-B** fee-mode on all 4 trade stores (NULL-not-guessed) · **OBJ-C** ONE shared `formatEntryFeeMode()` on 4 surfaces (RTB + paper open/closed + VTS ML open/closed; scope-correction: VTS surface = ML page NOT shadow-trades-tab). **Langston Step-4 APPROVE (independent diff verification) → confirm-B FIXED** (RTB refresh + VTS decision canonicalize the strategy key via `normalizeStrategy` → same-vintage urgency with gen-time `_canonicalStrategy`) **→ Step-8 PASS** (independent DB+deploy inspection). CC-A 2nd-eyes on the formatter. **§9.3:** VTS Open surface LIVE-verified (new crypto = Taker 0.80%, legacy = em-dash); the paper/RTB three render the same shared column but are empty until paper-active (B8) — honest boundary, Langston-accepted. Bench tsc-baseline GREEN + 13/13 + 2122 vitest. **DORMANT** (active trading OFF; the maker-order FILL lifecycle = B7.2c build-now + Phase-21 live). Governance: SYSTEM_MANUAL (decision-before-SQE + in-queue-lifecycle removal + VTS best-of-both gate) + SIM (rtb_signals shape + refresh + fee-mode cols + VTS decision) + DELETED_COMPONENTS_LOG + BATCH_CATALOG + PHASE_HISTORY + RUNNING_ISSUES #410/#412 + migration/MANIFEST + completion report + Langston MEMORY. **NEXT: P19-B7.2c (build-now paper+VTS pending maker-fill) → B7.2a (#330) → B-RENAME → gate-10 → B8.** | CC-B + Langston + CC-A |
| 2026-07-02 | **★ P19-B7.2c (post-promotion PENDING maker-fill lifecycle + VTS twins + never-filled records) CLOSED — shipped + deployed, with the lifecycle SIMPLIFIED BY KYLE mid-batch.** **Kyle decisions (LOCKED):** (a) "if it times out, it's dropped — that's the right way to handle that" → the tiered/convert-at-timeout valve was CUT (kills the #414 ordering hazard by construction); (b) marketable-at-placement → STORED gen-time `taker_net_ev` check approved ("fires off on the taker… I'm okay with that functionality in plumbing"); (c) VTS TWINS approved ("open both maker and taker in the VTS… learning from all the data") with chosen/not-chosen shown in the tables; (d) never-filled trades must SHOW in the closed records ("here was a trade that was open, it never actually traded") — implemented visible-≠-counted via typed discriminators. Head `b48aef51f`, CI `28612927459` 4-green, restart#432 w/ `db:migrate` (paper +3 cols, vts maker cols + CHECK+`pending`, 6 knob seeds — Langston verified all independently in Supabase). Shared pure `pending-maker-logic.ts` = paper/VTS parity by construction; zero-price fill-guard (Langston catch) on both paths; paper never-filled exclusion = storage-chokepoint typed SQL guard (Langston condition 4); twins slot/cap-exempt + close-cascade short-circuit + `twin_enabled` kill-knob; `maker_time_budget_ms` RE-PURPOSED (soft telemetry only, ADJUSTMENT_FRAMEWORK). **Langston Step-4 APPROVE-for-push (4 conditions → all dispositioned) → Step-8 PASS "cleared for close"** (independent DB/code verification from staging; governance-content push verified in the close commit). 11 new unit tests; bench 2133 vitest / tsc-baseline GREEN. **Soak home #433:** first pending/twin/never_filled occurrence evidence + the pending-notional exposure eyeball (Langston Step-8 rider). #412 RESOLVED · #414 CLOSED-overtaken. **NEXT: P19-B7.2a (#330 fee-resolver consolidation) → B-RENAME (#413) → gate-10 → B8.** | Kyle + CC-B + Langston |
| 2026-07-02 | **★ P19-B7.2a (#330 fee-resolver consolidation) CLOSED — shipped + deployed same day.** The residual second fee road (the cost-cache crypto lane: duplicate `resolveCryptoTakerFee`, per-symbol fee storage w/ 5-min TTL, `MAX_COST_BOUND` clamp on a GOVERNED value) removed structurally: the cache carries measured microstructure only; every fee read composes at read time from the B-4.5 merge site, class from each site's own context (Langston Step-2 CHANGE-1 — no blanket hardcode); 4 direct readers + 4 stat readers converted (both undercount catches — his and mine — folded via scope rev 2/3); tsc enforces (shape drop = compile error on any residual reader). Head `4b9d62fc9`, CI 4-green, restart#435, Step-7 live proof (642 symbols, all takerFee=0.008 merge-site). Named guards incl. the diverged-fees friction-identity leg. #330 RESOLVED with the TWO B81 forward-pointers (wrapper single-class avgFee + the telemetry ternary). **NEXT: P19-B-RENAME (#413, sign-off list first) → gate-10 → B8.** | CC-B + Langston |
| 2026-07-03 | **★ P19-B7.2d (#434 — xStock VTS lane maker/taker wiring) CLOSED — shipped + deployed same night.** Kyle sequenced B7.2d BEFORE the B-RENAME resume (same files). The gap: the xStock eval-cycle never ran `decideMakerTaker` (B7.2b's "VTS wired" was crypto-lane-only — the overclaim owned by CC-B + Langston, lesson logged). Shipped: the shared decision at the eval-cycle open seam (floor on best-of-both `chosenNetEV`), fee-mode stamp, B7.2c pending lifecycle (Q1: same shared registry → zero new resolve code), twins via the NEW shared `maybeOpenTwin` — crypto's inline twin block extracted under Langston's NARROW B79.0m.b lock-lift (twin sub-block only) with a both-branches crypto regression suite. Weekend suspend/restore hazard verified non-existent + guard-commented. Commit `127c6d845`, CI `28626488749` 4-green, deployed 22:52Z no-migration, Step-7 live (11/11 seed rows; decision running on xStock signals first cycle; ZRO pending twin survived restart). **NEXT: B-RENAME resume (Step-2 pre-audit) → gate-10 → B8.** | Kyle + CC-B + Langston |
| 2026-07-03 | **★ P19-B-RENAME (#413) CLOSED — the paper→active naming cleanup shipped in one overnight run (3 waves + residue, Langston-gated per step, TWO staged W3 deploys, soak PASSED).** All misleading "paper"-named active-path files/symbols/routes/events/keys/tables now carry honest active-*/closed_trades names; the persisted `paper_sim` learning discriminator KEEP-AS-DATA (#405, fence-tested); Walter tables deleted per Kyle; M5 harness FULL-SET deleted after the OPEN-3 evidence reversal. **BURIED-LOGIC FIND (#406): closed_trades has zero VTS writers — the Q5 close-migration is an UNWIRED design; VTS closes stay in-place in vts_open_trades + JSON. Interim invariant: closed_trades==0 while active-paper is dormant (tripwire).** **★ NEW NAMED GATE at B8 (Langston condition): the closed_trades growth/integrity soak gate re-homes to the B8 active-paper turn-on — on switch-on, verify active closes LAND in closed_trades (first rows ever) AND decide the Q5 design (wire the VTS migration or correct the header) AND verify heartbeat session recovery vs the #404 session.userId dormant-path.** CLAUDE.md rule-20 rewritten; SIM/SysManual carry the NAME KEY. **NEXT: gate-10 crypto lifecycle proof → B8 switch-on.** | Kyle + CC-B + Langston |
| 2026-07-03 | **★ NAMED GATE (Kyle question at the B-RENAME ack): the xSTOCK lifecycle proof gets its own explicit gate — "gate-10x" — at the xStock leg of the B8/B9 switch-on sequence.** gate-10 (crypto) is crypto-first ONLY because of the D7 validation discipline (ONE class active during the validation window) and because the maker build was the crypto fee-wall opener — NOT because xStock is exempt. When the switch-on sequence reaches xStock (B8 "crypto→xStock"), the SAME acceptance test runs: watch ≥1 FULL xStock trade open→exit→close→cooldown→telemetry on real inputs, PLUS the xStock-specific legs (market-hours/weekend boundary behavior on an open position + the B7.2d maker/pending path on the active side). Recorded here so the xStock proof cannot be silently assumed from the crypto pass (the #434 lane-scoped-claims lesson applied forward). | Kyle + CC-B |
| 2026-07-03 | **★ NAMING FIX (Kyle directive at the B-RENAME ack): "gate-10" / "gate-10x" are RENAMED to fit the batch-naming system — they are now the ACCEPTANCE CRITERIA of P19-B8: `P19-B8-AC1` (the crypto lifecycle acceptance proof — one full crypto trade open→exit→close→cooldown→telemetry verified at the crypto switch-on) and `P19-B8-AC2` (the xStock equivalent + weekend-boundary + maker/pending legs, at the xStock switch-on leg).** Rationale: "gate-10" predates the 2026-06-30 renumber (it was checklist item 10 of the B6.5-era crypto-resurrection audit) and carries no sequencing meaning — exactly the reorg/rename-era label problem Kyle flagged. Dated history entries below keep "gate-10" as written (this row is the decoder); the LIVING board rows use the new names. | Kyle + CC-B |
| 2026-07-03 | **★ B8 DESIGN LOCKED (Kyle design-intention discussion → CC-B/Langston triple-verdict consensus + commissioned field-research pass; record = `Langston Design Asks/P19_B8_DESIGN_INTENTIONS_v1.md` + CONSENSUS ADDENDUM + `P19_B8_BALANCE_POLICY_FIELD_RESEARCH.md`).** Three mode pages (Live top / Paper / Virtual Simulations), per-page start/stop (top-bar toggle retired; VTS has none), Shadows last-tab Paper+Live, Pattern Scanning DELETED, ghost balance defaults (10000/1000/800) wiped-with-replacement. **Balance policy (§4, Kyle A/A/A):** Kraken-mirror start ('new' only; 'continue' resumes persisted, never re-fetches; fail-hard on unreachable Kraken), compound freely pre-live, **AUTO re-anchor at a per-class PHYSICS trigger** (friction-divergence bps via the square-root impact law + min-notional breach — NOT a balance multiple; ratio = dashboard telemetry; ADJUSTMENT_FRAMEWORK knob), **hard re-anchor at live-launch**, re-anchor ≠ learning reset EVER, dollar-agnostic decision-path fence (thresholds/comparisons %/R/bps; enumerated permitted boundary = sizing notional + exchange min-notional + fee comp; convert-don't-enumerate on $ risk caps w/ per-conversion Kyle sign-off; CI property test). No scenario balances v1. **Higher-balance learning (#408 → enriches 25-16):** config-not-dollars verdict; scenario-tagged config sweeps on the ONE paper env; NO second standing engine unless the fence audit fails. Intra-B8 restructure: B8.1 UI reorg → B8.2 balance → B8.3 dashboards → B8.4 switch-on+AC1/AC2 (AC1 both-legs = hard gate). | Kyle + CC-B + Langston |
| 2026-07-04 | **★ P19-B8.1 CLOSED — the three-mode-page trading-UI reorganization shipped** (head `111b9d349`, restart#441, CI 4-green; Langston Step-1×2/2/4/8 ALL PASS, Step-8 independently reproduced; Kyle full-visual-audit walked every page/tab). Live/Paper/VTS pages via ONE shell + manifests (the mode-axis contract); toggle+modals → PaperTradingControls; ML page → calibration home; Pattern Scanning + Filter Insights deleted rule-18 (**re-trace SHRANK the delete set — pattern-pool dispatcher/filters KEPT: SQE/sizing consumers**); 4 diagnostics defects fixed at true sources — headline: the **module-constants delete-then-refetch refresher race** (live ~hourly, 2 modules) → swap-on-success + SWR w/ bounded+logged staleness in all 3 sync readers. Pre-audit §5 (b)/(c) determination CORRECTED at implementation (client shape-read + hardcoded-0 emission, not accumulators). Deferred w/ dated homes: xStock counter visual proof → **2026-07-06 system-alert (Jul-4 US market holiday), owner CC-B**; C4 modal-fire → B8.4. NEXT: B8.2 balance-policy scope (friction ESTIMATOR = Langston's first-read item). | Kyle + CC-B + Langston |
| 2026-07-05 | **★ P19-B8.2 CLOSED — the balance policy shipped same-day scope→deploy** (heads `3ac778d0c`+`71c9d81bd`, migration applied+live-verified, restart#443, CI 4-green ×3; Langston Step-1/2/4 + Step-7-delta ALL PROCEED w/ every condition evidence-cleared). Start-new = the Kraken-mirror (read-only confirm, fail-hard, NO input field; continue never fetches); 7+2 ghost deletions — the +2 being the hardcoded **$10k exposure/drawdown denominators (RISK-029, ~11× exposure understatement at the real balance)**; the $25k ghost row + the orphaned `dawntrader_v2` schema-copy deleted; anchor-event ledger + auto friction-divergence re-anchor (24h cooldown; learning structurally untouched — proven byte-identical at the synthetic live-row proof); the at-open ratio stamp (OLD Claude's 5 constraints verbatim); the dollar fence w/ red-proofed CI scan (ZERO live-path $ violations — the "5 violations" read was the two-guardrails-tables conflation; **Kyle sign-off list EMPTY, proven by reader enumeration**); #410 RESOLVED. **DECISION (Step-7, live-evidence-forced): the mirror pin widened from ZUSD-only to allowedQuotes parity (USD+USDT+USDC)** — the first real fetch showed the account is 100% USDC (824.11), zero free ZUSD; the strict pin would have refused every start (#435 narrowed → Phase-21 residual). #436 (throwing legacy-guardrails write trio) → B6.10. C4/C6 soak riders → B8.4. NEXT: B8.3 (dashboards + metrics-strip move + three-balances labeling + the Live-page mode-badge fix). | Kyle + CC-B + Langston |
| 2026-07-06 | **P19-B8.2b (hotfix): the Reward-vs-Risk/Reachability Gate card moved below the 24-Hour Rolling Aggregates** on all six FD tabs via the one shared panel (head `04d835d42`, CI 4-green, Langston byte-identical-move PROCEED, visual-verified crypto+xStock). Same-morning findings recorded: the "zero VTS trades" scare = a DISPLAY defect (silent empty tables on a failed/expired-session fetch — error-state fix → B8.3, with the gate relabels: true Rejected total + plain reason names); the open-rate dip = the Jul-3 holiday + weekend xStock dark window + normal weekend crypto thinness (cap 500 vs 191 used — no brake). | Kyle + CC-B + Langston |
| 2026-07-06 | **★ P19-B8.3 CLOSED same-day — per-mode dashboards + mode-honest FD tabs** (head `6ba933708`, restart#446, CI 4-green; Langston Step-1×2 / Step-2 staged-v1 / Step-4 PROCEED×2 + delta / Step-8 PASS — he independently reproduced all 5 endpoint checks). Dashboard landing tab on all 3 mode pages (Kyle's widget template + realized-balance chart + byAssetClass/byStrategy); analytics mode-parameterized + 6 new metrics in pure unit-tested math; **netPnlPercent always-0 root-fixed** (anchor-balance denominator, null-honest); **profitFactor null-on-no-losses** ("∞ (no losses)", never "0.00" on an all-wins window); calendar earnings survive an empty selected window (both zero-shapes); FD disposition model (VTS = Dropped/Tagged, Paper/Live = one true Rejected=Evals−Passed, identity test-pinned + verified exactly on live data); metrics strip re-homed to the pages w/ three-balance labels; visible fetch-failure states everywhere. **DECISIONS:** #417 (VTS funnel sub-blocks inside the shared scanner tables on Paper/Live — intended-but-incomplete) homed to **B8.3b** (Langston conceded: earlier than his B8.4 preference + data-and-display-together); #415/#416 riders → B8.3b. **SEQUENCE: B8.3b (pre-declared architecture class) → B8.4 THE SWITCH-ON.** Kyle decision pending at ack: legacy /dashboard retire-vs-redirect. | Kyle + CC-B + Langston |
| 2026-07-07 | **★ P19-B8.3b CLOSED same-day — the dashboard fast-follow** (heads `9e91245ab`+`23047f291`, restart#447-448, CI 4-green ×2; Langston Step-1 Option-A / Step-2 hard-gate-cleared / Step-4 APPROVED / Step-8 PASS, all independently staging-verified). Re-declared `non_architecture`. **Option A (Langston-approved, verified on staging): the fx5-scanner is MODE-MULTIPLEXED — keyed on `isEngineActive`, it BECOMES the active funnel at the B8.4 switch-on — so no dual-funnel build was needed; B8.3b is display-honesty + a dead-field retire + 2 riders.** `scanDiag.destinationCount`+`totalDestinationCount` RETIRED (mislabeled serialized-but-unrendered; the `routes.ts:7809/:7853` familyFanOut FD-response survivor untouched — naming-collision carved out). The #417 display separation: all 3 VTS-runner blocks gated to 'tag' + honest enforce placeholders; "→ VTS Destination" → "→ Survivors (shared scan feed)" on enforce. #415 headline netPnl → canonical net basis (gross→net; trace = benign not a friction gap). #416 balance-curve carrier seeded. **PROCESS: the §9.3 visual walk caught a THIRD VTS-runner block (the 24h-rolling eval block) the diff review structurally could not** (an ungated block isn't in a diff) — fixed in the follow-on `23047f291`, re-verified live; CHANGES_AND_FIXES FIX-2026-07-07-A carries the lesson. #415/#416/#417 RESOLVED. **DECISION: #418 → B8.4 named gate** (the switch-on active-path closer MUST populate `closed_trades.net_pnl` — the `default('0')` bridge would silently zero a real trade out of the displayed totals while the reconciliation test stays green). ON-state enforce funnel render → B8.4 §13. **NEXT: B8.3c** (Kyle-directed open/closed trade-count restore atop each tab — dropped in the B8.1 restructure; OLD-Claude-routed) → B8.4. | Kyle + CC-B + Langston |
| 2026-07-07 | **P19-B8.3c (tiny Kyle-directed, via OLD Claude): the open/closed trade COUNT restored atop the trade tabs** (dropped in the B8.1 client restructure). VtsOpen/VtsClosed show `(data.trades.length)`; ActiveTradesV2 "Active Trades (N)" (mode-scoped, shared Paper+Live). Finding: TradeHistoryTab (Paper/Live closed) already showed "{totalCount} total trades" — NOT dropped, left unchanged (no redundant count) → 3 components / 2 files, not the four the trace assumed. Counts = rendered rows (`.length`) so header-vs-table can't disagree. Head `315e9b680`, CI 4-green, Langston Step-4 PROCEED (source-verified count-matches-rows) + Step-8; Paper "Active Trades (0)" §9.3-confirmed live (VTS-tab nav blocked by a Claude-in-Chrome Radix-tab limitation — the VTS counts rest on Langston's source verification). non_architecture; SIM/SysManual N/A. OLD Claude owns the Kyle visual-verify follow-through. **NEXT: B8.4 THE SWITCH-ON.** | Kyle + CC-B + Langston |
| 2026-07-16 | **★ B-STAGING-LIVENESS-WATCH CLOSED same evening, 2 days ahead of the Friday deadline (#512a + #520 RESOLVED; Langston Step-1/2/4 ALL PASS; head `2d163cf08`).** Out-of-process watchdog + Helsinki host-down probe (both alarm paths drill-proven) + the engine-restart fix — the boot session sweep deleted, unattended resume PROVEN TWICE the same evening; the manual continue-start deploy ritual is retired. **★ #523 same evening: Kyle OVERRODE the [11.8B] netEV open-backstop ruling** (a backdoor gate around the refresh-suffices decision) — Langston concurred + amended his own call; converted to watch-and-alarm in paper after code-proof the refresh evicts negative-EV rows; LIVE keeps the block pending Kyle's ratification at the #522 pre-live gate. **★ Work division (Kyle-directed):** OLD Claude takes VENUE-ONLY-AT-SOURCE (handover posted: actionable path already venue-only since 347e9534b; his batch = remove the FETCHING + display-chain REST sites + the rule-18 third-party-display retirement) + FD banners later; CC-B keeps B8.7 Step-9 tables (+ Kyle's RTB column set). | Kyle + CC-B + Langston + CC-A |
| 2026-07-27 | **B-TSC-BASELINE-FIX SHIPPED — the CI type-check safety net (#579 RESOLVED); governance-tooling, does NOT move a switch-on gate.** The CI type-check gate could wave through a NEW type error if the same file already carried a baselined error of that code (A0's 2 phantom errors passed green). Rebuilt to key on the error's full identity (file + code + exact message) so a new error is always distinct and caught, even under a stale ceiling — and it also catches a one-for-one swap a count-only gate misses. Getting there surfaced + fixed three deeper reliability holes the gate had, each caught by a real red cold-CI run (the gate proving itself): the check now runs the type-checker fresh every time (a cached run had reported a wrong count), strips machine-specific file paths out of the messages (they differ between a laptop and the CI server), and renders long type messages in full before trimming to a fixed length (the type-checker was cutting them at different points on different machines). Head `a587408bd`, cold CI 4/4 green (`30298671677`, independently API-verified by Langston), no deploy (not runtime). Owner CC-B; Langston Step-1 + Step-4 approved. |
| 2026-07-27 | **P19-B8.5h SHIPPED — the xStock regime-score carry (#560 ≡ #377 RESOLVED).** xStock's regime score (DBS) was calculated but never reached the final buy-decision gate — the gate only pulled it from the crypto-specific data pool, which xStocks don't flow through — so xStock strong-trend setups were pinned at the lowest confidence floor and filtered out. Same defect we identified in June (#377), fix direction already reviewed. Fix: the gate now reads xStock's regime score from the regime engine's own memory (a class-aware source), crypto untouched; the separate directional-strength number is deliberately left for a later Phase-25 pass (it doesn't move the buy decision + is on a different scale). Head `8345718fd`, CI 4/4, deployed; Langston Step-1/2/4 approved. **Live confirmation DEFERRED to an automatic check** — the ready-to-buy pool is empty (the #570 drought), so no xStock signals to observe yet; the check confirms + reports the moment they flow. Deploy also surfaced a non-urgent engine session-record gap (filed #585). Owner CC-B. |
| 2026-07-27 | **P19-B8.5l SHIPPED — B-ATR-SOURCE-FIX (#581 RESOLVED): the shared-volatility root cause is fixed.** The pattern-detection pass (a separate pass after the main per-symbol loop) re-stamped regime/DBS per pattern-symbol but MISSED `sizingContext.atr` — so every pattern signal read the LAST quant symbol's stale volatility (the shared value the B8.5k live check caught). Fix: the pattern pass now re-stamps `sizingContext.atr = context.indicators?.atr` per pattern-symbol (RAW, not the synthetic-fallback local — gate parity per Langston), and the B8.5k carry to positions is re-enabled now the source is correct. Head `adf1002b1`, CI 4/4, deployed. Code + two independent reads (CC-B + Langston) + 5 source-guards verified. **The live "≥2 distinct volatilities per cycle" fence is DEFERRED** — no signals are reaching the buy-pool (the #570 net-EV/fee drought, working-as-designed, pre-dates this deploy; Langston independently confirmed the empty pool) — homed to a self-rescheduling CC-B alert (`p19-b8-5l-atr-fence`). #556 re-carry RESTORED + PENDING-VERIFY. |
| 2026-07-25 | **P19-B8.5k ROLLED BACK — the ATR carry (B-ATR-RESTORE) surfaced a real hidden defect and was reverted.** Restoring the dropped `atr` carry made the value visible for the first time; the §9.3 live-data check proved `sizingContext.atr` is a SINGLE SHARED value per scan cycle, not per-symbol (`0.0075` identical across ZEC/XRP/ONDO/LTC/LINK/SOL/ETH/AAVE; atr/price 0.000004→0.0196). tsc + full vitest + Langston Step-4 ALL passed it clean — only inspecting the live values caught it. The wrong value reached the live RTB rank risk floor + new-position training data. **Carry reverted (`4dc65b8f2`, deployed + verified atr-absent); 7 window positions quarantined (`atr_at_open`→0); #581 filed (owner CC-B, `B-ATR-SOURCE-FIX`) — root-cause discriminator FIRST, closed_trades sweep, re-carry only behind a ≥2-distinct-ATR-per-cycle fence test; #556 stays OPEN behind #581.** Net code zero; deliverable = the #581 discovery + the exit-neutrality tests + the quarantine. **Meta-lesson: the §9.3 live-DATA verification is load-bearing, not a formality.** |
| 2026-07-24 | **P19-B8.5j SHIPPED - the MAX-HOLD master switch (`#577`), the 24h force-close PAUSED.** P19-B8.5f repaired the maxHoldingMs carry, so the 24h max-hold exit fired for the first time in history (4 xStock vwap_pullback force-closed at exactly 24.00h, 3 at a loss). The 24h is the crypto-era 24-bars-x-60min value; xStocks run 15-min bars (24 bars = 6h) and have no row - a mis-calibrated, undebated rule that only went live because the carry got fixed (Rule-24 outcome 2, Kyle's policy call). **Kyle: no max-hold until we debate it.** Three DB-governed booleans (`max_hold_switch`, lane-keyed) gate the two force-close sites; ENFORCEMENT gated not stamping, so open positions are protected on deploy; fail-safe absent->OFF. **Seed asymmetric: paper/live OFF (the trade rule Kyle paused) but VTS ON** - Langston found (the scope missed it) that the VTS max-hold is the 7-day zombie-cleanup memory-safety valve, and seeding it off re-introduces the pre-Batch-18I unbounded-trade-map bug (B63->B64, `BATCH_64_SCOPE.md:91`); surfaced to Kyle before the migration ran, Kyle confirmed keeping the valve on. Deployed migration-first, 0 max_holding_period closes since. **The max-hold VALUE debate is homed `B-MAXHOLD-POLICY` - Kyle-gated, not scheduled.** Also makes #569's 'can't close on time' concern moot while off. |
| 2026-07-23 | **★ P19-B8.5i SHIPPED — the trailing master switch (`#562` RESOLVED).** Trailing now has an explicit on/off control in the same shape as `breakEvenEnabled`. **Kyle's ruling: TWO flags (one VTS, one active), gated at the single chokepoint `isMoonbagQualifier`** — this OVERRODE Langston's Step-1 Condition-1 options, because per-lane flags put the VTS sites IN scope instead of declaring them out. **Behaviour-neutral by construction: both seed FALSE — trailing stays OFF per Kyle's standing directive**, and the live data agrees (all 14 open positions `trade_mode='TARGET'`, zero `TRAILING_TAKE`). **Re-applied after a revert:** the first attempt (`21db08228`) went in red because a new `requireKey` hard-fails `primeTECConfig` in EVERY DB-mocking TEC test and only 1 of 8 was seeded; reverting to unblock the shared branch was the right call. **The re-apply also found a contradiction IN the approved diff** — the keys were `requireKey`d but never registered in `ALL_TEC_KEYS`, which the `b79-0n-tec-b` tripwire is built to catch (TEC keys 11 → 13). **Deploy was migration-FIRST with all 8 rows verified FALSE before any restart** — the discipline the B8.5e outage taught. **Surfaced `#573`:** the Paper dashboard's "Active Trades" card reads the retired `paper_trades` table (structurally always 0) while 15/15 shows correctly on the summary bar — a per-surface source split, homed as `B-PAPER-LEGACY-TABLE-REWIRE` (Langston: a PREREQUISITE of OPEN-2, not a passenger on it). |
| 2026-07-23 | **★ #569 REWRITTEN — its overnight-hold DEFECT CLAIM WAS RETRACTED (Kyle forced the thorough review).** I had claimed the system holds xStocks overnight and cannot exit them until US open — FALSE: xStocks closed overnight (SKHY/PDD stop_hit at 05:46 UTC), there is NO US-hours gate (only the weekend shutdown; B-NEW-36 retired RTH gating), and exits run continuously (just sparser off-hours). What survives: the slot pool is shared (`:1455`), and whether to segregate it is an OPEN DESIGN QUESTION — NOT a defect — carrying Kyle's constraint that allocation stay merit-based (no fixed per-class split) and that paper-vs-live behaviour be distinguished. The off-hours ceiling interaction folds into #566. **`B-OFFHOURS-BEHAVIOUR` is now a design-discussion doc, not a code batch.** Superseded row (was): , surfaced by Kyle as the real systemic stakes behind the freshness work.** Three facts verified in code: the **slot pool is SHARED across asset classes** (`active-execution-engine.ts:1455`), so xStocks filling all 15 slots starves crypto to zero; after the US close xStock marks go stale; and **a stale-marked position cannot close on TIME** because the B8.5e freshness gate `continue`s (`~:953`) UPSTREAM of the max-hold force-close inside `checkExitConditions` (`:1267`/`:1626`). NET: xStock slots jam through off-hours (no price-exit, no time-exit) and starve crypto. **This is a Phase-19 DEBUG item** (paper-active misbehaves at the US close), NOT a Phase-25 calibration residual, and NOT the same as #566 (σ-estimator). Deliverable = an **OPTIONS PAPER to Kyle** (rule-24 outcome-2 scope call, not unilateral code); candidate options include per-asset-class slot pools (leads — costs no learning either side), a stale-position time-exit that fires without a fresh mark, no-straddle-the-close entry rule, session-aware holds. Kyle decides the policy. Owner CC-B; sequence: after the active QUEUE-2 trailing-switch. Industry research done (session-aware overnight management). |
| 2026-07-22 | **★ P19-B8.5e SHIPPED (deployed, CI 4-green on `a4508353b`, Langston Step-4 APPROVED twice at the ref, §9.3 UI-verified) — the mark-staleness ceiling (`#548` RESOLVED).** The flat 90s xStock constant is replaced by `clamp(budget/σ_effective, floor, cap)` with `budget` a fraction of remaining room-to-stop ⇒ tolerance shrinks as danger rises; 3 new modules, 10 governed knobs, boot assertion (xstock_spot ONLY), old knob RETIRED per §18. Open Trades now shows 15/15 with a live Current price on every row (`positionsEvaluated=15 withoutPrice=0`). **Two fail-OPEN holes were found IN REVIEW and fixed at the find** — stale-LOW σ widening the window (ANALYST) and the class-wide σ never expiring (Langston); in both cases the module header claimed a fail-closed property the code lacked. **LULD dropped on arithmetic** (σ ceiling is structurally tighter than the 5% band below a ~727s cap). **INCIDENT: staging down ~2 min** — boot assertion added without registering `mark_staleness` in `PREFETCH_MODULES`, and the assertion's own text blamed a migration that HAD been applied ⇒ standing lesson: *a fail-closed assertion must not assert a CAUSE it has not checked*. Residuals homed, NOT closed: #563 (venue-resting exits, the real fix), #566 (σ spike-lag), #567 (value-plausibility, re-homing task #85 whose "superseded by LULD" note this batch falsified). |
| 2026-07-22 | **★ P19-B8.5f SHIPPED (deployed `58d8f8f94`, CI 4-green, Langston APPROVED at ref) — the metadata-rebuild drop: ONE root, THREE symptoms; #549 + #550 merged and B8.5g retired into it.** `signal-orchestrator.ts` rebuilds the sized-signal metadata from an EXPLICIT FIELD LIST and never spreads `rawSignal.metadata`, so `maxHoldingMs` and `atr` died there while `regime` (riding `_displayContext`, which IS spread) survived — **the 0/15-vs-13/15 asymmetry was TWO transit paths, not flaky data.** **OBJ-1** carries the max-hold (the 24h time-exit had NEVER fired: 0 of 15 positions carried it, 0 `max_holding_period` closes in all of `closed_trades`). **OBJ-2** replaces `Record<string, unknown>` — *the hole*, since it accepts any object — with a required-field transit contract, so a future omission is a COMPILE error, not a silent drop (CC-A's correction: an assertion list is itself a hand-maintained allow-list). **OBJ-3** `atr` deliberately NOT restored — B6.5b's floor governs it and restoring it would silently re-activate trailing that has never run on the active path; **Kyle scope call, surfaced not shipped.** **★ OBJ-5, the pre-audit's catch and invisible from the diff:** SysManual rated RISK-035 LOW *because the exit never fired*; OBJ-1 makes it fire, so time-exit closes would have landed as `'UNKNOWN'` in the trade tables — paired fix + RISK-035 now RESOLVED. ⚠️ **NOT YET CLOSED:** the live carry is UNIT-proven (5 fence tests fail on pre-fix sources, pass post-fix) but unproven in production — all 15 open positions predate the deploy, so 0/15 is EXPECTED not failed. Verification alert armed (fires 18:00Z) with an explicit REOPEN instruction. |
| 2026-07-16 | **★ Kyle evening directives (booked): (1) RTB-table column set FOLDED INTO the B8.7 Step-9 layout-identity batch** — asset-class column; stacked symbol+name (company/coin, VTS-style); fix the EMPTY Regime + Friction columns (data-wiring, not cosmetic); add DBS; add netEV (the expected-net-EV the admission runs on); **rankingScore REPLACES Final Score** (finalScore is retiring per the ratified scoring redesign; the pool is consumed best-ranked-first via getRankedSignals — verified at engine :2108); **Confidence column REMOVED** (it does not feed the R-multiple ranker — B8.5a severed it; RETIRE-as-input per the Phase-25 blueprint); maker/taker column already present. Live-mode tables stay stubbed → #517 (Phase-21, Kyle re-confirmed). **(2) #522 END-OF-PHASE-19 FULL RUNTIME PIPELINE AUDIT** — the closing gate of the phase: live-behavior verification that nothing legacy/hardcoded silently shapes evaluate/select/open/close; carries the AMR execution_entry confidence-leg consolidation check. | Kyle + CC-B |
| 2026-07-16 | **★ P19-B8.8 sizing-fallback fail-loud sweep SHIPPED same-day (#516 pulled forward per Kyle's fix-on-find directive; full workflow, Langston Step-1/2/4 ALL PASS, deployed `7de34c03d`).** The trace found the most serious hidden fallback of the arc: the position sizer itself carried backup numbers it silently substituted on a failed guardrails read — including one that quietly UNCAPPED portfolio exposure to 100%. Proven three ways that a missing value can only mean a real fault (schema forbids NULL; the sizer receives a full row or nothing; both live mode rows queried populated), then the whole 7-item family retired in one design: the sizer now refuses the signal loudly (never a fabricated number, never a broken loop) with a consecutive-refusal alert rail; the kill-switch and LPCP defaults, two fail-closed trade-safety checks, the goal-feasibility 100%-loosening default, a config route that served an entirely FABRICATED phantom guardrails row (now honest 404), and the m5e private guardrail copy all swept. Homes: #518 dormant-LPCP dated disposition (Phase-20), #519 daily-loss runtime-trip confirm (Phase-20), **#520 engine-halts-silently-on-every-restart (Step-8 finding: the orphan-recovery sweeper closes the running session at boot; folded into the #512 Friday liveness-watch batch as a second objective)**. AC2 clarified: the two 16:26Z xstock closed rows are never-filled maker pendings (B7.2c lifecycle, NULL pnl correct) — the first REAL xstock close is still pending. | Kyle + CC-B + Langston |
| 2026-07-16 | **★ P19-B8.7 trade-table parity SHIPPED same-day (Kyle's morning table findings → full workflow → deployed + §9.3-verified).** The three screenshot defects root-caused with evidence (CLASS = the API never sent the field; Strategy = WHITE-ON-WHITE text, present-but-invisible via a stale color-map fallback; slots "of 5" = a display-only exposure ratio the engine never enforces) and fixed at the source; slots re-keyed to guardrails max_open_positions at both display sites (kills the false OVER LIMIT); the hidden-fallback family retired with Langston's safe-degrade semantics (admissions halt loudly for a tick, loop alive — never a fabricated cap); +8/+5 VTS-mirror columns on paper open/closed; dynamic-slots deleted rule-18. Homes minted: #515 capture gaps, #516 P19-B8.8 sizing-fallback sweep (Langston: home-not-fold), #517 live-mode table wiring (Phase-21 pre-activation). OBJ-5 (paper-richer columns INTO VTS) + OBJ-6 (Closed-Trades analytics box delete) await Kyle rulings. Shared-tree commit incident (OLD Claude sweep of a staged deletion) owned + reverted in-channel; both sessions tightened commit habits. | Kyle + CC-B + Langston + CC-A |
| 2026-07-15 | **★ P19-B8.6 SHIPPED same-day (maker TARGET-exits) — the exit side of the maker/taker measurement now exists; runtime proof = open live-watch.** Kyle's urgency directive (“observing entry-only doesn't make sense”) executed as a full-workflow batch inside the B8.5 soak: exit-rest lifecycle live on paper (place at target-touch / fill=limit+maker-fee / deadline-convert books taker friction / absolute stop precedence / D2 never-strand), 4 cohort stamps feed the fills-vs-converts pFill denominator. Langston Step-4 caught (a) the convert path losing its rested-at price via the closePosition re-fetch (fixed: explicit exitRest option, all 3 sources) and (b) the B8.5 ENGINE leg (venue-only chain/C-gate delete/skip rail) sitting uncommitted while believed deployed — co-shipped + Step-8-verified live ([VENUE_ONLY] rejecting binance → direct Kraken REST). ALSO: the Open Trades whole-page crash root-caused (volume-tiers bare-string contract vs the client's typed object) + fixed; sizing tune-2 (risk 2.70→1.95% off 5 measured ~$209 opens → ~$150 pins). VTS exit parity armed at #513 (P19-B8.6v). | Kyle + CC-B + Langston |
| 2026-07-14 | **★ P19-B8.5c CLOSED — the #503 friction-units fix; the CLEAN VTS-EV calibration era starts today.** Kyle's opens-drop + taker-dominance investigation answered at the root and fixed same-day: the fraction-as-dollars kernel friction bug (both VTS standalone calls) DELETED via Langston's Step-1 design cut (one computation site); the 07-03 xStock admit collapse ruled CORRECT (the honest fee wall); taker dominance ruled BY-DESIGN (pFill=0.50 START-TIGHT until live fill data). Step-8 deploy-boundary split: 938/963 impossibility violations pre vs 0/22 post. ⚠ Whole-day VTS telemetry counts filter at 2026-07-14T10:14:49Z. With #500's DI boundary: all VTS learning data from 2026-07-14 forward is honest on BOTH DI and friction. | Kyle + CC-B + Langston |
| 2026-07-13 | **★ P19-B8.5b CLOSED — the pre-flip CAPTURE micro-batch; TWO of the four pre-flip gates now SEEN.** Same-day follow-through on the sequence reset below: the capture batch shipped (heads `3aab09d99`+`fe5040d47`, Langston Step-1/2/4/8 ALL PASS). Decisions ratified in-batch: (a) BOTH+substitution for #206's integrity leg — six decision-time scalars (CURRENT volume, not avg-volume: ZERO scalar consumers proven by-construction) + the `swh1:` settled-window hash, typed columns; (b) #500's proxy DELETE is a delete, not a fallback (rule 18) — honest-absent semantics ride the kernel's documented default; the two-DI-formulas divergence found in-flight got a named home (#502 → 25-4a) instead of a silent reconcile; (c) #498's regimeStability leg ruled INTENTIONAL-DEFERRED (contaminated gen-side source — the same confidence axis B8.5a severed) → 25-4b; sourcePool leg SHIPPED. Step-9 process lesson on record (FIX-2026-07-13-B): unit tests passed while the live hash wrote zero rows (ESM `require` eaten by a never-throw catch) — the Step-7 live-populate check is the gate for capture wiring, permanently. #206's defined-exit alert REGISTERED (`83afc970`, 2026-08-03T12Z). **Flip readiness: #500 ✅ + #498 ✅ (this batch); #499 + #501 (both CC-A) remain — B8.5 flips only when all four are SEEN.** | Kyle + CC-B + Langston |
| 2026-07-13 | **★ P19-B8.5a CLOSED (switch-on prep) + THE PRE-FLIP SEQUENCE RESET (Kyle).** Kyle ratified the PART II scoring redesign + green-lit prep-then-switch-on; B8.5a shipped same day (head `dbfb14460`, restart#468, Langston Step-1/2/4/8 ALL PASS + OLD Claude dual Step-4/8): ranker de-tinted (finalScore→measured per-class base rates, DB-governed), net-EV admission moved INTO the SQE ([11.8B] demoted to still-blocks backstop), confidence→sizing severed (B1), finalScore gate retired→shadow-log, VTS honest-capture fields. **★ SEQUENCING (Kyle, §9.2 delta): the backtest-baseline now lands BEFORE the flip** (was parallel/after; the metric-set dependency was discharged by the ratification). **Pre-flip order: B8.5a ✅ → the CAPTURE micro-batch (B-NEW-53.3 five scalars + #500 real-DI carry both lanes + bridge DELETE, CC-B) ∥ #501 backtest-baseline harness (CC-A) + #498 refresh-omission feed + #499 pm2-stdout restore (= B-OPS-PM2-LOG, CC-A) → THEN B8.5 THE SWITCH-ON** (which also carries the B8.5a behavioral gate proofs per §9.1). | Kyle + CC-B + Langston + CC-A |
| 2026-07-08 | **★ B8.4/B8.5 RENUMBER + P19-B8.4 CLOSED — the Filter-Diagnostics active-path instrumentation** (Kyle directive 2026-07-07: "the switch-on is now B8.5, NOT B8.4"). **RENUMBER (authoritative):** B8.4 is now the FD INSTRUMENTATION batch; **THE SWITCH-ON + AC1/AC2 + the #418 `closed_trades.net_pnl`-population gate + the ON-state enforce funnel render all move to B8.5** (prior decision-log/catalog cross-refs that read "→ B8.4" for switch-on items now mean B8.5 — recorded here rather than rewriting history). **B8.4 CLOSED:** Part-1 (display re-point + dormant rows + xStock-crypto-bleed fix, restart#452) + Part-2/B8.4b (engine emits — head `5058b8644`, CI 4-green `28903563905`, restart#454, non_architecture, NO migration). NEW **S22** `active-funnel-tracker` (durable mode+class-keyed singleton, mirrors the guard-eval-tracker pattern) wired into `buildSizedSignalForStrategy` + `evaluateSymbol` family loop + `rtb-refresh-service` + `refreshAndRank` + `checkRtbPromotion`, BOTH crypto + xStock. **Langston Step-4 finding + fix:** `family_imf` moved out of `preSqeRejects` into its OWN upstream `strategyAttrition` bucket so `preSqeRejects ⊆ signalsGenerated` holds BY CONSTRUCTION (a pre-generation filter can't pollute the denominator). Envelope `active-funnel/v3`, keySchema `funnel-v3`. **ALL DORMANT until B8.5** ("connected but unproven until switch-on"). Langston Step-4 APPROVED ("Ship it", both closing holds cleared) / Step-8 PASS (independent JWT, HEAD + v3-dormant both modes/classes). §9.3 walk (Radix `focus()`+Enter): Paper/Crypto + Paper/xStock (no crypto bleed) + Live/Crypto dormant-clean, VTS no regression. 12/12 tests. **#419** (refreshAndRank error-path sub-stage balance) homed → B8.5. SIM S22 added; SysManual N/A (telemetry observes, doesn't change, the SQE/pipeline). **NEXT: B8.5 THE SWITCH-ON.** | Kyle + CC-B + Langston |
| 2026-07-08 | **★ B-STORAGE-HARDEN Wave A CLOSED (OBJ-1 + OBJ-5) — cross-visibility note (standalone infra batch, NOT a P19-B* sequence item; owns its own BATCH_CATALOG/PHASE_HISTORY/CHANGES entries).** Triggered by Kyle's Supabase disk-expansion email (135→202 GB). CC-A, Kyle-authorized autonomous; Langston Step-2/Step-4 (r1→r4)/Step-8 APPROVED w/ independent staging DB verification. OBJ-1 = Backblaze COLD tier ACTIVATED (2 levers, reconciled on data: flipped `cold_rotator_dry_run` true→false [was true on arrival; `updated_at` is seed-time-only, not value-history] + installed the unscheduled monthly cron `0 3 1 * *`; B2 creds already present). Proven by a bounded real rotation of `context_bridge_log/2026-01`→cold + rehydrate-verify checksum match + weekly liveness canary. OBJ-5 = `database-monitor.ts` disk warn/crit now emits §10.5 (was console.warn-only) + a daily cron-silence/failed>0 archival-health watchdog. **The proof caught 2 dormant-path rotator bugs** (r3 stray `$2` param → every real rotation aborted; r4 `verified_at` never stamped → split-brain on the automated path) — both fixed + reproven. **Batch stays OPEN: Waves C (OBJ-2 = the B70 never-drop tiering fix — B70 DROP cron STAYS PAUSED until then) + D (OBJ-4/3) remain.** #430 V2 resolved; #431/#432/#433 OBJ-2 follow-ups homed. | CC-A + Langston (Kyle-authorized) |
| 2026-07-08 | **★ B-STORAGE-HARDEN Wave C CLOSED (OBJ-2 — the core never-drop fix) — cross-visibility (standalone infra batch).** The 5 B70 analytics tables no longer DROP-at-90d — routed through the proven B75 export→warm→cold move-not-delete path (NEW B70_TABLES inventory in b75-retention-sweep + per-table hot_retention_days=90 migration + O_EXCL run-lock; b70-retention-sweep.ts DELETED rule 18 + paused cron removed). Commit `0c22d0293`, CI 4-green, migration-first deploy, Langston Step-1/2/4/8 APPROVED (5 conditions resolved incl. the measured JSONB memory gate + the Q2 verify-before-asserting catch that KEPT archive-config.retentionDays for its live Drift Dashboard consumer). Proven end-to-end on exit_decision_archive/2026-05 (full hot→warm→cold+rehydrate). #430 FULLY RESOLVED (V1+V2); #432 resolved; #437 opened (db:migrate ledger drift). SYSTEM_MANUAL + SIM content updates. **Batch B-STORAGE-HARDEN: Waves A+C done; Wave D (OBJ-4/3 capture-reduction) remains.** Kyle confirms pair_scan KEEP→cold at close. | CC-A + Langston (Kyle-authorized) |
| 2026-07-08 | **★ B-STORAGE-HARDEN Wave D landed — the batch-closing wave (OBJ-3 daily partitioning + OBJ-4 capture-cadence) — cross-visibility (standalone infra batch).** OBJ-3: `xstock_spot_ticker_snap` (#1 hot consumer, ~63 GB/mo) transitioned MONTHLY→DAILY RANGE partitions at a 2026-08-01 month-boundary cutover (transition-forward — the 63 GB live table never repartitioned) for a true rolling-30 hot window; NEW daily creator (cron `0 1`, 14-day look-ahead) + monthly-creator exclusion + daily-first sweep parse (`classifyPartition`/`isPartitionEligible`, 18 golden tests) + per-table warm-window knob + independent forward-coverage watchdog + cutover migration (drop 9 empty future monthlies, seed 16 dailies); seam verified + bounded hot→warm→drop-after-verify proof. Commits `d66c856e7`+`e5cf6b8fd`+`c9c0ba3d7`, CI 4-green ×3, migration-first deploy, Langston Step-1/2/4+addendum APPROVED. OBJ-4: capture cadence flipped + live-measured per-symbol during RTH → **crew-consensus 4000 ms** (Langston+CC-B+CC-A; 8000 pushed 61 genuine symbols past the 15 s freshness gate vs 4000's 10; ~6× total hot reduction with OBJ-3), aggregation-safety confirmed (OHLC decision bars throttle-independent). **#438** (b74 creators' silent dotenv failure) fixed; **#439** (xStock OHLC-channel silent-stall, NOT throttle-caused → `B-XSTOCK-OHLC-STALL`) + **#440** (native thin-token freshness → Phase-25) homed. **B-STORAGE-HARDEN (A+C+D) CLOSED 2026-07-08 — Kyle confirmed conservative 4000 (profile dig: the ~10 affected are mid/high-vol tokens with occasional tail-pauses, NOT thin; new thin listings = #440 native-slow, gated at any cadence) + Langston Step-8 signed. Follow-up: B-XSTOCK-FRESHNESS-MONITOR (#441) — Kyle's condition, a weekly opportunity-loss/unintended-consequence report, built immediately.** | CC-A + Langston + CC-B (Kyle-authorized) |
| 2026-07-08 | **★ P19-B8.4c CLOSED — Filter-Diagnostics tab STANDARDIZATION (the six FD tabs; Kyle UI feedback, REV-1/2/3).** non_architecture (display + read-only telemetry surfacing). REV-3 head `623064821`, CI 4-green `28954002287`, restart#465, NO migration. **OBJ-6** split the mislabeled "Scanner Capacity" into **Per-Cycle Target** (crypto `SCANNER_PARAMS.BATCH_SIZE`=300 / xStock `CYCLE_BATCH_SIZE`=75 — a guaranteed floor + benchmark overshoot, "Target" not "Max") + live **Total Universe** (crypto `krakenUniverseSize` ~1,500 / xStock `lastUniverseSize` ~481) — surfaced read-only onto fx5 `ScanDiagnostics` (rides the vts route's whole-object `lastScan` passthrough) + the xStock scanner diag + `routes.ts:8156`; `ScannerCard` 5→6 fields, typed onto `FilterDiagnosticsData.lastScan` (dropped `as any`). **OBJ-7** `SHARED_DIAG_WIDTH` all six tabs. **OBJ-8** `DormantPipelineTables` mirrors the VTS 3-table structure DORMANT on Paper/Live (never bare-0). **OBJ-9** per-table blue/purple/teal filled bold headers. **OBJ-10** per-table `overflow-x-auto` + frozen first column. **§15 DELETED** `ActiveDownstreamFunnel` + `ActivePipelineTail` (superseded by DormantPipelineTables — B8.5 wires the new tables, not these; active-funnel WRITERS persist server-side, `/funnel` + `/pipeline-tail` client-unconsumed until B8.5) + archive + DELETED_COMPONENTS_LOG. **★ DECISION/PROCESS: Langston's initial 🔴 crypto-OBJ-6 blocker ("surfacing is a no-op, #421 not resolved") was a WRONG-ENDPOINT grep** (he grepped `server/routes.ts:7767`; the crypto FD feed is `server/routes/vts.ts:1629`, a whole-object `getLastScanDiagnostics()` passthrough that carries the new fields) — CC-B rebutted with the line-by-line chain, Langston independently verified on staging + RETRACTED. Runtime-confirmed post-deploy: crypto vts feed `krakenUniverseSize=1509`/`scanTargetPerCycle=300`; §9.3 Chrome walk shows crypto VTS + Paper cards render Total Universe 1,509 + Per-Cycle Target 300, and the Paper dormant 3-table mirror renders "awaiting activation" (never 0). **#421 RESOLVED (home B8.4c); #422** (`scan-24h` orphaned) → B8.5. SIM/SysManual N/A (display + read-only telemetry); GOVERNANCE_EXCEPTIONS class-override + SIM/SysManual N/A rows resolve alert 970f9467. **NEXT: B8.5 THE SWITCH-ON.** | Kyle + CC-B + Langston |
| 2026-07-31 | **B-XSTOCK-FEED-LIVENESS (#594) SHIPPED — the xStock feed's silent-stall alarm can now actually see a stalled feed.** It was watching whether the connection was *talking*, and a heartbeat counts as talking — so a feed sending heartbeats and **zero prices** read as perfectly healthy, indefinitely, on the only venue price source for xStocks. **This was a hard prerequisite for turning on xStock active fills (Langston-ruled), so it lands BEFORE that switch-on, not with it.** The provenance read changed the fix: the field was **correct** for the job it was built for at B74 (a health-log line answering *is the socket alive?*); the defect was P19-B4a C3 later attaching a *are prices arriving?* watchdog to it. ⇒ **added a second clock rather than repointing the first** — repointing would have silently inverted the health log, with nothing throwing and no test failing. ★ **The lesson the batch is worth remembering for:** the first six fences proved the READER while the defect was a WRITER — **re-introducing the exact bug left all six green.** Langston caught it at Step-8; six stamp-site fences now drive real frames through the handler, mutation-proved (bug restored ⇒ 4 fail). Head `e93136f81`, CI 4/4 verified job-by-job, deployed by naming the sha (#621), live-confirmed (`last_msg_age_ms=94 last_data_msg_age_ms=94 rows_persisted_60s=557` — the two agreeing is the healthy reading; divergence is the stall signature). **#635** (detects a TOTAL stall only — one symbol dark of ~485 still reads fresh) and **#636** (snap-arrival ≠ mark-freshness) homed, not folded in. Owner CC-B. | Kyle + CC-B + Langston |
| 2026-08-01 | **B-GOV-HEARTBEAT-REPAIR (#637) CLOSED — the dead-man switch on the governance checker could not clear its own alarm, and had been switched off for ~6 weeks.** The checker's healthy output is SILENCE, so without this switch a dead checker is indistinguishable from a clean repo. The heartbeat resolved without the `--evidence` flag that B-GOV-INTEGRITY-1 made mandatory — every call failed, the error was swallowed, and the alert handle was discarded in the same statement, so the alert could never clear and nothing could retry it. **Not latent: it fails on first use; it had simply never been used.** Fixed via a shared token-shape SSOT + publishing the graded sha across the process boundary (NULL on the fetch-fail path). Head `28b029528`, CI 4/4 job-by-job, Langston Step-4 APPROVED — **his blocker would have recreated the defect** (a bare `not found` match also catches `npm: command not found`). **Verified PROVOKED end-to-end** — a clean run exercises neither branch. ★ **Two catches carried: the fence caught a fabricated-provenance bug in the fix (an all-hex timestamp would have been stored as a git sha; the server validator is unanchored and would NOT have caught it), and `systemctl enable` reported enabled+active while scheduled for nothing.** #642 (ownership register) carried. Owner CC-B. | Kyle + CC-B + Langston |
| 2026-08-06 | **B-DEPLOY-LOCK (#649) CLOSED — one locked deploy command replaces the hand-typed chain; #140 closed with it.** Two sessions could silently deploy over each other mid-verification; now a second deploy is refused naming the holder, a dirty worktree is protected not destroyed, the database-migration step runs in-chain per the manual's own invariant, and every deploy proves its identity and the engine's resumption before recording. Proven by two live self-deploys (11-15s windows vs the watchdog's 300s bound) and three provoked refusals. Eleven substantive review catches across four gates; docs swept so nothing prescribes the bypass. Owner CC-B. | Kyle + CC-B + Langston |
| 2026-08-07 | **B-FILTER-DIAG-PAPER — the Paper Filter-Diagnostics tabs now explain the live pipeline (instrument for #648).** Per-strategy × per-stage attrition (class-scoped, lane-split, one column per writing service), the Net-Expectancy gate named instead of pooled in an unlabelled bucket, refresh-cycle fallout per gate, and six copy/data defects fixed — including a banner asserting active trading was off above live counters. The audit reframed Kyle's reported bug: the tabs were casualties of a page-level render storm (31% of wall time blocked while idle), which is carried as its own profiled hunt rather than patched blind. Owner CC-B. | Kyle + CC-B + Langston |
| 2026-08-07 | **B-FILTER-DIAG-STANDARDIZE — the six Filter-Diagnostics tabs made to match, and the per-strategy data made real.** Kyle rejected the predecessor's honest-placeholder approach outright: *"this batch isn't complete until we have all the data we need feeding into these tracking metrics."* **Root cause of the divergence: an early return in the shared panel, added for a guard condition that had expired eleven weeks earlier** — the tabs did not diverge by design. **The engineering worth keeping: the active path was already computing each strategy's decline reason and throwing it away**, so the fix READS what was there instead of authoring a second taxonomy — both pipelines now report identical categories *by construction*, sharing one module. The contract (*reset before each detect, read after null return*) now binds two callers and is safe only under strictly serial evaluation; both halves fail SILENTLY, so a committed fence pins `resets === records` — **and caught a real `range_trading`/`range_trade` mismatch on its first run.** ★ **Two findings withdrawn before filing:** a suspected classifier defect (the code comment named its own provenance — a deliberate no-reattribution-of-history decision) and a suspected wiring gap (the busy comparison numbers were the VTS — *a different population*). **Change-class ruled BOTH ways across Langston's stateless invocations**; he vacated his own later ruling once the timeline and the ref of each were reconstructed, and his pre-registered criterion decided it. ⚠ **Residual held open, not smoothed: Paper xStock per-strategy is still empty** (~190x rarer than crypto on the active path — 15-minute bars) — **recorded as unconfirmed-until-observed, not declared working.** Also decided: **#669's pre-existing red makes rule 19's "CI 4/4" unsatisfiable branch-wide**, so advance is gated on an IDENTITY-matched failure set, never a count. Owner CC-B. | Kyle + CC-B + Langston |

---

## §6 — SWITCH-ON PRE-FLIGHT CHECKLIST (Langston req. — de-facto gates live in earlier batches; nothing else aggregates them)

> **★ Renumber 2026-06-30:** the switch-on (formerly "B7b") now lives **inside P19-B8**. Every "B7b" reference below = the **B8 switch-on step**. Gate 10's crypto closed-lifecycle proof is now **P19-B7's gate-10 exit test** (absorbs B6.5g); B8 carries the matching **xStock** closed-lifecycle proof at the xStock flip.

ALL must be ✅ before the flip commit. Verify each at the **B8 switch-on** Step 2, cite evidence in the B8 scope:

| # | Gate | Built in | Status |
|---|---|---|---|
| 1 | Test suite green (no stale-failure noise floor) | B1 | ✅ 2026-06-13 (0 failed / 0 skipped, bench + CI) |
| 2 | #153 xstock pattern-pool-cap validated | B4a → **◐ INTERIM corrected 0.50→0.15** (C8; xStock less liquid → cap ≤ crypto; non-binding today). Final per-class evidence-calibration = Phase-25/#153 | ◐ interim |
| 3 | RTB `asset_class SET NOT NULL` Phase-4 flip | **DEFERRED → B7b post-activation real soak (#237, P19-B4a C7)** — nothing writes rtb_signals while dormant so a soak is vacuous; the C1 resolver-backed write is shipped; flip on observed-clean evidence post-activation | ☐ (deferred — apply at B7b after real soak) |
| 4 | xStock pricing latency + staleness/reconnect fitness verified for execution | B4a (C3) → **◐ freshness 15s + liquid-fill-window + silent-stall watchdog shipped** (DB-resolved fail-closed; thresholds from measured Fri session). Live exercise = B7b | ◐ built, exercised at B7b |
| 5 | Kraken `validate=true` round-trip smoke passed (Kraken has NO spot paper-order system — P19-B2 / rule-20 correction; validate-only is the real-venue contact) | B4b or B7b pre-flight | ☐ |
| 6 | All §19.0.5 capture hooks live (pre-filter, RTB TTL, TCL, paper admit) | B5 | ☐ |
| 7 | Loss-budget auto-trip armed AND force-trip-tested (trip + recovery path proven) | B6 | ✅ 2026-06-17 — auto-trip RESTORED + armed (gated `isEngineActive`, dormant til flip); force-trip PROVEN deterministically (5-case integration test: crossing-loss→trip+flatten+both-alerts, idempotent, recovery re-arms, gated-off). Live close-driven exercise lands at B7b turn-on |
| 8 | #213 legacy `/live-trading` routes confirmed inert (gate-bypassing legacy route + false "live-ON" broadcast are worse latents once execution machinery is hot) | ~~B7b pre-flight~~ → **B2** | **✅ RESOLVED 2026-06-13 by P19-B2 — routes + stub + false broadcast PHYSICALLY DELETED (commit 59d501fc4), not just confirmed inert** |
| 9 | Paper monitoring screens (both classes) merged + reviewed against fixture data | **B8** (built before the flip, same batch — was "B7a") | ☐ |
| 10 | **Crypto active-pipeline resurrection (P19-B6.5 = B6.5a + B6.5b + B6.5c + B6.5e + B6.5g) GREEN** — **B6.5a ✅:** per-asset-class active gate present + tested + the xStock-isolation acceptance test passing (zero xStock signals/opens during a crypto-only activation); **B6.5b ✅:** accretion-delta audit + crypto-only reverted dry-run proved the front half (scanner→pools→orchestrator→SQE fire for crypto); **B6.5c ✅:** the two ready-to-buy-insert breaks repaired (`cwqi` DB-drift DROP + pattern→canonical-strategy resolve) — crypto signals now reach ready-to-buy with canonical names; **B6.5e ✅ (2026-06-18):** the TCL→paper-execution-engine open-path silent failure (#325) REPAIRED — the open stage is now observable + accounted (`OpenOutcome` + `openFailed` 3rd term; I3 invariant `attempts === opened + blocked + openFailed`, proven live `openFailed=8 [EV_REJECT:8]`) and DIAGNOSED: the open path is sound and is honestly rejected at the EV gate on DEFAULT EV inputs (#233), so gate-10 needs the EV inputs fixed, not the open path; **B6.5g → ABSORBED INTO P19-B7 (renumber 2026-06-30) ☐:** the ≥1 FULL closed-trade-lifecycle proof (open→exit→close→cooldown→telemetry) on REAL EV inputs is now **B7's gate-10 exit test** — **the B8 crypto switch-on cannot open until B7's gate-10 closes clean** (Kyle 2026-06-15, #235; Langston Step-1 ruling 2026-06-17 = Option C; gate-10 ownership re-pointed B6.5e→B6.5g 2026-06-18 when B6.5e proved the blocker is EV-input math, not the open path) | B6.5a/b/c/e done + **P19-B7 gate-10 exit proof** (renumber 2026-06-30 — absorbs B6.5g: the crypto closed-lifecycle on real EV inputs, after B7.2 maker build clears the fee wall) + **B8 carries the xStock equivalent** at the xStock flip | ☐ (B7) |
| 11 | **Price-discovery-liveness fill gate (P19-B6.6) GREEN** — closes the holiday/half-day hole in the clock-only liquid-fill-window (a fresh-`captured_at`-but-stale-`last` quote on a closed book passes both the clock + freshness gates); require `last` to have actually MOVED before a fill (P19-B4a C3 / #236; B7b cannot enable fills until this closes clean) | B6.6 | ✅ **DONE 2026-06-26 (B6.6 closed, staging restart#420).** The gate is built + wired (open seam, after depth, xStock-only, fail-closed, 45-min window) + DORMANT-til-B7b; at B7b verify it's `enabled` for any class being switched on (it is, for xstock_spot). |
| 12 | **`strategy_gates` NON-EMPTY for any class being switched on** — the default-open active strategy gate (P19-B4a C5) must not silently run the FULL strategy set on a thin/unseeded class; crypto_spot has zero rows today; seed crypto_spot + assert non-empty per class before the flip (Langston Step-4 / #238) | B7b pre-flight | ☐ |
| 13 | **Per-class FILL-QUALITY GATE present + wired for any class being switched on** (P19-B4b.1 / #295 / Langston Step-2 C-Q3 + Step-4): active trading BLOCKS if, for a class being flipped on, its applicable fill-quality gate is absent/unwired — **crypto_spot:** the 24/5 book-depth-sufficiency + warmth gate (`_evaluateOpenDepthGate`, `fill_depth_gate` seeded); **xstock_spot:** BOTH the depth-sufficiency gate (B4b.1, top-of-book) **AND** the #236/B6.6 price-discovery-liveness gate. Verify both per class at B7b Step-2 | B4b.1 (crypto + xStock depth) + B6.6 (xStock liveness) | ✅ **crypto+xStock-depth (B4b.1) + xStock-liveness (B6.6, closed 2026-06-26) ALL SHIPPED + wired.** At B7b verify per class that the applicable gates are `enabled` (xstock_spot = depth + liveness both). |
| 14 | **Paper-mode FULL guardrail set present + working** (P19-B6.8) — every user-settable guardrail separated per-mode with **paper's set complete + functional**; live structure-present but wiring/functionality NOT required (Kyle 2026-06-16). Paper-only gate | B6.8 | ☐ (paper-only) |

---

## §7 — RUNNING_ISSUES PHASE-19 DISPOSITION TABLE (Langston req. — every Phase-19-homed open issue gets an explicit home or an explicit Kyle-signed deferral)

| # | Issue (one line) | Disposition |
|---|---|---|
| **#732** | ★★ **A plain TARGET hit is labelled `trailing_stop_hit` — and two of the seven exited ABOVE their own target.** Trailing is OFF and genuinely never runs; `targetLatched` is set OUTSIDE the moonbag gate, so the consumer (`tec-evaluator.ts:405-407`) branches on the latch rather than on ladder entry. | ⛔⛔ **HOME: `B-EXIT-LABEL-TRUTH`, owner CC-A, placed at the BACK OF PHASE 19 — after the `reorg-B` sequence and immediately BEFORE the closing full runtime pipeline audit (rule 23).** ★ **Kyle 2026-08-27, on severity: *"it can be towards the back of phase nineteen. It’s not really that important."*** ⚠️ **It was briefly placed FIRST-after-governance earlier the same day; that was my ordering, not his, and he corrected it.** ★ **The position is still a real one anyone can look up — late is not the same as unplaced, and unplaced is what it was for a week.** ⚠️ **BEFORE 2026-08-27 THIS ISSUE HAD NO PLACEMENT ANYWHERE — Kyle asked where it was slotted and the honest answer was NOWHERE.** It was measured, deprioritised on severity (Kyle 2026-08-20) and given a tripwire on the weekly mistake pass — **and every one of those ceremonies was performed while the item still had no position in any plan. Naming is not placing (§9.4).** ⛔ **TWO LEGS, ORDER FIXED: consequence 2 (the hold-past-target) is resolved FIRST — relabelling first would hide the symptom that surfaced it.** ✅ **Tripwire re-run 2026-08-27: 14 `trailing_stop_hit` rows, 0 breaches, control 651 rows — the deferral still holds on its own terms, and the population has doubled since the 7-for-7 record.** |
| #92 | Wire xstockSpotScanner through signal-orchestration | → **B4** (core of the merged wire-in) |
| #137 | Active-trading-path restoration intake (54 files / 231 errors + routes/storage share; baseline tags still TBD) | → **B3** (opens with tag-triage pass) |
| #139 | vts-runner 9 remaining throwing resolveAssetClass sites | → **B3** |
| #153 | xstock 0.50 pattern-pool-cap placeholder validation | → **B4** (pre-flight gate #2) |
| #138 | Hybrid first-confluence label verification (fires on first confluence under active trading) | → **B9 watch-list** |
| #95 | xStock real-time WS pricing adapter | → **SUPERSEDED by reconciliation §3.1** (B74 WS feed live since archiver batch); B4 residual = latency/staleness/reconnect fitness; rewrite-or-close #95 at B4 close |
| #96 | Sector-aware portfolio-cluster prevention | → **B11** (19-16) |
| #97 | xStock characteristics inventory (earnings/market-cap/P/E/IV) | → **B13** (19-15) |
| #83 | Boot Readiness Coordinator (boot-ordering half) | → **B14** (conditional, triggers per roadmap 19.x) |
| #236 | xStock liquid-fill-window holiday/half-day hole → price-discovery-liveness gate | → **B6.6** (NEW; §6 gate 11; hard-gates B7b) |
| #237 | RTB `asset_class SET NOT NULL` deferred (vacuous soak while dormant) | → **B7b post-activation real soak** (§6 gate 3) |
| #238 | C5 default-open: `strategy_gates` seed + B7b non-empty assertion | → **B7b pre-flight** (§6 gate 12; crypto_spot seed before flip) |
| #153 | xstock pattern-pool-cap | **◐ INTERIM corrected 0.50→0.15 (P19-B4a C8)**; final per-class calibration → Phase-25 (§6 gate 2) |
| #221 | getTopSignal cross-class sort on clamped finalScore (return-ceiling) | → **Phase-25 + B9 calibration-aware watch** (B4a reachability-answered) |
| #231 | orchestrator active-signal ablation integer-signalId gap | → **Phase-20 numbered** (B67 ablation activation; refused rule-15 string→int force) |
| #56 residue | §19.0.5 promoted capture hooks (FX5 pre-filter + active-path SQE/RTB) | → **B5** |
| #86 | Continuous Q-D friction probe + dedicated history table (gates xstock friction extraction) | → **B5 scope decision** (capture-infrastructure family; confirm or re-home at B5 Step 1) |
| #94 | xstock macro confidence modifiers (currently deliberate NO-OP = 1.0; issue argues no-macro-awareness shouldn't ship into active trading) | → **✅ HOMED — KYLE CONFIRMED 2026-06-13.** BUILD = Phase-25 item **25-7** (already-existing roadmap item; needs paper-run outcomes). CAPTURE precondition = **P19-B5** (every xStock decision record carries the VIX+DXY snapshot so 25-7 has build material). Interim safety: class-level AMR brakes already macro-aware (active ~1wk into paper run). Both written into the roadmap (25-7) + this plan (B5 row). Defer-build-but-capture-now doctrine (per 19-20 / AMR body-vs-brain). |
| #80 | Extend B73 exit-strategy ablation to xstock_spot (drives per-class TEC config) | → **✅ DEFERRED to Phase 25 — KYLE DECISION 2026-06-13** ("for the exit strategy study, yes, let's wait for phase twenty five") |

---

*Maintained by the implementing CC session. Update §1 status + §5 decisions every batch/sub-batch close; re-sequence only with Kyle's sign-off recorded here.*

---

### B-CONDUCT-FILE — closed 2026-08-20 (CC-A) · §5 decision-log entry

**DECISION 1 — conduct rules get a SEPARATE auto-loaded file rather than a better position inside `CLAUDE.md`.** The boundary test (Langston-corrected at r2) is ***"can the failure be detected mechanically AT THE MOMENT IT HAPPENS?"*** — YES → a hook plus one line; NO or PARTIAL → conduct. r1's test ("no downstream backstop") measured the COST OF A MISS and carried a perverse incentive: build a hook and the rule gets demoted out of the loaded file, leaving the hook as sole custodian.
**DECISION 2 — the cap is in BYTES (16,384 ≈ 4k tokens), enforced IN THE LOADER, and never drops rules.** Lines were rejected because the 200-line MEMORY cap is routinely breached by dense paragraphs while formally compliant. Headroom is deliberate: *"a cap equal to the delivered size means the first new rule breaches it"* — the 200-line cap died from being met in form.
**DECISION 3 — rule 24 is STAGED, not deleted, until B-RULES-1d builds its skill.** Langston: *"APPROVED as sequencing, not hedging."*
**DECISION 4 — the loader has NO clone gate, deliberately unlike its sibling**, and the SIM records the divergence because it otherwise reads as an omission.
**OPEN:** §7(a) native-load observation, alert `441abe49`. **Rule 21's removal remains GATED** behind alert `9c3037f0` and did not ride this batch.

---

### ▶ THE GOVERNANCE / RULES QUEUE — **PLACED ORDER, NO DATES (§9.4)**

★ **THIS BLOCK EXISTS BECAUSE NAMING A BATCH IS NOT PLACING IT (Kyle, 2026-08-26).** A session can name a batch, mint an issue number, declare it "locked" and correctly refuse a due date — **and the item still has no position anyone can look up.** Every governance batch below has a POSITION, and the `RUNNING_ISSUES` entries point HERE.

| # | batch | owner | state | what has to be true before it starts |
|---|---|---|---|---|
| 1 | **`B-RULES-1e`** — mechanisms for three rules followed zero times (#739, #740, #741-CC-A) | CC-A | **IN FLIGHT, Step 2 with Langston** | — |
| 2 | **`B-CROSS-SESSION-BLEED`** (#753) — another session’s uncommitted work materialising in a session’s own checkout | **CC-B** *(transferred from CC-A 2026-08-31 by Kyle)* | **NEXT UP — KYLE 2026-08-27** | ⛔⛔ **Kyle directed a FULL INVESTIGATION AND FIX, placed AHEAD of `B-MEASURE-GATE`.** ★ **Three instances, all on `RUNNING_ISSUES.md`, across three different sessions — and the mechanism has never been established, because instance 1’s evidence was destroyed by dropping the stash once the work was proven safe.** ★ **Instance 3 breaks the standing hypothesis: 1 and 2 were STAGED, 3 was UNSTAGED ⇒ whatever wrote it did not use the index.** ⚠️ **Two retained stashes are the evidence; §25.c forbids dropping them until the cause is known.** **Why it outranks a tidiness issue: the PATH IS ALWAYS RIGHT, so the mandated explicit-path commit stages the correct filename and the wrong content — all three were caught by eye, and eyes are not a mechanism.** |
| 2.4 | ★ **`B-ALERT-ACTOR-ALLOWLIST`** (#987) — the alert owner record is free text and `CLAUDE.md` §10.5 mandates `cc-session-<date>`, a name bound to nobody: 23 distinct strings for ~5 actors in 30 days. Refuse any `--by` outside a roster-derived actor set, **in the server function** so every path is bound (the F3b evidence-gate model); fix the two disagreeing docs; never rewrite history. Not authentication — the claim becomes well-formed, `resolved_by_transport` stays the verifiable half. | **CC-B** | ✅ **CLOSED 2026-09-02** — deployed `fa563982c` 21:05Z; Langston Steps 1/2/4/8 approved; conservation proven 784/784; OBJ-7 added and delivered; follow-ons 2.4a + 2.4b placed | Kyle sequenced it next, after the #605 pin proof |
| 2.4a | ✅ **CLOSED 2026-09-04** — deployed `a4bcbe3c1`, Langston Step-4 APPROVED + Step-8 CONFIRMED at the box; `#656` residual closed, `#1000` found-and-fixed in flight, **`#1004` opened (deploy-path provenance — its own class, homed P19-B12)**. ★ **`B-DEPLOY-ACTOR-ALLOWLIST`** (`#656` residual; Langston Step-4 §13 find on `B-ALERT-ACTOR-ALLOWLIST`, 2026-09-02) — **the deploy record's `--by` is validated by SHAPE ONLY**: `scripts/dt-deploy.sh:81` `^[A-Za-z0-9_-]{2,24}$`, so `cc-session-2026-09-02` passes and `deployed_by_claimed` is free text one system over from the alert file this batch just fixed. Same fix shape: the canonical actor set (`cc-a|cc-b|cc-c|cc-infra|kyle|langston` — `dt-deploy.sh:46` today cannot even name CC-INFRA) applied exactly, refused otherwise; the deploy record stores the canonical. **Owner CC-B.** **FIRST ITEM (Langston, #987 Step 11): date the bare `782 rows` in the `ALERT_ACTORS` header comment at `server/services/system-alerts.ts:186` — the one carrier of an undated count left in the deployed tree, because this batch imports that table and opens that file anyway.** | CC-B | **placed immediately AFTER `B-ALERT-ACTOR-ALLOWLIST` (2.4)** — same defect class, one namespace over; the actor table lands first so this batch imports it rather than minting a second | Langston Step-4 condition 4 |
| **2.4-FEE** | ⭐⭐ **`B-XSTOCK-FEE-CONTRACT`** (`#1010`) — **change-class `architecture` (Langston, 2026-09-06). Owner CC-B.** ⛔⛔ **WE CHARGE xSTOCK THE CRYPTO FEE SCHEDULE.** `fee_model|*|xstock_spot` holds `spot_taker_fee 0.008` / `spot_maker_fee 0.004`, **byte-identical to the crypto rows including `updated_at 2026-06-10 21:50:44.215225+00` and `updated_by b45-tier1-seed`** — swept UNBOUNDED across every scope (43 rows, no module filter), and **Langston re-ran that sweep himself and fetched Kraken's live page rather than take the transcription.** **ACCOUNT-CONFIRMED, not merely published: `pro.kraken.com/app/trade/xstocks-nvda-usd` quotes rung 1 `maker −0.02 % / taker 0.10 %`, the market header reads `FEES Maker rebate −0.02% / 0.10%`, and a live limit order's own estimate reads `Est. trading fee Maker rebate −0.0001 USD`.** ⇒ **taker 8× too high; maker the WRONG SIGN.** Taker/taker round trip: we model **1.60 %**, actual **0.20 %**. ★★ **THE MAGNITUDE IS A SELECTION NUMBER, NOT A BOOKKEEPING ONE — and the two legs have DIFFERENT REACHES; Langston ratified keeping them apart. TAKER** feeds `computeNetExpectancyKernel` → the SQE's `netEV <= 0` admission gate (`signal_quality_evaluator.ts:363`, `:572`, a pure sign check), the VTS net-EV floor, and the RTB rank ⇒ **every xStock candidate for three months was admitted or refused against ~8× the real cost.** **MAKER** has **no live consumer on the pre-decision path** (`cost-model.ts:114`) — it lands on `maker-taker-decision.ts:294-295` and the F-G-2 booked-friction path. ✅ **Signal GENERATION is fee-free** (detector geometry is price/ATR/structure), so the candidate SET is uncontaminated — only filtering, mode choice and ranking. ⛔⛔ **NOT A CONSTANT EDIT, AND THIS IS WHY IT IS A BATCH: `b72-warmup.ts:236-239` refuses any fee outside `(0, 0.05]` — `if (!(v > 0 && v <= 0.05)) throw … refusing to start`. Write `−0.0002` and THE SERVER WILL NOT BOOT.** The rail was written when a fee could only be a cost. **Futures maker also goes negative from rung 11, so the rail is made schedule-aware ONCE for both products, not twice.** **IN SCOPE (Langston):** the xStock rates · the negative-fee rail · **`drizzle/migrations/2026-06-11-b45-fee-model-tier1.sql:39-42`, which seeds all four rows from ONE Tier-1 literal and would re-seed the defect into any fresh DB** · per-product fee identity · a stamped epoch boundary instant · **the ~8 test files hardcoding `0.008/0.004` for `xstock_spot`, treated SUBJECT-vs-PROBE: `b45-fee-model.test.ts` + `cost_cache.test.ts` are corrected; the three `b79-0n-*` files use the value as a probe for a resolver invariant and are RE-POINTED, never blind-swapped.** ⚠️ **THE `asset_class` DIMENSION ALREADY EXISTS AND IS HONOURED** (`cost-model.ts:114-119`, `slippage-fee-model.ts:39-42`) — **my entry first said it did not, and Langston corrected it at the ref. The defect is the VALUE seeded into an existing per-class slot**, which makes the identity work smaller than “a schema”. ⛔ **CENSUS ITEM BEFORE SCOPE (Langston): the schedule has AT LEAST FOUR stored homes, not two** — add `pre-execution-validator.ts:189/198/401` (reads `makerFeePct`/`takerFeePct` off `systemContext` as STRINGS at the execution gate) and `drizzle/migrations/2026-06-02b-calscore-comprehensive.sql:86` (`xstock_spot feeRateMaker '0.16%'`, a THIRD transcription carrying a DIFFERENT wrong number). **The scope must say which store is CANONICAL or `#1011`'s watcher reads green while a stale copy feeds a gate.** ⚠️ **PRE-AUDIT ITEM, not a defect claim: `slippage-fee-model.ts:39 resolveFee` is a SECOND direct `fee_model` reader**, against `SYSTEM_MANUAL.md:476`'s claim that the single merge site is *“literally true across the codebase.”* Values cannot drift (same rows) so this may be taxonomy outcome 2 — **but the sentence needs re-testing under a SIGNED maker rate, and that site is in the sign census.** ⛔ **LANGSTON'S BINDING CONDITION ON THE SPLIT: resolution stays behind a per-product RESOLVER so `B-FEE-TIER-RESOLUTION` later changes a FUNCTION BODY, not a call graph. “If your fix hardcodes two literals at the read sites, I will send it back.”** | **CC-B** | ⛔⛔ **PLACED HERE 2026-09-06, AHEAD OF `2.4b` AND EVERYTHING ELSE IN CC-B's QUEUE, ON KYLE'S DIRECTIVE.** ⚠️ **AND PLACED LATE: I wrote “placed in `PHASE_19_PLAN.md`” into `#1010` and the task list and then DID NOT WRITE THE ROW — `§9.4`'s own named failure, *naming is not placing*, committed by the session that had just cited `#1005` for it. Kyle caught it.** | ★ **Why it outranks the queue: every xStock viability question behind it is unanswerable until the cost input is right, and the Codex data export is gated on the same correction.** ✅✅ **DEPLOYED 2026-09-11 20:09:47Z at `b597f1bf2`; Step 8 CONFIRMED by Langston 2026-09-12 00:32Z, re-derived on staging. xStock now `0.0010` taker / `-0.0002` maker; crypto untouched on its original seed timestamps; `cost_model` deleted (`#133`/`#134`); epochs stamped xStock-only (`vts 6→7`, `paper_sim 3→4`, class-scoped `live` created at `3`). First negative fee in the system's history booked 2026-09-12 00:16:31Z: `CRM/USD` maker exit `-0.03387422` = `-0.000200`, against a 92-row control all at `+0.004000`.** ⏳ **OBSERVATION OPEN, not closed:** P8 (xStock maker share ≤ 1.0% at n≥300, zero class-(iii) rows) and Arm B (EV-gate admission) are three-week windows opened at the deploy, with 17 alias symbols excluded until `#1024` and a flip on restoring them ruling the arm INCONCLUSIVE-EXTEND. |
| **2.4-FEE-b** | **`B-KRAKEN-FEE-WATCH`** (`#1011`) — **Owner CC-B.** The venue revised its schedule **2026-07-09**; we found out **2026-09-06** because Kyle looked at the page. **The pages are PUBLIC — no login, no paywall, no credential.** ⛔ **THREE OBJECTS, NOT TWO — the live pages, the transcription (`external-references/KRAKEN_FEE_SCHEDULE_REFERENCE.md`) and `module_constants`. A page-only diff catches the NEXT venue change and is STRUCTURALLY BLIND to `#1010`.** ⚠️ **AND THE TRANSCRIPTION LEG CANNOT BE AUTOMATED (Langston): it is a human read of an unparseable image — `pdftotext` returns ZERO lines from all three captures. So it needs a DATED RE-CAPTURE OBLIGATION, never the pretence of a third machine source.** ⚠️ Carries the `cost_model\|kraken\|*` `default_taker_fee 0.0026` residue (pre-July rate, written 2026-05-05) — **reach UNMEASURED; establish readers before dispositioning.** | **CC-B** | **IMMEDIATELY AFTER `2.4-FEE`, deliberately** — a watcher built against uncorrected values bakes the error into its own baseline. | Langston agreed the ordering; the canonical-store question from `2.4-FEE`'s census is its precondition. ⛔⛔ **DEFERRED BY KYLE 2026-09-12 AND MOVED TO THE END OF PHASE 19 — SEE ROW 12.9. DO NOT WORK THIS.** His reasoning, adopted: **the venue changes its fee schedule rarely, so a checker for it is low value against everything else queued.** Step 1 closed (9 review rounds, Langston-cleared at `61ad3e112`; harness 21 cases / 49 assertions). **Step 2 was opened and is STOPPED mid-audit.** ✅ **Nothing beyond the scope and the instrument was built, so nothing is lost by the move.** |
| **2.4-FEE-c** | **`T-W20C-SCALAR-LEG`** (B-NEW-53 parity, roadmap 25-12; alert `a3610acf`, successor to `7c4a873f`) — **Owner CC-B.** No longer data-blocked: `signal_eval_provenance` held 15,293,173 August rows with `settled_window_hash` 100.00 % (measured 2026-08-31). **Work:** ⚠️ **re-scope first** — July aged out of rolling-30 retention, so the `#206` window from 2026-07-13 no longer exists and its 84.07 % / 70.73 % figures are a DIFFERENT POPULATION (label, never trend); then build `b5-w20c-provenance-replay.ts`'s two legs — (a) hash-verify: recompute `swh1:` over snapshot bars and compare; (b) fed-scalars: persisted `ind_*` into the detect path — to separate drifted-window rows from recompute-divergence rows, and re-run against the 99 % Tier-1 gate. **PASS unblocks 25-12.** | **CC-B** | **PLACED 2026-09-11 behind 2.4-FEE on Langston's routing** (his Step-1 approval of `B-XSTOCK-FEE-CONTRACT`), after 2.4-FEE-b, which stays immediately after 2.4-FEE by his earlier ruling. The alert's own sequencing ("after `#682`") is superseded by that routing; `#682` is placed next. | alert `a3610acf` stays active and owned — routed, not acked (`#982`) — until this row closes |
| **2.4-FEE-c-ii** | ⭐ **`B-PAPER-LANE-PROVENANCE`** (`#1059`) — **Owner CC-B.** ⛔⛔ **THE PAPER LANE IS UNREPLAYABLE: `signal_eval_provenance` IS WRITTEN BY THE VTS LANE ONLY.** MEASURED 2026-09-13 on `xstock_spot`/`vwap_pullback`, 2026-08-01 → 09-11T20:09:47Z: of **1,173** `admitted` archive rows, the **566** carrying provenance are **ALL** `source=vts-runner` / `mode=vts`, and the **607** without are **ALL** `mode=paper_sim` — **541** `signal-orchestrator` + **66** `active-execution-engine`. **Zero overlap; 566+541+66 = 1,173 closes.** `archiveSignalEval` writes a provenance sibling only when the CALLER supplies one (`signal-eval-archiver.ts:~324`), and the three paper-lane sites do not: `signal-orchestrator.ts:1858`/`:1209`, `active-execution-engine.ts:4604`/`:4223`, `ready_to_buy_service.ts:1657`/`:2143`. **Langston re-derived all of it at `38982b31f`.** ✅ **Outcome (2) — working as designed** (the archiver concedes `coverage < 100%, allowed by C1`); what was never recorded is that *partial* means **the trading lane is invisible**. ⛔⛔ **RIDER (Langston, binding): THE THREE SITES ARE NECESSARY, NOT SUFFICIENT — `provenanceCaptureEnabled(assetClass)` GATES THE WRITE TOO, so supplying the object without the class flag reproduces the SAME ZERO on a NEW mechanism, and that failure is SILENT.** | **CC-B** | **PLACED 2026-09-13 immediately after 2.4-FEE-c, on Langston's `HOME:` line.** Surfaced by the `T-W20C-SCALAR-LEG` join-loss diagnosis — record `T_W20C_SCALAR_LEG_ADMITTED_JOIN_LOSS_RESULT.md`, pre-registration `63dc5927c`. ⚠️ **2.4-FEE-c's parity claim is NARROWED TO THE `vts` LANE until this lands — the stratum label travels with the figure, never in a footnote.** | — |
| **2.4-FEE-d** | **`B-FILTER-DIAG-XSTOCK`** (`#682`) — **Owner CC-B. change-class `architecture`** (Langston-ruled at filing: new active-path emission). Wire the per-strategy decline taxonomy into the xStock active path (`xstock_spot/eval-cycle.ts` → `dispatchXstockActiveSignal`) so the Paper/Live xStock per-strategy decline table carries real data, matching crypto. ⚠️ **The table was still EMPTY when last checked — do not claim it works until a non-empty row is SEEN.** | **CC-B** | **PLACED 2026-09-11 — it had NO plan row.** `#682` was filed 2026-08-07 with a `DUE` date and never written into this plan (0 hits) — `§9.4`'s *naming is not placing*, on my own issue. Found while placing 2.4-FEE-c, whose alert named it as a predecessor. Position proposed to Langston in the `B-XSTOCK-FEE-CONTRACT` Step-2 dispatch. | — |
| **2.4-FEE-e** | **`B-SCRIPTS-TSC-COVERAGE`** (`#1058`) — **Owner CC-B.** ⛔ **`scripts/**` SITS IN NO COMPILATION GRAPH, so the tsc baseline gate has NEVER graded it.** MEASURED 2026-09-13 by enumerating every tracked `tsconfig*.json`: the root `tsconfig.json` includes only `client/src/**`, `shared/**`, `server/**`; `server/tsconfig.json` is rooted at `server/`. **38 tracked TypeScript files under `scripts/` are graded by nothing.** ⚠️ **The gate's silence over them is therefore NON-EVIDENTIAL — a type error in a script cannot turn CI red**, which is the `#453` shape inside the instrument we cite as coverage. Work: decide whether `scripts/**` joins the graded set (and pay down whatever it surfaces) or is explicitly declared ungraded with the reason recorded — **but not left silently uncovered.** | **CC-B** | **PLACED 2026-09-13, after 2.4-FEE-d.** Surfaced by Langston (§13) while censusing the `T-W20C-SCALAR-LEG` replay surface — the harness sits in no graph, which is how it was noticed. **His ruling: "yours to home, not to absorb — it is not this leg's to carry."** | — |
| 2.4b | ★ **`B-ALERT-QUEUE-INTEGRITY`** ➕ **FOLDED 2026-09-03 (Langston §13, not its own batch): a NEGATIVE-PATTERN EXEMPTION in `.gitignore` for the INSTRUMENT CLASS — `!scripts/**/*.sql`.** ⚠️ **WIDENED FROM MY OWN FIRST DRAFT, which was `fix-follows-pointer` again and Langston caught it before Step 4: I wrote the exemption as `scripts/batch-verify/**/*.sql`, which covers 2 files. CENSUS RE-DERIVED BY ME at the ref — 289 committed `.sql` across 13 directories, ALL under the same blanket `.gitignore:47 *.sql`, ALL added with `-f`.** My own justification was the argument against the narrow form: *"`-f` per file is a HABIT, not a GUARD"* is a statement about the CLASS. **`!scripts/**/*.sql` covers 27: `scripts/` 13 + `scripts/analysis/` 12 (CC-C's live instruments, including the composition query this design is keyed against) + `scripts/batch-verify/` 2.** ⛔ **DELIBERATELY LEFT IGNORED, and named per his condition rather than left silent: `drizzle/migrations/` (247), `migrations/` (4), `server/migrations/manual/` (2) — migrations are a GOVERNED force-add discipline with a `MANIFEST.txt`, where the explicit `-f` IS the gate and blanket re-inclusion would remove it; and the one-off dumps (`backups and data dumps/`, `PHASE_27.F.13.O_BACKUP.sql`, `audit/`, `diagnostics/`) which should not be committed by default at all.** (`#647` — the named home its ledger entry lacked; Langston Step-4 condition 4, 2026-09-02) — three named items, all in the alert-file family: **(i) the liveness watchdog appends WITHOUT the library lock** (`server/scripts/staging-liveness-watchdog.mjs:108`) — a row appended inside the library's read→tmp→rename window is lost; **(ii) every library rewrite is LOSSY** — `readAllAlerts` drops unparseable / shape-invalid rows (`system-alerts.ts:301-312`) and `writeAllAlertsAtomic` serialises only the parsed array (`:320-325`), against a header that promises in-place operator repair; **(iii) twin-site classifier drift** — `heartbeat-check.mjs:63` narrowed its benign-failure test to `/Alert \S+ not found/` and documented why, while `poller.mjs:394` still uses `/not found|already|terminal|resolved/i`; two sibling classifiers at different strictness, the same shape as the twin-site drift on `B-CROSS-SESSION-BLEED`. ➕ **(iv) (2026-09-22, `#1074`, Langston §13): the checker's open-batch backstops `gov-staleopen` / `gov-openmaxage` have NO resolve edge** — block (2) resolves only the malformed-open key (`poller.mjs:290`) and the orphan sweep only `gov-docgap`, so a raised backstop never clears and is stranded at close. **Owner CC-B.** | CC-B | **AFTER 2.4a** — (i)/(ii) were cross-referenced on `#647` by the allowlist pre-audit (A9) and left unfixed there by scope; (iii) is the item Langston named at Step 4 | measured 2026-09-02: 782 lines / 0 unparseable / 0 duplicate ids — the invariants hold TODAY, which is why they are a batch and not a hotfix |
| 2.4c | ★ **`B-RTB-SIGNAL-IDENTITY`** (`#1006`) — **the RTB signal queue's database identity does not match its own schema, and neither includes the asset class.** Live, `rtb_signals` is unique on `rtb_signals_symbol_strategy_idx (mode, symbol, strategy)`. The checked-in `shared/schema.ts` declares an index of the SAME NAME over four columns (it adds `status`), and the migration snapshot agrees with the schema, not with the database. So a database built fresh from the repository gets an index the RTB upsert's conflict target cannot use. Separately, an xStock and a crypto coin that share a ticker alias at every RTB identity operation in `ready_to_buy_service.ts`: promotion cleanup, incumbent selection, and R-multiple replacement. ⇒ **One identity, reconciled across the live index, `shared/schema.ts`, the migration snapshot and the upsert conflict target, that includes `asset_class`** (Kyle, 2026-09-09: identity = `(symbol, asset_class)`), **with the RTB identity reads moved with it.** ⛔ **Any string key it needs comes from `#1024`'s encoding SSOT (`assetSymbolKey(assetClass, symbol)`, `B-SYMBOL-CLASS-IDENTITY` OBJ-1) — never its own** (Langston's two-batch ruling). **Owner CC-B.** | **CC-B** | **PLACED 2026-09-11.** Langston's two-batch ruling assigned this row to CC-B on 2026-09-09; it is recorded in `#1024`'s entry (`bb4d3c695`). The row itself was never written until Infra Claude found the gap. After 2.4b, before 2.4d. | ⚠️ **Realized incidence is UNMEASURED.** `rtb_signals` is a continuously cleared working queue (one row at the `#1006` measurement), so *zero collisions* there cannot detect one. The `DASH/USD` cross-class closed trades are the evidence that the aliasing reaches trades. The index and schema reconciliation needs nothing from `#1024`. Only a string-key read, if the design needs one, waits for its encoding SSOT. |
| 2.4d | ★ **`B-WRITER-ACTOR-ALLOWLIST`** (`#1046`) — **`langston-memory-write` records `--by` as free text into a permanent append-only index**, a week after `#987` and `B-DEPLOY-ACTOR-ALLOWLIST` made the other two tools' actors canonical. ⇒ **refuse a `--by` outside the canonical actor set, with the derivation stated in the tool.** | **Infra Claude** | **PLACED 2026-09-11 — Langston §13 on `B-LANGSTON-CONTEXT` P-6b, disposition 2; after 2.4b, which owns append-only-index integrity** | ⚠️ Until it lands, `--by` is recorded as a CLAIM beside the real uid, which is honest but not canonical. ➕ **FOLDED, Langston 19:06Z (amendment D): the writer is `750 root:langston`, but the reader it IMPORTS to decide whether content is restored (`/opt/langston-memory/bin/langston_memory.py`) is `langston:langston 755` inside a langston-owned `bin` — so the verification code is writable by the account the writer's mode excludes.** Not a live exploit (sessions arrive as root; langston is an authorised caller), so this is a disposition, not a defect: give the reader the same mode and a root-owned parent, or state what the `750` actually buys. ⚠️ **Renumbered from 2.4c the same day:** Langston's ruling recorded at `RUNNING_ISSUES` `#1006` already places `B-RTB-SIGNAL-IDENTITY` (CC-B) at 2.4c, although this plan carries no row for it — told to CC-B. |
| 2.4e | ★ **`B-PYCACHE-PREFIX-INVOCATION`** (`#1048`) — `langston-memory-write`'s guard (c) pins `sys.pycache_prefix` to close the reader's cache leg, but `PYTHONPYCACHEPREFIX` still relocates ~18 startup-module caches before the pin runs, and a header-matched planted `.pyc` there executes (Langston reproduced it). ⇒ **total closure at the invocation** — run the reader-executing tools under `python3 -E`/`-I`, plus a check that no unit or wrapper exports `PYTHON*`. | **Infra Claude** | **PLACED 2026-09-11 — Langston GATE-1 on `B-LANGSTON-CONTEXT` P-6b guard (c), §9.4 disposition 3; after 2.4d, sequenced after the P-6b install** | ⚠️ Latent, not live: measured today, no `PYTHON*` var in any unit, `/etc/environment` or `/etc/profile.d`. The pin covers the reader leg; the honest-reach note is in the guard docstring. Reach limit: root's crontab and rc files unread. |
| 2.4g | ★ **`B-WAKE-SOURCE-TRUTH`** (`#1054`) — the wake-source docs name `/var/log/langston-alert-invokes.log` as a live source for Langston's alert completions, but it is 0 bytes since 2026-06-28: its writer (`langston-alert-handler.sh`) was retired with the Telegram-era alert path at the Discord cutover, and alert wakes now ride the Discord inbox (`langston_alert_inbound`, 1,402 rows as at 2026-09-12T10:26Z, live and growing). ⇒ **correct wake-source #2 across `CLAUDE.md`, shared `MEMORY.md` §4.5 and the wake-watcher runbook, and drop the dead file from the tail command.** | **Infra Claude** | **PLACED 2026-09-12 — Langston in-channel hypothesis, investigated by CC-INFRA, §9.4 disposition 3; after 2.4e** | ⚠️ Not a live break — no wake is lost (the Discord leg covers it). The risk is a false-absence trap: a session reading the empty log as *no alerts*. Bug-taxonomy outcome (3). Langston's own `CLAUDE.md` §5.3 carries the matching stale line, flagged to him. |
| 2.4f | ★ **`B-ARCHIVE-RETENTION-SIZING`** (`#592`, alert `74424570`) — ⭐⭐ **KYLE DECIDED 2026-09-23: NO ACTION ON THE 72 % DISK WARNING (`d30fa956`).** *"I am not worried about the database because the full month of August will migrate to the warm storage at the beginning of October."* ✅ **MECHANISM VERIFIED AT THE LIVE CONFIG, not taken on trust:** the high-volume archive tables — `signal_eval_archive`, `signal_eval_provenance`, `exit_decision_archive`, `pair_scan_archive`, `macro_feed_archive` — all carry `data_lifecycle.*.hot_retention_days = 30` and are MONTHLY partitions, so August's partition leaves hot storage once its last day is 30 days old: **the start of October**, exactly as Kyle said. ⚠️ **Scope of that: the 365-day tables (`closed_trades`, the OHLC tables) do NOT move, so the relief is the archive tables' share, not the whole database.** A higher disk rung re-mints its own alert. — **the database disk alert is a SCOPE DECISION, not a broken sweep.** Langston's triage of this class, re-stated at the 2026-09-12 fee-batch Step-8 routing: **the retention windows were never sized to the write rate**, so the sweep is doing exactly what it was told and the cap is reached anyway. `#592` holds the measurement (`signal_eval_archive_2026_07` at 23.60 GB, the 2nd-largest object; `switch_on_shadow_evidence` actively written) and has sat **OWNER UNASSIGNED since 2026-07-28**. ⛔ **The deliverable is a DECISION, not a fix: for each actively-written archive family, the window that its write rate implies at the 200 GB cap, priced against what the data is FOR** (calibration needs history; `#592`'s own note is that thin history is a retention artefact, not a young system). ⭐⭐ **THE NUMBER THIS ROW NEEDS IS THE SLOPE, NOT THE LEVEL (Langston, 2026-09-13).** ⛔ **AND NOBODY MAY QUOTE THE ALERT BODY: `74424570` is frozen at its 2026-09-11 mint (130.6 GB / 65.3 %, `resurface_count=2`) and a resurface REPLAYS that snapshot rather than re-measuring — I quoted it twice to Kyle as current before being caught.** ✅ **MEASURED LIVE 2026-09-13 ~06:45Z, INDEPENDENTLY TWICE: CC-B 132 GB / 65.82 %, Langston 131.67 GB / 65.83 % — two instruments agreeing.** ⇒ **SLOPE: +1.07 GB over ~2 days between mint and now.** ⚠️ **TWO POINTS IS A SLOPE, NOT YET A TREND, and it is stated as such because it is the only slope we have.** **Window sizing remains Kyle's decision; this row exists to hand him a RATE rather than a level.** ⭐⭐ **MEASURED 2026-09-13 — A DECISION ALREADY TAKEN HAS NOT EXECUTED, AND EXECUTING IT NEEDS NOTHING FROM KYLE.** ⛔ **8.29 GB sits in partitions whose ENTIRE range predates 2026-08-01** — i.e. ≥44 days old against a 30-day policy — **enumerated, 13 partitions, not sampled.** ✅ **ONE of the 13 is correct: `switch_on_shadow_evidence_2026_07` (60 MB) is the DOCUMENTED 90-day exception.** ⇒ **the other 12, ~8.23 GB, are the four `*_ohlc_1m` tables** — April, May, June and July partitions still hot, largest `xstock_spot_ohlc_1m_2026_05` at **2,905 MB**. ⭐ **`STORAGE_POLICY` §3 RULED those four at 30 d on 2026-08-17 (Langston ruling (C)) — but the flip was made CONDITIONAL on the `#685` deploy proving the family tierable, and it evidently has not run.** ⇒ **Tiering them moves ~8.23 GB out of the HOT tier — ~4.1 % of the 200 GB cap, or **~16 days of runway** at the measured +1.07 GB / ~2 days. Move-not-delete: warm→cold preserves everything.** ⚠️ **AND THE SHAPE OF THE REST IS A SINGLE TABLE: `signal_eval_archive_2026_08` alone is 26 GB, four times the next largest.** ⚠️ **NOTE THE MONTHLY-PARTITION EFFECT before reading any of these as over-retention: a MONTHLY table under a 30-day policy legitimately holds up to ~60 days, because whole-partition drop cannot fire until the newest row in the partition ages out. `signal_eval_provenance` holding 2026-08-01→09-13 is CORRECT, not a defect — I checked the partitioning before filing it as one.** ⛔⛔ **CLASS SPLIT, DEMANDED BY LANGSTON AND IT CHANGES THE EVIDENCE THOUGH NOT THE NUMBER:** I recorded the twelve as one class without checking. **ENUMERATED at `data_lifecycle` (the table name is INSIDE `constant_name`, which is why my first two probes returned empty):** `crypto_spot_ohlc_1m` **365** (2026-05-18) · `xstock_spot_ohlc_1m` **365** (2026-05-10) · `xstock_perp_ohlc_1m` **365** (2026-05-10) · `crypto_perp_ohlc_1m` **30** (2026-08-18). ✅ **ALL TWELVE PRE-AUGUST PARTITIONS BELONG TO THE THREE STILL AT 365 — `crypto_perp` has ZERO**, which makes it the **POSITIVE CONTROL: the sweep DOES tier when the value is 30.** ⇒ **one class after all — UNEXECUTED DECISION, not sweep failure — but now on evidence rather than assumption.** Split by parent: `xstock_spot_ohlc_1m` 4 parts / 4,766 MB · `crypto_spot_ohlc_1m` 4 / 2,884 MB · `xstock_perp_ohlc_1m` 4 / 776 MB · `switch_on_shadow_evidence` 1 / 60 MB (the documented 90-d exception, correct).
⭐⭐ **AND THE REFINEMENT THAT MATTERS MOST FOR KYLE'S DECISION: THE 26 GB TABLE IS ALREADY AT THE TIGHTEST RETENTION WE ALLOW.** `signal_eval_archive.hot_retention_days` = **30** (2026-08-18). ⇒ **its 26 GB is 30 days of one table, held up to ~60 by monthly partitioning — so it is NOT an available lever without CHANGING POLICY, which is Kyle's call. The ~8.23 GB OHLC item IS available and needs no decision from him at all.** | **CC-B** | **PLACED 2026-09-12 — Langston's §10.5 routing of alert `74424570` (`owner=CC-B action="place the retention-sizing scope decision; do not re-file as a sweep failure"`); positioned after 2.4e and before 2.5, i.e. behind the fee batch and the actor-allowlist work, ahead of the freshness-log reader** | ⚠️ **The window sizes are KYLE's call, not mine** — this row places the decision and prepares the options; it does not pre-empt them. ⛔ **Do NOT re-file this as a sweep failure** (Langston, explicit): the sweep's durability defect is a DIFFERENT item, already recorded on `#592` as the resume-via-HEAD ruling. ⚠️ The alert row stays **ACTIVE and unacked** — ack silences the dedupe key and only a resolve frees it (`#982`), and the disk does not stop growing because the row went quiet. ➕ **FOLDED 2026-09-12 (CC-B, re-derived at the database and at the ref — not taken from the alert body): THIS IS A DATED SEQUENCING PROBLEM, NOT A CAPACITY ONE, AND ITS CAUSE IS BY-DESIGN.** **(1) THE STRANDED BLOCK:** `signal_eval_archive_2026_08` is a **single month-long partition, 38 GB heap / 50 GB with indexes**, while September runs **156 partition objects across daily children** (~1.5 GB heap, ~1.9 GB with indexes, each). **A month-long block cannot age out of the 30-day hot window until EVERY day inside it is older than 30 days ⇒ no relief before 2026-10-01.** **(2) THE CAUSE, ESTABLISHED AT THE CODE — AND IT MEANS OCTOBER CANNOT REPEAT IT:** `daily-partition-cutover.ts:45` sets `{ table: 'signal_eval_archive', cutoverDate: '2026-09-01' }`. August was monthly **by design** (the table had a live monthly era, so it cut at the first clean future month boundary after that batch's deploy; the cutover migration dropped eleven pre-created future monthlies and the monthly creator gained a matching guard). **September onward is already daily and the exclusion is enforced by `isDailyPartitionedForMonth`, so there is NO recurring defect to fix** — only this one stranded block. **(3) MY FIGURES, WHICH CORRECT THREE OF THE ONES IN CIRCULATION:** database **130 GB = 65.2 %** of the 200 GB cap (the alert body and the 02:19Z triage say 131.3 GB / 65.7 % — an older snapshot; a re-surfaced alert replays its original numbers). The `signal_eval_archive` family is **68 GB = 52.5 % of the database**, not the 40 % quoted — **the dominance is WORSE than stated, which strengthens the case rather than weakening it.** And "August is 1 partition" is **6 objects** (1 table + 5 indexes), one *table* — stated because the next reader runs the same query. **(4) THE ACCRUAL, MEASURED PER DAY RATHER THAN ESTIMATED:** weekdays **1.87-1.96 GB/day** including indexes (09-01..04, 09-08..11), weekends **1.13-1.23 GB** (09-05..07) ⇒ **~12.6 GB/week**. ⛔ **The ~2.9 GB/weekday figure in circulation is HIGH.** Against **30 GB of headroom to the 80 % critical line (160 GB)** that is **~2.4 weeks ⇒ the line is reached around 2026-09-29..30**, not 09-22..30. **The conclusion survives the correction and is the whole point: the critical line still plausibly arrives BEFORE the 10-01 relief — but by DAYS, not by a week.** **(5) THE DECISION THIS ROW NOW CARRIES:** split or forward-tier the August block so the 30-day rule can bite it now, **or** accept touching the 80 % line for a few days. ⚠️ **Nothing breaks at 80 % — it is a louder alarm, not a cliff.** ⛔ **AND THE QUESTION UNDERNEATH IT IS KYLE'S, NOT MINE (Langston raised it, I agree): we keep 30 days of every evaluation the system makes, and that single choice is most of the storage bill. Whether 30 days is the right number is a judgement about how far back we ever need to look.** ⚠️ Alert `74424570` stays **ACTIVE and UNACKED** — deliberately, so it keeps surfacing until the block is actually dealt with. |
| 2.4g | ★★ **`B-OUTCOME-CORPUS-CAPTURE`** (`#596`, `RUNNING_ISSUES:98-99`) — **THE ONLY RECORD OF WHAT A TRADE ACTUALLY EARNED IS AN UNBACKED, UNTIERED LOG SINK, AND CLOSED TRADES ARE HARD-DELETED AT 90 DAYS.** ⛔ **MEASURED at the object 2026-09-12 (CC-B): the DB copy carries NO outcome fields — no `netProfit`, no `exitPrice`, no `pnl`, and ZERO such keys in `context` across all 39,080 closed rows**; `vts_open_trades` is absent from the retention sweep and its GC is a `DELETE`. ⭐⭐ **THIS IS A CLOCK, NOT A BACKLOG ITEM (Langston, 2026-09-12): every day of deferral destroys corpus that the calibration cannot later recover.** ⭐ **IT IS ITS OWN ROW, IN FRONT OF 2.4h, RATHER THAN 2.4h's FIRST OBJECTIVE — CC-B's call on Langston's explicit option.** Reason: capture is **cheap and separable**, it serves the Phase-25 calibration **independently of whether the evaluator batch survives scoping**, and an objective buried inside a batch that may be re-scoped lets the delete keep running while the scope is argued. ⛔ **FOLDED IN (Langston §13, same subject, not a separate row): `active-execution-engine.ts:4432` `di_at_open: diAtQueue ?? metadata.DI ?? 50` — an ABSENT DI is persisted as the LITERAL 50, indistinguishable from a measured 50, in exactly the store a pWin calibration would read. `#546` absent-as-valid, in the position where it does the most damage.** ⚠️ **PRECISION (Langston): these are JSONB METADATA KEYS on `active_open_positions`, NOT typed columns — `P19_B7_2c_SCOPE.md:11` explicitly CUT the typed `di_at_open` / `dbs_score_at_open` / `vol_noise_at_open` columns. Do not go looking for a column.** ⛔⛔ **AND THE READER RE-APPLIES THE SAME SENTINELS — `active-execution-engine.ts:2042-2044` resolves `atr_at_open` to `0`, `di_at_open` to `50`, `vol_noise_at_open` to `0.3`. SO BOTH SITES MOVE OR NEITHER DOES: an honest write is laundered straight back by the read. It is TRUTHINESS, not nullish (`? :`, not `??`), so a genuinely measured `0` is replaced by the sentinel too — the `#984` shape in a second place.** ⭐⭐ **MEASURED ON LIVE ROWS 2026-09-12 (CC-B), and it splits BY CLASS: of 5 open positions, both CRYPTO rows carry real values (`AERO/USD` atr 0.0163 / DI 1.93; `SUI/USD` atr 0.0139 / DI 21.12) while ALL THREE xSTOCK rows carry BOTH sentinels (`CRWD`, `GEV`, `MDB`: atr `0`, DI `50`). Langston predicted the DI split; it extends to ATR, which nobody claimed.** ➕ **AND A SECOND INSTANCE ON THE NEXT LINE, found by CC-B while verifying his: `vol_noise_at_open: metadata.VolNoise ?? 0.3` — same pattern, same write, second field.** ⛔⛔ **OBJECTIVE WIDENED 2026-09-12, AND FLAGGED AS A CHANGE RATHER THAN FOLDED IN SILENTLY (Langston's governance point — this is not a typo correction): FROM *capture* TO *PROVENANCE ON THE STAMP*.** Each value must carry **a flag saying whether it was MEASURED, DEFAULTED, or MISSING** — not merely be stored. **WHY THE WIDENING IS FORCED:** the DI sentinel has a **SECOND MINT SITE INSIDE THE EVALUATOR** — `tec-evaluator.ts:331` `DI: input.DI ?? 50` (and `:332` `VolNoise: input.volNoise ?? 0.3`), **re-derived by CC-B** ⇒ a genuinely absent DI is manufactured as `50` *before the state machine ever sees it*, so a live row reading `50` **cannot be distinguished from a measurement at EITHER end**. `#546` in its purest form. ⇒ **capture alone would preserve an ambiguity it can never later resolve.** ➕ **PRECISION: `:333` passes `ATR: input.atr` with NO sentinel — so ATR differs from the other two: its `0` is minted only at the persist-site truthiness read, not inside TEC. Four mint sites, two fields, two layers, and they are not symmetric.** ⛔ **AND THE OBVIOUS INSTRUMENT IS UNAVAILABLE — DO NOT PLAN AROUND IT.** The `[ATR_FLOOR]` warn line needs `viaAtrFloor = useTrailing && atrUnavailable`, and **MEASURED at the database by CC-B: `trailing_enabled_active` = `false` and `trailing_enabled_vts` = `false` on ALL FOUR classes since 2026-07-23, `moonbag_qualifying_strategies` = `[]` since 2026-05-26** ⇒ the floor is always entered by the `!useTrailing` arm and **that tag has ZERO EMISSION OPPORTUNITY** (`#661` leg 3). ⚠️ **The constant→`useTrailing` hop at every caller is NOT traced — Langston flagged it unverified and neither of us has closed it.** ⚠️ **Rider: `console.warn` goes to STDERR**, so any capture wired here lands in `error.log` (~14d) and NOT `out.log` (~2d) — a wrong-stream read is already a logged miss here. **Deliverables:** tier and back the earnings record · **stamp provenance (measured / defaulted / missing) on every kernel input, at BOTH the write and the read** — the reader at `active-execution-engine.ts:2042-2044` re-applies the same sentinels, so a one-sided fix delivers nothing · establish whether a **target-distance-conditioned outcome corpus with ordering against the stop** exists at all. | CC-B | **PLACED** | ⛔ **NOTHING BLOCKS IT, AND THAT IS THE POINT — the 90-day delete is running NOW.** Needs no scope decision and no prior batch; it is capture and retention, separable from every policy question downstream. |
| 2.4j | ⭐ **`B-TEC-PRIME-BOOT-RACE`** (`#1053`) — Owner **CC-B**. **Found by Langston off-scope at `8a` Step 7, 2026-09-12; re-derived at the object by CC-C before filing.** ⛔ **ON EVERY RESTART THE CRYPTO EXIT LOOP THROWS ON OPEN POSITIONS FOR A TICK** — `[TEC_CACHE_MISS_FATAL] resolveTECConfig … assetClass=crypto_spot but cache has no entry` → `Error checking position … checkExitConditions`, because `primeTECConfig()` is not warm yet. **MEASURED: 8 lines = 4 pairs at EXACTLY TWO timestamps, 13:32:19 and 23:30:41 — BOTH BOOTS, two of two** (`AERO/USD`, `SUI/USD`). ★ **This batch own subject one lane over: `8a` is the exit path ACTING on a price it should have refused; this is the exit path FAILING TO ACT AT ALL.** ⚠️ **NOT established: the window length in ticks, and whether an exit condition was live in it.** | CC-B | **PLACED 2026-09-12** | after the last `2.4g`, at the first FREE id. ⚠️ **NOTE: TWO rows are numbered `2.4g` (`B-WAKE-SOURCE-TRUTH` and `B-OUTCOME-CORPUS-CAPTURE`) — not mine, flagged to the owner, not silently renumbered.** ⛔ **DELIBERATELY NOT FOLDED INTO `8a`** — it would contaminate that row pre-registered window with restart-time noise in the same sink. |
| 2.4g-2 | ⭐⭐ **`B-GEOMETRY-REACH-BASELINE`** (`#1052`) — Owner **CC-B**, **KYLE-DIRECTED 2026-09-12: he gave CC-B the ball on the COMBINED batch.** ⛔ **THIS ROW'S PREVIOUS CONTENT (`B-PAPER-OPEN-ENTRYPOINT-CENSUS`) IS WITHDRAWN — see the `#1051` banner; the runtime contradicted the claim.** ⭐ **ONE batch: reward-to-risk and reachability are the same dial and are not separable; the reachability leg ABSORBS what had been scoped as a separate CC-C batch.** Scope: `Scope Files/B_GEOMETRY_REACH_BASELINE_SCOPE.md`, change-class `architecture`. ⭐⭐ **THE CEILING IS A HOLDING-HORIZON STATEMENT, NOT AN ARBITRARY NUMBER — the part every earlier account INCLUDING MINE got wrong.** `P19_REORG_B2_PRE_AUDIT.md:41-43`: the rule is `c·√H`, and *“H≈16 (√H=4), c≈1”* ⇒ ⛔ **r8 WITHDRAWS “ENCODES SIXTEEN HOURS”: `reach_atr_max = 4.0` is `INFERRED-FROM-CODE-AND-FORM`, NOT ESTABLISHED.** The pre-audit's general rule puts 4.0 inside its stated √H band, but its worked example specialises to the FLOOR and outputs a different quantity, and **how 4.0 was chosen is recorded nowhere.** Inverted: **3.12 ATR = our measured 9.76 h median hold**; **4.00 = the seeded 16 h**; **6.00 = what `strong_bull_trend` needs (36 h)**. ⛔ **Three horizons at once — and Kyle has DECLINED a max hold, so we are not committing to sixteen hours at all. The inconsistency is the finding; 4.0 is not.** ✅ **The original author seeded it *“CONSERVATIVELY … let the by-reason counts calibrate”* — this batch IS the calibration he asked for.** **MEASURED: `strong_bull_trend` (6.0/3.0, R=2.00) exceeds 4.0 on every signal; every `unreachable` line in the live log is that one strategy, dropped on the active lane.** **PROBLEM 2: the 29 filled September pattern trades are +$9.50 gross, $18.35 fees, −$8.85 net — all 29 reconciling, mutation-proved.** ⛔⛔ **NEITHER establishes that these strategies cannot be profitable: a zero-drift barrier benchmark gives 37.6 % against an observed 41.4 % on n=29 — ~4 pp over a coin flip, not distinguishable from noise.** | CC-B | ✅✅ **CLOSED 2026-09-13 — deployed `022fd27ada7fc149ec910adeb5d9ea92a971a29f`, CI run `34740307488` 4/4, Step-8 CONFIRMED. Observation window opens 2026-09-13T05:33:17.640Z; report `Batch Completion/B_GEOMETRY_REACH_BASELINE_COMPLETION_REPORT.md`.**
⛔⛔ **THIS CELL READ “STEP 4 of 11” AND “NEXT: 3 of 11” UNTIL 2026-09-13 WHILE THE COMPLETION REPORT SAID CLOSED — THREE STATES FOR ONE BATCH, IN THE DOCUMENT THAT IS THE AUTHORITY ON WHERE WORK STANDS.** ⭐ **Found when CC-C, asked to consider taking the batch over, read this row and asked which step I was on. A reader arriving here would have started work on a closed batch.** ✅ **Recorded rather than fixed silently: the failure is that nothing reconciles a status cell against its own completion report, and a clean cell would hide that.** *(Handover package: `Scope Files/B_GEOMETRY_REACH_BASELINE_HANDOVER_TO_CC_C.md`.)*
*(Historical, retained: the Step-4 record.)* **Step 4 of 11 — code review, revised diff with Langston.** ⛔⛔ **OBJ-A PART 3 SEEDS *ZERO* ROWS — all four derived ceilings REFUSED on a measured blast radius (Langston CONDITION-1, refusal APPROVED): three are a redundant re-encoding of `target_exit_atr_multiplier` against a spike, and `sma_trend_ride` is a live 57.49 % throttle on a genuinely continuous distribution — TWO SEPARATE REASONS, recorded separately.** ✅ **Parts 1, 2 and 4 ship: the resolver fix, the fail-closed `reach_atr_max_unknown_floor` (the §8 #10 defect — `min_rr` failed closed on a drifted token and reach did not), and the docstring. Earlier: STEP 2 PRE-AUDIT APPROVED WITH CONDITIONS** (Langston, 2026-09-13, read at `74db289c6`). Step 1 approved at `d174ed7a9` (scope r11). ⛔ **OBJ-A SEEDS NOTHING until the three same-commit fixes land AND the pre-registered lane ARBITER has run** — §1b-ter of the pre-audit. ~~NEXT: 3 of 11.~~ ⛔ **STALE — superseded by the CLOSED banner at the head of this cell.** | ⛔ **GATES `2.4h`.** ⛔ **r9 REPLACES THIS ROW'S ORDER CLAUSE — it still bound on objectives that no longer exist, which is `fix-follows-pointer`: the earlier correction went to the sentence Langston pointed at and not to the row.** **CURRENT ORDER, FIVE objectives (A–E) — the same five the scope and `#1052` carry: OBJ-A** (the per-strategy ceiling — a CODE change to `expectancy.ts` plus ⛔ **ZERO** tightening rows — all four derived ceilings REFUSED on a measured blast radius (Langston CONDITION-1, refusal approved): `inside_bar_reversal` 2.36 would have newly refused **95.32 %** of its signals, `sma_trend_ride` 1.97 **57.49 %**, `pivot_shift` 2.72 **55.43 %**, and `morning_star`'s 0.64 % is a hair-trigger sitting 0.02 ATR above a spike holding 84.62 % of its signals. TWO reasons, recorded separately: redundant re-encoding of `target_exit_atr_multiplier` for the spike strategies, and a live throttle on a genuinely continuous distribution for `sma_trend_ride`, and a fail-closed unknown-token floor **on the full key set, which is NOT crypto-only**) **→ OBJ-B** (read the existing fee findings, each with its era) **→ OBJ-C** (name the three record defects) **→ OBJ-D** (correct the governed artifacts carrying withdrawn claims) **→ OBJ-E** (governance). ⛔ **The paper-only reachability release is WITHDRAWN — already simulated 4,675 times in VTS — and the target re-derivation has moved to `B-EXCURSION-RECORD`.** ✅ **Kyle's binding objection still met: OBJ-A ships live configuration AND a code change.** |
| 2.4g-3 | ⛔⛔ **POPULATION BOUNDARY — ADDED 2026-09-13 (Langston rider on `#1063`): THE `B-PATTERN-ENUM-DRIFT` DEPLOY (row 3m-ENUM) ADMITS A CLASS OF OPENS THAT CANNOT OPEN TODAY** — the pattern-confirmed `volatility_edge` signals the `ABCD` enum gap currently rejects. ⇒ **the realised-excursion corpus THIS ROW's ratchet is waiting on gains that class at that deploy.** ⛔ **DO NOT POOL PRE- AND POST-ENUM EXCURSIONS.** ⭐ **Why it is written HERE and not only in `#1063`: the ratchet says the four refused `reach_atr_max` ceilings may not be re-derived until realised-excursion data exists — so the session that finally re-derives them is the one at risk, and it will read THIS row, not that issue.** ⭐⭐ **`B-EXCURSION-RECORD`** — **THE EXCURSION RECORD DOES NOT EXIST AND MUST BE *DESIGNED*, NOT WIRED.** `mfe`/`mae` are four lines of schema on two EMPTY tables (`trades`, `paper_trades` — the latter retired at `#573`), **ZERO code references tree-wide**, and the corpus tables have no `pgTable` at all. ⛔ **SAMPLING IS A REAL DESIGN CALL: `latestEquityTick` is last-write-wins on its own poll cycle (`RUNNING_ISSUES:1495`), so a sub-4s excursion is invisible and an MFE taken off it is THE MFE OF THE SAMPLER.** ⭐ **IT IS THE ONLY THING THAT CAN SETTLE TIME-TO-TARGET**, which is the estimand `2.4g-2` proved this corpus cannot yield. **CARRIES FORWARD FROM `2.4g-2`:** the five non-shipping tightening rows (`inside_bar_reversal`, `pivot_shift`, `sma_trend_ride`, `volatility_edge`, `range_trade`) and `vwap_pullback`, whose two lanes disagree 3.3x with a 48.17 %-censored shadow cell. ⛔⛔ **STANDING CONSTRAINT, WRITTEN HERE BECAUSE NAMING IS NOT PLACING (Langston CONDITION-2, `B-GEOMETRY-REACH-BASELINE` Step 4): THE FOUR REFUSED CEILINGS — `pivot_shift` 2.72, `morning_star` 2.52, `inside_bar_reversal` 2.36, `sma_trend_ride` 1.97 — AND ANY SUCCESSOR TO THEM MAY **NOT** BE RE-DERIVED FROM POST-DEPLOY HOLDS UNTIL REALISED-EXCURSION DATA EXISTS.** ⭐ **Why it is a RATCHET and not a caveat:** `H` is endogenous to the gate — a tighter ceiling blocks far targets, those trades exit sooner, `H` shortens, and the next derivation reads a corpus the last one narrowed. ⛔ **And the gate applies to the VTS lane too (`vts-runner.ts:1719`), so the learning corpus narrows with it — there is no untouched population to appeal to.** ⛔ **The standing rule forbids any row LOOSER than the class default without realised-excursion evidence, so the ratchet has no exit until THIS batch lands.** ✅ **`B-GEOMETRY-REACH-BASELINE` shipped ZERO ceilings, so the ratchet is not yet engaged — it is recorded now precisely so a later session cannot engage it without seeing this line.** | CC-B | **PLACED — not started** | ⛔ **AHEAD OF `2.4g-4` DELIBERATELY — same tables, and the joinability work must not land first and be re-opened.** ⚠️ **PLACED 2026-09-13: it was NAMED with a `HOME:` line in the `2.4g-2` scope at r7 and NEVER WRITTEN INTO THIS PLAN — the §9.4 failure that rule states in its own words, *“naming is not placing.”* Langston asked whether a second item was being minted; it was not — but only because I checked.** |
| 2.4g-4 | ⭐ **`B-TRADE-RECORD-JOINABILITY`** — **TWO CLASSES, STATED AS TWO, AND THE MEMBERS ARE NAMED BECAUSE A COUNT IS NOT A SET.** *Joinability:* **(i)** the `vts_open_trades` ↔ `exit_strategy_alternates` **id-space split** (0 of 200 sampled ids match; `shadow_*` / `vts_SYM_STRATEGY_ts` against 100 % `vts_SYM_ts`) and **(ii)** the **archive stage split** (probability at `pre_filter`, geometry at `sqe`/`admitted`). *Writer:* **(iii)** `realDiAtOpen` **NULL on all 3,685 rows** it is stamped on, and **(iv) NEW from `2.4g-2`: the xStock SHADOW writer stamps `atrAtOpen = 0` on 1,029 of 1,029 rows — 100 %, never once a real ATR** (crypto is clean on both lanes at 0 of 29,873, and xStock's own VTS lane is clean at 0 of 1,183, so the defect is exactly one writer on one class). | CC-B | **PLACED — not started** | ⚠️ **PLACED 2026-09-13, same `§9.4` miss as `2.4g-3`.** ⛔ **`diAtOpen` is NOT a member and was withdrawn as WRONG OBJECT (Langston):** at the VTS writer the `50` is a deliberate, documented live trailing-engine input with `realDiAtOpen` beside it as the honest capture — the opposite of absent-as-valid, successor already shipped. |
| 2.4g-5 | ⭐⭐ **`B-TARGET-MULTIPLE-VS-HORIZON`** — **THE SCOPE CALL `B-GEOMETRY-REACH-BASELINE` SURFACED AND DELIBERATELY DID NOT MAKE.** ⛔ **MEASURED: for multiplier-derived strategies the reachability ceiling and `target_exit_atr_multiplier` ARE THE SAME KNOB** — `targetPrice = entryPrice + target_exit_atr_multiplier * effectiveATR`, so `atrsToTarget` IS that multiplier and its distribution is a SPIKE (p90−p10: `morning_star` **0.001**, `support_bounce` 0.002, `inside_bar_reversal` 0.008, `volatility_edge` 0.008 over 27,370 crypto shadow rows). ⇒ **a ceiling above the spike changes nothing and one below it turns the strategy OFF; there is no intermediate regime.** ✅ **So if a strategy's target is genuinely too far for its horizon, the lever is the MULTIPLIER — which moves the geometry — not the ceiling, which refuses the signal.** ⚠️ **AND THE LEVER IS NOT THE SAME FOR EVERY STRATEGY: `sma_trend_ride` has NO ATR term (`strategy-engine.ts:520-533`); its levers are `break_target_r_multiple` and `trailing_strength_factor`, and its `atrsToTarget` IS genuinely continuous (560 distinct values, p90−p10 **2.991**). Do not carry one lever across both regimes.** ⭐ **This also explains the observation the whole batch started from: `strong_bull_trend` is not *occasionally* unreachable — its declared multiplier is 6.0 against a 4.0 ceiling, so it is categorically OFF.**  ⛔⛔ **NAMED FIRST ITEM — `strong_bull_trend` HAS PASSED THIS GATE **ZERO** TIMES OUT OF **298,731** EVALUATIONS (live Reachability Gate panel, cumulative since 2026-08-23; 293,871 of the rejections are `Target Unreachable`).** ⭐ **Its declared `target_exit_atr_multiplier` is 6.0 against a 4.0 ceiling, so it is not *occasionally* unreachable — it is CATEGORICALLY OFF, and nobody decided that.** ⛔ **`B-GEOMETRY-REACH-BASELINE` CHANGED NOTHING FOR IT** — its ceiling is still 4.0, zero rows were seeded, and the `#1052` r7 HYPOTHESIS relabel is the ANALYSIS, not a home (Langston Step-8 F5). ✅ **DISPOSITION: bug-taxonomy outcome (2), working-as-designed-but-UNADDRESSED — a DECISION, never unilateral code.** ⚠️ **Whether it resolves as §9.4 (3) RECONNECT (move the multiplier or the ceiling so it can trade) or (4) REMOVE (retire a strategy that has never traded) is exactly what this row exists to decide, and it cannot be decided without the realised-excursion evidence from `2.4g-3`.** | Kyle (decider) · CC-B (options paper) | **PLACED — not started** | ⛔ **GATED ON `2.4g-3`: it is a bug-taxonomy outcome (2), working-as-designed-but-UNADDRESSED — a DECISION, never unilateral code — and the decision needs realised-excursion evidence to be made honestly.** ⚠️ **Changing a multiplier moves live trade geometry on the active lane, so it is Kyle's call and not a fix.** |
| 2.4h | ★★ **`B-EXIT-POLICY-EVALUATOR`** — **our expectancy model cannot RANK exit alternatives, and the ready-to-buy queue already ranks live signals on a key that is monotone in target distance.** ⛔⛔ **NOT A DEFECT — bug-taxonomy outcome (2), working-as-designed-but-unaddressed, so it is a SCOPE DECISION FOR KYLE and never a unilateral fix (Langston).** The kernel is a **gate** scored on ONE supplied triple and was never handed a choice set; using its score as an objective function over alternatives it was never calibrated against is a **category error in the proposer**. **MEASURED:** `netEV` is **exactly affine** in target distance with slope `pWin` clamped to `[0.40, 0.60]` ⇒ slope bounded below by 0.40 — all three friction producers price on ENTRY notional only. **The finding is the omission of `P(target first GIVEN distance)`**, plus a clamp that structurally cannot express a low-probability far target. **LIVE FOOTPRINT:** the RTB ranker scores queued signals on `netRewardToRisk`, and a **fabricated 2 % target** (`#927`) feeds that key when none is supplied. ⚠️ **THE DI READING SPLITS BY CLASS (Langston, re-derived; my earlier 'nearly pure geometry' was right for xStock and WRONG for crypto):** `di_at_queue` IS recorded since reorg-B3; the `DI = 50` default fires **only on a NULL**; **xStock pins at the pWin ceiling because P19-B8.5h deliberately did not carry DI**, while non-strong-trend crypto carries a real DI (live pWin ≈ 0.46). **Framing:** `Langston Design Asks/B_EXIT_POLICY_EVALUATOR_PROBLEM_FRAMING_r3.md` (Coltrane early-framing + Langston, accepted 2026-09-12). ⭐ **DISCHARGES PART OF ROADMAP `25-19`** (Net-Expectancy gate judgment-quality validation, booked by Kyle 2026-06-21 — the SAME DAY he made that gate the cost-coverage judge and deleted the floor-lift). ⛔ **`25-19` STAYS OPEN; this row discharges part of it, it does not close it.** ⛔ **`B-R-FROM-COST` IS DEAD** — it presupposed its answer and named a mechanism Kyle deleted. ➕ **ADDED 2026-09-12 (§9.4 disposition 2, out of the 2.4-FEE-b Step-2 audit): THE ROUND-TRIP FEE IS THE ADDITIVE SHORTHAND, NOT THE EXACT HURDLE.** `expectancy.ts` uses `fee * 2` at five sites; the exact break-even is `P1/P0 = (1 + entryFee) / (1 - exitFee)`, so the additive form understates by roughly `2f²` — **~1.3 bps at the crypto 0.80 % taker, ~0.02 bps at the new xStock rate, ALWAYS in the optimistic direction.** Grouped here because it is the SAME expression as the entry-notional-versus-exit-notional asymmetry Langston named (~3 bps, correct sign) — one measurement pass settles both, and **neither may be cited as a fix for the ranking problem; both are two orders of magnitude too small.** ⚠️ **NOT a restatement of the known `pre-execution-validator.ts:198` both-legs-one-mode simplification — a different defect on the same line; searched with a control that DID find the known note, and this point is unrecorded.** ⭐ **PRIOR ART (Codex audit3), better framed than mine: CLASS IS NOT A COMPLETE VENUE FEE-GROUP KEY** — `fee_model` keys by asset class while the venue keys by INSTRUMENT (stablecoins by base currency, FX pairs on a separate schedule, plus the selected-pair rebate). **That audit declared the instrument-to-fee-group map INSUFFICIENT and requested it; OBJ-10 of row 2.4-FEE-b IS that request.** | CC-B | **PLACED** | **2.4g must land first** — it is the outcome corpus this batch reads. Sequenced BEFORE any consuming policy batch: the policy question is a CONSUMER of the evaluator, so parallel is not available (Langston). |
| 2.4i | ⭐⭐ **`B-VPNL-WRITER-BOUND`** — **`exit_strategy_alternates.virtual_pnl_pct` IS WRITTEN UNBOUNDED, AND A MEAN TAKEN FROM IT REVERSED A CONCLUSION I HAD ALREADY PUBLISHED.** MEASURED 2026-09-12 (CC-B, re-deriving Langston): `crypto_spot` n=100,212 with **21 rows above 100 pct in absolute value and 1,887 above 20 pct in absolute value** — 1.9 % of rows — max 220.98 absolute; `xstock_spot` n=70,524 with 2 and 8, max **448,211.38**. ⛔ **THE CONSEQUENCE, NOT HYPOTHETICAL: the untrimmed mean says re-enabling trailing costs 0.70 pp; trimmed it GAINS 0.12 pp and on medians it GAINS 2.66 pp. Direction, not magnitude.** ⛔⛔ **FIRST ITEM IS A TAXONOMY READ, NOT A TRIM PATCH (Langston): whether the extreme rows are a real writer defect or a legitimate simulator output on a degenerate price is NOT SETTLED, and clamping before knowing which would hide the second case.** Then: a bound at the writer, medians published beside every mean, and `trade_source` named in any heading that cites the corpus (it is **`vts` on 100 % of 173,952 rows — zero active-path trades**). ⭐ **GATES any row or document that cites this corpus**, including 2.4g/2.4h and `RR_BASELINE_RECOVERED_EVIDENCE`. ⚠️ **The gate is stated here rather than implied by row order** — Langston asked for it placed ahead of its consumers and I declined to renumber live rows to achieve that; if he wants it physically earlier, say so and I will move it. | CC-B | **PLACED** | Nothing blocks it. It is small, and every hour it is not done is an hour someone can quote a mean off an unbounded column. |
| 2.5 | ★★ **`B-FRESHNESS-LOG-READER`** — **NOTHING READS `~/.claude/dt-fresh-rules.jsonl`.** The hook has written a per-run record for months and no eye or process has ever looked at it, so every signal below is currently latent-by-construction. Deliverables: **(a)** a freeze detector — a path in `skipped_dirty` on **N consecutive runs** is a frozen path, which is Langston's own framing for why the force-push residual belongs here and not in the hook; **(b)** **P10 from `B-CROSS-SESSION-BLEED`** — per-skipped-path staleness (how long skipped, how far behind origin). ⚠ **P10 is NOT dissolved by the P5 fix**: residue now refreshes, but a GENUINE local edit still holds a whole directory entry indefinitely, all-or-nothing, and 14 days of that produced no signal at all; **(c)** watch `self_at_origin === false` (OBJ-1) across clones — that is the A4 "the repair lands one session late" signal, now emitted and still unread; **(d)** ⛔ **ALLOWLIST the `clone` field and REPORT the excluded count.** My own rig wrote **34 of 466 records** stamped `c1`, concentrated in exactly the freeze and residue classes this reader detects. ⚠ **A BASENAME allowlist is NOT sufficient (Langston, 2026-08-31): `DawnTraderV3` is the name `git clone` produces by DEFAULT**, so a throwaway rig is indistinguishable from the real spare clone — discriminate on absolute path or a marker file. **And the forward control is `USERPROFILE`, not `HOME`: MEASURED 2026-08-31, node's `os.homedir()` ignores `HOME` on Windows, so the scratch-HOME control Langston proposed silently writes to the REAL log on this platform.** | **CC-B** | **PLACED 2026-08-31** | ★ **Starts when `B-CROSS-SESSION-BLEED` closes** — it consumes that batch's `residue_refreshed` / `index_leaks` / `self_at_origin` fields, which do not exist before it. ⛔ **PLACED HERE BECAUSE IT WAS NOT PLACED ANYWHERE: I asserted twice to Langston that this batch was "already homed in the governance queue" and it was in NO plan file at all** — `§9.4`'s own named failure, *naming is not placing*, committed by the session quoting the rule. |
| 2.6 | ★★ **`B-SHARED-TMP-ISOLATION`** (#979) — **ALL FOUR SESSIONS SHARE `/tmp`, AND A COMMIT MESSAGE IS CONTENT.** MEASURED 2026-08-31: `git commit -F /tmp/m14.txt -- <my paths>` produced MY change set carrying ANOTHER SESSION'S message; artifact at `a9366f5e4`, corrected at `0264f24f7` with an identical tree hash. ★ **It sharpens `#753` rather than overturning it: no session wrote into another session's CLONE, but the sessions share a HOST** — that investigation scoped its population to git and could not have seen this. ⛔ **It defeats all three existing guards by construction:** rule 25's explicit paths protect the `--` side and the message arrives via `-F`; rule 25.c says read the staged CONTENT and is silent on the MESSAGE; `guard-bare-commit` checks the FORM only. **Deliverables (a) and (b) REWRITTEN 2026-08-31 after Langston ruled both repeated the census error one level up):** (a) **SWEEP THE WRITERS, NOT THE ONE SYSCALL** — the class is *a fixed name in a shared namespace* (`git apply /tmp/*.patch`, diff staging, hook temp files, **and the Helsinki `scp` path every Langston dispatch uses**), not `commit -F`; **REPORT THE WINDOW SWEPT** — reflog expiry bounds the instrument, so state its reach rather than implying none exist; (b) an **ALLOWLIST that REFUSES** — permit `-F` only under the session-scratchpad UUID path; ⛔ **NOT a `/tmp` denylist: a denylist is a predicate narrower than the population, and MEASURED 2026-08-31 the safe and unsafe paths SHARE A ROOT** (`/tmp` → `C:/Users/kyleg/AppData/Local/Temp`, and the scratchpad is `…/Temp/claude/<slug>/<uuid>/`), so the isolation is the UUID segment; (c) name the gap in `CLAUDE.md` rule 25.c — *the message is content too*; (d) ★ **AN ARCHIVE THAT NORMALISES IS NOT AN ARCHIVE — guard the CLASS** (Langston's §13, folded here at his invitation): `core.autocrlf` rewrote two CRLF-stored `.removed` archives to LF on `git add`; today that is a comment covering three files and **every future `.removed` archive of a CRLF-stored file fails silently.** Mechanism: **assert archived-blob sha == source-blob sha at archive time.** | **CC-B** | **PLACED 2026-08-31** | ★ **Independent of `B-CROSS-SESSION-BLEED`** — different mechanism, different surface; it is placed here only because the same session found it. ⚠ **REACH UNMEASURED and deliberately not guessed at: the population is every `git commit -F /tmp/...` any session has ever run on this machine, and nothing records that.** |
| 2.7 | ★ **`B-CHANGE-CLASS-DOCSET-FIT`** — the change-class matrix welds `SYSTEM_MANUAL` to `SYSTEM_IMPACT_MAP` as one REQUIRED pair, but their triggers differ (System Manual is trading-system-scoped; SIM is any component or cross-cutting state), so **infrastructure batches — hooks, bridges, alerting, governance tooling — have NO class that fits**: architectural in substance, SIM-applicable, System-Manual-inapplicable. Every one either under-declares or takes a mis-tiered `N/A` (six rows in `GOVERNANCE_EXCEPTIONS` already; `B-DIAG-387` `:39` now marked superseded in place). `CODE_PREFIXES` has no `.claude/` entry. **Bug-taxonomy outcome (2): working-as-designed, unaddressed — a scope decision.** | **CC-B** | **PLACED 2026-09-01** (Langston §13 on `B-CROSS-SESSION-BLEED`) | after 2.6 |
| 2.8 | ★ **`B-LANGSTON-LEDGER-SPLIT`** — ⛔⛔ **THE `34,605 B` FIGURE IN THIS ROW IS FALSE AND THE ARGUMENT BUILT ON IT IS WITHDRAWN (corrected in place 2026-09-04, `B-LANGSTON-CONTEXT` Step-2; CC-INFRA measured 11,245 B, LANGSTON RE-DERIVED IT INDEPENDENTLY AND GOT THE SAME).** The ledger is **11,245 B — UNDER the 24,576 B cap** — so *"the two rules cannot both hold"* is refuted: they can. ⚠️ **A LATER MEASURER GOT 22,799 B FROM A HEADING-RANGE READ, AND THAT DISAGREEMENT IS ITSELF THE FINDING: THE LEDGER SECTION HAS NO CLOSING BOUNDARY, so every figure is an artifact of where the measurer cut. Give it a boundary before quoting any size.** ✅ **THE SPLIT MAY STILL BE RIGHT — but on a DIFFERENT reason (the file is 61,155 B and the ledger is ~18% of it), never on this one. `B-LANGSTON-CONTEXT` P-4 ABSORBS THIS ROW, and absorption inherits the premise, which is why this is corrected here rather than only superseded elsewhere.** Split the ledger to its own auto-loaded file. *(Langston: "that is a batch, not an edit.")* | **Langston + Infra Claude** | **PLACED 2026-09-01** | after 2.7 |
| 2.8a | ⭐⭐ **`B-LANGSTON-FILE-FLOOR`** — **MEASURE THE FLOOR OF LANGSTON'S THREE ALWAYS-LOADED ARTIFACTS, THEN SET THE REAL NUMBER.** ★ **PLACED HERE ON LANGSTON'S OWN RULING (2026-09-05): *"same files, same hands"* as 2.8, which it follows deliberately.** ⛔⛔ **WHY IT IS A SEPARATE BATCH AND NOT A CONDITION ON 2.8: an absolute cap set below an unmeasured floor is a FAKE COMMITMENT — the same class §9.4 forbids for dates.** `#946` establishes that `24,576 B` is **unreachable**; it establishes **nothing about what IS reachable**, and every number I put to Langston (24,576 / 40,960-per-file / a reasoned third) was **rejected on exactly that ground.** ⇒ **DELIVERABLE: enumerate every block in all three files as load-bearing or not, and derive the floor from the enumeration — then, and only then, propose the absolute number.** ⚠️ **UNTIL IT LANDS THE RATCHET IS THE ONLY BINDING RULE (see 2.8b), and that is by design: the ratchet is meetable today, the number is not.** ★ **OPTIONAL READ, folded here rather than given its own batch (`B-LANGSTON-CONTEXT` §18.e): the ONE degradation instrument whose birthdate is NOT confounded — corrections of Langston RECORDED BY OTHER SESSIONS, via `langston-recall`, which reaches the pre-growth window because the CC sessions' recording practice did not change on 07-10, 07-28 or 08-05.** ⛔ **It partly measures the CORRECTORS' vigilance, it is not clean, and it is NOT a gate on this batch.** | **Infra Claude + Langston** | **PLACED 2026-09-05** | ⛔ **DO NOT re-propose an absolute cap before this batch runs — three were already rejected, with reasons, at `B-LANGSTON-CONTEXT` §18.a.** |
| 2.8b | ⭐⭐ **`B-LANGSTON-LOAD-RATCHET`** — ⛔ **THE BINDING RULE UNTIL 2.8a SETS A NUMBER, and it ships INSIDE `B-LANGSTON-CONTEXT` as row `P-5`, not as a standalone batch.** **THE RULE, ruled by Langston 2026-09-05 and approved by Kyle the same day:** *"the CEILING is the SUM of everything auto-loaded per invoke — `143,856 B` measured 2026-09-05 — and it may ONLY EVER DECREASE. Any batch touching any of the three leaves the total ≤ the value it FOUND."* ⛔⛔ **THE UNIT IS THE SUM, NEVER THE FILE — DO NOT RESTATE IT PER-FILE ANYWHERE.** A per-file cap is satisfiable by **SPLITTING a file while the loaded total is unchanged**, which is the same shape as the line-rule failure already lived through (compliant in form, 5.8 KB on one line); **`40,960 B` per file would license ~92 KB while wearing the word "compliant."** ⛔⛔ **AND THE CURRENT VALUE MAY NOT LIVE IN PROSE — A RATCHET RATCHETS, so the number CHANGES BY DESIGN on the next successful trim.** ⇒ **the live value lives in a STATE FILE the watch reads and writes; governance text names the FILE, never the number** (the shared `MEMORY.md`'s own standing lesson; cost of ignoring it = the Langston-model line wrong for **17 days** and row 2.8's `34,605 B` false for weeks). ★ **THE CEILING IS THE VALUE; THE `P-5` WATCH IS THE INSTRUMENT — they ship together or the ceiling is decoration.** ⚠️ **NOT hypothetical: `CLAUDE.md` §3.2 records that our own cap has *"NOTHING [enforcing] it… which is why it has been breached repeatedly"*, and the 2026-08-05 plan's item 2 CONTAINED this very watch, was never started, and that is precisely why his `MEMORY.md` grew `+137%` in 28 days unnoticed — the alarm was itself the unfinished item.** | **Infra Claude** | **PLACED 2026-09-05 — in flight as `B-LANGSTON-CONTEXT` `P-5`** | ★ **Where the cheap bytes are, per Langston: `CLAUDE.md` is RULES, should be stable, is the LARGEST at `66,994 B`, and has NEVER been governed at all.** ⛔ **The trim is argued from DIRECTION (monotone growth, no shedding lever), NEVER from demonstrated harm — we measured size and a growth trend; we did NOT measure degradation (`B-LANGSTON-CONTEXT` §17/§18.d).** |
| 2.8c | ⭐⭐ **`B-LEDGER-HEADLINE-INJECT`** — ⛔⛔ **THE CONDITION 2.8 CANNOT SHIP WITHOUT, AND IT IS LANGSTON'S OWN (ruled 2026-09-09, `B-LANGSTON-CONTEXT` Step-7):** *"moving the ledger to `LEDGER.md` saves ZERO bytes if it stays auto-loaded, and if it does not, my retractions become pull-only — a rule, not a mechanism, which is `#998`'s complaint."* ⇒ **the split needs a PUSH: the bridge injects the ~9 retraction HEADLINES (ref + verdict, one line each) in place of `11,245 B` of full text.** ⛔ **UNTIL THIS LANDS THE LEDGER STAYS IN `MEMORY.md` — do not trade a byte problem for a correctness one.** ⚠️ **It is a SEPARATE batch because bridges are OUT of `B-LANGSTON-CONTEXT` by that batch's own §5**, and folding it in would breach a boundary the scope set for itself. ★ **The retraction banner is the thing being protected: `langston-recall` prints retractions before every result, so a retraction that stops reaching him stops correcting anything — silently.** | **Infra Claude** | **PLACED 2026-09-09 — Langston-ruled, after 2.8's family** | ⚠️ **A headline is `ref + verdict` ONLY. The moment it carries reasoning it is the ledger again, one indirection out, and the byte problem returns wearing a new name.** |
| 2.8d | ★ **`B-LANGSTON-RECONCILE-VERB`** (`#1057`) — **the composer has NO reviewed reconcile verb for an out-of-band edit to `MEMORY.md`.** MEASURED 2026-09-13: a retraction written straight into the composed file (not via the tool) is correctly refused by every write path (`--compose` AND `--part` both trip the out-of-band `last_written_sha` check), so "recompose" cannot recover it — reconciliation took manual surgery (set the part to the current stamp-stripped body + reset compose-state + re-migrate). ⇒ **build a reviewed `--reconcile`/`--adopt` verb: adopt the current out-of-band `MEMORY.md` body as the part content, re-establish state, run the guards, and DECLARE the ledger/retraction deltas the adoption represents** (so it is not a silent clobber). The first incident was recovered by hand and read-back-verified (`#1057`); this verb is so the next one is one tool call, not surgery. | **Infra Claude** | **PLACED 2026-09-13 — Langston authorised the manual reconcile; the verb is the §9.4 disposition of the gap it exposed; after 2.8c (the memory-tooling family)** | ⚠️ The stamp-`body-sha` guidance that hid this is already FIXED (`workflow-10` SKILL, `#1057` FIX 1); this row is the tooling half only. Sequence behind the retrofit (2.8) if they contend for the same file. |
| 2.9 | ★ **`B-UMBRELLA-OPEN-STATE`** — the governance checker has NO state for a batch that is legitimately open until a phase closes. `GOVERNANCE_EXCEPTIONS`'s own header describes `umbrella-namespace` / `umbrella-done` rows, **but the poller parses only `open`, `na-skip` and `class-override`** (measured 2026-09-02, unbounded grep of `scripts/governance-checker/`: the strings appear in README only). So the `P19-B8.5` umbrella — which Kyle ruled stays open until Phase 19 closes — hit the 48h and 7d backstops and produced 6 stale-open alerts plus a max-age escalation with no honest way to answer them. **Build the designed row type:** an `umbrella-namespace` row suspends both backstops for that batch-id until an `umbrella-done` row lands, and `umbrella-done` is REJECTED while any sub-batch in the namespace is still open (the header's stated rule). Small; touches `poller.mjs loadExceptions` and the stale-open branch; unit-fenced like #605. | **CC-B** | **PLACED 2026-09-02** (Kyle's umbrella ruling) | after 2.8 |
| 2.10 | ~~`B-VOLATILITY-CACHE-RETIRE`~~ **MOVED 2026-09-02 (Kyle: "it needs to be set in the plan of batches") — it is a `server/core` code deletion, not governance; now a §1 STATUS BOARD row directly after `B-TEC-REGIME-PARAM-REMOVAL`, its sibling regime-path dead-code cleanup.** | CC-B | moved | — |
| — | ~~`B-RETIRED-SCORE-REMOVAL` (#558)~~ **MOVED OUT OF PHASE 19 → `POST_AUDIT_ROADMAP` §16.7 (Kyle ruling 2026-09-02).** It was never a row here — it lived only in `#558`'s ledger entry as *"sits INSIDE Phase 19, owner first-available"* — and is recorded so the departure is visible from the plan. Remaining A2 + Phase B legs are CC-B's, in Phase 16. | CC-B | moved | — |
| 3 | ★ **`B-GDRIVE-UNMOUNT`** (#757) **+ `#759` the blind alert check** — unmount the retired `/mnt/gdrive`, disable `rclone-gdrive.service` | **Infra Claude** (root required) | **PLACED 2026-08-28** | ★ **Replaces a deleted guard. Langston: *"unmount and `find /` stops hanging for every actor, on every box, with no matcher — that is a MECHANISM; a regex over free-form shell is a HABIT."*** ⚠️ **Kyle retired the mount 2026-07-23; it is still active five weeks on.** ⛔ **A ROOT-SIDE reader/open-handle census runs FIRST.** ★★ **AND IT NOW ALSO CARRIES `#759`: the mandated per-turn alert check reads `tail -50` of a 736-row log whose six live alerts sit 117-385 rows from the end — it sees ZERO of them, for all four sessions.** ⚠️ **Moved here from `B-GATE-GUARD` (position 9) on Kyle’s approval: I had parked it there because both were "small guard-ish things", which is convenient rather than correct — and it left a structurally blind check queued behind seven batches — Langston’s census was bounded to what `langston` can read and he said so (#453).** |
| 3.5 | ★★ **`B-INSTRUMENTS-OVER-RULES`** — the tools-not-rules batch (Kyle 2026-08-30): ⛔ **OBJ-1 REOPENED 2026-09-11 (`#1038`): the code search tool had never reached a session — CAUSE FOUND 14:12Z (installed and enabled, but never copied into the plugin cache sessions load from); the cache copy now exists and a fresh session gets the `LSP` tool; 14:36Z the fragile Windows launch was replaced by a local plugin (`dt-typescript-lsp`, launched via `node`) and verified END TO END — 65 references in 10 files on `toCanonical`, after a ~10-second warm-up during which it answers from one file only. OPEN: the in-session demo, and a pre-registered 14-day usage measure (`#1038`)** — installed on record and enabled, but a fresh session loads no plugins and has no `LSP` tool; the *"LIVE (OBJ-1 done)"* written here 2026-09-03 rested on a scratch-client test of the language SERVER, never on the tool; OBJ-8 done; OBJ-6 done; OBJ-4 struck. OPEN: OBJ-2 strip the 116-line review-history comment from `market-scanner.ts`; OBJ-3 change WHAT LOADS (path-scoped rule files so a session loads the repo-wide rules plus only its area); OBJ-5 governance | CC-A | **PLACED 2026-09-02 — it was IN FLIGHT with no row (found while writing the CC-A task list at Kyle's request); positioned here because it is already open and its remaining objectives are small** | scope `Claude Comms and Packages/Scope Files/B_INSTRUMENTS_OVER_RULES_SCOPE.md`; its OBJ-7 recommendation to Kyle (hold vs build the pricing-feed work in parallel) was delivered at Step-1 close. |
| 4 | ★ **`B-REVIEWER-LOOP`** (#758) — approach-round first; mechanism claims cited or labelled | CC-A | **PLACED 2026-08-28** | ★ **Before the four rules batches behind it, because they all USE the loop.** ⛔ **Langston: the loop asked *is this built correctly* three times and never *is this the right thing to build* — three correct reviews of an artifact that was unsalvageable from its first line.** ⚠️ **And the ACTIONABLE error rate is higher than the FALSE-FINDING rate: a session told "fix X because Y" acts on Y, so an invented mechanism sends it to rebuild the wrong thing — indistinguishable from a false finding, from the inside.** |
| 4.5 | ★ **`B-WAKE-QUIET`** (#995) — the running commentary Kyle flagged 2026-09-03: **all three sessions answer ~97% of automatic wakes with text**, which `CONDUCT.md` §5 forbids (measured; no setting changed, no clone stale). OBJ-1 add `CONDUCT.md` to the push notice AND `fresh-rules.mjs` (it is on NEITHER, yet injected every start from an unrefreshed local copy) · OBJ-2 drop the rule-history archive from the escalation list · OBJ-3 ⛔**KYLE'S CALL** route ledger-only pushes to the routine notice (the mid-task alarm is 73% false, but the list is his and the issue-number collision is live) · OBJ-4 deliver *act-don't-report* AT the event in the wake filter (`#694` piece 2; the rule at announce time does not hold) | CC-A | ✅✅ **CLOSED 2026-09-05. Langston APPROVED Step 4 at `fc0aaf174` and CONFIRMED Step 8 at `80936b434`; CI 4/4 per job on run `33870742602`; Step 6 recorded NOT-APPLICABLE (Kyle-approved — the batch's 26 files touch nothing under `server/`).** ★★ **THE RESULT IS NEGATIVE AND IT IS THE POINT: three instruction-shaped fixes measured, all three failed — the rule as written (96-98% speak rate), the rule restated, and an instruction delivered AT the event (0.5% and 11.8%). Only NOT DELIVERING works.** SHIPPED: the two cuts (Kyle: *"Yes, cut both"*), `CONDUCT.md` onto the notice + `fresh-rules.mjs`, the ledger and rule-history archive OFF the escalated notice, the recovery recipe fixed, `dt-push-notice.sh` in the repo for the first time with a drift check, and the measurement as a committed instrument with baselines. STRUCK WITH EVIDENCE: OBJ-4/4′, OBJ-5, OBJ-6/6′. ⛔ **LANGSTON CONDITION STANDS: re-opened if row 10 `B-ISSUE-BLOCK-GUARD` (#745) drifts.** ⛔ **AND THE BATCH SKIPPED ITS OWN STEP 2 — `#1005`, the `#754` shape, tripwire hit recorded.** Spawned `#1001` (staging 55 commits behind, engine undeployed), `#1002` (row 4.55), `#999` (row 4.7), `#998` (row 4.6). Record: `B_WAKE_QUIET_COMPLETION_REPORT.md` | ⚠️ **Honest reach: touches 35 of 73 wakes / ~40% of volume; the 52% Langston majority stays a rules problem.** |
| 4.51 | ⏳ **CLOSING 2026-09-11 — Langston APPROVED Step 4 at `c05e9d1fa` and CONFIRMED Step 8 (re-derived on the live reply); FINDING-A folded at `32780211d`, FINDING-D at `0df01c687`; installed on the laptop; closes on Kyle's acknowledgement. Record `B_WAKE_LEAD_NAME_COMPLETION_REPORT.md`. Spawned `#1043` → row 4.51a.** ★ **`B-WAKE-LEAD-NAME`** (#1040) — **the wake filter silently drops a Langston reply addressed to a session whenever that reply ENDS with another session's alert marker.** Measured by replaying all 409 of his replies since 2026-09-03: OLD 25 of 50, NEW 27 of 50, ANALYST 26 of 134, Infra 15 of 24 never woke the session they opened with — each count equal to the other-owner-marker count. `#995` made the marker a suppressor when replies rarely carried other sessions' triage; they now routinely do. ⇒ **an other-owner marker suppresses only when the reply does not OPEN with this session's name.** | **Infra Claude** | **PLACED 2026-09-11 — found in `B-LANGSTON-CONTEXT` §21.7; not a hotfix (no money, data, pipeline or UI), so a small batch right after the change that introduced it** | ⚠️ **Verification is the replay, both directions: dropped-with-leading-name → 0 for all four sessions AND no change in wakes on replies that do not open with the session's name.** |
| 4.51a | ★ **`B-READ-MODEL-BLOB-VERIFY`** (`#1043`) — **Langston's sha-pinned `raw.githubusercontent.com` read served the WRONG FILE under HTTP 200, twice on 2026-09-11** (a 31-line chunking test for a 97-line test file; 8,162 B of another batch's pre-audit for a 19,199 B one). Pinning the sha closes the mutation channel and leaves the delivery channel open. ⇒ **verify the git blob sha in `dt-review show`** (the contents API returns one that checks locally) **plus the matching amendment to his read-model text.** | **Infra Claude** | **PLACED 2026-09-11 — Langston's Step-8 FINDING-C on `B-WAKE-LEAD-NAME`, §9.4 disposition 3; placed directly after the batch under review when it was found** | ⚠️ **Interim, Langston on himself: a single-file read carrying a ruling gets a first-line and byte-count check before he quotes it.** ★ **PATTERN INSTANCE FOLDED HERE (Langston, `B-WAKE-LEAD-NAME` Step 11, §9.4 disposition 1):** this is `B-CANONICAL-BRIDGE-CHURN`'s 404-read-as-zero one rung out — a 200 serving the wrong blob against a read model that validates nothing. The `MISTAKE_PATTERNS` entry lands with this batch, where the mechanism gets proven. |
| 4.55 | ✅✅ **CRITERION 4 SATISFIED 2026-09-09T09:17:01Z — UNATTENDED, ON THE EXIT `#1021` REPAIRED.** The hourly cron cleared BOTH open rows with no human trigger: `NO_RUNTIME_PATHS resolved=2 failed=0`, `14b031e8` (active) and `c588d5cb` (**acknowledged**) both → `resolved`, `resolved_by=deploy-drift-monitor`, evidence `NO_RUNTIME_PATHS at … deployed=… head=…`. ⭐ **THE EVIDENCE SAYS `NO_RUNTIME_PATHS`, NOT `ZERO` — `ZERO` cleared rows BEFORE `#1021` and would have proved nothing; Langston's condition that each resolve NAME its condition is what makes that checkable, and it earned its keep on first firing.** ⭐ **An acked row was swept — his ruling that this is REQUIRED (else an acked rung sits non-terminal forever and blocks every re-mint of that key), now shown live.** ⇒ **CRITERIA 1, 2, 3 AND 4 ALL PASS.** ⛔ **NOT claimed: `ZERO` unattended is UNTESTED (pre-existing behaviour); `BELOW_FLOOR` clearing is construction-and-dry-run only.** Fix installed `67b71933…` 06:57Z, piped from the blob, digest verified at a temp path, atomic `mv`, clear of `:17`. ⚠️ **A prediction of mine FAILED first: I said the 07:17 run would clear, and it correctly logged `RUNG=2` because my own one-line `server/` edit was undeployed — I built the expectation from a `--base` dry-run instead of the real state. The job was right; I was wrong.** ⛔ **RE-OPENED AT STEP 3 ON 2026-09-09 — `#1021` FOLDED IN (§9.4 disposition 1, Langston's ruling: this is the batch's OWN deliverable failing in the exact behaviour criterion 4 measures, so running criterion 4 now would measure the defect).** **THE DEFECT: the clearing path was reachable from only ONE of four terminal exits.** Three exits mean *nothing to report* and only `ZERO` (deployed==head exactly) cleared rows — so a deploy that GENUINELY FIXED the drift left every rung open the moment any non-runtime commit moved the head. **MEASURED, prediction written first: probe `c588d5cb` stayed active across NINE consecutive unattended runs.** ⭐ **And it had already bitten: at 20:17 the cron logged `NO_RUNTIME_PATHS age=0h total=5` — had the job not been run BY HAND at 19:52, the two rungs the `B-CANONICAL-BRIDGE-CHURN` deploy cleared would still be open.** FIX: clearing hoisted to `clear_open_rows()`, called from `ZERO`, `NO_RUNTIME_PATHS` and `BELOW_FLOOR` (the third found by Langston, not me), each evidence string naming WHICH condition discharged the row. ⛔ **CRITERION 4 CANNOT CLOSE UNTIL THIS SHIPS.** ⭐ **Criteria 1, 2 AND 3 now PASS — criterion 3 (escalation) had its opportunity unprompted: rung 2 (09-07T20:17) → rung 3 (09-08T08:17), one continuous episode crossing a worse threshold twelve hours later.** ★ **`B-DEPLOY-DRIFT-LINE`** (#1002) — **nothing compares the deployed sha to the branch head.** `dt-deploy.sh:189-191` gates the deploy EVENT (refuses a sha not on the review branch) and the daily observation compares the RECORD to the LIVE MACHINE — overwrite, crash-restart, tampered binary. **None of them notices the branch moving on afterwards**, which is how staging sat 55 commits behind with the execution engine and signal orchestrator undeployed (`#1001`) while every check reported healthy. FIX: one line in the daily check — `git rev-list --count <record.sha>..origin/migration/aws-supabase`, and the same against `main`, **after a `git fetch` or it reads a stale pointer** | CC-A | ⛔ **OPEN — OBSERVATION WINDOW. Installed 2026-09-07, hourly on Helsinki; Langston CONFIRMED Step 8 at `5e9296ebf` on his own commands with controls; CI 4/4 per job on run `34025649938`.** ★ **NO DEPLOY WAS NEEDED — CC-C's 09:30 deploy carried the one server-side line (the machine actor), so this batch shipped nothing of theirs.** ⭐ **FIRST TRUE POSITIVE 2026-09-07: alert `81135510` (rung 2) fired at the first hourly run after the gate opened, and ALL FOUR magnitudes re-derived against `git` at the two shas it named — criteria 1+2 PASS.** ⛔ **CRITERIA 3 (escalation) AND 4 (a deploy clears it) HAVE HAD ZERO OPPORTUNITY — the batch STAYS OPEN.** 🟨 **The episode also widened `#1016`: the one file that opened the gate was a COMMENT-ONLY change, so the same predicate OVER-reports as well as under-reports.** ⛔ **(superseded, kept for the record) DOES NOT CLOSE ON THE INSTALL: the job has not yet fired a CORRECT alert in production** — every firing so far was a test or the ungated one, since withdrawn. A first true positive needs a real gap carrying runtime files. ★ **The defect that outlives it: a 404 from the compare API RENDERED AS ZERO DRIFT, because the error body fills the same field a success fills.** Spawned `#1016` (row 4.56) and a two-clause rider on `#744`. Record: `B_DEPLOY_DRIFT_LINE_PROGRESS_REPORT.md` | **PLACED 2026-09-04 — Kyle: *"slot it after this batch"*. Positioned BEFORE 4.6 deliberately: one line, and the blind spot is on the live trading path.** | ⚠️ The daily check is CC-B's (alert `7814ebc5`); told them — if they would rather own the edit to their own instrument, it is theirs. |
| 4.56 | ✅ **CLOSED 2026-09-08 — Langston Step-8 CONFIRMED, all re-derived. Four sinks replace the folder convention; `audit/coherency_rules.yaml` (the Core-Four risk envelope) and `drizzle/migrations/**` are now visible to the gate. Discriminating class measured at 66 of 4,890 commits. Spawned `B-CANONICAL-BRIDGE-CHURN` (row 4.56a). Record: `B_DRIFT_RUNTIME_PREDICATE_COMPLETION_REPORT.md`.** ★ **`B-DRIFT-RUNTIME-PREDICATE`** (`#1016`) — **LANGSTON'S STEP-8 FINDING on `B-DEPLOY-DRIFT-LINE`, taxonomy outcome (1), a real defect.** The drift gate defines runtime as `server/ client/ shared/` — a DIRECTORY CONVENTION — but **`dt-deploy` EXECUTES more than that**: `npm ci` on a `package-lock.json` diff, `npm run build` (reading `vite.config.ts`, `tsconfig.json`, `tailwind.config.ts`, `postcss.config.js`), and **`npm run db:migrate` over `drizzle/migrations/**`**. ⛔ **A gap made only of those logs `NO_RUNTIME_PATHS` and files nothing — and an UNDEPLOYED SCHEMA MIGRATION is the highest runtime risk we carry.** The predicate does not answer its own stated question. FIX: derive the set from what the deploy actually executes, not from where files live. | CC-A | **PLACED 2026-09-07 — Langston's own placement, immediately after 4.55.** ⚠️ **NOT BLOCKING and he said so: the shipped gate is strictly better than the ungated version it replaced.** | ✅ **REACHABILITY MEASURED, NOT FELT — last 400 commits on the review branch: 3 touch a deploy-executed path, and ALL 3 also carry `server/` files ⇒ ZERO gate-invisible. LATENT, not live.** ★ **Near-miss named: `3b2c4966c` (`B-XSTOCK-FEED-SANITY`, deploy held four days) carried `drizzle/migrations/*.sql` + `package.json` and only spoke because runtime files rode along.** ⚠️ **And he corrected his own first pass in flight** — `scripts/` + `migrations/` returned 42, over-broad on `scripts/` (mostly analysis) and a FALSE ZERO on `migrations/`, because the real path is `drizzle/migrations/`. The 3/0 is the narrow verified set. |
| 4.56a | ✅ **CLOSED 2026-09-11 — U-1 MET (Langston 13:30Z, re-derived): two unattended daily runs logged `1 unchanged` on the fixed build and file mtimes show the tree stayed clean. The pre-registered `0 updated` was unreachable by construction and ruled mis-specified. Report `B_CANONICAL_BRIDGE_CHURN_COMPLETION_REPORT.md`. Fifth residual (`2 updated` on a no-change day) → `P19-B12`; `#1039` found here → row 4.58.** ~~OPEN — OBSERVATION.~~ DEPLOYED `977e61d7d` 2026-09-08; Langston Step-2 ACCEPTED, Step-4 APPROVED at `238c2a321`, Step-8 CONFIRMED all re-derived at the deployed sha; CI 4/4 per job. Governance landed: BATCH_CATALOG, PHASE_HISTORY, CHANGES_AND_FIXES, SYSTEM_MANUAL (RISK-017 DOWNGRADED MEDIUM→LOW, not closed), SYSTEM_IMPACT_MAP (:866 supersedes B59's fresh-date-every-sync), GOVERNANCE_EXCEPTIONS, RUNNING_ISSUES.** ⛔ **WHY IT STAYS OPEN: proved on the CODE PATH, not on the SCHEDULE — the force-sync endpoint and the daily scheduler share a module and a process, so the endpoint is a fair proxy for the code path and NOT for the scheduling, which is the thing that dirtied the tree for months. Close criterion PRE-REGISTERED BEFORE THE DATA in `B_CANONICAL_BRIDGE_CHURN_STEP7_EVIDENCE.md` §8: an unattended daily run leaves `git status` EMPTY AND the scheduler logs `0 updated, 1 unchanged` — the log line is the positive emission, since a clean tree alone cannot tell 'skipped correctly' from 'never ran'.** ⭐ **SPAWNED `#1021`** — the drift monitor's clearing path reachable from only ONE of four exits, so a deploy that fixed the drift left every rung open; folded into row 4.55 on Langston's ruling (§9.4 disposition 1), not a new batch. ⭐⭐ **PREDICTION CONFIRMED 2026-09-08T07:50:18Z — the staging tree IS dirty and the next deploy WILL be refused. Written before the event; fired one second off the predicted time; exactly one file, exactly the two timestamp lines.** ★ **`B-CANONICAL-BRIDGE-CHURN`** (under existing **`#402`**, OPEN since 2026-06-30) — **A DAILY SCHEDULED TASK REWRITES A TRACKED FILE WITH A FRESH TIMESTAMP, DIRTYING THE STAGING TREE, AND `dt-deploy` REFUSES A DIRTY TREE.** `autonomy-scheduler.ts:608` registers `canonical_bridge_sync`, `:1056` starts it (*"Daily — B59"*); `sync-canonical-bridge.ts:136-137` stamps `updatedAt`/`generatedAt` with `new Date().toISOString()` ⇒ every run differs ⇒ `git status` non-empty ⇒ `dt-deploy` exits 3. ⛔ **NOT HYPOTHETICAL: `#402`'s 2026-08-17 annotation records the `P19-B-FEEVIABILITY` deploy REFUSED by exactly this.** ⭐ **THE a/b QUESTION `#402` LEFT OPEN IS NOW SETTLED BY `B-DRIFT-RUNTIME-PREDICATE`'s census: `routes.ts:2083-2085` reads the file FROM DISK at runtime, so it is OPTION (b), CONTENT-HASH COMPARE — skip the write when only timestamps would change. GITIGNORE (a) IS OFF THE TABLE.** **Owner CC-A. Placed here by Langston 2026-09-08: *"a daily task that can refuse deploys, has refused one, and sits under an issue open since June has outgrown a list"* — small and disjoint from 4.56, so it follows rather than interleaves.** ⚠️ **KYLE: THIS PUSHES `B-TASK-LIST-SLOT` BACK A SECOND TIME.** You directed it to follow `B-DEPLOY-DRIFT-LINE`; `4.56` and now `4.56a` have both been slotted ahead of it by findings. **Say the word and it jumps the queue.** |
| 4.57 | ✅ **CLOSED 2026-09-18 — P5 PASSED: primary 3 of 3 at close (1 caught-and-fixed by the check, 2 unprompted), secondary 2 of 3 (Infra Claude ×1, CC-C ×2). Langston CONFIRMED Step 11. Record `B_TASK_LIST_SLOT_COMPLETION_REPORT.md`.** ★ **`B-TASK-LIST-SLOT`** (`#1009`, filed as `#1006` — renumbered on collision with CC-B's `#1006`) — **KYLE-DIRECTED 2026-09-05, and he named the design question himself: *"I don't think that this becomes a new rule… maybe it is its own skill file — anytime a session has to create or slot in a new task, whether it be a batch, a sub-batch, investigation or hotfix, that skill is triggered."* **THE STATE TODAY IS HALF-BUILT AND THAT IS THE PROBLEM:** the CLOSE-time half has a slot (`workflow-10` Tier-1 row, covering all four lists) **that 1 of 3 completion reports filled (measured 2026-09-09)**; the **SLOT-time half has only a WEAK trigger — a one-line duty at `CLAUDE.md:539` whose mechanics resolve to `workflow-10-governance`, gated `STEP 10 ONLY`**. ⚠️ *Corrected in place 2026-09-11 (Langston Step-2 condition 1): this read "the SLOT-time half has no trigger at all", and the pre-audit's retraction cited this row as corroboration — same author, so not independent.* **MEASURED 2026-09-05: of four sessions, ONE list conforms (CC-A), one is non-conforming (`CC_INFRA`, 48 KB), one is in the wrong folder under a phase-scoped name (`1-system-manual/CLAUDE_NEW_PHASE_19_TASK_LIST.md`), and CC-C HAS NONE.** OBJ: decide skill-vs-step-skills and say why · build it · normalise the four lists to one shape and location · keep `CLAUDE.md` to the pointer it now holds · ⭐ **KYLE AMENDMENT 2026-09-05 — THE LIST'S PRIMARY SECTION IS *OPEN AND STALLED*, NOT THE QUEUE:** every batch the session OPENED and did not close, with the step it stalled at and what it waits on (*"started and are just left sitting there… flapping in the wind"*; CC-A had FOUR, invisible until 2026-09-05) · ⛔ **AND CLOSE THE REVIEW GAP HE ASKED ABOUT: `workflow-10` HAS NO REVIEW GATE AND THE CHECKER'S `DOCS` TABLE CANNOT SEE THE TASK LISTS** — either teach the governance checker the three-destination rule (it already reads at the ref and alerts on a miss) OR make the filled ledger a named Step-11 item Langston must rule on. **Argue which; do not do both by default** ✅ **RULED 2026-09-09 (Langston, Step 2): the governance checker ONLY — he declined the Step-11 gate. Step 2 approved with ten conditions, all applied in pre-audit r3; the slot-time half is split out to row 4.8.** | CC-A | **PLACED 2026-09-05 — after 4.55, before 4.6. Small, and it makes a rule real that is currently half-wired.** | ✅✅ **POSITION FIXED BY KYLE 2026-09-05, after he read the queue: *"let's do the deploy drift line as you suggested… and then I would say that we go to the b task list slot next, because this is something I'm dealing with with each of the sessions and it needs to be cleaned up."* ⭐ **THAT MOVES IT AHEAD OF 4.6 `B-RULES-LAYER`, which he had previously directed to follow `B-WAKE-QUIET`.** The re-order is his and is deliberate.** ⛔ **DO NOT add the rule text back to `CLAUDE.md`: he struck a six-line version the same morning, and `#998` is the reason.** |
| 4.58 | ★ **`B-SCHEDULER-FIRST-TICK`** (`#1039`) — found 2026-09-11 verifying 4.56a: **every scheduler-registry task runs TWICE at its first interval after a restart** (`startTask` arms a `setTimeout` and a `setInterval` for the same interval; seen on staging for both 24-hour tasks at 2026-09-10 08:46:47, while later-interval tasks in the same second ran once). Every deploy is a restart. **Impact per task UNMEASURED.** OBJ: the first run happens once · census which of the ~30 tasks are not idempotent and whether any double run did harm · name the legacy candidates (rule 18) | CC-A | **PLACED 2026-09-11 — after 4.57, before 4.6: rule 23 makes a found defect a mini-cycle, not a queued item. Starts when 4.57 closes.** | `RUNNING_ISSUES` `#1039` |
| 4.6 | ★★ **`B-RULES-LAYER`** (#998) — **KYLE-DIRECTED 2026-09-04, to start the moment 4.5 closes.** Our behavioural rules sit in the WEAKEST layer: Anthropic documents that `CLAUDE.md` is a **user message after the system prompt** with *"no guarantee of strict compliance"*, while **output styles modify the SYSTEM PROMPT and are re-injected every turn**. And the vendor states plainly that **bloated rules files cause Claude to ignore instructions** — *"if Claude keeps doing something despite a rule against it, the file is probably too long and the rule is getting lost"*, target **under 200 lines** against our ~100 KB. ⇒ every rule we added to fix a behaviour weakened the others, **including the risk rules in the same file.** OBJ: move behavioural rules to an output style · replace prohibitions with ONE default reply shape · **CUT (this REVERSES `#339` NO-TRIM and needs Kyle)** · keep a post-hoc pass as the fallback | CC-A | **PLACED 2026-09-04 — Kyle: *"finish the current batch… and then we move into this batch"*** ⚠️ **ORDER SUPERSEDED 2026-09-05: Kyle put `B-TASK-LIST-SLOT` (4.57) ahead of this after reading the CC-A queue — *"it needs to be cleaned up"*. This batch is still his and still next-but-one; only the position moved.** | ⚠️ **PRE-REGISTERED FAILURE CONDITION: if the speak-rate does not move, the layer hypothesis is WRONG and the batch says so.** `B-WAKE-QUIET` already killed three instruction-shaped fixes; this is a fourth of the same family, differing only in WHERE the instruction lives. |
| 4.7 | ★ **`B-HEARTBEAT-RESCOPE`** (#999) — the hourly heartbeat task still declares the WAKE its primary value, in a file now committed, after `#995` made that true for only ~2% of runs (Langston measured 755 suppressed / 13 delivered on 768 live rows). Re-scope the task to exception-only, or state why it still runs hourly · **carries the structured health field**: `_HEARTBEAT_BAD` is a hand-written word list and all 13 deliveries came through the `STALE` arm, six of them matching the word inside a NEGATION | CC-A | **PLACED 2026-09-04 — Langston's condition for clearing `#995` Step 4: *"'its own small piece' isn't a home. Give it a row."*** | ⛔ **Deliberately NOT folded into 4.5:** it changes what the TASK does, not what the filter delivers, and *"bolting a schema onto a regex mid-batch is how the third version of your drift check happened."* |
| 4.8 | ★ **`B-SLOT-PLACEMENT-CHECK`** (`#1009` P2 — split out of 4.57 at Step 2, 2026-09-11) — **the SLOT-time half of Kyle's task-list rule, which 4.57 does NOT fix.** §9.4 says a slotted item must be PLACED in three destinations — `RUNNING_ISSUES.md`, the phase plan, and the owning session's task list — and *"NAMING IS NOT PLACING"*. The governance checker cannot see that today: its only join key is a batch-id, many slotted items carry none, a presence test sees naming and not placing, and the task lists are not in its document table. OBJ-A: define a join key, then a placement check across the three destinations · OBJ-B: extend 4.57's ledger-row check to the two other Tier-1 rows measured missing (shared `MEMORY.md`, Langston's `MEMORY.md`), deriving and pre-registering the already-closed batches it would alert on first | CC-A | **PLACED 2026-09-11 — after 4.7, before 5. Kept behind Kyle's own order (4.57 → 4.6 → 4.7).** | pre-audit `B_TASK_LIST_SLOT_PRE_AUDIT.md` P2 + §5 |
| 5 | ★ **`B-CHUNK-ADDRESSING`** (#749, #761) — stamp recipients on every continuation chunk | CC-A | **PLACED 2026-08-29** | ⛔ **PULLED OUT OF `B-CLAUDEMD-SLIM`, which is a documentation batch.** ⚠️ **This is a live-service change and it has already taken CC sending down for ~4 minutes (`#761`), cause still unknown.** ★ **Gate: reproduce the failure OFFLINE against the preserved broken copy BEFORE designing anything.** |
| 6 | ★★ **`B-MEASURE-GATE` leg 2** — the hooks that gate `wrong-object`. ✅ **MEASURED AND PINNED AT `24f4b39bb`: 80 instances · 46.8% of 171 well-formed trailers across 44 slugs · 6.2× second place.** ★ **And that pattern is ALREADY the most heavily ruled one we own — which is the evidence that the output must be a GATE, not another rule.** | CC-A | **STEP 1 APPROVED 2026-08-31 — IN FLIGHT AT STEP 2** | ⚠️ **`[C1]` THIS ROW CARRIED A SUPERSEDED MEASUREMENT AND A FALSE RANK UNTIL 2026-08-31, AND LANGSTON CAUGHT BOTH AT STEP 1.** It read *"77 instances across 24 batches = 48% … six times second place"* and *"RE-RANKED TO THE TOP OF THE UNSTARTED WORK."* ⛔ **THE BATCH READING IS WITHDRAWN OUTRIGHT: 26 bracket-tokens is not 26 batches (~12 by subject-line id), so NO across-batches denominator is claimed at all.** ⛔ **AND "TOP OF THE UNSTARTED WORK" WAS FALSE ON ITS OWN PAGE — the row sits at position 6 with four unstarted items above it.** ★★ **THE KEEPER, and it is this batch’s own subject one level up: I corrected the stale HOME in `MISTAKE_PATTERNS.md` and left the withdrawn MEASUREMENT one file over — in the row anyone reads to decide priority. `fix-follows-pointer`, inside this batch’s own consolidation round.** ★ **`B-STATE-ASSERTION-LINT` (`#978`, row 6.5) exists for exactly this shape: a sentence TRUE WHEN WRITTEN, wrong now, with nothing comparing it to anything.** ★★ **TWO LANGSTON CONDITIONS RECORDED 2026-08-31, BOTH VERIFICATION-TIME, BOTH EASY TO LOSE:** **(1) STEP-7 — the channel probe measured `probe-warn-delivery.mjs`; OBJ-4 SHIPS A DIFFERENT FILE with its own registration, into an estate MEASURED to run THREE versions of one hook. *"The channel delivers" ≠ "your hook’s output delivers."* ⇒ **demonstrate `additionalContext` arriving FROM THE SHIPPED OBJ-4 HOOK, stamped with its own `hook_sha`; citing the probe run there is the adjacent object.** **(2) NON-BLOCKING IS THE DEFAULT, NOT THE CEILING** — his Step-1 reason was never *"blocking is wrong"*, it was that a HEURISTIC predicate must not be able to wedge a session. ⛔ **A deterministic predicate with a measured near-zero FP rate REMAINS ELIGIBLE to block. Do not let "no forced escalation" harden into "blocking retired."** ✅ **LEG 2 CLOSED 2026-09-02** (Steps 4 + 8 Langston-approved at `a452ad29b`; CI 4/4; no deploy; OBJ-4 live window OPENS at this close; leg 3 = the `CLAUDE.md` conversion sweep + #984). |
| 6.5 | ★ **`B-STATE-ASSERTION-LINT`** — a sentence that was TRUE WHEN WRITTEN and is wrong now: an asserted live VALUE with no read-site, or a claimed STATE the register contradicts | CC-A | **PLACED 2026-08-31** | ⛔ **PLACED BECAUSE A SECOND READER CAUGHT IT NAMED-BUT-NOT-PLACED — §9.4’s own failure, committed while citing §9.4.** ★ **Langston-ruled its own batch: AFTER `B-MEASURE-GATE` leg 2 (converting rules while duplicate copies survive multiplies the copies) and BEFORE the leg-3 rule-conversion sweep.** ⚠️ **Inherits a blind spot at birth, named not discovered: a fix landing in one of two copies has NO register entry, so claimed-vs-register returns CLEAR over a corpus that excludes it.** |
| 6.6 | ★ **`B-CLAIM-REDERIVE`** (#981) — the claim-sourced re-derivation Langston would accept in place of OBJ-6d: an agent hook at the **`Stop` boundary**, gated deterministically on *the turn's final message carrying a load-bearing number*, re-deriving THAT number against the object and blocking the stop with the re-derivation when they disagree. **Not a result-inspection gate** — both OBJ-6d shapes were refused because they gated on 6c's fire, which is blind by construction to instance 8 (a JSON 404 body parsed cleanly to "0 of 0"). | CC-A | **PLACED 2026-09-02** | ⛔ **PLACED AFTER `B-MEASURE-GATE` LEG 3 (#623's own next leg) at Langston's direction, 2026-09-02.** Design inputs already measured in `B_MEASURE_GATE_LEG2_SCOPE.md` [r7]/[r11]: an agent hook FIRES on `PostToolUse` (undocumented), its `reason` is delivered non-fatally, it can Read/Grep/Bash but not Write, the `if` argument gate does NOT hold for agent hooks, and a subagent's own Bash calls re-enter `PostToolUse` — `Stop` is the event that avoids the last. Bars to be pre-registered at its own Step 1; OBJ-6d's bars (≥5 of 8 known positives, ≤2% escalation) do NOT carry over — they were written for result-inspection. |
| 6.9 | ★ **`B-JULY-RETENTION-SWEEP`** — Kyle directive 2026-09-01: July needs the treatment June got | CC-A | ✅ **CLOSED AS REVIEW 2026-09-02 (#991) — NOTHING TO SWEEP: the 2026-09-01 nightly completed `xstock_spot_ticker_snap/2026-07` on its own (31/31 slices verified, hot partition dropped). June was the same sweep run by hand ONCE (35 min), not a multi-night script.** The upload path still has no retry; the decision NOT to build it (daily partitions since 08-01 ship under the single-call threshold) was found living only in a board card and is now recorded on #991. | no work; re-open only on consecutive-night whole-object failures. |
| 6.95 | ★ **`B-ROUTINE-REHOME`** (#739) — the rule-21 daily model check has been SKIPPED by the app scheduler since 2026-07-24 (its working folder is the retired Drive path; `lastRunAt` is stamped on the skip): OBJ-1 recreate with a living folder + disable the old row · OBJ-2 verify the next dispatch leaves a session record + a committed RUN LOG row (alert `9c3037f0` PASS) · OBJ-3 disable-and-annotate the two spent retired-folder one-shots · OBJ-4 re-pin the heartbeat routine's legacy `claude-opus-4-8` model (Kyle's call, rule 21) | CC-A | ✅ **OBJ-1 + OBJ-2 + OBJ-3 DONE — the 09-04 dispatch left a session record AND committed its RUN LOG row (`937f187e7`, inside the session lifetime); alert `9c3037f0` RESOLVED 2026-09-05 after 10 re-surfaces. OBJ-4 (heartbeat model pin) remains Kyle-gated. Prior state: 2026-09-03 (`daily-claude-currency-check` live, cwd exists, old row retired; enabled-rows-with-missing-cwd 0). OPEN: OBJ-2 (the 09-04 dispatch must leave a COMMITTED RUN LOG row — the only close condition) + OBJ-4 (heartbeat model pin, Kyle's call, needs a recreate). PLACED 2026-09-03, Langston-approved as placed — GATE INSIDE: Kyle's say-so on OBJ-1 AND OBJ-4 (persistent configuration + a model pin), asked 2026-09-03. Closes ONLY on OBJ-2's committed RUN LOG row (Langston: the census and the 07-23 floor are laptop-side, RULED ON REPORTED FACT). Alert `9c3037f0` is ACKED = silenced since 08-22 (#982); resolve-with-evidence at OBJ-2 + fresh mint.** | #739 addendum 2 holds the census (11 rows: 3 FAIL, 8 PASS), the last genuine run (07-23) and every voided `lastRunAt` consumer. |
| 7 | ★★ **`B-EXIT-LATCH-INVESTIGATION`** (#732) — **AN INVESTIGATION, NOT A FIX.** Deliverable is a DISPOSITION: is the hold-past-target a labelling artefact, or a symptom of a live exit-evaluation defect? | CC-A | **PLACED — KYLE 2026-08-27, after `B-MEASURE-GATE`** | ⛔⛔ **Kyle: *"I just wanna make sure it’s not a symptom of a much bigger and uglier problem before I just write it off as absolutely nothing."*** ★ **§9.4 disposition 4 — a scheduled review, deliberately SPLIT from the fix.** The fix (`B-EXIT-LABEL-TRUTH`, back of Phase 19) is re-timed on what this finds; **Kyle decides when the fix runs, after the answer exists.** ⚠️ **The question it must answer is NOT the label.** A wrong label explains a wrong NAME. **It does not explain why two of seven trades HELD PAST their own target and exited ABOVE it** (`VVV/USD` tp `14.64157143` exit `14.79200000`; `CRV/USD` tp `0.27197786` exit `0.27254000`). |
| 7.5 | ★ **`B-HOOK-ESTATE-VERSION`** — the hook estate has no coherent version story: FIVE clones run THREE different versions of one hook file, and HEAD does not tell you which | **CC-C** | **PLACED 2026-08-31 — Langston-homed, after `B-EXIT-LATCH-INVESTIGATION`** | ✅ **MEASURED, not argued** (`B-MEASURE-GATE` leg 2 pre-audit §13): `-old`/`-new`/`-analyst` hold the current copy, `-infra` an intermediate, and the spare `C:\DawnTraderV3` still executes **the ORIGINAL 08-07 probe**. ★★ **`-new` and `-analyst` sit BEHIND `-old` in commits yet hold the SAME current hook — because `fresh-rules.mjs` writes `.claude/*` from origin into the working tree at session start.** ⇒ **THE ESTATE IS VERSIONED BY WHEN EACH SESSION LAST STARTED, NOT BY ITS COMMIT.** ⛔ **Langston: a spare clone executing a three-week-old probe on a live path is LINGERING LEGACY (rule 15/18), and a coherent estate version story is not a line of OBJ-5.** ★ **Placed BEHIND the Kyle-reopened `#732` deliberately — the hash stamp lands in `B-MEASURE-GATE` so the divergence stays VISIBLE while this waits.** ⚠️ **Langston flagged the placement itself as a judgement, not a finding — open to being read differently.** |
| 8 | **`B-GOV-REPORTING`** — the reporting + progress-report rules landed 2026-08-26, **PLUS the change-class ledger matrix (`d8d4999bb`) and the matrix’s missing BLOCKED state** ⛔ **`#946` LANGSTON’S `MEMORY.md` TRIM WAS REMOVED FROM THIS BATCH BY KYLE 2026-08-30** — *"that is the responsibility of Infra Claude … I don’t wanna mix that work"*; it belongs to the instruction-file workstream started weeks ago and interrupted by hotfixes. ★ **The BLOCKED-state gap STAYS here — it is a defect in the ledger THIS batch owns, and it was only surfaced BY the trim, not part of it.** ★★ **THREE ITEMS ADDED 2026-08-31 FROM `B-MEASURE-GATE` STEP 2 (§9.4 disposition 2 — added to an existing batch, NOT a new one):** **(i) THE MATRIX HAS NO `BLOCKED` STATE AND NO CLASS FOR *"AFFECTS EVERY SESSION’S TOOLING, TOUCHES ZERO TRADING ARCHITECTURE."*** `architecture` makes `system_manual` REQUIRED (`config.mjs:127`) and a required row cannot take `N/A`; the class is not wrong; **so there is NO CORRECT EXIT and the row takes a ✅ it has not earned.** **(ii) THE LEDGER DOES NOT PREVENT A FALSE TICK — IT ONLY MAKES THE ROW EXIST TO BE LIED IN.** ⛔ **MEASURED: `B-DISAGREEMENT-FINDER` closed with `batch_catalog` and `phase_history` marked ✅ AND DESCRIBED, and both files held ZERO occurrences** (control: `B-CLAUDEMD-SLIM` returns 1 in each). **The checker caught it, not the author — for the SECOND time in three days, on the same class, by the same author, and `a25799c8` was the first, on the very batch this ledger format came from.** ⇒ **the independent detector is the checker; the ledger is a PROMPT, not a PROOF, and it must say so in its own header.** ⛔ **(iii) WAS WITHDRAWN THE SAME DAY IT WAS FILED, AND THE WITHDRAWAL CARRIES THE CITATION THAT DISSOLVES IT (§9.4 disposition 5).** I filed *"sync-gate check 4 has no fourth outcome for DELIBERATELY UNCOMMITTED"* against the gate for sweeping up `probe-warn-delivery.mjs`. ✅ **A fresh reader refuted it at the object: the arming commit `3fd7ed119` says the probe was *"never registered, sitting untracked for days — an instrument measuring nothing"*, and `RUNNING_ISSUES:2198` says the same.** ⇒ **it was NOT deliberately uncommitted; it was ORPHANED, and check 4 caught exactly what check 4 exists to catch.** ★ **The real finding is one document over and it is `#978` shape A: the pre-audit ASSERTED it was "wired locally" and nothing ever compared that to `settings.local.json`.** ★★ **FOURTH ITEM ADDED 2026-09-01 — `#980`, THE PER-TURN MANDATED READS ARE SPECIFIED THREE TIMES AND THE THREE DISAGREE.** Repo `CLAUDE.md:564-565` says `tail -50`; `/home/langston/CLAUDE.md:356` says **unbounded `cat`** with a different user; shared `MEMORY.md:16-17` is a third home Langston did not name. ⛔ **SUBSTANTIVE: the repo form reads a TRUNCATED 50 rows while his own file reads the whole file, so the two produce DIFFERENT POPULATIONS for the same mandated alert check — and the repo form can miss a due alert older than 50 rows.** ★ **Surfaced by `B-MEASURE-GATE` OBJ-4, because a guard fired on a command the rules oblige every session to run every turn.** ⚠️ **Langston homed it in `B-CLAUDEMD-SLIM`, which CLOSED 2026-08-29 (row 12) — he is stateless per-invoke; re-homed here and flagged to him rather than silently moved.** ⛔ **The `/home/langston/CLAUDE.md` half is INFRA CLAUDE’S per Kyle 2026-08-30 and is NOT assigned here.** ★★ **ITEM (iv) ADDED 2026-09-01 — AND `#980` IS NOW MEASURED, NOT ARGUED: A SESSION ON THE MANDATED REPO FORM SEES 4 OF 11 DUE ALERTS.** Seven sit at lines 250-664 of a 768-line file; a `tail -50` reaches 719-768. ⛔ **The tail is STRUCTURALLY wrong, not merely narrow: the file is append-ordered by MINT time while due-ness is `triggers_at`, so the OLDEST due items are the ones a tail is MOST likely to miss.** ⛔ **The tool’s own `list` is not the answer either — measured: it emits all 768 rows unfiltered.** ✅ **Converges on an unbounded read with a `state`/`triggers_at` filter, which returns ELEVEN lines rather than fifty — more complete AND smaller. A tested interim command is recorded on `#980` so any session can read the full queue today.** | CC-A | **PUSHED, UNREVIEWED — gate owed** | goes to Langston as its own gate; **does not wait for 1e**, but is dispatched AFTER it so he holds one of mine at a time (#741) ★ **(v) `#982` (2026-09-02, Langston):** an `unack` verb for `system-alerts` and the protocol correction — ack is for alerts whose follow-through is WORK; an EVENT-WAIT alert is ROUTED (`[[ALERT … owner=…]]`) and left active, because an acknowledged keyed row blocks the next mint and stops surfacing. Three KEYED rows (`f6ae5419`, `c5cf4a87`, `b31833f3` — the last inside #951's observation window) and eleven keyless ones are silenced this way until the verb exists. ★ **(vi) `#985` (2026-09-02):** repo-side-but-not-app work (hooks, governance tooling) has no change-class that fits — a correct BLOCKED on the System Manual reads to the checker as a missing required doc (alert `e38db085`, and the same wall on `B-CROSS-SESSION-BLEED`). **RESOLVED same day by Langston's re-ruling: re-declared `non_architecture` on precedent; the class-gap itself is CC-B's placed work, not a new item here.** |
| 8.5 | ★★ **`B-DISAGREEMENT-FINDER`** | CC-A | ⛔ **CLOSED 2026-08-31 ON A NEGATIVE RESULT** — the pre-registered gate FAILED (7 detector-error of 9; still 5 of 9 under the most generous repair). ★ **THE AXIS WAS WRONG: it asks "do two copies DISAGREE" and the costly failure is every copy AGREEING while all of them lag — CONSENSUS STALENESS.** ✅ Delivered anyway: `#732` stale guard fixed · `#970` measurement · `#974` open-under-closed-heading · the mutation test caught 2 of my own bugs pre-review. ⇒ re-aim ranked in the completion report §6 |
| 8.7 | ★ **`B-ALERT-WINDOW-EXPIRY`** — a scheduled verification carries an IMPLICIT OBSERVABILITY WINDOW, and we have no terminal state for *"the thing this was written to observe can no longer be observed"* | CC-A | **PLACED 2026-08-30 (Langston §13 home)** | ⛔ **STANDS ALONE — he ruled explicitly: it touches the alert SCHEMA + CLI terminal states, which is ALERT-LIFECYCLE lineage, NOT reporting. DO NOT ride it on `B-GOV-REPORTING`.** ★ **Evidenced by two live instances: `23f004a4` check 2 (population drained — no pre-deploy position remains) and `f6ae5419` (waits on a market event that may never occur).** ⚠️ **Collapsing that into `resolved` LAUNDERS it; leaving it `active` is the rotting queue.** |
| 9 | **`B-EOL-NORMALISE`** (#751) — `*.md text eol=lf` + renormalise | CC-A | queued | ⛔ **RATIONALE CORRECTED 2026-08-28: this is NO LONGER a gate on the slim.** ★ **Re-derived: the rules file is 117,191 B at the ref and stores ZERO CRLF — the blob is one object, identical for every clone. The per-checkout distortion exists only when measuring a WORKING COPY, which the slim’s verification must not do.** ⇒ **the slim measures at the REF and this batch is independent of it.** ⚠️ ** is still real — 119 markdown files DO store CRLF — it is simply not a blocker.** |
| 10 | **`B-GATE-GUARD`** (#744) + **`B-ISSUE-BLOCK-GUARD`** (#745) — one hook, two checks | CC-A | queued | after `B-RULES-1e` ships, because both are the same hook family and 1e sets the CI-job precedent (audit A4) |
| 11 | **`B-CREW-BOARD-REMOVAL`** — retired board code + the unroutable `CC-INFRA` owner alias | CC-A | **gated on KYLE** | follows Infra Claude’s onboarding. **Not a date — a dependency.** |
| 11.5 | **`B-TRADING-ENGINE-REMOVAL`** (#578) — delete the legacy `TradingEngine` that runs in neither mode (paper never starts it; live is Phase-21-gated and refuses); `active-execution-engine` is the real paper+live pipeline | CC-A | **PLACED 2026-09-02** — Kyle-ruled legacy in July; held BEHIND the governance programme (rule 18 disposition (b): a named batch at a named position), re-order on Kyle's word | own batch; DELETED_COMPONENTS_LOG entry at removal; rule-18 blast-radius census first. |
| 11.6 | **`B-FINALSCORE-TELEMETRY-RETIRE`** (#582) — retire the report-only readers of the retired `finalScore` (the prerequisite for `B-RETIRED-SCORE-REMOVAL` Phase B's column drop, which is CC-B's now) | CC-A | **PLACED 2026-09-02** — Langston Step-4 deferral from #558 A1 with a named home; held behind the programme, ahead of CC-B's Phase-B drop which depends on it | own batch. |
| 12 | **`B-CLAUDEMD-SLIM`** — the always-loaded file is **112 KB, ~64% of everything a session loads before it acts** | CC-A | ✅ **CLOSED 2026-08-29** — 117,191 → 112,081 B (−5,110) at the ref; 9 cut-and-point · 2 carried-then-cut · 4 deliberately stayed | ★ **Placed 2026-08-27 on Langston’s rider, applying the rule shipped the same day: it was NAMED for weeks and never given a position, which is the exact failure §9.4 now forbids.** Follows the guard batches — it is the largest single lever on context and the least urgent. | ★ **ABSORBED 2026-08-27 (Kyle): `#749` chunk-addressing and `#750` the unbuilt bug-investigation skill fold in as Class C** — both were found by this batch’s own scan, and parking them as separate queued batches read as handling them.
| 12.1 | ★★ **rulings-durability fix** (#671, separable half) — Langston's ~3,028 Discord rulings sit in ONE 19 MB file on ONE box outside git; copy to a READ-ONLY replica (Helsinki file stays the only writable home, #671 condition (ii)); DONE = a restore rehearsed once | CC-A | **PLACED 2026-09-02 — FIRST BREAK, exempt from every sequencing below (Langston: irreversible loss, cheapest row on the list)** | no scope needed beyond #671's three conditions. |
| 12.2 | ★ **lookalike register** (#672, separable) — one page of the pairs that already caused wrong calls (working vs archive table, shadow vs real, JSON snapshot vs typed columns, `out.log` vs stderr, `active-*` vs old names) with the one distinguishing test each | CC-A | **PLACED 2026-09-02 — FIRST BREAK, by Langston's #672 refusal to queue it behind the catalogue** | lands as a section of `STORAGE_POLICY.md` or its own reference doc, referenced from the SIM. |
| 12.3 | **`B-DECISION-RECORDS`** (#671) — the Decision Register (Kyle's decisions verbatim) + the Retraction Register (claims overturned by measurement), harvested from git + reports, each marked CONTEMPORANEOUS or RECONSTRUCTED | CC-A | **PLACED 2026-09-02 — after the rules arc's SUBSTANTIVE legs (1a-1d, all CLOSED); the `B-RULES-1e` gate is LIFTED (Langston 09-02: 1e is optional and parked behind the very items it would gate — circular)** | Kyle's recorded order: rules file → decision history → catalogues (`B_GOVERNANCE_REMEDIATION_PLAN_r1.md` §4). |
| 12.4 | **`B-CATALOG-1`** (#672) — the generated table catalogue from `pg_catalog` (every live table: purpose, owning subsystem, which instrument truly reads it; nightly diff fails CI; a migration adding a table must add a row). **Named objectives inside it (Langston rider (a) — objectives, not prose): OBJ-A catalogue · OBJ-B ORM↔DB drift check (54 live tables lack an ORM definition) · OBJ-C one-page glossary of terms (Kyle 07-31) · OBJ-D close `B-STORAGE-CATALOG` on paper (BATCH_CATALOG row + card) · OBJ-E #601's log-folder retention remainder (the trailing-state file stays with #678 `B-TEC-STATE-DURABILITY`)** | CC-A | **PLACED 2026-09-02 — after 12.3** | Langston PROCEED TO SCOPE 07-31; never scoped until now. |
| 12.5 | **`B-CATALOG-2` family** — one small batch per catalogue, in this order: diagnostic-coverage map (Kyle: *"where we have diagnostics and where we don't. I don't know"*) · log streams + reach · scheduled-work census (crons/timers) · config-knob catalogue (`module_constants`) | CC-A | **PLACED 2026-09-02 — after 12.4** | remediation plan §3.5 said "homed, not scoped" with no row; this is the row. |
| 12.6 | **decommission residue** — a rule-18 removal batch: 9 `walter_*` tables (139 MB) + 4 `*_backup_20251023` + 5 `*_user_archive` (a CENSUS, disposition PER TABLE per §15, never a bulk count) — and, as a DIFFERENT population, the 49 tables no document mentions, which 12.4's catalogue NAMES before anything is deleted | CC-A | **PLACED 2026-09-02 — after 12.4 (the catalogue names what is dead first)** | Langston rider (b). |
| 12.7 | ★★ **`B-QUOTE-CURRENCY-DENOMINATION`** (`#937` + `#966` + `#967`) — **KYLE-PLACED 2026-09-12, in his own words:** *“For the quote currency fix, let’s put that as a batch at the end of phase nineteen. And at the end of phase nineteen, when this batch slots in, we’ll take a look at it and decide if we wanna move forward with it when we get to it.”* **THE DEFECT:** a non-USD quote leg means the trade settles in that currency and every number we record on it — entry, stop, target, P&L, the `$0.25` price floor — is stored as though it were dollars. **MEASURED UNBOUNDED 2026-09-12, quote-derived:** `closed_trades WHERE closed_at IS NOT NULL AND asset_class='crypto_spot'`, n = 479 → **96 fiat-quoted (EUR 54 · GBP 20 · CHF 9 · AUD 8 · CAD 5) = 20.0%, net −$77.69**, against USD’s +$166.15 on 343; USDC/USDT 40, net +$9.92, which track the dollar and are NOT the defect. **`vts_open_trades` carries 10,346 fiat-quoted rows against 62,806 USD** ⇒ the learning surface is far more contaminated than the active one. ⛔ **THERE IS NO GATE TO RE-ARM:** the one implemented quote test is a REPORTING counter, fed a hard-coded empty local literal and reached only from two HTTP endpoints — full re-derivation in the `#937` amendment of 2026-09-12. **TWO HALVES, and the second is the one Kyle actually wants:** **(i) ADMIT-OR-BLOCK** — bind the already-declared, NEVER-READ `quoteCurrencies` parameter (`kraken.ts:695`) and ADD the test block — `:724` today is a DEAD ASSIGNMENT binding a different key (`allowedTradingPairs`) and nothing reads it, so this is an addition rather than a rewire — governed by the existing `screener_filters.quote_currencies` column and therefore inert until a row is populated; **(ii) CONVERT** — the exchange service he asked for, normalising quote-denominated prices and results to USD so a genuinely good opportunity in a non-USD pair is EVALUATED correctly rather than excluded. ★ **His framing, and it settles the order:** *“I’d rather work on something that creates more opportunities for us as opposed to closes windows and doors”* ⇒ **(ii) is the end state; (i) is the interim, not the answer.** ⚠️ **The `$0.25` active price floor is applied in the QUOTE currency — about $0.16 on an AUD pair and about $0.33 on a GBP pair — the same defect pointing opposite ways (`#966`, `#967`).** | CC-C | **PLACED 2026-09-12 — LAST ITEM OF THE PHASE** | after 12.6. ⛔ **DELIBERATELY NOT FOLDED INTO `3n` `B-PRICE-SIDE-BY-JOB`**, Kyle 2026-09-12: *“I don’t want to add this in and interrupt the price fixing stuff that we’re working on now.”* ✅✅ **CORRECTED 2026-09-12: there is NO open interim decision — half (i) IS ALREADY SCOPED AND APPROVED as `3n` scope row `8f` implementing `D9` (DECIDED 2026-09-11), and lands with OBJ-8.** **This row is therefore ONLY half (ii), the CONVERSION — and `D9`'s own *"until a timestamped currency conversion exists (`#966`)"* clause makes this row the thing that LIFTS `8f`'s block rather than an alternative to it.** ⚠️ **Open scoping question carried from the 2026-09-12 census, for `8f` at Step 3, not for this row: D9's *"not quoted in USD"* literally refuses `USDC`/`USDT` quotes (40 closed trades, net +$9.92) alongside the 96 genuine fiat-quoted ones (net −$77.69). The discriminator should be PEGGED versus FLOATING, not USD versus not-USD.** |
| 12.8 | ★ **`B-SCAN-BREADTH-DECLINE`** (`#1049`) — **A SCHEDULED REVIEW, NOT A FIX.** Kyle asked why the scanner sees so few pairs. **MEASURED 2026-09-12: it is not the scanner.** `SCANNER_PARAMS.BATCH_SIZE = 300` (`system-guards.ts:179`); the Kraken universe read **1,449 on all 401 cycles**; **evaluated 319-356 per cycle**; the batch genuinely rotates (**Ideal 42-46 / Rotational 244-254 / Excluded 0 over 408 cycles**); `[#396] SHORT UNIVERSE` has never fired and structurally could not. xStock coverage is complete — **457 distinct of 475 live**. ⛔ **The collapse is at the FILTER STACK: only 134 DISTINCT crypto symbols a day reach regime evaluation, about 9% of the venue** — and the count has fallen **216 → 134 in six days, a 38% decline**. ⚠️ **The cause is NOT established** — market, a moved threshold, or an instrument change, and the three have different remedies. **DELIVERABLE: a count at every hop of 1,449 → ~340 → 39-92 → 134, and an attribution for the decline.** ⛔ **It may NOT open by proposing a threshold change** — that is the shape that “fixes” a filter which was working. | CC-C | **PLACED 2026-09-12** | after 12.7. Independent of the currency work; shares only the observation that `universeSize` and `allowedQuotes` are both parsed-and-never-read on this path. |
| 12.9 | ⭐ **`B-KRAKEN-FEE-WATCH`** (`#1011`) — **MOVED HERE FROM 2.4-FEE-b BY KYLE, 2026-09-12.** A scheduled check that what Kraken publishes still matches what we charge. **Kyle's reasoning for the move, verbatim in substance: the venue will not change its fees often, so this is not where the time goes.** ✅ **STEP 1 IS COMPLETE AND KEEPS ITS VALUE:** scope at r9 + the clearing condition (`61ad3e112`), Langston-cleared after nine rounds, and a committed instrument of 21 cases / 49 assertions where **no remaining defect can produce a plausible wrong fact instead of a loud failure** (his exit test). ⛔ **STEP 2 WAS STOPPED MID-AUDIT** — what exists of it: the install model is `dt-deploy-drift.sh` (Helsinki, hourly, as `langston`, reaching staging over `ssh staging` + the CLI), and **the DB operand is reachable that way — my earlier claim that it was not was FALSE and is withdrawn.** ⭐ **THE ONE FINDING WORTH CARRYING OUT OF IT:** the 2026-07-09 revision changed **how the TIER is determined** (best of 30-day spot volume, futures volume, or Assets on Platform), not just the rates — so a rate-table watcher would have missed the half the venue led with. **OBJ-11 exists for that.** | CC-B | **DEFERRED — Step 1 done, Step 2 stopped** | Nothing blocks it; it is a priority call, not a dependency. Resume only on Kyle. |

⚠️ **#2 IS PLACED HONESTLY, NOT FLATTERINGLY: its changes are ALREADY LIVE AND UNREVIEWED.** The position states the gate is owed rather than pretending the work is pending.

### B-RULES-1c / 1d — **CLOSED 2026-08-25** (CC-A) · §5 decision-log entry

**DECISION 1 — the workflow is ELEVEN SKILLS, not one, and the INDEX LIVES IN THE RULES FILE.** Kyle’s call, against my first build. Two triggers, explicitly unequal: the `§0.a` table is an ordinary file read and does NOT depend on skill auto-invocation (measured-unreliable); each step being its own skill gives ELEVEN independent backup triggers instead of one. Cost measured before deciding: ~512 extra tokens always-on, 0.5% of the context floor. The umbrella skill was DELETED — nothing makes an umbrella fire first, so it could not route.
**DECISION 2 — `§2` REMOVED COMPLETELY, with a forwarding heading.** Kyle: *"there shouldn’t be a second set of references to it."* The forwarder survives on measurement, not preference: 136 citations across 107 files, mostly frozen completion reports that are not rewritten.
**DECISION 3 — the HOTFIX path is quicker but NOT unreviewed.** Kyle: Langston reviews it BEFORE staging. If in doubt it is a batch.
**DECISION 4 — depth goes to runbooks, but the runbook strategy is now MEASURED AND WEAK.** Across five runbooks: 3 commits and 9 mentions ever (control: RUNNING_ISSUES has 370). **So §6 was NOT gutted** — Kyle’s point stands that the comms rules got followed BECAUSE they were in the always-loaded file.
**DECISION 5 — an always-loaded file must not assert a live config value.** Name where to read it. Adopted after the shared MEMORY carried the wrong Langston model for 17 days.
**DEFERRED WITH A NAME, not dropped:** Infra Claude’s onboarding into the crew comms (Kyle, ~1-2 weeks, date open) · the crew-board keep-narrow-or-kill decision (delegated to CC-A + Langston) · the `read-the-field` promotion argument · #739 and #740 to `B-RULES-1e`, due 2026-09-05.

### B-MISTAKES-FILE — closed 2026-08-20 (CC-A) · §5 decision-log entry

**DECISION 1 — DO NOT BUILD THE APPEND-ONLY LOG.** The record exists in git at 36% density with 98.6% marker recall; a second store is the #641 two-copies shape. **git = instance layer · `MISTAKE_PATTERNS.md` = pattern layer · `CONDUCT.md` §13 = the short auto-loaded list.**
**DECISION 2 — PROMOTION THRESHOLD: 3+ instances across 2+ distinct batches, as a FLOOR** (Langston). A predicate, not a judgment — the trailer carries the batch id so it is one grep. **No severity override:** a single severe mistake gets a **mechanism**, not a rule slot.
**DECISION 3 — A LIVE RULE IS NEVER DISPLACED BY NEWNESS** (Kyle's defect). Cap pressure means **mechanise the oldest**, or raise the cap. **The cost is that mechanism-building becomes mandatory.**
**DECISION 4 — CONDUCT CAP RAISED 4,096 → 6,144 TOKENS (Kyle), EXPLICITLY TEMPORARY.** The whole four-file auto-load allocation is revisited once `CLAUDE.md` is slimmed.
**DECISION 5 — PIECE (5) ADOPTED (Langston):** pre-audit + implementation plan = ONE document, audit before plan, every plan item back-referencing its finding.
**OPEN:** the weekly pass has never run (first firing 2026-08-27; until a dated row exists its liveness is unproven — the batch's own standard applied to itself). `wrong-object` sits in §13 with **no mechanism**, its home *approved-and-unbuilt* — the first real test of Decision 3.
