# Grouped findings: dispositions and proposed designs

2026-09-11 · Coltrane · Kyle's experiment, steps 1–2 only.

**Read ref: `77fbc16c2b63ce71decd23f760d49292cfca125e`.** The mirror returned this ref before the read and again before drafting. The input is r3 of `Claude Comms and Packages/Scope Files/CODEX_FINDINGS_BY_GROUP.md`. This is a design report, not a completion report, implementation authorization, independent code review, or staging verification. No application code, settings, data or deployment is changed.

**My recommendation:** repair the contracts that make prices, costs, identities and results mean what their consumers think they mean. Preserve deliberate policies until their replacement is specified and justified. Then test strategy and allocation changes prospectively. Correcting those contracts does not establish profitability, an optimal holding period, or competitive superiority.

## Provenance and interpretation

I read the substantive findings and designs in all five reports, both question lists, and selected original completion records, canonical history, issue amendments, plan entries and implementing code. References below resolve to files read through `coltrane-review` at the read ref. A citation to an earlier report establishes what that report found at its own source pin; it is not a claim that I independently reran its measurements or audited every current caller.

Source shorthand used throughout:

| Name | Repository path |
|---|---|
| AU1 | `Claude Comms and Packages/Codex Audits/audit1-full-system/FULL_SYSTEM_AUDIT_2026-09-05.md` |
| AU2 | `Claude Comms and Packages/Codex Audits/audit2-fee-viability/CODEX_AUDIT_2_REPORT.md` |
| AS | `Claude Comms and Packages/Codex Audits/astra-independent/REPORT.md` |
| AU3 | `Claude Comms and Packages/Codex Audits/audit3/REPORT.md` |
| PR | `Claude Comms and Packages/Codex Audits/pricing-architecture-review/REPORT.md` |
| RI | `1-system-manual/RUNNING_ISSUES.md` |
| REG | `1-system-manual/CODEX_FINDINGS_REGISTER.md` |
| PLAN | `1-system-manual/PHASE_19_PLAN.md` |
| BC/name | `Claude Comms and Packages/Batch Completion/name` |
| CAN/name | `bridge/canonical/name` |
| CENSUS | `Claude Comms and Packages/Scope Files/B_PRICE_SIDE_BY_JOB_LEVEL_CENSUS.md` |

AU1's source pin was `5a7fc2eccef6c8d30b35006f3c27c53fb4d21da1`; AU2 and AS used `c7f18c5c7291b14a21a654ca3a35625c0533ca38`; PR used `9c2b50e7391758ef7fd2df0a48f0368fbcef13e0`. AU3's `d0eda8ae4ab86e5968f08710be8e382a946126fd` is an **export assertion**, not a Git-verified source pin: AU3:3–6 expressly records that limitation. Reading its committed report now does not repair the original export provenance.

I also read bounded `coltrane-review log` output. It returned the named introduction/change commits `95c9fd79f` (DHMA), `8dd10c747` (per-class RTB), `a3d5c4f50` (older filter/database work), `b74526dc3` (depth fills), `213e162dc` (fixed-notional sizing), `acf683c5d` (live gate), `884b9289e` (level basis), and `3f88e2e4a` (venue timestamp). Their subjects corroborate chronology; the rationale below comes from the cited records. I did not independently rerun the earlier audits' `git log -S`/blame investigations; the wrapper exposes recent-log, show and grep operations, and the working clone was used only for authoring.

The five requested provenance classes are abbreviated **P1 still right; P2 right but needs updating to today's intent; P3 disconnected and should be reconnected; P4 connected and should be removed; P5 dead and should remain dead or be deleted.** These classify components, not entire mixed groups. An unresolved intent is marked **LOOK**, rather than assigned an invented history. All designs are proposals inferred from these sources.

## Disposition at a glance

| Group | Disposition | Proposed result |
|---|---|---|
| 1 Pricing | FIX contracts and lifecycle; LOOK at source allocation, freshness and cache topology | Identified observations, coherent consumers and explicit order-specific pricing |
| 2 Fees | FIX through existing owner; LOOK at maker advantage and economic viability | Applicable, versioned signed fees and exactly-once cost accounting |
| 3 Simulation | FIX evidence and missing-input semantics; LOOK before replacing ratified fill policies | Every modeled fill exposes its assumptions and unsupported quantity |
| 4 Geometry | FIX dimensional/missing-geometry contracts; LOOK at DHMA intent and silent strategies | Native branch geometry that can be explained and tested |
| 5 Decision maths | LOOK at prediction/allocation objective; FIX semantic records | One declared prediction object and explicit routing/ranking/sizing jobs |
| 6 Identity | FIX under existing ownership | Instrument and mode isolation through keys, events, persistence and resets |
| 7 Risk/live | FIX known operational holes; preserve policy decisions pending their owners | Truthful risk state, legacy-route removal and verified live execution obligations |
| 8 Opportunity | KEEP boundaries; LOOK prospectively | Measured exclusions and frozen variants against existing strategies |
| 9 Evidence | FIX capture; retain refusals for unrecoverable history | Complete prospective lifecycle, risk sets and execution/config eras |
| 10 Documents | FIX descriptions through document owners | One current account, with historical claims clearly bounded |

## 1. Pricing

**Provenance first.** The original cache design consolidated sources under rate control, with distinct cadence buckets (CAN/DawnTrader_System_Architecture_Execution_Flow.md:257–287,633). That is a purpose, not proof that two caches are wrong: RI:6476–6503 explicitly leaves their divergence unresolved. The missing open-position enrollment is different: the recorded design specified the subscription and the implementation omitted it (RI:6804–6820). CENSUS:243–260 settles the printed-or-transactable level basis and preserves the filter's separate feature job. BC/F_G_2_PROGRESS_REPORT.md:29–37 separates shipped level work, trigger coherence and the voided shadow window. PR:27–43,73–81,99–141 supplies the remaining cache/book mechanisms.

**Reconciliation:** I accept the corrected level-basis rule. AU3:74 says midpoint detection is defensible; PR:238 says a *smoothed* midpoint is not proven best. Those statements coexist: a quote feature may be legitimate without being an approved monetary level anchor or a demonstrated superior detector input. Keep feature experiments separate from the already-decided level repair. A historical close passes the basis test, not every freshness or executability test. Preserve crypto's 60-minute and xStock's 15-minute signal intervals; 60-minute regime refresh is a separate input contract.

| Finding set | Class and short design |
|---|---|
| Level basis, per-leg conversion and trigger coherence; 3n/3c | **P1** for the corrected rule and already-built separation; **P2** for remaining quant wiring. Carry the actual printed/transactable anchor and its age into the level constructor, preserving structural support/ATR/extreme semantics. Bind any side conversion to the declared order type and the cost convention. Do not generically shift every stop/target. OBJ-3b remains with F-G-2's disposition; a native trigger reference and a custom liquidation trigger are different contracts. |
| Smoothed quant input, repeated cached reads and restart jumps; S26/PR-A6 | **P2.** Keep raw observations separate from versioned features. Advance the adaptive EMA once per identified observation under defined time semantics. Carry warm/cold state and feature version; choose deterministic rehydration from retained observations or an explicit cold-start eligibility policy. Bound/evict unused state. Do not automatically persist today's estimator or replace it with a new filter. Its level-setting job is removed under 3n; its feature usefulness remains an empirical question. |
| Provenance, last-versus-mid, invented sides and transport counters; #952/PR-A9 | **P2.** Preserve producer-specific immutable observations: instrument, venue/channel, price kind, observed sides/sizes, event ID, source time, receipt time and continuity epoch. Store absent sides as absent. Keep a true last-trade field distinct from a derived midpoint. Selected-cache results retain the winning observation and reason. Counters classify the actual producer/transport, not the code branch that obtained it. |
| MCE reuse and forming bars; PR-A5/A6 | **P2.** Cache by equivalent feature inputs: instrument, bar interval and version, price basis/observation, transform/config version, and lane where semantics differ. Do not prohibit useful sharing when inputs are identical. Bars carry boundaries, last constituent observation, availability time, coverage and forming/final state. Preserve forming-bar behavior unless its strategy contract is separately changed. |
| Queue books, reconnects, unsubscribe and watchdog; PR-A1/A13 | **P1** for existing crypto queue subscription; **P3** for desired membership surviving reconnect and the intended per-symbol watchdog. Maintain queued/held/diagnostic reasons independently of ACK and health. Reconcile their union after reconnect and release a channel only when no reason remains. Make ticker/book subscription and unsubscription explicit, including raw soft-resync operations. Pinning never prevents integrity repair. Quiet symbols require liveness evidence, not forced reconnects merely for lacking trades. |
| CRC, unknown precision and resync; PR-A2 | **P2.** Carry checksum/precision/sync status with each book. Invalidate its continuity segment on corruption/reconnect and admit it for execution only after a fresh valid snapshot. ACK or a 500 ms timer is not readiness. Unknown precision is an explicit unusable/limited state under the consumer policy. |
| Trade-trigger ticker, BBO, xStock L2 and comparison; #1017/#949/PR-A10/A14 | **LOOK/P2.** BBO is a candidate default for touch prices, not already ratified superiority. Verify channel acceptance/behavior, including price-level versus size-only updates. Keep trade-trigger ticker, BBO ticker and book states independent; producer tags alone cannot recover overwritten observations. Compare the same instrument/time slices, valid continuity segments, sides, depth at intended size, tails, latency and changed decisions. Predeclare decision-relevant tolerances and a finite INSUFFICIENT outcome. Census feeds by adapters/capabilities, including futures syntax, without enabling perpetual trading. Measure request batching, connection/load constraints and storage loss before expanding coverage. |
| xStock 00:15 event, wide quotes, exit versus fill; #943/#959 | **P1** for folding #959 into #943; **LOOK** for feed truth. Preserve #943's owner and observation protocol. Capture the exact trigger/fill/witness observations and comparator provenance. A wide symmetric book is not automatically a corrupt book; an upstream-sharing witness does not establish independent truth. Do not launch a separate 14%-gap fix or remove xStock clamps ahead of the owner's disposition. |
| True age, source/receipt clocks, entry/exit freshness; #951/3b.f-b/3b.f-c | **P1** for shipped age carriage; **P2** for truthful consumption. Cache re-serves preserve observation time. Record source age unknown and clock uncertainty; track connection health separately. Evaluate the policy at consumption/arrival, not only acquisition. Keep entry and exit requirements explicit; no extension of an exit-only ruling to entries and no invented universal millisecond ceiling. A refused exit quote leaves exposure present. |
| Open-position cadence, VTS cadence and two caches; #977/#971 | **P3** for enrolling/restoring/removing open-position subscriptions with correct reference ownership. **LOOK** for VTS-serving-active cadence and physical cache consolidation. Finish #971's intent/caller/limiter read, including the direct REST escape route, before source relabeling activates refusal. A common observation contract can cover two justified caches; independent producer records are compatible with one governed rate layer. Do not merge caches merely to match an old diagram. |
| Gate-time taker snapshot, skipped validation and actual exposure; PR-A4 | **P2.** Stamp gate/arrival/fill times and snapshot IDs. Acquire a valid arrival snapshot for paper simulation after asynchronous work and repeat relevant feasibility checks. If unavailable, record an explicit rejection or unpriced modeled outcome, not fresh liquidity. Keep fair-value marks separate from size-aware liquidation estimates. Preserve `skipped` validation as a distinct result; it cannot support a venue-vetted claim. Its existing paper continuation policy needs an explicit fidelity decision before changing it; live readiness cannot inherit paper acceptance. Group 5 owns allocation, Group 7 the shared hard-limit guarantee. |
| Quote denomination; #966 | **P2.** Monetary values carry currency; USD thresholds require a timestamped conversion, including quote exposure and fee currency. Keep native canonical symbols correct. Until a quote has an approved valuation/conversion path, reject admission with a specific reason rather than letting a naming failure enforce policy or assuming parity with USD. Existing holdings require a management path, not disappearance from monitoring. |
| Alerting, archive throttle, source-history limits | **P1.** Preserve the exit-skip notification ruling and its owner's emit/notify separation. Keep the deliberate archive throttle and archive-only perpetual feeds. Record missing historical coverage; do not promise REST reconstruction or claim xStock REST history is universally unavailable. |

**Missing/narrowed in r3:** #966 is broader than non-fiat quotes: its amendment covers any non-USD quote and corrects “31 pairs become tradable” to “eligible to be assessed” (RI:6151–6167). PR:212 proposes evaluating broad BBO coverage; r3 §1c makes that sound settled. CENSUS:255 still says “up to 15 minutes old,” which PR:69 and AU3:88 show is not an information-age bound. These corrections affect the scope, not the accepted printed-or-transactable rule.

**Acceptance/falsifier:** exercise a known valid observation through every touched consumer, then mutate producer, instrument, timestamp, checksum and input version one at a time. Re-serving the same observation must not rejuvenate it or advance smoothing. Reconnect a queued-only symbol; remove one of two subscription reasons; invalidate CRC during a pin. Compare rejection reasons and conserved membership against controls. A delayed old message with recent receipt must not become a known-fresh event. No such tests were run in this report.

**Ownership:** Coltrane's present scope is design only. Future delegated work can implement the agreed observation/MCE/lifecycle contract after an exact handoff. 3b.b, 3c, age/cadence work and #971 remain with CC-C; xStock book and BBO work follow their existing plan homes. #1024 supplies identity; do not invent a competing encoder here. No trigger, freshness, notification or live setting is self-assigned.

## 2. Trading costs and fees

**Provenance first.** B-4.5 deliberately centralized fees, installed strictly positive rates, and deferred tier automation (BC/B_4_5_COMPLETION_REPORT.md:9–16). That centralization remains right; the product/domain assumption must change. RI:7735–7754 records the later authenticated-account evidence that resolves AU2/AS's numeric hypothesis. I read the record, not the private screenshots or a current authenticated response. At the read ref `server/startup/b72-warmup.ts:236–239` still refuses zero and negative fees.

| Finding set | Class and short design |
|---|---|
| xStock mismatch, signed rebate and startup domain; #1010 | **P2.** One versioned fee contract identifies venue product/entity, account applicability, instrument/rate group, maker/taker leg, fee currency, effective interval and source. Permit only finite signed values within justified product-specific bounds. Update validators and every resolved consumer together; do not edit two DB values beneath an incompatible boot guard. Preserve DB governance and the centralized resolver. |
| Tier changes, special pairs, source identity | **P2.** Resolve the account's applicable contract per product/pair from an authoritative response or explicit verified mapping. Distinguish crypto tier criteria from xStock criteria; do not generalize assets-on-platform eligibility between them. Effective fee versions travel with decisions and each fill, including fills after a tier change. Unknown/expired applicability refuses new admission; servicing positions still retains actual charges/unknowns. |
| Spread credit for maker entry; AU1-3 | **LOOK.** Join actual resting limit, contemporaneous BBO, order mode and lane before changing the advantage formula. Quant midpoint and pattern close are separate populations. Compute the economic advantage from the actual placement relative to the taker alternative; neither “always full spread” nor “always half spread” is a universal replacement. |
| Sided geometry versus friction; AU3/AS | **P1** for separate expected and booked costs; **P2** for explicit basis metadata. Every leg states which spread/slippage is already in its price. Reconcile net cashflow from quantity, fills and explicit fees; retain shortfall as an anchored diagnostic when embedded in fills. Changing ask-side entry and removing a spread charge must be consistent at the consuming call, not a global deletion from still-midpoint calculations. |
| Fee hurdles, long-only viability and horizon | **P1/LOOK.** Keep exact filled-trade hurdle `(1+fe)/(1-fx)-1` under its currency/quantity assumptions. Report price-excursion evidence as conditional market opportunity, not capture or profitability. No threshold reduction or strategy ranking follows from a fee-only comparison. |

**Missing:** carry the already-homed #1011 fee watch, not a second new task: RI:7758–7770 distinguishes current venue authority, the verified transcription and applied DB rates. A page-change watcher alone misses existing divergence. Transcription that cannot be parsed needs an explicit human verification step. The old `default_taker_fee` row at RI:7768 needs a reader census before choosing P4/P5 or migration; its reach is unmeasured. Do not describe any fee schedule as permanent.

**Acceptance/falsifier:** positive, zero and authenticated-rebate fixtures must resolve; malformed/unmapped contracts must reject. Mutate a product mapping or one leg's sign and require reconciliation to fail. Recompute partial and multi-fill cashflows, fee currencies and fee-era transitions; adding a second spread deduction must turn a correct ledger red. These are control tests, not a profitability pass.

**Ownership:** CC-B retains 2.4-FEE and 2.4-FEE-b. Coltrane may supply the contract/design and isolated reviewable artifacts if subsequently delegated; no parallel fee migration, tier implementation or watcher is claimed here.

## 3. Simulation realism

**Provenance first.** B4b.1 explicitly chose full closes to avoid partial-close accounting errors, while adding genuine partial opens (BC/P19_B4b_1_COMPLETION_REPORT.md:15,21–23,36). B7.2c deliberately chose pending makers, drop-on-timeout, marketable-at-placement handling, twins and never-filled records (BC/P19_B7_2c_COMPLETION_REPORT.md:15–34). Its fill-wins precedence is ratified. REG:95–104 records why the 50 bp penalty exists and why the missing split field must cover all close arms. Those decisions cannot be replaced as accidental bugs.

| Finding set | Class and short design |
|---|---|
| Maker entry/exit full-at-limit, no queue/partials, post-deadline sample | **P2/LOOK.** Keep the current model as a named baseline. Record each attempt's posting, expiry, observed events, cancellation, partial quantity and resolution. Add prospective uncertainty/stress arms only with available primitives; L2 cannot reveal our queue priority. Preserve fill-wins precedence in the baseline. Changing it or inventing a queue model is a separately versioned policy, not a timeout patch. |
| Beyond-depth completion and discarded split | **P2.** First add an execution-estimate record without changing the ratified 50 bp behavior: modeled quantity, observed-depth quantity, unsupported quantity, price anchor, penalty/config version and branch. Use branches `walk`, `cold_book`, `missing_config`. For a walk, observed plus extrapolated quantity equals requested quantity. For cold book, observed quantity is zero, unsupported quantity is all requested, with no fictitious last walked level. Missing config is an invalid model result, not a zero-slippage success; mark its estimate/penalty unavailable explicitly. The three current paths are visible at `server/services/execution/order-placer.ts:98–121`. |
| Policy after measurement | **LOOK.** A fixed penalty is a scenario, not a proven conservative loss bound. A future partial/unknown-close model must retain residual quantity and risk, pending exit intent, retries, restart recovery and idempotent fee/close accounting. Compare that prospective model with the ratified baseline before replacing it. Never erase exposure merely because liquidity was not observed. |
| VTS crypto mark booking, xStock clamps, maker costs | **P1** for the shipped F-G-2 changes and separate epochs; **P2** for descriptive/model contracts. Carry lane and execution-model version. Keep xStock clamp removal with #943/3c dependencies. Reconcile modeled friction against the price movement already booked. VTS has a maker lifecycle; adding one is not the task. |
| Static slippage, cached spreads, gaps/session exits, witness dependence | **P2/LOOK.** Record actual input ages and modeled versus observed execution components. Treat unavailable liquidity as uncertainty, include session/feed-stall transitions, and expose shared upstreams. Do not claim a same-source witness validates market truth or that static slippage is always optimistic. |
| Ranking shadow “net,” closed-only labels and holding caps | **P2.** Label the legacy zero-friction arm as gross and retain its original scoring contract. New net-wealth books require fee/quantity/execution primitives; a rename cannot supply them. Include waiting, never-filled and unresolved exposure with separate arm clocks and cap outcomes. |
| Trailing, moonbag and latches | **P1** for the recorded inert configurations. Do not enable or redesign them under this group. Recheck effective flags before an eventual exit patch; a dormant-path reference is not evidence it currently affects closes. |

**Acceptance/falsifier:** run all three close branches; an absent split must not masquerade as zero extrapolation. A midpoint reaching a maker limit without trade/queue evidence must not receive an evidence-qualified fill label. Inject a partial close, duplicate execution and restart into a future replacement; position and cash conservation must fail if residuals or fees are dropped/doubled. Keep the known partial-open behavior as a positive control.

**Ownership:** a subsequently delegated Coltrane unit could add the explicit model-evidence contract. Changes to F-G-2/#943 booking or existing maker policy remain with their owners until handed off. No trailing/moonbag calibration work is annexed.

## 4. Strategy geometry

**Provenance first.** Original DHMA is described as an HMA crossover in CAN/DawnTrader_Regime_Strategy_Mapping.md:75. AU3:109–117 traces later candle approximations, return volatility and class-seeded coefficients; AS:124 traces the defective expression to `95c9fd79f`. That is an unresolved strategy-intent history, not permission to replace it with whichever indicator is convenient. Batch 63 intentionally introduced strong-trend geometry (BATCH_CATALOG.md:172), while B-4.7 explicitly preserved eligibility independently of favored lists (BC/B_4_7_COMPLETION_REPORT.md:22).

| Finding set | Class and short design |
|---|---|
| DHMA dimensions, price ceiling, RR and misleading microstructure name | **P2 plus LOOK.** First settle whether today's intended object is the current candle/return-volatility detector or the older HMA/microstructure concept. If preserving the current estimator, the proposed dimensional repair is `V=P*sigma` at its stated horizon. It does not fix symmetric offsets around P combined with entry above P: AU3:140 proves raw RR remains below 1 where the target clears entry. Keep invalid geometry rejected. Do not widen stops/targets or lower minRR to make it trade. A useful entry/exit redesign needs a new explicit strategy hypothesis. Correct the feature description regardless. |
| Target-family proposal and all 19 strategies | **P1** for valid structural branches; **P2** for the analysis contract. Capture native E/S/T, coefficient units, anchors, ATR input, chosen min/max/override branch, normalized/grid levels and guard verdict. Evaluate the composite output, not the family's name. Preserve range_trade's genuine shrinking-headroom finding and Batch 63's intended override. |
| Invented 2% target; #927/PR-A11 | **P4** for authoritative missing-target fabrication. Represent missing geometry and its source, then reject/quarantine it from executable ranking instead of repairing it silently. Keep any diagnostic-only fallback explicitly diagnostic until its owner removes it. Trace both the fallback evaluation and later chosen-EV override; do not assume every fallback determines final rank. |
| Strong-trend zero, declines and never-traded strategies | **LOOK/P3** for missing durable evidence. Retain eligibility and guards; persist route→invoke→null/geometry→guard→queue outcomes with one intent ID and resolved settings. Use a functioning strategy in the same lane/window as a positive control. The old 2.5 floor is not today's established cause: AU3:142 records the 1.95 seed. No outcome condemnation from absent detector records. |
| Seeded DHMA/range parameters | **LOOK.** Preserve DB control, inspect original coefficient units and selection evidence, then version any prospective parameter study. REG:304–312's write-history census is a lead to provenance; a seed author's name/date is not proof that nobody ever reconsidered a value. |

**Acceptance/falsifier:** scale all prices by a common factor with dimensionless parameters fixed; monetary outputs must scale before grid and obey bounded rounding after it. Vary ATR across selector boundaries with anchors held fixed. Missing targets and invalid long geometry must reject. These mathematical checks cannot award economic validity or explain a historical zero.

**Ownership:** Coltrane may later implement the agreed dimensional/geometry evidence unit after intent is settled. Existing #927, #648, reachability and strong-trend homes remain in place. This report proposes no 19-strategy retune.

## 5. Prediction, regime, ranking and sizing

**Provenance first.** The R ranker was deliberately introduced with a geometry floor and deferred calibration (BC/P19_B7_1_COMPLETION_REPORT.md:18–22; RI:2860). B8.5a changed maker `signalStrength` and retired finalScore gating while retaining other controls (BC/P19_B8_5a_COMPLETION_REPORT.md:15–26). Fixed-notional sizing is a later explicit decision, directly stated at `server/services/active-position-sizing.ts:211–229` and corroborated by `213e162dc`. These are distinct choices whose composition needs a decision, not evidence to reconnect retired scores or restore stop-distance sizing.

| Finding set | Class and short design |
|---|---|
| Uncalibrated EV, DI semantics and unsigned DBS | **P2.** Keep the central kernel as the explicit baseline. Define a prediction target under a named execution/exit policy and horizon, including fill/nonfill and unresolved outcomes. Stamp actual kernel inputs, pWin, maker signalStrength, fee and geometry versions. Class-native DI meanings remain explicit until a justified reconciliation; signed direction is separate from strength. Fit/test a replacement only on future compatible observations. Farther targets cannot earn a claim of improved expectancy from unchanged p alone. |
| Ranking objective and slot time | **LOOK/P2.** My proposed objective is incremental net portfolio wealth versus cash under the actual fixed-notional allocation and current caps. Kyle must choose whether that or risk efficiency is intended. Start with a transparent net-payoff shadow against the configured R ranker; include pending reservations and holding time. Do not divide by guessed duration or invent an opportunity-cost coefficient. AS:450–462 supplies the optional future learning protocol; no Kelly sizing is introduced. |
| Regime label, SQE weight and confidence | **P1** for different jobs; **P2** for a common versioned decision record. Keep all three quantities, routing membership and the effective gate result. A route pass followed by SQE rejection can be legitimate. Preserve reviewed inequalities and TFS momentum replacement; add explicit signed long-eligibility semantics appropriate to each strategy, not a blanket ban on every rebound setup in a negative regime. Rename/document DX truthfully before any separately justified indicator replacement. |
| Telemetry versus decisions, AMR modes and indirect VTS feedback | **LOOK/P2.** Read current effective flags before claiming enforcement. Trace producer→consumer→effective decision, including VTS→EV-gap→AMR. Record would-change and did-change separately. Preserve legitimate market-data sharing; audit outcome-population transfer. Refreshing EV with queued geometry must retain the original setup identity and geometry version. |
| Retired score calculations | **P4/P5** according to the live consumer census. R3 omits #558's later removal ruling: RI:168 assigns retired-score cleanup to CC-B/Phase 16. Retain historical evidence, do not redesign or reconnect finalScore/hybridScore, and do not misclassify still-gating regimeWeight as retired. This experiment does not accelerate that cleanup. |
| Sizing branches, post-fill notional and stop risk | **P1** for fixed-notional policy; **P2** for faithful enforcement/telemetry. Evaluate the actual mode branch and executable price. Recheck rounded quantity, price changes, pending commitments and fees against the existing limits before dispatch; reconcile partial/live fills afterward. Refuse/downsize proposed exposure where necessary under those limits. A post-fill check cannot undo an executed breach. Moving stop alone does not shrink size; wider stops must not buy a larger approved loss budget. |
| Existing ranking evidence | **P1.** Keep within-cycle, first-appearance, complete-pool restrictions and selection-versus-execution distinction. A tie/loss remains INCONCLUSIVE for that instrument. Do not reuse its fee-free outputs as a validated net-payoff control. |

**Acceptance/falsifier:** reproduce target-distance/pWin and equal-notional/R counterexamples; mutate the input sign or feature meaning and require the contract check to notice. Prove a telemetry-only change cannot alter admission while an enforcing AMR fixture can. Test delayed partial fills and concurrent pending reservations at a cap. A better calibration score alone does not pass an economic experiment.

**Ownership:** Coltrane can later supply an explicitly selected shadow design, not alter active ranking, sizing or AMR flags. #399/#502 calibration homes, #558 cleanup and Phase-25 work retain their owners. Group 1 supplies coherent price snapshots; Group 6 supplies identity; Group 7 owns the common admission/risk invariant.

## 6. Data identity and isolation

**Provenance first.** Canonical T2 was explicitly per-mode and T7 required paper/live isolation (CAN/DawnTrader_System_Invariants_Design_Guarantees.md:28–34,70–77). B79.0n.RTB added class plumbing without completing identity (BC/B79_0n_RTB_COMPLETION_REPORT.md:15–23). BC/BATCH_79_0m_a_COMPLETION_REPORT.md:66–87 documents the earlier index collision and requested retrospective. RI:7532 records Kyle's later instrument identity ruling. RI:7541–7560 settles the historical staging index as three columns, unlike the declared four: **the upsert worked there; fresh provisioning was the reproducibility hazard.** I did not query today's DB.

| Finding set | Class and short design |
|---|---|
| RTB index/upsert, overwrite, comparison and cleanup; #1006 | **P2.** CC-B reconciles live indexes, migrations, snapshots and declaration against one queue lifecycle contract. Use mode + instrument + strategy for the intended one-row lifecycle unless the owner explicitly changes status semantics. Carry asset class in lookup/replacement/promotion cleanup; prevent identity mutation during upsert. Decide whether promotion removes sibling strategies for the same instrument explicitly. Test fresh and upgraded databases, not schema text alone. |
| In-memory collisions, xStock writer; #1024 | **P2.** Complete the writer/reader/reset census and use the already-required single exported `assetSymbolKey(assetClass,symbol)` and inverse (REG:134–137). Do not equate every `Map<string,...>` with an unsafe key without inspecting its encoding and purpose. Market observations can be shared across modes; orders, risk and lifecycle state cannot lose mode. Avoid a second encoding in the pricing group. |
| Positions, mode-free APIs and destructive resets; #618 | **P2.** Add/backfill mode from evidence, not a default assumption; quarantine ambiguous rows. Scope reads, updates, event handling and every destructive reset for positions, closed trades and logs. Include migration checks that reject an unsafe downgrade once distinct identities coexist. |
| Symbol-only position uniqueness | **P2 with an explicit invariant update.** My interpretation of Kyle's instrument ruling plus per-mode T2 is one position per `(mode, asset_class, symbol)`. Confirm its application to the position surface with the owner; if any cross-instrument exclusivity remains desired, express it as a separate portfolio rule, not symbol aliasing. The grouped document should not leave this disconnected from the later ruling. |
| Dead scanner duplicate gate; #965 | **P5.** Retire it through its existing home; do not reconnect it to a different table. RI:6294–6308 identifies the actual enforcement path. Preserve and test that path under the new identity. |
| Retrospective and runtime schema drift | **P3.** Complete the requested class/index census, inventory columns/unique and partial indexes/conflict targets/foreign keys, and compare an introspected migrated DB with the declared contract. Do not treat “column exists” as identity correctness. |

**Acceptance/falsifier:** put the same symbol in both classes and both modes, with multiple strategy/status transitions. One promotion/reset must affect exactly its intended population. A symbol-only mutation must corrupt/reject a known fixture and turn the check red. Upgrade from both documented index shapes and provision from empty; both must converge. Preserve rows rather than silently deduplicating ambiguous history.

**Ownership:** 2.4c is CC-B; #1024 is CC-C; the prior Coltrane in-memory census boundary does not authorize RTB edits. Existing #618 and #965 homes remain. REG:134 explicitly separates database and in-memory batches; this grouping does not merge or block them. A future Coltrane unit must name its assigned subset and dependencies first.

## 7. Risk envelope and live readiness

**Provenance first.** Canonical T4 blocks new trades while allowing monitoring/closing (CAN/DawnTrader_System_Invariants_Design_Guarantees.md:47–52). RI:2789 records realized-only loss as a faithful restore with the unrealized extension deferred. RI:1933–1951 records restart re-anchoring as Kyle's decision, now requiring reconsideration at live launch. RI:142 chooses the active engine and removes the legacy engine; REG:179–193 adds the reachable close route and separate missing live-flatten obligation. Do not redesign a second live engine.

| Finding set | Class and short design |
|---|---|
| Realized-only budget and reset anchor; #303/#632 | **P1** for the current stated policy, **P2/LOOK** for the broader desired promise. Keep the current threshold/enforcer. Design a separate conservative marked-loss observation and durable session/reset record; decide the numerator, anchor and action with the owner before enforcement. Unrealized gains must not silently expand allowance. Neither code history nor this report authorizes a new threshold or automatic reset policy. |
| Missing cooldown, exposure errors, nonfinite P&L, loss-evaluator failures; #634 | **P2/P3.** Distinguish known-safe, known-breached and unknown risk state. Unknown mandatory admission inputs block new exposure, emit an actionable reason and have an explicit recovery path. Retain positions, exit intent and monitoring during outages. Do not turn every read error into a forced liquidation or turn unknown loss into zero. Protect reservations and kill state across races/restarts. |
| Live kill/flatten | **P3** for the intended live implementation, after the active execution contract exists. Persist kill first, prevent new/residual maker exposure, cancel pending entry orders, reconcile racing fills, request liquidation and track partials/rejections until the venue confirms the resulting inventory. Do not report flat on request acceptance. Preserve the kill latch until explicit authorized rearm. Paper tests alone cannot certify it. |
| Legacy engine, reachable close route and security composition; #578/#1022/#1023 | **P4** for reachable legacy exchange routes; **P5** for the retired engine. Follow the existing removal decision and its caller/state-reader census. Verify registered routes cannot reach legacy order/cancel actions, including with a seeded legacy live row. An empty table or disabled start gate is not that proof. Authorization/credential remediation stays with its owner; this report neither reproduces secrets nor re-files those findings. |
| Active live placer, execution reports and parity | **P3/LOOK.** Complete only the chosen active-engine seam under its Phase-21 owner. Specify order IDs, partial/rejected/cancelled states, fee currency, idempotency and restart reconciliation against venue inventory. Demonstrate parity per reachable lifecycle branch; mode-literal counts prove nothing. |
| Wider-stop loss, unobserved L2, mock/test routes | **P1/P2.** Preserve all current caps and check executable loss/exposure through Group 5. Durable full-L2 capture remains unestablished; request a storage/retention census with known ticker capture as control. Inspect effective mock/test-route access before declaring them inert; neither a function's existence nor a historical empty environment search settles current reachability. |

**Acceptance/falsifier:** controlled fixtures for evaluator failure, missing cooldown, cap contention, nonfinite loss, restart while killed, partial liquidation and duplicate execution reports. Seed a legacy live row and spy on the exchange seam: removal must block the registered route despite that positive reachability control. Run these isolated from real orders. No live-readiness pass is claimed here.

**Ownership:** #578 and the security composition remain with CC-A/assigned security owners; #634 is CC-B; #632's policy is Kyle's, carried by CC-C; #303 remains Phase 25. Coltrane has a design role only. The already-described reachable route exposure deserves its existing owner's attention on its own schedule; “before go-live” is a floor, not proof of present containment.

## 8. Opportunity boundaries

**Provenance first.** AS:295–307 establishes deliberate long-only routing and capture limitations, with positive detector counterexamples. RI:6173–6187 files the nominal price floor as an undecided policy question, not a defect. AS:366–382 and 404–462 define future variants against existing strategies, with cash and unresolved positions included.

| Finding set | Class and short design |
|---|---|
| Long-only, defensive hedge and allowed quotes | **P1.** Keep long-only and supported-product boundaries. Describe defensive_hedge as a long diversifier/rebound concept unless measured portfolio behavior supports a stronger claim. Record loader/admission exclusions by stage; quote conversion work belongs to Group 1. No shorting or perpetual trading. |
| Nominal price floor; #967 | **LOOK.** Keep the existing DB setting. Measure which instruments it excludes relative to liquidity, tick size and executable costs; report the active/passive population difference. Kyle decides whether the proxy still serves its purpose. The historical DOGE/ADA examples do not establish their current prices or current rejection counts. |
| No xStock decline recorder | **P3.** Add the durable rejection/eligibility record at the actual xStock stage, with a positive control, under Group 9's contract. Empty capture is not zero declines and passive breadth is not the active denominator. |
| AS-P3 VWAP, P4 ORB, P5 strong bull; P6 allocation is Group 5 | **LOOK.** Preserve each existing detector as its control; treatment adds the specified observed-headroom, direction or execution qualification and may choose cash. Freeze all inputs, session calendars, risk, fees and execution assumptions before T0. No fabricated target, widened counterpart stop, lower floor or manufactured holding horizon. |

**Acceptance/falsifier:** retain the AS protocol: 56 days of admissions, 84 days of observation, matched capital/risk books, attempt-level capture and unresolved terminal marks. Preserve its information floors, dependence-aware inference, multiplicity plan and conservative execution sensitivity; counts alone do not establish power. PASS/FAIL/INSUFFICIENT are prospective simulation verdicts, not live authorization. No launch until prerequisites exist; no backtest or extending a disappointing window.

**Ownership:** #967 remains Kyle's decision with CC-C; recorder work uses the evidence owner; strategy variants are uncommitted proposals. Coltrane does not widen the active universe or choose new strategies here.

## 9. Evidence

**Provenance first.** AU3:179–196 refuses the holding-period question for three distinct missing inputs: complete risk sets, enumerated deployments and reconciled chronology. AS:341–348 preserves original arms and scoring restrictions. RI:7466–7484,7571 records deployed/review drift and its later correction. BC/P19_B7_2c_COMPLETION_REPORT.md:20,31 intentionally distinguishes visible never-fills from filled-trade outcome aggregates. Better capture must preserve those objects rather than merge them.

| Finding set | Class and short design |
|---|---|
| Decision/order/fill joins and missing exports | **P3.** A durable intent ID joins routed opportunity, branch geometry, resolved config, queue membership/refresh, reservation, order attempt, partial fill and exit. Retain event/source/receipt/decision/dispatch/fill clocks separately. Export rejected and never-filled attempts, within-cycle candidate membership, original and refreshed snapshots, instrument/fee/tick identity, plus explicit gaps. |
| Silent strong trend and sink coverage | **P3.** Reconcile counts between adjacent lifecycle stages, including failures and persistence errors. Identify producer, consumer and retention for each sink. Test a known successful event and an injected rejection through the same sink before interpreting zero. Existing in-memory counters are useful controls, not durable history. |
| Holding/censor population | **P2.** Fix an observation cutoff and inventory every admitted intent: pending, filled-open, closed, never-filled and censored, with arm-specific clocks and policy caps. Keep competing exits in the original risk set. A cap exit is an outcome under that policy; administrative censoring is different. Do not retrofit missing historical geometry or infer complete open inventory from null close timestamps. |
| Deploy/build/config/reset chronology; #1001/#1002 | **P3.** Export process-start/build identity, actual deploy records and effective config/reset changes, with their timestamp definitions and uncertainty intervals. Compare observed deployed build to the intended ref without treating branch distance as an automatic deploy command. Recover historical boundaries only from independent operational records. Source touches and Git times remain candidate evidence, not deployment events. |
| Corrected fees and contaminated selection | **P1.** Preserve old eras as named data. Repricing old accepted trades cannot recover rejected opportunities. New controls require new capture; epoch integers are class/source-specific, not globally comparable version numbers. |
| Horizons, performance claims and “right decisions” | **LOOK.** Define the objective as feasible net wealth including cash under unchanged constraints. Report costs, coverage, waiting, nonfill, open mark, capital-time and tail risk. Neither red days after reset nor hindsight excursions establish a cause, edge or superiority. Repair full-tick/seven-day/uniform-behavior claims to the measured populations. |

**Acceptance/falsifier:** delete one lifecycle transition, mislabel one mode or inject a persistence failure; the completeness instrument must go red while preserving the missing entity. Demonstrate the deploy enumerator on a known deployment in the target interval before accepting an empty intersection. Reconcile quantity/cash/state totals and all row exclusions. Historical questions remain INSUFFICIENT wherever the required population or boundary cannot be recovered.

**Ownership:** Coltrane may later implement an agreed capture/export unit; existing deployment instrumentation stays with CC-A and strategy/simulation sinks with their owners. Additive capture may precede economic changes, but its runtime cost and window effect still need classification. No measurement script or operational query was run against staging for this report.

## 10. Document corrections

**Provenance first.** PR:145–174 lists inaccurate capacity, coverage, archive and runtime claims. REG:335 explicitly assigns the pricing-document repair to the crew. Canonical history itself illustrates the danger: CAN/DawnTrader_System_Invariants_Design_Guarantees.md:85–115 mixes actual-fill gross with old fixed slippage/fee constants, while B-4.5 and B4b.1 later change those contracts. Historical intent guides the read; old constants and descriptions do not override current decisions.

**P2 for current descriptions; P4 for withdrawn claims still presented as current.** Owners should repair the live prose/table in place, retaining dated history separately:

| Corrections | Required account |
|---|---|
| REST request cost; archive/cache/subscription sets; universe refresh; futures/dashboard readers | Describe actual batching and independently selected sets, distinguish a refresh result from live subscription mutation, and include configuration-driven dashboard readers. |
| Unwired volume-floor dial | Say file/option-controlled at the inspected path. Do not add DB wiring just to make the prose true; first establish an approved requirement for that separate change. |
| Bar close/midpoint; archived bars reaching trading; venue timestamps | Name the actual object, class, interval and consumer. Do not derive information age from a bar's interval. Parsed timestamp support does not certify historical payload coverage. |
| Partial opens; full modeled closes; gate snapshot age; maker/VTS descriptions | Describe partial-entry capacity correctly and expose modeled exit assumptions. “System is fine” applies to a corrected false accusation, not to every residual risk under that heading. |
| Soft resubscribe “fixed by construction”; independent feed agreement | Remove contradicted present-tense conclusions, not merely append caveats. Link residual lifecycle work to Group 1. |
| “pWin is flat”; sizing header | Distinguish maker signalStrength from kernel probability. Correct the stale stop-risk sizing docblock (`server/services/active-position-sizing.ts:123–131`) against its later implementation, without changing the formula. |
| xStock REST history, spread-based stop-out prediction, uniform live parity | State what was actually demonstrated and what remains unknown. Preserve the PR-A14/A8 qualifications; no permanent impossibility or frequency forecast from a spread percentile. |
| Already-corrected plan rows 3n/3b.c | Retain their correction; no new fix or revived disagreement. |

**Acceptance:** verify each corrected claim and its inbound references at the publishing ref, including tables and summaries. A text-only report needs reference/coverage checks, not an application test suite. Current-state documents should point to immutable historical records without forcing readers to resolve contradictory active claims.

**Ownership:** crew document owners retain `PRICING_DATA_ARCHITECTURE.md` and their completion/plan records. Coltrane supplies this report and the corrections above; it does not edit canonical history or owner documents.

## Cross-group order, landing and rollback

1. Preserve existing urgent owner work and correct misleading current descriptions. Group 2 can proceed through CC-B without waiting for all ten groups. Group 6's database and in-memory work retain their independently assigned sequence.
2. Agree instrument identity, observation semantics and additive evidence interfaces between Groups 6, 1 and 9. Provide explicit version adapters where groups land separately; missing required semantics fail visibly. Pricing must not introduce its own identity convention while #1024 is in flight.
3. Complete the assigned pricing and cost contracts, then expose/qualify simulation assumptions. Group 3's first evidence change need not change its fill policy. Group 7's legacy-route/security obligations are not held behind an economic study.
4. After trustworthy inputs and denominators exist, run Group 4 mathematical checks and the separately chosen Group 5/8 future experiments. Mathematical correctness, capture integrity and economic benefit receive separate verdicts.

**One experiment landing unit per group, not one ten-group patch.** Each eventually assigned unit requires an exact scope, parent/commit, rollback artifact, change-class and document set. Existing owner batches are dependencies, not silently absorbed subcommits. If the approved subset cannot be landed/rolled back independently, it is not ready for implementation; bring that boundary back in design review. This report does not authorize step 5.

Rollback must conserve historical facts and open obligations. Keep old fee/model/config versions for old rows; never rewrite their era to match the rollback. An identity/schema rollback that would collapse newly distinct rows is unsafe: retain the additive schema and stop/revert the affected consumer rather than merge assets. A simulation rollback retains modeled residuals, partial-fill history and fees. Never reintroduce a legacy exchange route or reset risk state to make a rollback easy. Exact migration/consumer compatibility must be designed before code, not supplied as a generic “git revert.”

**Window boundary:** the read ref records F-G-2's original window as VOID, awaiting a new anchor; it does not establish a currently open window from old criteria (BC/F_G_2_PROGRESS_REPORT.md:6–12). Preserve 3c, #951 and #943 as separately owned observations. Before any later deploy, owners establish each actual window/status, apply that window's own split/void rules, and enumerate all deploy records with a positive control. F-G-2:73 distinguishes cadence splits; :75–79 makes an in-window crypto level-basis change VOID, not split. Its disposition/3n C1 must be at the ref before crypto deployment. Existing positions with old levels require an explicit transition cohort, not silent rewriting (:37). Even capture-only code is not assumed window-neutral without checking its path and timing effect.

## The two question lists: coverage and remaining decisions

The AU1 question artifact remains contaminated (`audit1-full-system/BLIND_SPOT_DELTA_audit1.md:5`); the AS artifact records its pre-section-6 freeze (`astra-independent/BLIND_SPOT_DELTA_astra.md:3`). Neither is evidence of independent agreement with this report. Both are useful design questions. All 30 numbered questions are mapped below; “covered” means addressed by the proposed contract, not answered empirically.

| Theme | AU1 questions | AS questions | Design home / unresolved evidence |
|---|---|---|---|
| Objective, prediction, calibration and capital | 1, 12 | 1, 5, 12, 15 | Groups 5/9; intended objective, current flags and compatible future labels still required |
| Costs, geometry, units and boundaries | 2, 8, 15 | 2, 3 | Groups 1/2/4/5; authenticated pair applicability and DHMA intent still required |
| Risk after execution, cold start and safe failure | 3, 5, 11 | 6, 10 | Groups 1/3/6/7; execution fixtures and reset/unknown-risk decisions still required |
| Identity, alternate paths and process state | 4, 9, 10, 14 | 11, 13 | Groups 1/6/7/9; complete writer/reader and route censuses precede implementation |
| Clocks, horizon and prospective evidence | 6 | 4, 14 | Groups 1/3/8/9; missing historical populations are not supplied by a design |
| Regime and exclusions | 7 | 7, 8, 9 | Groups 5/8; causal traces and recorder controls precede policy changes |
| Historical intent versus current description | 13 | — | All provenance paragraphs and Group 10 |

The decisions this report leaves for review are specific: DHMA's present feature/geometry intent; the selection/allocation objective; the price floor; cache topology/cadence where not already decided; freshness by job; and whether/when to replace ratified simulated completion with a residual-position policy. It does not reopen the printed-or-transactable rule, long-only, fixed-notional sizing, current hard limits, active max-hold choice, or the legacy-engine removal decision.

## Validation and limits of this deliverable

Read coverage: the five reports' substantive findings and proposed designs, both complete numbered question lists, selected RI/REG/PLAN/BATCH_CATALOG passages, the cited canonical excerpts and original completion passages, and current startup fee validation, paper order-placer branches and fixed-notional sizing code. Some broad catalog/register reads were truncated; only the bounded passages explicitly cited above are relied on. Historical audit source-line citations remain those audits' evidence, not a fresh whole-code census.

I did not query staging, inspect its rendered pages, access private fee evidence, enumerate current deploy records, measure runtime frequencies, rerun audit data analyses, run application tests, or perform the proposed application mutations/experiments. No runtime absence or zero is newly asserted. Quantitative audit examples remain attached to their original populations; this report does not convert them into current measurements. `SOURCE_MANIFEST.json` records source hashes and bounded citation checks; it is not a whole-file reading certificate. The deliverable itself is checked for group/question coverage, including a deliberately omitted-heading control, and a report-only Git diff before its work-repository handoff.
